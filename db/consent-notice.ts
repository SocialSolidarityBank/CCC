/**
 * ② 'AI를 활용한 녹취기록' 동의 문안의 정본 조각 (D49 · ADR-0027).
 *
 * 화면에서 ②를 체크하는 순간 이 버전·해시가 `pilot_text_ai_consent_evidence` 에
 * 함께 남는다 — 나중에 "어느 문안에 동의했는가"에 답할 수 있어야 하기 때문이다
 * (D46 으로 법률 검토가 동결됐어도, 재개 시점에 필요한 것은 이 기록이다).
 *
 * 화면 문안(`apps/web/.../consent-copy.ts`)은 이 상수를 그대로 렌더한다 —
 * 문안을 고치면 **반드시 버전도 함께 올린다**. 버전이 그대로면 옛 문안에 동의한
 * 기록과 새 문안이 구분되지 않는다.
 *
 * v0.4(2026-08-16, D64): 감정 추이 분석은 보류 중이라 문구를 지우지 않고
 * "현재 제공하지 않음" 표시만 덧붙였다. 다시 켤 때는 표시만 지우면 된다.
 * 기존 동의자와의 정합은 법률 검토 대상이다(`docs/consent/legal-review-open-items-v1.md` 6번 항목).
 */
export const CONSENT_TEXT_AI_NOTICE_VERSION = 'consent-draft-v0.4';

/** 5절 ② 항목 본문. 해시의 입력이므로 공백 한 칸도 의미가 있다. */
export const CONSENT_TEXT_AI_NOTICE_TEXT =
  '[선택] AI를 활용한 녹취기록 동의: 상담 내용을 녹음해 음성-텍스트 전환(전사)과 감정 추이 분석(현재 제공하지 않음, D64 보류)에 이용하고, '
  + '상담 기록(수기 메모·전사)을 AI가 요약·정리합니다. 음성 원본은 업로드 후 30일 이내 자동 삭제됩니다. '
  + 'AI 처리 전에 성명·연락처와 구체적인 질병명 등을 가명·마스킹 처리한 뒤 외부 AI 서비스에 위탁 처리하며, '
  + '자동화 기술의 특성상 드물게 일부 정보가 완전히 제거되지 않을 수 있는 잔여 위험이 있습니다. '
  + '이를 최소화하기 위한 2단계 보호 조치를 적용합니다.';
