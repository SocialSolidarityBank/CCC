# 비영리 사례관리 프로그램: 개발 작업 문서

> 이 문서는 Claude Code 프로젝트 루트에 `CLAUDE.md`로 두는 것을 전제로 작성했다.
> Claude Code는 세션을 시작할 때 이 파일을 읽으므로, 여기 적힌 규칙은 모든 작업에 자동 적용된다.
> PRD 원본: 노션 "비영리 조직 사례 관리툴" 페이지 (문서 제목은 고유명사라 구 용어 유지)
> 2026-07-08 설계 인터뷰로 확정된 결정은 9장에 정리했다. 본문과 9장이 충돌하면 9장이 우선한다.

---

## 1. 프로젝트 한 줄 정의

금전 지원 사업 당사자 상담을 인테이크부터 종결까지 기록하고, AI가 정리한 누적 맥락을 **상담 5분 전에 한 화면으로** 보여주는 웹앱.
핵심 가치는 기록이 아니라 브리핑이다. 모든 설계는 "나중에 5분 안에 훑기 좋은 형태"에서 역산한다.

**사용 범위**: 여러 기관이 각자 쓰는 서비스로 방향 확정(D27, 구 D1 "내부 전용" 대체). 현 단계는 사회연대은행 단일 기관 위에 구현하며, 외부 기관 온보딩·자체 인증은 다음 단계다.

## 2. 확정된 기술 스택

| 구성 | 도구 | 메모 |
| --- | --- | --- |
| 인증 | Cloudflare Access | 기관, 팀 단위 초대제. 역할은 기관 관리자, 실무자 2단계. 공개 가입 페이지 없음 |
| DB | Cloudflare D1 | SQLite 기반. RLS 없음: 아래 개발 규칙 R1 필수 |
| 서버 로직 | Cloudflare Workers | |
| 파일 저장 | Cloudflare R2 | 음성 임시 보관. 수명 규칙으로 30일 뒤 자동 삭제 |
| AI 처리 | **처리 장비**(기관 내부) | 전사·화자 분리·감정 분석·**2차 마스킹**. 오디오 없는 텍스트 작업도 여기서 돈다 (D51·D57). 보안 기준은 D13. 실제 기기 사양은 `docs/ops.md` |
| 요약, 대조 | **OpenAI API** (`apps/api/src/ai-provider.ts`) | 장비가 2차 마스킹한 스냅샷만 전송 (2단 방어, D2·R3). 호출 자체는 Workers 에서 하되, **마스킹 스냅샷이 없는 텍스트는 나갈 수 없다** (D57·ADR-0027 — 구 "Claude API"·"호출은 처리 장비에서만"을 대체) |
| 프론트 | Next.js (Cloudflare Pages 배포) | |

## 3. 데이터 설계 원칙

### 테이블 구조

```
participant_pii_vault     실명, 연락처, 계좌. 앱 레벨 AES-GCM 암호화(키는 Workers 시크릿, D3). 관리자 권한만 조회
support_cases             가명 ID(동물 슬러그, 예: swallow-003 — D20·ADR-0004), 기관 ID, 담당 실무자 ID, GAS 기준, closed_at, purge_due (D10)
support_case_assignees    케이스별 담당 실무자 매핑. 공동 담당·이관 지원 (D7)
goals            세부 목표(D62 위계 전체 > 세부 > 세션의 2층. 전체 목표는 support_cases.overall_goal, 세션 목표는 schedule_session_goals). 문구·연결·상태만 운영(GAS 채점은 D43 보류), 수정 허용+이전 문구 이력 보존(D62 가 D12 수정 금지 대체). status(active/closed) + closed_reason(달성/중단/재설정 = achieved/stopped/reset)
goal_revisions   전체·세부 목표 문구 이력(D62, 마이그레이션 0031). 덧붙이기 전용, 한 행 = 한 번의 확정(최초 작성 포함), goal_id NULL 이면 전체 목표. 감사 조회(admin 전용)와 열람 범위가 달라 audit_log detail 이 아닌 전용 표다(CCC-71 결정)
sessions         세션 기록. 분류 스키마 필드 + 승인 플래그(approved_at)
action_items     액션 아이템. 담당, 기한, 해결 여부
flags            리스크 플래그. 고정 유형 + 전사 발언 인용 + 실무자 확인(맞음/틀림) (D9)
audit_log        열람·변경·PII 복호화·내보내기 전부 기록. append-only (D14)
```

`yellow` 구 이름 `pii_vault`·`cases`·`case_assignees` 는 **컷오버 전 표**이고 `db/gateway.ts` 에 아직 일부 남아 있다(각 6·21·14회 vs 현행 29·183·71회). **새 코드는 위 이름을 쓴다.** 컷오버 흔적: `migrations/` 의 `participant_support_case_cutover_*`.

### 스키마 3층 구조

- 코어(전 케이스 고정): 목표별 GAS 점수와 근거, 액션 아이템, 리스크 플래그, 감정 추이. 브리핑과 통계는 코어만 사용
- 템플릿(사업 유형별): 금전지원형 v1부터 시작
- 확장 슬롯(케이스별): 실무자 자유 추가. JSON 컬럼 하나로 격리, 통계와 브리핑에서 제외

GAS(목표달성척도): **세부 목표마다** -2(악화)부터 +2(기대 초과)까지 5단계 점수(D33 — 전체 목표·세션 목표에는 점수를 매기지 않는다). 세부 목표는 측정 가능한 문장으로 쓴다. **점수는 실무자가 직접 매긴다. AI는 전사에서 근거 발언 발췌만 제안한다 (D6).**

## 4. 개발 규칙 (위반 시 커밋 차단 대상)

**R1. DB 접근 단일 관문**
모든 D1 조회와 쓰기는 `db/gateway.ts`의 공용 함수만 거친다. 이 함수는 기관 ID와 접근 권한(담당 실무자 또는 관리자, D7)을 반드시 검사한다. 다른 파일에서 D1을 직접 호출하는 코드는 금지. Guard Hook으로 `env.DB.prepare` 직접 호출을 gateway 외부에서 발견하면 커밋을 차단한다. 게이트웨이 함수는 audit_log 기록을 내장한다 (D14).

**R2. 승인 전 초안은 공식 기록이 아니다 — 승인 = 정합성 검증**
AI가 생성한 세션 요약은 `approved_at`이 채워지기 전까지 브리핑, 통계, 보고서 어디에도 나가지 않는다. 이 검사를 gateway 함수 안에 내장한다.
승인 화면의 핵심은 **수기 기록 ↔ 녹취 기반 AI 정리의 대조**다. 대조 출력 3종(메모에 없는 내용, 음성에 없는 내용, 미논의 목표)을 실무자가 처리하는 것이 곧 정합성 검증 프로세스다.
실무자가 직접 쓴 수기 메모는 작성 즉시 공식 기록이며 승인 대상이 아니다 (D5).

**R3. PII는 파이프라인에 들어오지 않는다 — 2단 방어**
AI 사업자 호출부, 로그, 에러 메시지에 실명, 연락처, 계좌가 섞이면 안 된다.
1차: pii_vault 등록값(당사자 실명·연락처·계좌)을 가명 ID로 결정론적 치환.
2차: **처리 장비**에서 개체명 인식(NER)으로 대화 속 제3자 인명·전화번호 패턴을 추가 마스킹한 뒤에만 사업자로 전송.
**사업자로 나가는 텍스트는 장비가 만든 마스킹 스냅샷(`ai_masked_source_snapshots`)뿐이다** — 수기 메모만 있는 회차도 예외가 아니라 텍스트 일감 큐를 거쳐 장비가 먼저 마스킹한다(D57, ADR-0027 — 구 "호출도 처리 장비에서만"을 대체). 스냅샷이 없으면 그 회차는 재료에서 빠진다.
완벽하지 않은 잔여 리스크는 동의서 문안에 반영한다 (D2).

**R4. 감정 지표는 숫자만**
"불안하다" 같은 단정 문장을 생성하거나 저장하지 않는다. 점수와 추이만 저장하고, 화면에는 "지난 세션 대비 저조" 수준으로만 표시한다. 감정 점수는 당사자 발화만 집계한다 (D11).

**R5. AI 금지 영역**
지원 지속이나 중단 판단, 심리 진단, 승인 없는 기록 확정 기능은 어떤 형태로도 구현하지 않는다. GAS 점수 산정과 리스크 플래그 확정도 실무자 몫이다 — AI는 근거 발췌와 고정 유형 제안까지만 (D6, D9).

## 5. AI 파이프라인 스펙

파이프라인은 **두 갈래**다 — 녹음이 있는 회차는 아래 전 구간을 돌고, 수기 메모만 있는 회차는 전사·감정을 건너뛰고 2차 마스킹부터 시작한다(텍스트 일감 큐 `ai_text_work_queue`, D57). **마스킹은 둘 다 처리 장비에서** 돌고, 마스킹이 끝난 스냅샷을 재료로 **사업자 호출은 Workers 에서** 한다(D57·ADR-0027).

```
녹음 업로드(웹앱) → R2 임시 저장
  → 처리 장비 폴링(Workers API 경유, Access 서비스 토큰 인증, D13):
      전사(무음 경계 청크 분할 + 반복 검사 필수 — D53) + pyannote 화자 분리
  → 화자 매핑 자동 추정(발화량·패턴) → 검토 화면에서 실무자가 1회 확인 (D11)
  → 감정 분석(당사자 발화만): wav2vec2-xlsr(음성, 낮은 가중치) + HowRU-KoELECTRA(텍스트, 높은 가중치)
  → PII 2단 마스킹(등록값 치환 + 로컬 NER, D2)
  → 마스킹 스냅샷만 Workers 경유 D1 저장(ai_masked_source_snapshots), 음성 원본은 R2에서 30일 뒤 자동 삭제
  → OpenAI API 호출 ①(승인 대상): 대조 3종 + 핵심 한 줄 + AI 제안 3개
      대조 3종 = 메모에 없는 내용(누락), 음성에 없는 내용(확인 필요), 미논의 목표
  → OpenAI API 호출 ②(승인 대상 아님): 내용 불일치 — 양쪽 원문 인용 + 회차 링크만 (R5)
      스냅샷이 도착하는 시점에 돈다. 저장 즉시 브리핑에 뜬다. 호출 ①이 실패해도 ②는 나온다 (D51·D57)
  → 실무자 검토 화면(대조 3종 + 화자 매핑) → 승인 시 approved_at 기록
```

분류 스키마 초안(`aiSchema`) 생성은 **보류**다 — 상담 기록지가 고정 폼이 되어 조립할 것이 없다(D52, ADR-0023). GAS 근거 확인도 보류(D43).

처리 기한(SLA): 업로드 후 **다음 영업일 내** 검토 대기 상태 도달. 처리 장비가 6시간 이상 폴링하지 않으면 관리자 알림(녹음·텍스트 두 일감 큐 합산). 지연·장애 시 브리핑은 수기 메모 폴백으로 동작하므로 서비스는 멈추지 않는다 (D8).

모델 정보:
- LLM: **OpenAI**(`apps/api/src/ai-provider.ts`, 프로바이더 슬러그 `codex`). 모델·프롬프트·스키마 버전은 활성 프로바이더 설정 해시로 고정되고, 어긋나면 재활성화 전까지 fail-closed 다(D57·ADR-0027)
- STT: **엔진 미확정(D53)** — 현행 openai/whisper 유지, 후보 1순위는 Qwen3-ASR-1.7B(Apache-2.0). 확정은 실측 게이트 G1~G3 통과 후이며 그 세션은 처리 장비 앞에서 해야 한다. 엔진과 무관하게 **무음 경계 청크 분할 + 반복 검사**는 필수(ADR-0024). 근거: `docs/aside/` 조사 3건
- 인명 마스킹(2차 방어의 인명 계층): **FrameByFrame/korean-pii-e5-base** (MIT, 2026-08-01 Q 승인). 대화체 KDPII F1 0.943 — 뉴스로 학습한 일반 NER 과 달리 상담 말투 분포에 맞다. 라벨은 `private_person` 계열(BIOES). **이름과 함께 주소도 가린다**(2026-08-01 Q 결정) — 각각 `[인명]`·`[주소]` 로 갈라 남긴다. 생년월일은 상담 맥락이라 일부러 제외. **미설정이면 장비가 뜨지 않는다**(R3 — 인명 계층이 빈 채로 돌 수 없다). 상세: `apps/pipeline/README.md`
- 화자 분리: pyannote.audio
- 음성 감정: jungjongho/wav2vec2-xlsr-korean-speech-emotion-recognition (Apache-2.0, 정확도 0.77)
- 텍스트 감정: LimYeri/HowRU-KoELECTRA-Emotion-Classifier (MIT, 정확도 0.95)
- 라이선스 미표기 저장소는 사용 금지

## 6. 15초 페이지(브리핑) 요구사항

> **2026-08-08 Q 용어 개편**: 화면 이름 '상담 준비' → **'15초 페이지'**, HERO 프라이머리 '상담 시작' → **'상담 기록'**, 일정 화면 이름 '상담 일정' → **'다가오는 일정'**. 모든 화면·문구에 통일 적용. 아래 본문의 구 용어는 결정 당시 기록이다.

**2026-07-29 D45로 전면 재구성** (ADR-0018) — D43의 재설계 봉인이 이 결정으로 풀렸다(CCC-15 착수 가능). 한 화면, 스크롤 최소, 15초 훑기는 불변.

화면 골격(위에서 아래로):

1. **HERO 카드** — 유지 (D37·D38, `ParticipantHeroCard`. 이름 `이름 (가명 ID)` + '상담 준비' 상태 태그 + 메타 한 줄, 우상단 행동 2개: `당사자 정보` 세컨더리 → `상담 시작` 프라이머리. 상세 DESIGN.md §5)
2. **리스크 경고 배너** — 유지: 확인된 플래그가 있을 때만 HERO 바로 아래, 접힘 불가 (D9)
3. **전체 목표 카드** — 카드형 한 줄로 방향 표시(점수·게이지 없음, D43). 비어 있으면 "설정 전" + **그 자리에서 바로 입력·수정**(**담당 실무자 + 기관 관리자** — 2026-07-30 Q 결정으로 구 '담당 실무자만' 대체, ADR-0018 개정) — 첫 상담에서 당사자와 대화로 정한 뒤 바로 적는 흐름(D43 미결 해소). **2026-08-09 D62 개정**: 주 입력 자리는 인테이크 기록 화면으로 옮기고 이 카드는 보조 입력·수정 자리다. 카드 아래에 활성 세부 목표를 기본 펼침 최대 3줄로 표시한다(ADR-0032)
4. **영역 ① 오늘 만나기 전 꼭 기억할 것** — 구 '지난 상담 브리핑'·'오늘 확인할 질문' 카드를 대체. **실무자 입력(세션 목표·맞춤형 질문)이 위, AI 제안이 아래**. AI 제안은 최대 3개, 각각 **짧은 제목 + 확인 이유 + 근거 회차/원문 링크** 필수(근거 없는 항목 금지). 재료는 공식 기록만(수기 메모 + 승인된 AI 정리, R2)
5. **영역 ② 상담 내용 회차별 정리** — 회차마다 한 줄: **상담일 · 상담 유형 · 핵심 한 줄**. 유형은 현행 2종(인테이크/기본 상담)으로 시작, 세분 유형(초기상담·사정·개입 등)은 §8 미결과 함께. 핵심 한 줄은 AI가 만들고 승인 흐름에서 함께 승인(R2), 승인 전·녹음 없음이면 수기 메모 앞부분 + '수기' 배지(D5). '승인 대기 N건' 배지는 이 영역 머리
6. **영역 ③ 내용 불일치** — 회차 간 불일치 + 회차 내 모순. **AI는 판단하지 않고 양쪽 원문 인용 + 회차 링크만 나란히**(R5). 검출은 기록 공식화 시점(수기 저장·AI 승인)에 실행·저장, 브리핑은 저장된 결과만 읽는다. 처리 3종(**상황 변경/기록 오류/확인 완료**)은 표시일 뿐 **원본 불변** — '기록 오류' 처리 시 해당 기록 옆에 처리됨 표시만. 권한은 담당 실무자·기관 관리자(D7), 전건 감사(D14), 처리 항목은 접힌 이력으로 보존
7. **미해결 액션 · 개인정보 카드** — 유지 (개인정보는 역할 기준 직표시, 감사 조회당 1건 — D24, ADR-0005)
8. **`자세한 상담 기록 보기`** — 맨 아래 한 줄, 전체 상세 기록의 입구 (D37)

GAS 아코디언은 코드에서 제거한다(D43 이행 — 스키마·데이터는 유지). 감정은 추이 지표로만 표시한다. 레이아웃 수치는 D37 계약을 따른다.

`yellow` **아직 없는 것**: **추가 정보(확장 슬롯 자동 생성 필드)** 는 **보류**다 — 기록지가 고정 폼이 되어 조립할 것이 없다(D52·ADR-0023). **Q 입력 대기가 아니다**: 항목 풀 미결은 2026-07-31 해소됐고(8장), D29를 재개할 때 함께 푼다. **상담 유형 세분화는 여전히 8장 미결**이다.

## 7. 작업 순서

**진행 상황 파일을 두지 않는다** (2026-07-31 Q 지시로 `STATUS.md` 폐지). 한 일은 **커밋 메시지**, 다음 할 일은 **티켓**이다 — 실행 티켓은 Linear(워크스페이스 CCC), 스펙은 GitHub 이슈다(아래 'Issue tracker').

`yellow` **없앤 이유**: `STATUS.md` 가 4일 만에 132,000자(est 33,000토큰)로 불어 이 파일의 4배가 됐는데, 매 세션이 같은 두 줄(Last updated 도장 · History 최신 행)을 고쳐 동시 세션이 계속 충돌했다. 커밋과 티켓은 이미 있는 기록이고 자동 병합된다. **옛 내용은 지워지지 않았다** — `git show <폐지-직전-커밋>:STATUS.md` 로 언제든 꺼낸다.

## 8. 미결 사항 (작업 전 확인)

- `yellow` **STT 실측 게이트 G1~G3 미실행**(D53): G1 후보 엔진이 처리 장비에서 구동되는가 · G2 한국어 **대화체** 실오디오에서 CER·반복률이 수용 기준을 넘는가(공개 수치는 전부 낭독체라 상담 성능을 보증하지 않는다) · G3 엔진 맞대결. 처리 장비 앞 세션 필요
- 감정 모델 실측: 실제 상담 음성 샘플로 정확도 검증 (특히 2인 대면·당사자 발화 분리 조건)
- 녹음 캡처 표준화: 조사가 CONFIRMED로 판정한 **가장 큰 레버는 모델이 아니라 마이크 거리·SNR**(근접 4.2% vs 원거리 14.3% WER). 조용한 상담실 + 테이블 중앙 근접 마이크 배치를 운영 지침으로 정해야 한다 — 코드가 아니라 Q 액션
- 기관 규모: Cloudflare Access 무료 한도 50명 이내인지 확인
- Cloudflare 기관 계정 개설 + Access 설정: 현재 미개설. 스캐폴드는 로컬 실행 기준으로 진행하고 계정 개설은 병렬 액션 (D16)
- ~~항목 풀 확정~~ `green` **해소(2026-07-31 D52)**: 인테이크는 D41(`PRD/intake-questionnaire-v1.md` 정본), 정기 상담은 **고정 폼**으로 확정하고 D29의 AI 조립을 보류했다 — 항목 풀은 상담 기록 작성 화면의 칸 목록이며 Q가 새로 제공할 것이 없다(ADR-0023). 상담 유형 세분화만 아래 미결로 남는다
- 상담 유형 세분화: 현행 2종(인테이크/기본 상담)으로 시작. 초기상담·사정·개입 등으로 나눌지와 sessions 유형 컬럼은 미결
- 동의서 문안 확정: 동의는 **2종**이다 — ① 개인정보 수집·이용 ② AI를 활용한 녹취기록(녹음·전사·감정 분석 + 텍스트 AI 정리를 한 항목으로, **D49가 D15의 분리 동의를 대체**). PII 마스킹의 잔여 리스크 고지 포함. 열람 범위에 "담당 실무자 및 기관 관리자" 명시 (D25) + **"같은 기관 실무자에게는 참여 중인 사업 목록과 담당 실무자 이름이 보인다"(D36)** 추가. ~~법률 검토 필요~~ **법률 검토는 MVP 범위에서 종료(2026-07-30 Q 결정)** — 현행 문안으로 충분, 패킷은 보존, 실제 당사자 운영 개시 전 재개(상세: `docs/consent/consent-implementation-gates-v1.md` '법률 검토 트랙 종료 기록')
- PII 보관 기간 확정: 기본 1년으로 결정(D32) — 내부 규정·사업 지침 근거 확인은 Q 액션. 보존 상한은 5년으로 확정(D46). ~~아카이브 보존 상한·최종 파기 시점은 동의서 법률 검토와 함께~~ **동의 게이트·법률 검토 두 트랙 모두 MVP 범위에서 종료(2026-07-30 Q 결정)** — 실서비스 개시 전 재개(D46, 게이트 문서 §3·§5)
- `yellow` **보존·파기 파이프라인 전체 미구현** (2026-07-30 실측으로 등급 하향 — 구 `red` "아카이브 전환 미구현, cron 이 즉시 파기함"은 **사실이 아니었다**): 아카이브 전환이 없는 것은 맞지만 **파기도 일어나지 않는다.** `purge_due` 에 값을 넣는 코드가 레포 전체에 0건이고 `closeSupportCase` 도 설정하지 않아, D10 의 보존 시계가 **애초에 시작되지 않는다**(모든 쓰기가 `purge_due = NULL`). 파기 cron 은 배포돼 있으나 후보를 영영 찾지 못한다 — 운영 D1 실측: vault 22행 전부 미파기, `purge_due` 보유 0건, 도래 0건, 종결 케이스 0건. 따라서 **데이터가 지워질 위험은 현재 없고**, 남은 일은 보존 시계 설정 + 아카이브 전환 + 5년 상한 큐를 **함께** 만드는 것이다. G2 의 법적 보류(브랜치 `feat/consent-g2`)는 그 위에 얹는다. **실서비스 개시 전 필수** (D10·D32·D46)
- ~~운영 D1 마이그레이션 1개 미적용(0029 텍스트 일감 큐)~~ `green` **해소(2026-08-01 같은 날 2차 적용)**: 0012~0028 에 이어 0029 도 2026-08-01 운영 적용 완료 — Time Travel 북마크 기록·`ai_text_work_queue` 표 생성(0행)·`wrangler d1 migrations list` 미적용 0건. 2026-08-05 운영 배포 preflight 에서 "운영 D1 스키마 = 레포와 같음" 재확인. 적용 기록: `artifacts/prod-migration-2026-08-01/0029.md`
- 플래그 고정 유형 목록 확정: 4~6종 초안(위기 발언, 연락 두절 위험, 주거·생계 급변, 부채 악화, 약속 불이행 반복)을 현장 실무자와 검증 (D9)

## 9. 확정된 설계 결정 (2026-07-08 인터뷰 D1~D15 · 2026-07-08 그릴링 D16~D18 · 2026-07-15 그릴링 D19~D23 · 2026-07-17 그릴링 D24~D29 · 2026-07-18 그릴링 D31 · 2026-07-24 결정 D32 · 2026-07-25 결정 D34 · 2026-07-26 결정 D33 · 2026-07-26 그릴링 D35 · 2026-07-26 결정 D36 · 2026-07-27 결정 D37 · 2026-07-27 그릴링 D38 · 2026-07-27 그릴링 D39 · 2026-07-28 그릴링 D40~D43 · 2026-07-29 결정 D44 · 2026-07-29 그릴링 D45 · 2026-07-29 결정 + 2026-07-30 종료 D46 · 2026-07-30 그릴링 D47 · 2026-07-30 결정 D48 · 2026-07-30 결정 D49 · 2026-07-31 결정 D50 · 2026-07-31 그릴링 D51~D53 · 2026-07-31 그릴링 D54 · 2026-07-31 결정 D55~D56 · 2026-08-01 결정 D57 · 2026-08-02 그릴링 D58 · 2026-08-04 그릴링 D59 · 2026-08-05 결정 D60 · 2026-08-07 결정 D61 · 2026-08-09 그릴링 D62)

> **2026-07-31 압축**: ADR 문서가 따로 있는 24개 결정은 한 줄 색인으로 줄이고, **원문은 한 글자도 바꾸지 않고** `docs/decisions-detail.md` 로 옮겼다(이 장이 64KB 중 43KB를 차지해 매 세션 약 10,700토큰을 먹고 있었다). ADR 이 없는 29개 결정은 여기가 유일한 사본이라 그대로 뒀다. **판정 우선순위는 불변** — 본문과 충돌하면 9장이 우선하고, 색인 행으로 부족하면 원문 파일을 읽는다.
>
> D1~D38 행은 결정 당시의 구 용어(참여자·상담사·담당자·조직·시스템 관리자·배정 책임자)를 그대로 둔다(역사 기록). 현행 용어의 정본은 D40과 CONTEXT.md다.
> D39 행만 예외로 신 용어로 쓴다 — 결정 날짜는 D40 이전이지만 이 문단이 **처음 쓰이는 시점**이 D40 이후라, 구 용어로 적었다가 다시 고치는 왕복이 생긴다.

| # | 결정 | 내용 |
| --- | --- | --- |
| D1 | 사용 범위 | **D27이 대체(2026-07-17)** — 구: 내부 전용, 조직 ID만 유지 |
| D2 | 전사 PII | 2단 방어: vault 등록값 결정론 치환 + Mac Mini 로컬 NER 마스킹 후 API 전송. 잔여 리스크는 동의서에 고지 |
| D3 | PII 암호화 | 앱 레벨 AES-GCM, 키는 Cloudflare Workers 환경 시크릿 |
| D4 | 상담 방식 | v1은 대면만. 전화·화상은 수기 경로로 처리, 파일럿 이후 확장 |
| D5 | 승인 공백 | 수기 메모는 즉시 공식 기록. AI 산출물만 승인 대상. 미승인 시 브리핑은 수기 메모 폴백 + 승인 대기 배지 |
| D6 | GAS 주체 | AI는 목표별 근거 발언 발췌만, 점수 선택은 상담사 |
| D7 | 접근 모델 | 담당자+관리자 열람. case_assignees로 공동 담당·이관. 모든 접근 audit_log |
| D8 | 처리 SLA | 다음 영업일 내. 폴링 6시간 중단 시 관리자 알림. 실시간 약속 없음 |
| D9 | 리스크 플래그 | 사전 정의 고정 유형 4~6종. AI 제안은 전사 발언 인용 필수(사실 표시, 진단 아님) |
| D10 | 보존·파기 | 종결 후 유예기간 경과 시 pii_vault만 파기, 가명 기록은 통계용 보존. closed_at·purge_due 컬럼. 실행 방식(경과 시 즉시 파기)은 **D32가 대체(2026-07-24)** — 아카이브 전환 후 별도 파기 |
| D11 | 감정 분석 대상 | 참여자 발화만. 화자 매핑은 자동 추정 + 검토 화면에서 상담사 1회 확인 |
| D12 | 목표 변경 | **D33이 대체(2026-07-26)** — 수정 금지·종료+신설 원칙은 GAS가 붙는 **세부 목표에만** 남고, 전체 목표는 수정 가능. 구: 문구 수정 금지. 기존 목표 종료(사유 기록) + 새 목표 신설. GAS 이력 연속성 보존 |
| D13 | Mac Mini 보안 | R2·D1 직접 접근 금지, Workers API만 호출(Access 서비스 토큰). FileVault, 중간 파일 즉시 삭제, 전용 계정 |
| D14 | 감사 범위 | 열람+변경+PII 복호화+내보내기 전부 기록. audit_log는 append-only |
| D15 | 미동의 경로 | 녹음 미동의여도 수기 메모의 텍스트 AI 정리(요약·스키마 초안·브리핑 질문)는 제공. 동의서는 녹음/텍스트 항목 분리 |
| D16 | 스캐폴드 선행 | 양식 확정 전에 웹 3종(화면·API·DB 마이그레이션+Guard Hook) 스캐폴드를 실행 가능한 코드로 먼저 작성. 수용 기준은 로컬 실행(Cloudflare 계정 미개설). 양식 의존 3건(승인 UI 상세, 대조 프롬프트, ai_schema 내부 구조)은 양식 확정 후 |
| D17 | 레포 구조 | 한 레포, 배포 2단위(apps/web + apps/api). Mac Mini 스켈레톤은 2단계로 미루고 Workers 쪽 API 계약만 1단계에 정의. 상세는 docs/adr/0001 |
| D18 | 양식 경계 | **D29가 대체(2026-07-17)** — 구: 외부 팀 양식 회신 대기. 양식 팀 의존 폐기, 항목 설계 주도권은 내부(Q가 항목 풀 제공) |
| D19 | 플랫폼 | 웹 우선(반응형). PWA는 외부 배포 시, 네이티브는 필요 증명 시에만. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0003 |
| D20 | 가명 ID 형식 | 동물 슬러그+순차번호(swallow-003). **한글 표시는 D31이 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0004 |
| D21 | 상담 일정 화면 | '오늘 상담'+'상담 기록'을 '상담 일정'으로 병합. 오늘/다가오는 2섹션, 카드 클릭→상담 준비. **셸 부분은 D35가 대체**. 원문: [decisions-detail](docs/decisions-detail.md) |
| D22 | 상담 준비 개편 | HERO+리스크 배너+GAS 라디얼+아코디언 4종. **PII 표시는 D24가, 아코디언은 D45가 대체**. 원문: [decisions-detail](docs/decisions-detail.md) |
| D23 | 동의 기록 | 동의는 오프라인(종이/구두)으로 받고, 시스템엔 참여자 등록 시 항목별 체크(녹음 동의·텍스트 AI 동의 분리, D15)+일시+기록자만 저장. 전자서명은 동의서 문안 법률 검토 후 |
| D24 | PII 화면 표시 | 역할 기준 실명 기본 표시. **D22 마스킹 부분 대체**, 잔여는 **D31이 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0005 |
| D25 | 배정 책임자 PII | PII 열람을 담당자와 동일 확대. 상담 내용은 여전히 본인 담당만. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0002 |
| D26 | 참여자 직접 접속 | 당사자 본인이 링크로 가입·작성하는 별도 영역. 이번엔 IA 분리까지만, 입력은 실무자 대행 유지(D23). 구현은 법률 검토 후. 원문: [decisions-detail](docs/decisions-detail.md) |
| D27 | 멀티테넌트 | 여러 기관이 각자 쓰는 서비스로 확정. **D1 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0007 |
| D28 | 세션 목표 | 상담 등록 시 '이번 상담의 목표'를 케이스 목표 하위로 기록. **D33이 대체**. 원문: [decisions-detail](docs/decisions-detail.md) |
| D29 | 상담 템플릿 | AI가 회차별 상담 템플릿 생성. **D18 대체 → D52가 보류**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0006 |
| D30 | 디자인 레퍼런스 락 | shadcn 골격+웜 뉴트럴. **색 규율은 D34가 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0008 |
| D31 | 가명 ID 재정의 | 가명 ID는 기계 식별자·경계 토큰. **D20 한글 표시·D24 잔여 마스킹 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0009 |
| D32 | 보존·아카이브 | 기간 경과 시 즉시 파기 아닌 **자동 아카이브 전환**. **D10 실행 방식 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0010 |
| D33 | 목표 3층 구조 | 전체→세션→세부. GAS는 세부에만. **D12·D28 대체 → D43이 부분 보류**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0013 |
| D34 | 파스텔 3색 액센트 | 웜 뉴트럴+파스텔 3계열+리스크 레드. **D30 색 규율 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0012 |
| D35 | 사업 워크스페이스 IA | 좌측 사이드바 240px, 사이드바=장소·우상단=행동. **D21 셸 부분 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0014 |
| D36 | 참여자 허브 열람 범위 | 상담 내용은 담당만, 참여 사업 존재·담당자 이름은 기관 내 공개. **D7 재진술**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0014 |
| D37 | 레이아웃 계약 | 1440 캔버스 역산 — 사이드바 240+컨테이너 1120, 브레이크포인트 767px 하나. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0015 |
| D38 | 참여자 HERO 공통 계약 | `/participants/:id/**` 는 공통 `ParticipantHeroCard` 를 단다 — 고정 1층(이름)+슬롯 3층(상태 태그·메타 한 줄·행동 버튼 최대 2개). 상세 DESIGN.md §5. 원문: [decisions-detail](docs/decisions-detail.md) |
| D39 | MVP 가입 흐름 프로토타입 | 온보딩→실무자 초대→당사자 가입 링크→인테이크→브리핑 화면 완주. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0016 |
| D40 | 용어 개편 | 조직→기관, 상담사→실무자, 참여자→당사자, 관리자 역할 통합. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0017 |
| D41 | 인테이크 정본 양식 | Q 제공 질문지 v1 로 확정 — 4부 구성, 전 항목 필수+무응답 원칙, 기본정보 PII 는 금고에만 저장, 인테이크는 고정 폼, '긴급도'는 리스크 플래그와 별개. 정본 `PRD/intake-questionnaire-v1.md`. 원문: [decisions-detail](docs/decisions-detail.md) |
| D42 | 인테이크 화면 계약 | 위저드를 질문지 4부와 1:1 인 4단계로. 1단계 기본정보는 표시만, 동의 UI 는 당사자 등록으로 이동, **목표 입력 전부 제거**, 금고 4컬럼 전부 암호화. 원문: [decisions-detail](docs/decisions-detail.md) |
| D43 | GAS 보류 + 목표 2층 운영 | **GAS 와 세부 목표 층 전체 보류**(스키마·데이터는 유지). 전체 목표·세션 목표 2층만 운영. **D33 부분 보류, 브리핑 봉인은 D45가 해제**. 원문: [decisions-detail](docs/decisions-detail.md) |
| D44 | 동의 3종 흐름 | 동의는 당사자 등록 시 받고 정보 페이지에서 수정·철회, 인테이크는 읽기만. 현재값은 `support_cases.consent_*_at`, 이력은 append-only. **① 지위는 D46이, '3종' 표기는 D49가 대체**. 원문: [decisions-detail](docs/decisions-detail.md) |
| D45 | 브리핑 3영역 재구성 | ①꼭 기억할 것 ②회차별 정리 ③내용 불일치. **D22 아코디언 대체, D43 봉인 해제**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0018 |
| D46 | 동의 게이트·보존 상한 | 개인정보 동의를 등록 **하드 게이트**로 승격(긴급 등록은 예외). 보존 **총 5년 상한** 후 파기 검토 큐 → 기관 관리자 확인. 마스킹을 질병명까지 확대. **2026-07-30 구현·법률 검토 두 트랙 모두 MVP 범위에서 종료**(G2 는 구조·API 까지, 실서비스 개시 전 재개). 게이트 문서 `docs/consent/consent-implementation-gates-v1.md`. 원문: [decisions-detail](docs/decisions-detail.md) |
| D47 | 상담 기록 화면 정리 | 최신 회차만 펼침, GAS 제거, HERO 버튼 2개, 리스크 배너 없음. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0019 |
| D49 | 동의 2종 통합 | 개인정보 + AI 녹취기록 2종. **D15 분리 동의·D44 3종 표기 대체**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0021 |
| D50 | 셸 내비게이션 5건 | 기관명=홈, 사업 전환기 드롭다운, 로그아웃 신설, 뒤로가기 노출. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0014 |
| D51 | AI 처리 위치 | LLM 호출은 처리 장비에서만. 텍스트 일감 표 신설, 동의 배선. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0022 |
| D52 | 상담 기록지 고정 | 정기 상담도 고정 폼. **D29·ADR-0006 보류**. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0023 |
| D53 | STT 안전장치 선행 | 무음 경계 분할+반복 검사+엔진 추상화 먼저. 엔진 확정은 G1~G3 이후. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0024 |
| D55 | 디자인 토큰 계단 | **색 외 축의 빈자리를 채운다(2026-07-31)**: 글자 크기 토큰이 하나도 없어 `font-size` 가 전부 원시 px 이었고 14px 한 값이 다섯 역할에 쓰였다(89곳). 크기 5단·행간 6종·광학 하프스텝 4종을 신설하고 리터럴 200여 곳을 치환(**시각 변화 0**). 버튼의 일반↔강조 구분을 테두리에서 **면·깊이**로 옮기고 호버·누름 신설, 체크박스·라디오·고른 행의 켬을 **프라이머리와 같은 그라데이션 면 채움**으로(Q 결정), 카드 안 좌우 패딩을 24 로 통일. 대체된 결정의 잔여 CSS 68개(클래스의 19%)와 D35 가 기각한 브레드크럼 1곳 제거. **색값은 한 개도 바꾸지 않았다** — pen 이 SSOT 다. `pnpm guard:tokens` 를 CI 에 연결해 계약을 결정론으로 강제한다. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0025 |
| D56 | 다크 모드 | **`<html data-theme="dark">` 로 토큰만 갈아끼운다(2026-07-31, `data-contrast` 와 같은 모양 — 마크업 변경 0)**. pen 에 다크 값이 없어(그 Dark 선언은 §8 이 폐기한 Pencil 템플릿) 값은 **`[도출]`·잠정**이고 Q 가 덮어쓸 수 있다. 오래 가는 것은 값이 아니라 **뒤집히는 계약 셋**이다 — ① tint 층이 뒤집혀 deep 이 자기 tint 보다 밝아진다(색상 H 는 불변) ② 깊이가 그림자에서 **면의 밝기 단계**로 바뀐다 ③ 채운 면은 두 테마에서 같다(체크 획이 data URI 안 hex 라 `var()` 가 닿지 않는다) → `--on-action`·`--line-on-action` 신설. **토글은 사이드바 발치**에 있다(로그아웃과 같은 서버 액션 폼 — 쿠키를 서버가 써야 첫 페인트부터 테마가 맞는다). `prefers-color-scheme` 만 **일부러 안 붙였다**: 자동 감지는 Q 가 안 본 팔레트를 검수자에게 내보낸다. `green` **§9 접근성 예외 7건이 다크에서는 전부 해소된다**(deep/tint 1.76~2.11 → 8.53~9.20 · 리스크 2.72 → 7.46) — §9 는 라이트 전용 표가 된다. 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0026 |
| D48 | 날짜 입력 두 갈래 | 아는 값=글자 입력칸, 먼 날짜=달력+직접 입력 병행(react-day-picker). 원문: [decisions-detail](docs/decisions-detail.md) · 상세 ADR-0020 |
| D57 | AI 사업자 = OpenAI + 마스킹 스냅샷 게이트 | **ADR-0022 의 사업자 정체성 부분 대체**(처리 위치 결론은 유지·강화). ① 사업자는 **OpenAI** — 코드는 ADR-0022 이전부터 OpenAI 를 부르고 있었고 ADR-0022 의 "호출 코드 0건"은 `anthropic` 만 grep 한 결과였다. OpenRouter 는 수령자가 단일하지 않아 국외 이전 분석 전제를 깨므로 기각. ② **사업자로 나가는 텍스트는 장비 2차 마스킹 스냅샷뿐** — 불일치 검출이 수기 메모 원문을 1차 치환만 걸어 보내던 R3 위반을 닫는다(켜진 적은 없어 실제 유출 없음). ③ **텍스트 일감 큐** 신설(마이그레이션 0029)로 수기 메모 회차도 장비 마스킹을 거친다. ④ **동의 근거 배선** — ② 체크가 `pilot_text_ai_consent_evidence` 행을 문안 버전·해시와 함께 만든다. 상세 ADR-0027 |
| D54 | 전체 일정 화면 | 지난·앞으로를 한 달 창으로 함께 훑는 화면. **날짜별 목록**(달력 격자 아님), 필터 없음. **D35를 채우는 것이지 바꾸는 것이 아니다** — 사업 범위·담당 범위·가로축은 기존 결정이 닫는다. 서버가 달을 정하고(기관 시간대), 새 SQL·새 색·새 마이그레이션 0개. 원문: [decisions-detail](docs/decisions-detail.md) |
| D58 | 디자인 리프레시 v1 | 색값 불변(pen SSOT 유지) — 쓰임 규칙 7건 개정(2026-08-02): ① 그라데이션은 형태로 가른다 — 알약은 기본(중요=채움·일반=`--gradient-brand` 아웃라인), 카드는 선택·활성만, 리스크 배너 테두리는 `--risk` 단색으로(**D55 세컨더리 테두리·2026-07-26 배너 결정 부분 대체**. **①의 알약 기본은 2026-08-06 Q 부분 개정: 이동·보기 조작 알약(뒤로·시간순·달 이동)은 그레이 아웃라인(neutral) 단이고, 컬러는 중요 행동만 — DESIGN.md §5 버튼 5종**) ② 모션 3종(흐름·눌림·떠오름)만 허용, §6 v1 스크롤 금지 개정 — **떠오름은 2026-08-04 폐지, 모션은 흐름·눌림 2종(트리거는 사용자 입력뿐, ADR-0028 개정 기록)** ③ 크기 5단·하한 14 유지 + 역할표를 계약으로 승격 ④ 글자색 다양화는 배지 중심 + 계열 의미 고정(블루=일정·민트=상태·라벤더=AI·핑크=경고) ⑤ 사이드바 280px·뷰포트 고정·하단 묶음 상시 노출(**D37 부분 개정**) ⑥ 단독 클릭 요소는 전부 알약, 인라인 참조만 텍스트 ⑦ 아이콘은 사이드바 SVG 시스템 공용 승격, 문자 글리프 금지. 상세 ADR-0028 |
| D59 | 카드 성격 규칙 + 가명 ID 표시 폐지 | ① **카드는 성격으로 가른다**(2026-08-04): 클릭·집어 드는 단위 + 강조 컨테이너(HERO·리스크 배너·안내줄) + 폼만 카드, 읽기 구획(브리핑 3영역·설정·날짜 묶음·전체 목표)은 플랫(제목+`--gradient-brand` 1px 구분선+여백) — **①의 플랫 규칙은 D60 이 대체(2026-08-05)**. 카드 안 카드 금지 — 카드 안 묶음은 `--line` 구분선(유지) ② **가명 ID 는 화면에 표시하지 않는다** — 백엔드 전용(**D31 화면 표시 정당화·2026-07-26 이름 표기 대체**). **②는 2026-08-06 Q 부분 재개정: 당사자 카드(일정·당사자 목록)의 정보 칸에는 이름 옆에 표시한다**(DESIGN.md §5 '당사자 카드'). 이름 없는 두 경우(무응답 등록·파기 후)만 이름 자리 폴백. 동명이인 구분은 **전화번호 전체**를 이름 옆에(D24 틀 안) ③ 당사자 허브 HERO: 이름 h2(18)+연락처 나란히(구 개인정보 접힘 폐지), `--nudge-hangul` 0 확정(실측 — 잉크는 이미 중앙). **③의 허브 이름 크기는 2026-08-09 Q 부분 개정: h2 단 폐지, HERO 실명은 전 화면 hero 28 통일(CCC-77, DESIGN.md §5)**. 상세 ADR-0029 |
| D60 | 아웃라인 카드 전면 통일 | **D59 결정 1(읽기 구획 플랫)을 대체**(2026-08-05, Infisical·Cloudflare·Vercel·Supabase 레퍼런스): ① 본문 카드에서 **그림자 폐지** — 경계는 선 2종(비선택 `--line` 1px / 선택·활성 `--gradient-brand` 1px), 그림자는 떠 있는 층(팝오버·스티키·모달)만 ② 플랫 구획을 **전부 카드 컴포넌트로 복귀**(WireCard·WireCardDetails·tone="info" 안내줄 — 손 카드 div 금지, 답답함의 원인이던 그림자는 ①이 제거) ③ **여백 3단**: 페이지 스택 24 / 목록 안 카드 20 / 행 스택 12 ④ **호버·선택 테마 규칙** — 면 호버는 tint 쌍, 선택은 두 테마 공통(그라데이션 아웃라인·`--gradient-action`+`--on-action`), 반전 호버는 라이트 "어두운 면+그라데이션 글자" ↔ 다크 "그라데이션 면+`--on-action` 글자". 색값 변경 0(pen SSOT 불변). 상세 ADR-0030 |
| D61 | 디자인 규칙 리팩터 | 색값 변경 0(pen SSOT 불변, 2026-08-07 Q 지시 12건): ① **버튼 전 종류 직사각 radius 6**(D58 ⑥ "전부 알약"의 형태 부분 대체 — 알약은 배지 전유물, 원형은 32px 아이콘 버튼·라디오만) ② **배지 단일 계약** — 화면별 복사본 8종을 `.wire-badge`(WireBadge)+톤 5종(risk 신설)으로 통합, HERO 상태 태그도 `.wire-status-tag` 하나로 ③ **타이포 짝 계약** — 카드 제목 18/600+본문 16/400, 소제목 16/600+설명 14/400, 카드 안 구획 제목은 14/600 `--sub` 라벨, 메타·설명은 400 ④ 카드 패딩 4종(본문 24 사방)+구분선 `.wire-card-divider` 단일화 ⑤ **콜아웃(WireCallout, 톤 3종)·인용(WireQuote) 컴포넌트 확정**, 손 안내 상자 금지 ⑥ 그라데이션 어휘 요약표 승격(3색은 셸 프레임 전용) ⑦ 화면 문자열 긴 대시·구분자 가운뎃점 제거 + `guard:tokens` 검사 2종(pill 허용목록·부호) 신설. 상세 ADR-0031 |
| D62 | 목표 위계·수립 흐름 재정의 | **위계를 전체 > 세부 > 세션으로 재정렬**(D33 층 순서 대체. DB 는 이미 이 방향이라 구조 변경 없음, 2026-08-09 그릴링+외부 검수 1회): 수립은 상담 단계에 맞춰 3번. ① 인테이크=전체 목표(인테이크 기록 화면에 칸 신설, D42 부분 개정. D45 의 주 입력 자리를 대체하고 15초 페이지 카드는 보조) ② 본 상담 1회차=세부 목표(D43 부분 해제, 문구·연결·상태만 부활, GAS 는 계속 보류) ③ 매 회기=세션 목표(일정 시작 시각 전까지 수정, 후 잠금. 수정 기능 신설). 어느 층도 입력 강제 없음, 전체 목표 미설정이어도 AI 제안은 차단 없이 안내 한 줄. 전체·세부 목표는 언제든 수정하되 이전 문구·수정자·시각을 이력으로 보존, D12 수정 금지는 폐지(트리거 제거 마이그레이션 1건). 닫기는 활성/종료 2종+사유(달성/중단/재설정)·재개 불가, 죽은 '종료+신설' 코드 제거. 노출은 당사자 정보에 목표 트리 구획 신설, 15초 페이지에 활성 세부 목표 기본 펼침 최대 3줄. 상담 1건=케이스 1개 유지. 상세 ADR-0032 |

## 10. 조직 시크릿 접근 (bss-infra)

조직(SSB) 시크릿이 필요하면 이 경로만 쓴다 (Cloudflare 시크릿·AES 키 등 2단계부터 해당):

- 값의 원본은 **Infisical**. 사람용 금고(Bitwarden)와 그 마스터 비밀번호는 절대 조회·요청하지 않는다.
- 에이전트 인증(읽기 전용 Machine Identity, 이 Mac에 배치 완료):
  ```bash
  set -a; source ~/.config/infisical-agent/credentials; set +a
  infisical run --projectId=<ID> --env=<env> -- <명령>
  ```
  값은 주입만 한다 — `secrets get` 등으로 stdout·채팅·로그에 출력 금지.
- **값이 stdout에 닿는 명령은 실행 자체 금지**: `infisical secrets`/`export`/`get`, `--plain`, 자격증명 파일 열람. 이름 확인이 필요해도 값 섞인 출력을 만들지 않는다. (과거 실제 노출로 전 키 로테이션을 치른 규칙 — `~/.claude/hooks/secret-dump-guard.sh` 훅이 결정론 차단)
- .env는 이름만 커밋(`.env.example`), 값 커밋 금지. 절차 SSOT: `~/DEVELOPER/PROJECTS/BSS/bss-infra/docs/machine-access.md` (레포 `SocialSolidarityBank/bss-infra`).
- 시크릿 값이 채팅·커밋에 노출되면 즉시 보고하고 해당 키 로테이션을 안내한다.

## 11. 한국어 산문 부호 (2026-08-06 Q)

화면 문안뿐 아니라 이 레포의 한국어 산출물 전반(문서, 커밋 메시지, PR, 티켓, 코드 주석)에서 **긴 대시(`—`)는 쓰지 않고, 가운데 점(`·`)은 자제한다.** 한 호흡으로 읽는 병렬(`이름·연락처·계좌` 류)만 예외. 이유: 둘 다 AI 문체 신호다. 상세와 UI 문자열 규칙은 DESIGN.md §10 (기존 문서 소급 수정은 하지 않는다).

## Agent skills

### Issue tracker

GitHub Issues(`SocialSolidarityBank/CCC`)를 `gh` CLI로 사용한다. 상세: `docs/agents/issue-tracker.md`.
**구현 실행 티켓은 Linear**(2026-07-25~): 워크스페이스 **CCC**(bss-ccc), Composio 백엔드로 접근(절차·함정: `~/developer/tools/portwright/services/linear.md`). 스펙은 GitHub 이슈, 실행·선행 관계는 Linear의 blocks. **티켓 완료 규약**: Linear 티켓에 쉬운 한글 완료 보고 코멘트 + Done 처리 → Linear Slack 연동이 팀 채널로 전달한다.

### 티켓 스레드 규칙 (#ccc-tickets, 2026-07-26 Q 확정 — 위반 시 게시 금지)

에이전트가 `#ccc-tickets`에 올리는 티켓 소식은 **반드시 스레드**로 올린다. 채널 본문에는 부모 글 1개만 두고 상세는 전부 댓글로 밀어 넣는다(티켓 1건 = 스레드 1개, 묶음 금지).

1. **부모 글** = `티켓 번호 · 개발 용어 없는 쉬운 한 줄`. Linear 제목을 그대로 복사하지 않는다
2. **첫 댓글** = 비개발자가 30초에 읽는 분량으로 ① 무엇이 달라지나 ② 왜 하나 ③ 나에게 무슨 영향인가. 파일 경로·함수·마이그레이션 번호·ADR/D번호·완료 기준은 **두 번째 댓글부터**
3. **강조는 이모지 또는 인라인 코드**로, 한 글에 최대 2개(🚨 확인 필요 / ⚠️ 방식 변경 / ✅ 완료 / 🔵 착수 / 🧭 방향 결정)
4. 부모 글과 첫 댓글은 **`humanize-korean` 스킬로 윤문 필수** — 짧고 쉽게 만드는 것이 목적
5. 올리기 전 점검 5개(본문 1개 / 쉬운 한 줄 / 첫 댓글 자족 / 강조 2개 이하 / 윤문) 전부 통과해야 게시한다

전문·예시·나쁨↔좋음 대조: `docs/agents/ticket-thread-protocol.md`.
**채널 분리**: `#ccc-tickets`는 **이 규칙대로 만든 스레드만**, `#ccc-linear`(공개)는 Linear 자동 알림 전문. 채널을 다시 만들 일이 있으면 **공개**로 만든다(Slack에 비공개→공개 전환 API가 없다). 경위·API 확인 결과·운영 ID·검증 함정: `~/developer/tools/portwright/services/linear.md`(레포 밖 — 공개 레포에 운영 ID를 두지 않는다).

### 미리보기 환경 (CCC-6, 2026-07-25~)

팀원 피드백용: https://ccc-preview.account-855.workers.dev — 링크+지정 코드(Infisical 보관)만으로 열람, **가상 시드 전용**(운영 PII·운영 키 미연결), main 머지마다 자동 재배포. 상세·프로비저닝: `docs/ops.md` "미리보기 환경 (CCC-6)".

### Triage labels

기본 5개 역할(needs-triage/needs-info/ready-for-agent/ready-for-human/wontfix) 그대로 사용. 상세: `docs/agents/triage-labels.md`.

### Domain docs

단일 컨텍스트: 루트 `CONTEXT.md` + `docs/adr/`. 상세: `docs/agents/domain.md`.

### Design docs

**화면(페이지·컴포넌트·CSS) 작업 전에 루트 `DESIGN-RULES.md` 를 읽는다** (2026-08-10, ADR-0033). 지금 지켜야 하는 규칙만 추린 요약본이고, 근거와 이력은 `DESIGN.md` 에 그대로 있다(색값 SSOT 는 여전히 pen). 규칙을 바꾸는 커밋은 요약본도 같은 커밋에서 고친다.

### 문서 배치 규칙 (2026-07-25 Q 확정)

- HTML 시안·용어집 등 **아티팩트는 루트 `artifacts/`** (구 docs/artifacts 폐지)
- **PRD·제품 정의 문서는 루트 `PRD/`**
- 폐기 문서는 `docs/archive/` (폐기 헤더 필수)
