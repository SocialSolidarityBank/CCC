# 비영리 사례관리 프로그램: 개발 작업 문서

> 이 문서는 Claude Code 프로젝트 루트에 `CLAUDE.md`로 두는 것을 전제로 작성했다.
> Claude Code는 세션을 시작할 때 이 파일을 읽으므로, 여기 적힌 규칙은 모든 작업에 자동 적용된다.
> PRD 원본: 노션 "비영리 조직 사례 관리툴" 페이지 (문서 제목은 고유명사라 구 용어 유지)
> 2026-07-08 설계 인터뷰로 확정된 결정은 9장에 정리했다. 본문과 9장이 충돌하면 9장이 우선한다.

---

## 1. 프로젝트 한 줄 정의

금전 지원 사업 당사자 상담을 인테이크부터 종결까지 기록하고, AI가 정리한 누적 맥락을 **상담 5분 전에 한 화면으로** 보여주는 웹앱.
핵심 가치는 기록이 아니라 브리핑이다. 모든 설계는 "나중에 5분 안에 훑기 좋은 형태"에서 역산한다.

**사용 범위**: 여러 기관이 각자 쓰는 서비스로 방향 확정(D27, 구 D1 "내부 전용" 대체). 현 단계는 사회연대은행 단일 기관 위에 구현하며, 외부 기관 온보딩·자체 인증은 다음 단계다.

## 2. 확정된 기술 스택

| 구성 | 도구 | 메모 |
| --- | --- | --- |
| 인증 | Cloudflare Access | 기관, 팀 단위 초대제. 역할은 기관 관리자, 실무자 2단계. 공개 가입 페이지 없음 |
| DB | Cloudflare D1 | SQLite 기반. RLS 없음: 아래 개발 규칙 R1 필수 |
| 서버 로직 | Cloudflare Workers | |
| 파일 저장 | Cloudflare R2 | 음성 임시 보관. 수명 규칙으로 30일 뒤 자동 삭제 |
| 음성 처리 | Mac Mini 로컬 | Whisper 전사, pyannote 화자 분리, 감정 분석. 보안 기준은 D13 |
| 요약, 대조 | Claude API | 가명 처리된 텍스트만 전송 (2단 방어, D2) |
| 프론트 | Next.js (Cloudflare Pages 배포) | |

## 3. 데이터 설계 원칙

### 테이블 구조

```
pii_vault        실명, 연락처, 계좌. 앱 레벨 AES-GCM 암호화(키는 Workers 시크릿, D3). 관리자 권한만 조회
cases            가명 ID(동물 슬러그, 예: swallow-003 — D20·ADR-0004), 기관 ID, 담당 실무자 ID, GAS 기준, closed_at, purge_due (D10)
case_assignees   케이스별 담당 실무자 매핑. 공동 담당·이관 지원 (D7)
goals            목표 3층(D33): 전체 목표(케이스당 1개·수정 가능·점수 없음) / 세션 목표(회차별) / 세부 목표(GAS 채점·수정 금지, 종료+신설만). status(active/closed) + closed_reason
sessions         세션 기록. 분류 스키마 필드 + 승인 플래그(approved_at)
action_items     액션 아이템. 담당, 기한, 해결 여부
flags            리스크 플래그. 고정 유형 + 전사 발언 인용 + 실무자 확인(맞음/틀림) (D9)
audit_log        열람·변경·PII 복호화·내보내기 전부 기록. append-only (D14)
```

### 스키마 3층 구조

- 코어(전 케이스 고정): 목표별 GAS 점수와 근거, 액션 아이템, 리스크 플래그, 감정 추이. 브리핑과 통계는 코어만 사용
- 템플릿(사업 유형별): 금전지원형 v1부터 시작
- 확장 슬롯(케이스별): 실무자 자유 추가. JSON 컬럼 하나로 격리, 통계와 브리핑에서 제외

GAS(목표달성척도): **세부 목표마다** -2(악화)부터 +2(기대 초과)까지 5단계 점수(D33 — 전체 목표·세션 목표에는 점수를 매기지 않는다). 세부 목표는 측정 가능한 문장으로 쓴다. **점수는 실무자가 직접 매긴다. AI는 전사에서 근거 발언 발췌만 제안한다 (D6).**

## 4. 개발 규칙 (위반 시 커밋 차단 대상)

**R1. DB 접근 단일 관문**
모든 D1 조회와 쓰기는 `db/gateway.ts`의 공용 함수만 거친다. 이 함수는 기관 ID와 접근 권한(담당 실무자 또는 관리자, D7)을 반드시 검사한다. 다른 파일에서 D1을 직접 호출하는 코드는 금지. Guard Hook으로 `env.DB.prepare` 직접 호출을 gateway 외부에서 발견하면 커밋을 차단한다. 게이트웨이 함수는 audit_log 기록을 내장한다 (D14).

**R2. 승인 전 초안은 공식 기록이 아니다 — 승인 = 정합성 검증**
AI가 생성한 세션 요약은 `approved_at`이 채워지기 전까지 브리핑, 통계, 보고서 어디에도 나가지 않는다. 이 검사를 gateway 함수 안에 내장한다.
승인 화면의 핵심은 **수기 기록 ↔ 녹취 기반 AI 정리의 대조**다. 대조 출력 3종(메모에 없는 내용, 음성에 없는 내용, 미논의 목표)을 실무자가 처리하는 것이 곧 정합성 검증 프로세스다.
실무자가 직접 쓴 수기 메모는 작성 즉시 공식 기록이며 승인 대상이 아니다 (D5).

**R3. PII는 파이프라인에 들어오지 않는다 — 2단 방어**
Claude API 호출부, 로그, 에러 메시지에 실명, 연락처, 계좌가 섞이면 안 된다.
1차: pii_vault 등록값(당사자 실명·연락처·계좌)을 가명 ID로 결정론적 치환.
2차: Mac Mini 로컬에서 개체명 인식(NER)으로 대화 속 제3자 인명·전화번호 패턴을 추가 마스킹한 뒤에만 Claude API로 전송.
완벽하지 않은 잔여 리스크는 동의서 문안에 반영한다 (D2).

**R4. 감정 지표는 숫자만**
"불안하다" 같은 단정 문장을 생성하거나 저장하지 않는다. 점수와 추이만 저장하고, 화면에는 "지난 세션 대비 저조" 수준으로만 표시한다. 감정 점수는 당사자 발화만 집계한다 (D11).

**R5. AI 금지 영역**
지원 지속이나 중단 판단, 심리 진단, 승인 없는 기록 확정 기능은 어떤 형태로도 구현하지 않는다. GAS 점수 산정과 리스크 플래그 확정도 실무자 몫이다 — AI는 근거 발췌와 고정 유형 제안까지만 (D6, D9).

## 5. AI 파이프라인 스펙

```
녹음 업로드(웹앱) → R2 임시 저장
  → Mac Mini 폴링(Workers API 경유, Access 서비스 토큰 인증, D13):
      Whisper 전사 + pyannote 화자 분리
  → 화자 매핑 자동 추정(발화량·패턴) → 검토 화면에서 실무자가 1회 확인 (D11)
  → 감정 분석(당사자 발화만): wav2vec2-xlsr(음성, 낮은 가중치) + HowRU-KoELECTRA(텍스트, 높은 가중치)
  → PII 2단 마스킹(등록값 치환 + 로컬 NER, D2)
  → 결과 텍스트만 Workers 경유 D1 저장, 음성 원본은 R2에서 30일 뒤 자동 삭제
  → Claude API 대조: 전사 vs 실무자 메모
      출력 3종: 메모에 없는 내용(누락), 음성에 없는 내용(확인 필요), 미논의 목표
  → 분류 스키마 초안 생성 → 실무자 검토 화면(대조 3종 + 화자 매핑 + GAS 근거 확인) → 승인 시 approved_at 기록
```

처리 기한(SLA): 업로드 후 **다음 영업일 내** 검토 대기 상태 도달. Mac Mini가 6시간 이상 폴링하지 않으면 관리자 알림. 지연·장애 시 브리핑은 수기 메모 폴백으로 동작하므로 서비스는 멈추지 않는다 (D8).

모델 정보:
- STT: openai/whisper (한국어 검증됨)
- 화자 분리: pyannote.audio
- 음성 감정: jungjongho/wav2vec2-xlsr-korean-speech-emotion-recognition (Apache-2.0, 정확도 0.77)
- 텍스트 감정: LimYeri/HowRU-KoELECTRA-Emotion-Classifier (MIT, 정확도 0.95)
- 라이선스 미표기 저장소는 사용 금지

## 6. 상담 준비 화면(브리핑) 요구사항

한 화면, 스크롤 최소. 2026-07-15 그릴링으로 구조 개편 (D22 — 이전 "5항목 고정·리스크 최우선 배치" 결정을 대체).

- **HERO**: 당사자 실명을 크게 + '상담 준비' 상태 태그 버튼 — **2026-07-18 D31**: 가명 ID 한글 표시(구 D22) 폐기, 실명 기본(실명 없으면 슬러그 폴백). **2026-07-26 D35**: 표기는 `이름 (가명 ID)` 한 줄(D34·DESIGN.md §5). **2026-07-27 D37로 이동 버튼 배치 확정**(구 D35의 "HERO 카드 밖 바로 아래 줄" 대체): HERO는 카드이고 그 안에 **이름 + 상태 태그 + 메타 한 줄**(사업명·일시·대면)이 들어가며, **우상단은 행동 2개**(`당사자 정보` 세컨더리 → `상담 시작` 프라이머리)다. `상담 기록`은 이름 아래에서 사라지고 **맨 아래 `자세한 상담 기록 보기`**로 내려간다. `상담 시작`의 목적지는 기록 작성 화면이다 — 이 앱에 별도 '세션 시작' 상태가 없고 상담 중 입력이 곧 기록지다. '상담 준비'는 데이터가 아니라 화면 상태 태그다(`sourceSupportCase.status`는 active/closed뿐). `← 목록으로`·`전체 사업 보기`는 삭제 — 워크스페이스 복귀는 사이드바가 담당. **2026-07-27 D38**: 이 HERO는 브리핑 전용이 아니라 **당사자 중심 화면 전부(`/participants/:id/**`)의 공통 부품**(`ParticipantHeroCard`)이다 — 고정 1층(이름) + 슬롯 3층(상태 태그·메타·행동). 상태 태그는 케이스 1개 화면에서만 필수, 허브는 생략. 상세 DESIGN.md §5
- **리스크 경고 배너**: 확인된 플래그가 있을 때만 HERO 바로 아래 표시, 접힘 불가 — 기존 "최우선 배치" 원칙을 배너로 구현 (D9)
- **전체 목표**: HERO 아래 한 줄로 방향 표시(게이지 없음, D33)
- **GAS**: **진행 중인 세부 목표**별(최대 3, 가로 배치) 원형 게이지(최신 점수) + 게이지 아래 미니 추이(직전 점수 흐름, 종료 시점 표시). 진행 중이 4개 이상이면 최근 갱신순 3개 + "외 N개" (D33). **2026-07-27 구현 확정**: 제목 '진행 중인 세부 목표'의 **전폭 아코디언 하나**이고 안쪽이 조밀 그리드 3열이다 — 2열 카드(폭 510) 안에 넣으면 최소 폭 280 으로 3열이 안 나오고 계약이 2열을 금지해(세부 목표 3개가 둘 + 외톨이로 앉는다) 게이지가 세로로 쌓인다. 전폭 1040 에서 3열 각 333. 게이지는 **96px 원형**(DESIGN.md §5): 트랙은 계열 tint·채움은 같은 계열 base·중앙 점수 24/700이고, 채움 비율은 −2~+2를 0~100%로 편 값이다. **계열은 점수가 아니라 목표 순서가 정한다**(D6·R4 — 점수의 좋고 나쁨을 색으로 알리지 않는다)
- **아코디언 4종** (섹션 제목 '상담 준비 자료' 아래 웹 2열 / 모바일 1열) — **2026-07-27**: 구 '기본정보' 카드는 없앴다. 담고 있던 셋이 전부 다른 자리로 갔기 때문이다(시간·이름 → HERO, 연락처 → 개인정보 카드, 전체 참여사업 링크 → HERO의 `당사자 정보`). 여닫기는 **버튼 하나**(`전체 접기`↔`전체 열기`)이고 **GAS 아코디언까지 함께** 여닫는다:
  1. 지난 상담 브리핑 — 승인된 AI 요약, 없으면 수기 메모 폴백 + "승인 대기 N건" 배지 (D5)
  2. 오늘 확인할 질문 2~3개 (AI 생성, 승인된 기록 기반)
  3. 미해결 액션 아이템
  4. 개인정보 + 전체 참여사업 확인 링크 — **2026-07-17 D24로 대체**: 담당 실무자·기관 관리자에게는 실명·연락처·계좌를 클릭 없이 기본 표시(역할 기준), 감사는 화면 조회당 1건. 마스킹·전체 보기 클릭 흐름(구 D22) 폐기. 상세 ADR-0005

전체 상세 기록은 클릭해야 펼쳐진다 — 페이지 **맨 아래 `자세한 상담 기록 보기`** 한 줄이 그 입구다(별개 화면이 아니라 이 브리핑의 아래쪽 끝, D37). 감정은 추이 지표로만 표시한다.

`yellow` **아직 없는 것 2가지**(시안 `artifacts/layout-frame-v1/briefing.html`에는 있다): **전체 목표 한 줄**은 D33 3층 목표가 필요해 **CCC-15** 몫이고, **추가 정보(확장 슬롯 자동 생성 필드)** 는 항목 풀이 미확정이라 8장 미결(D29)에 걸려 있다. 시안의 설명 팝오버(`i` 툴팁)도 계약이 아니다 — 승격하려면 `DESIGN.md` §5에 따로 적는다.

## 7. 작업 순서

진행 상황의 정본은 **`STATUS.md`** 다 — 단계별 완료·잔여·다음 재개점이 거기 있고 매 작업 종료 시 갱신된다.
여기에 사본을 두면 두 문서가 갈라져 어느 쪽이 맞는지 알 수 없게 되므로 옮기지 않는다.
실행 티켓은 Linear(워크스페이스 CCC), 스펙은 GitHub 이슈다(아래 'Issue tracker').

## 8. 미결 사항 (작업 전 확인)

- 감정 모델 실측: 실제 상담 음성 샘플로 정확도 검증 (특히 2인 대면·당사자 발화 분리 조건)
- 기관 규모: Cloudflare Access 무료 한도 50명 이내인지 확인
- Cloudflare 기관 계정 개설 + Access 설정: 현재 미개설. 스캐폴드는 로컬 실행 기준으로 진행하고 계정 개설은 병렬 액션 (D16)
- 항목 풀 확정: **인테이크는 D41로 해소**(`PRD/intake-questionnaire-v1.md` 정본, 고정 폼). 정기 상담 항목 풀은 여전히 미결 — Q가 제공하면 상담 템플릿 항목 상세·자유도 설계 착수 (D29). 기록 종류(인테이크/정기 등) 구분과 sessions 유형 컬럼도 이때 함께 확정
- 동의서 문안 확정: 녹음·음성 분석 항목과 텍스트 AI 정리 항목을 **분리 동의**로 설계 (D15). PII 마스킹의 잔여 리스크 고지 포함. 열람 범위에 "담당 실무자 및 기관 관리자" 명시 (D25) + **"같은 기관 실무자에게는 참여 중인 사업 목록과 담당 실무자 이름이 보인다"(D36)** 추가. 법률 검토 필요
- PII 보관 기간 확정: 기본 1년으로 결정(D32) — 내부 규정·사업 지침 근거 확인은 Q 액션. 아카이브 보존 상한·최종 파기 시점은 동의서 법률 검토와 함께 (D10·D32, ADR-0010)
- 플래그 고정 유형 목록 확정: 4~6종 초안(위기 발언, 연락 두절 위험, 주거·생계 급변, 부채 악화, 약속 불이행 반복)을 현장 실무자와 검증 (D9)

## 9. 확정된 설계 결정 (2026-07-08 인터뷰 D1~D15 · 2026-07-08 그릴링 D16~D18 · 2026-07-15 그릴링 D19~D23 · 2026-07-17 그릴링 D24~D29 · 2026-07-18 그릴링 D31 · 2026-07-24 결정 D32 · 2026-07-25 결정 D34 · 2026-07-26 결정 D33 · 2026-07-26 그릴링 D35 · 2026-07-26 결정 D36 · 2026-07-27 결정 D37 · 2026-07-27 그릴링 D38 · 2026-07-28 그릴링 D40~D41)

> D1~D38 행은 결정 당시의 구 용어(참여자·상담사·담당자·조직·시스템 관리자·배정 책임자)를 그대로 둔다(역사 기록). 현행 용어의 정본은 D40과 CONTEXT.md다.

| # | 결정 | 내용 |
| --- | --- | --- |
| D1 | 사용 범위 | **D27이 대체(2026-07-17)** — 구: 내부 전용, 조직 ID만 유지 |
| D2 | 전사 PII | 2단 방어: vault 등록값 결정론 치환 + Mac Mini 로컬 NER 마스킹 후 API 전송. 잔여 리스크는 동의서에 고지 |
| D3 | PII 암호화 | 앱 레벨 AES-GCM, 키는 Cloudflare Workers 환경 시크릿 |
| D4 | 상담 방식 | v1은 대면만. 전화·화상은 수기 경로로 처리, 파일럿 이후 확장 |
| D5 | 승인 공백 | 수기 메모는 즉시 공식 기록. AI 산출물만 승인 대상. 미승인 시 브리핑은 수기 메모 폴백 + 승인 대기 배지 |
| D6 | GAS 주체 | AI는 목표별 근거 발언 발췌만, 점수 선택은 상담사 |
| D7 | 접근 모델 | 담당자+관리자 열람. case_assignees로 공동 담당·이관. 모든 접근 audit_log |
| D8 | 처리 SLA | 다음 영업일 내. 폴링 6시간 중단 시 관리자 알림. 실시간 약속 없음 |
| D9 | 리스크 플래그 | 사전 정의 고정 유형 4~6종. AI 제안은 전사 발언 인용 필수(사실 표시, 진단 아님) |
| D10 | 보존·파기 | 종결 후 유예기간 경과 시 pii_vault만 파기, 가명 기록은 통계용 보존. closed_at·purge_due 컬럼. 실행 방식(경과 시 즉시 파기)은 **D32가 대체(2026-07-24)** — 아카이브 전환 후 별도 파기 |
| D11 | 감정 분석 대상 | 참여자 발화만. 화자 매핑은 자동 추정 + 검토 화면에서 상담사 1회 확인 |
| D12 | 목표 변경 | **D33이 대체(2026-07-26)** — 수정 금지·종료+신설 원칙은 GAS가 붙는 **세부 목표에만** 남고, 전체 목표는 수정 가능. 구: 문구 수정 금지. 기존 목표 종료(사유 기록) + 새 목표 신설. GAS 이력 연속성 보존 |
| D13 | Mac Mini 보안 | R2·D1 직접 접근 금지, Workers API만 호출(Access 서비스 토큰). FileVault, 중간 파일 즉시 삭제, 전용 계정 |
| D14 | 감사 범위 | 열람+변경+PII 복호화+내보내기 전부 기록. audit_log는 append-only |
| D15 | 미동의 경로 | 녹음 미동의여도 수기 메모의 텍스트 AI 정리(요약·스키마 초안·브리핑 질문)는 제공. 동의서는 녹음/텍스트 항목 분리 |
| D16 | 스캐폴드 선행 | 양식 확정 전에 웹 3종(화면·API·DB 마이그레이션+Guard Hook) 스캐폴드를 실행 가능한 코드로 먼저 작성. 수용 기준은 로컬 실행(Cloudflare 계정 미개설). 양식 의존 3건(승인 UI 상세, 대조 프롬프트, ai_schema 내부 구조)은 양식 확정 후 |
| D17 | 레포 구조 | 한 레포, 배포 2단위(apps/web + apps/api). Mac Mini 스켈레톤은 2단계로 미루고 Workers 쪽 API 계약만 1단계에 정의. 상세는 docs/adr/0001 |
| D18 | 양식 경계 | **D29가 대체(2026-07-17)** — 구: 외부 팀 양식 회신 대기. 양식 팀 의존 폐기, 항목 설계 주도권은 내부(Q가 항목 풀 제공) |
| D19 | 플랫폼 | 웹 우선(반응형 웹앱). 외부 배포 시 PWA 추가, 네이티브 앱은 필요 증명 시에만 재검토. 상세 ADR-0003 |
| D20 | 가명 ID 형식 | 희망 상징 동물 영문 슬러그+동물별 순차번호(예: swallow-003). 가입일자 등 의미 정보 금지(역추적 단서). 화면 한글 표시·A017 마이그레이션은 **D31이 대체(2026-07-18)** — 폐기. 상세 ADR-0004 |
| D21 | 상담 일정 화면 | **셸(상단 헤더·메뉴 진입)은 D35가 대체(2026-07-26)** — 화면 내용 결정은 유효. '오늘 상담'+'상담 기록' 메뉴를 '상담 일정'으로 병합. 상단 참여자 검색+선택(선택 시 참여자 페이지로), '오늘의 상담'(펼침)·'다가오는 상담'(향후 7일, 날짜별 그룹, 화살표 아코디언 접힘) 2섹션, 카드 클릭→상담 준비. 액션: 상담 등록·참여자 등록·설정(내 계정+상담사 목록, 시스템 관리자만) |
| D22 | 상담 준비 개편 | HERO 가명 ID+상태 태그, 리스크는 접힘 불가 경고 배너(확인된 플래그 있을 때만), GAS 라디얼+미니 추이, 아코디언 4종(브리핑→질문→액션→개인정보+참여사업, 웹 2열/모바일 1열, 전체 열기/닫기), PII 표시는 **D24가 대체**(구: 부분 노출 마스킹 기본+클릭 복호화). 상세 6장 |
| D23 | 동의 기록 | 동의는 오프라인(종이/구두)으로 받고, 시스템엔 참여자 등록 시 항목별 체크(녹음 동의·텍스트 AI 동의 분리, D15)+일시+기록자만 저장. 전자서명은 동의서 문안 법률 검토 후 |
| D24 | PII 화면 표시 | **D22의 마스킹 부분을 대체(2026-07-17)**: 내부 화면은 역할 기준 실명 기본 표시. "그 외 사용자 마스킹" 잔여 부분은 **D31이 대체(2026-07-18)** — 내부 인증 사용자 전원 실명. 감사는 PII 표시 화면 조회당 1건(유지). 상세 ADR-0005 |
| D25 | 배정 책임자 PII | PII 열람을 담당자와 동일하게 확대(ADR-0002 일부 대체). 상담 내용(세션 기록·브리핑)은 여전히 본인 담당 케이스만. 동의서 문안 열람 범위에 반영 필요 |
| D26 | 참여자 직접 접속 | 참여자 등록·동의서는 최종적으로 참여자 본인이 링크로 가입·로그인·작성하는 별도 화면 영역. 이번 개편에서는 **IA(메뉴 구조) 분리까지만** — 상담사 메인 흐름에서 빼되 입력은 상담사 대행 유지(D23). 구현은 동의서 법률 검토 후 |
| D27 | 멀티테넌트 | **D1 대체(2026-07-17)**: 여러 조직이 각자 쓰는 서비스로 방향 확정. 이번 개편은 새 IA(조직→사업→카드)를 단일 조직 위에 구현, Access 인증 유지. 자체 인증(이메일+패스워드 가입)·외부 조직 온보딩은 다음 단계. 상세 ADR-0007 |
| D28 | 세션 목표 | **D33이 대체(2026-07-26)** — 세션 목표는 전체 목표의 하위가 되고, 그 아래 세부 목표가 신설된다. GAS는 세션 목표가 아니라 세부 목표에 매긴다. 구: 상담 일정 등록 시 "이번 상담의 목표"를 케이스 목표(D12)의 하위 목표로 연결해 기록(복수·미연결 허용). GAS는 여전히 케이스 목표에만. 맞춤형 질문은 AI 생성 질문과 별개로 병행 |
| D29 | 상담 템플릿 | **D18 대체(2026-07-17), AI 역할의 중심**: Q가 제공하는 항목 풀(전 항목 필수/선택 분류)에서 AI가 세션 목표·질문·맥락 기반으로 선택 항목을 재구성해 회차별 상담 템플릿 생성. 코어(GAS 근거·액션·플래그)는 항상 포함, 상담사가 상담 전 수정 가능, AI 실패 시 코어 폴백. 항목 상세·자유도는 보류 — 이번 범위는 화면 플로우까지. 상세 ADR-0006 |
| D30 | 디자인 레퍼런스 락 | **색 규율은 D34가 대체(2026-07-25)** — 화면 개편의 시각 언어를 Refero 리서치로 락: **shadcn/ui 골격 + Dock의 웜 뉴트럴(#faf9f7)·밀도만 차용**, 색은 semantic 전용(리스크 #c22b10이 화면의 유일한 색 — D9를 디자인으로 강제), 타이포 Pretendard(한글 트래킹 -0.01em). 디스플레이 세리프+아이보리 계열은 한글 대체 서체 부재로 기각. 새 색 도입은 ADR 수정 사항. 상세 ADR-0008(색 규율은 ADR-0012) |
| D31 | 가명 ID 재정의 | **D20 한글 표시·D24 잔여 마스킹 대체(2026-07-18)**: 가명 ID는 사람용 표시 장치가 아니라 **기계 식별자·경계 통과 토큰** — ① DB 키·URL·참여자 구분 ② AI 전송 시 실명 치환 앵커(D2 유지) ③ 외부 반출 시 대체 식별자 ④ 파기 후 통계 보존 연결 고리(D10 유지). 내부 인증 사용자(관리자·상담사 전원) 실명 기본 표시, PII 화면 조회 감사(D14)·상담 내용 접근 범위(D7·D25)는 불변. 한글 표시명("제비 003") 폐기, A001 혼재 허용(CCC-archive#15 닫음). 외부 반출 마스킹은 옵션 4종(이름·연락처·이메일·본문 민감 정보 — D2 2차 마스킹 재사용), 기본 전체 적용·해제 선택형. 온보딩 원칙(참여 허들 최소화: 간편 가입+최소 수집)은 방향만 — 채널 확정은 D26 법률 검토 후. 상세 ADR-0009 |
| D32 | 보존·아카이브 | **D10 실행 방식 대체(2026-07-24)**: 보관 기간(기본 1년, 조직 설정 — 내부 규정 확인 후 확정) 경과 시 PII를 즉시 파기하지 않고 **자동 아카이브 전환** — 열람 권한 축소(일반 조회 제외, 예외 열람 전건 감사). 원천 파기는 별도 트리거(동의 철회·본인/법적 요청·아카이브 후 파기 검토)로 분리, 기존 관리자 단건 파기 경로 유지. 오프라인 반출 기본 불가 — 보관 기간 중에만 다운로드 가능하며 행위자 전건 감사(책임 추적, D14), 아카이브 후엔 다운로드도 불가. 자동 파기 cron은 아카이브 전환으로 교체(이슈 CCC-archive#52). 아카이브 열람 역할 범위·보존 상한(법 제21조 정합)은 법률 검토와 함께 확정. 상세 ADR-0010 |
| D33 | 목표 3층 구조 | **D12·D28 대체(2026-07-26)**: 목표를 **전체 목표 → 세션 목표 → 세부 목표** 3층으로 나눈다. 전체 목표는 케이스당 **1개·수정 가능·점수 없음**(방향), 세션 목표는 회차마다(전체 목표의 하위), **세부 목표가 GAS 채점 단위**이며 수정 금지(종료+신설). 이로써 D12의 이력 연속성은 점수가 붙는 층에서만 강제되고 방향은 자유롭게 고칠 수 있다. **세부 목표 ↔ 액션 아이템은 별개**("상태가 나아졌나" vs "하기로 한 걸 했나") — 액션이 세부 목표를 선택 참조. 브리핑은 **진행 중 세부 목표 최대 3개** 게이지(4개 이상이면 최근 갱신순 3개 + "외 N개"), 전체 목표는 HERO 아래 한 줄. 기존 목표는 전부 세부 목표로 내리고 전체 목표는 빈칸(자동 승격 안 함). 상세 ADR-0013 |
| D34 | 파스텔 3색 액센트 | **D30 색 규율 대체(2026-07-25)**: 무채색 전용 → **웜 뉴트럴 바탕 + 저채도 파스텔 3계열(블루=시간·상태 / 민트=사람·소속 / 라벤더=AI·승인 대기) + 리스크 레드**. Q가 Pen(pen.dev)에서 정밀화한 상담 준비 브리핑 화면에서 역추출. 파스텔은 장식이 아니라 정보 분류 신호이며 계열별 base(면)/deep(글자)/tint(배경) 3단으로 고정. 깊이 모델도 보더 → 이중 파스텔 그림자(`--shadow-soft`)로 전환. **색값은 시안 그대로**(Q 결정) — deep 3색과 추이 트랙이 WCAG 미달(4.40·2.33·3.78·1.31)이나 보정안을 화면 전체에 적용해 렌더 비교한 뒤 시안 톤 유지를 선택했고, 수치·영향(보조 정보 한정)·완화 3종·고대비 모드 대안을 DESIGN.md §9 '알려진 접근성 예외'에 기록. **렌더 결함 1건 수정**: 그라데이션 테두리를 `border-image`로 만들면 브라우저가 `border-radius`를 무시해 리스크 배너 모서리가 사각으로 렌더 → 배경 2겹(padding-box + border-box) 방식으로 교체, 시안 의도(라운드 12px)를 살림. D30의 타이포·액센트 띠 금지·4배수 간격은 존치, 리스크 배너 테두리 규정(균일 2px `--risk`)은 D34가 대체(리스크 독점은 배너 내부 요소가 담당). 화면 1개 기반이라 입력칸·모달·포커스·다크 모드·모바일은 미관찰(DESIGN.md `[추론]` 표기). 상세 ADR-0012 |
| D35 | 사업 워크스페이스 IA | **D21의 셸 부분 대체(2026-07-26)**: 용어를 **사업**(조직이 운영하는 프로그램) / **참여 사업**(=케이스의 화면 표기) / **워크스페이스**(선택된 사업 컨텍스트)로 가른다. 셸은 상단 헤더 폐기 → **좌측 사이드바(240px)**, 축은 **사이드바=장소 / 페이지 우상단=행동**. 사이드바는 조직 → **사업 전환기**(범위를 정하므로 메뉴 위) → 다가오는 일정 · 전체 일정(신규) · 참여자 · 설정(1곳만). 등록 2개는 사이드바에 넣지 않고 페이지 우상단 유지. `/`는 마지막 선택 사업의 일정으로 직행. **참여자 정보 페이지**를 허브로 승격(워크스페이스 범위를 벗어나 전 참여 사업 표시). 브리핑 출구는 **버튼 2개(참여자 정보·상담 기록)를 이름 바로 아래** — 브레드크럼 기각(상담사에게 비관례), `← 목록으로`·`전체 사업 보기` 삭제. 상담 등록 1단계는 **참여자 → 상담 유형 → 일시** 순이며 조직·참여 사업 선택 삭제(워크스페이스가 정함)·상담 방법 숨김, 상담 유형은 케이스 상태로 자동 기본값 + 다른 유형은 접힘(인테이크 재선택 시 경고+기존 기록 링크, CCC-14 경고형 유지). 상세 ADR-0014 |
| D36 | 참여자 허브 열람 범위 | **D7을 좁혀 재진술(2026-07-26)** — 넓히는 것이 아니다: 담당 케이스만 열리는 것은 **상담 내용**(세션 기록·브리핑·목표)이고, **참여 사업의 존재와 담당자 이름**은 같은 조직 상담사에게 보인다. 참여자 정보 페이지는 그 참여자의 **조직 내 전 참여 사업**을 표시하되 비담당 사업은 라벨+담당자만(링크 없음). 근거는 "이 페이지를 여는 사람은 이미 그 참여자의 담당자라 PII를 보고 있다"(Q) — 그래서 **전제 게이트를 유지한다**: 그 참여자의 케이스를 1건도 담당하지 않으면 페이지 자체가 안 열린다(`ForbiddenError`). 구현은 **접근 판정용 집합(담당)과 표시용 집합(조직 전체)을 분리**하고 게이트를 테스트로 고정한다. D25(배정 책임자 PII) 불변. **동의서 열람 범위 문안에 반영 필요**(§8). 함께 확정: `/` 직행용 마지막 선택 사업은 **계정 설정(DB)** 저장 — 기기가 바뀌어도 유지, 값이 없거나 사라진 사업이면 첫 사업 폴백. 상세 ADR-0014 '개정' 절 |
| D37 | 레이아웃 계약 | **D30·D34가 비워 둔 자리를 채운다(2026-07-27)**: 색·컴포넌트에는 계약이 20개 넘는데 레이아웃은 세 줄뿐이라 도구가 매번 폭·여백을 새로 발명했다. 숫자는 전부 **1440 캔버스에서 역산** — 사이드바 240(왼쪽 끝 고정) + 페이지 컨테이너 **1120**(패딩 40 포함, `box-sizing:border-box`) → **글 폭 1040**(한 줄 65자). 카드는 **열 수를 지정하지 않고 최소 폭이 정한다**(표준 420 → 2열 / 조밀 280 → 3열, gap 20). **조밀은 3열 아니면 1열**(2열이면 GAS 3개가 둘+외톨이로 앉음). 브레이크포인트는 **`max-width:767px` 하나**(구 5벌 수렴), 768 미만 사이드바는 **드로어로 숨김**. 세로 리듬 40/24/16/20. **화면의 모든 글자는 카드 안에**(HERO도 카드, 예외는 섹션 제목뿐). 락 6개를 `DESIGN.md` §7에 추가하고 그중 4개는 `guard:layout`으로 기계 검사. **D35의 이동 버튼 2개 배치만 수정** — `참여자 정보`는 HERO 우상단 세컨더리로, `상담 기록`은 `자세한 상담 기록 보기`로 브리핑 맨 아래. 실측 검증 시안 `artifacts/layout-frame-v1/`. 상세 ADR-0015 |
| D38 | 참여자 HERO 공통 계약 | **브리핑 전용이던 HERO 카드를 참여자 중심 화면 전부로 승격(2026-07-27 그릴링)**: URL이 특정 참여자를 가리키는 화면(`/participants/:id/**`)은 **공통 머리 부품(`ParticipantHeroCard`, `components/wire/`)** 을 단다. 부품은 **고정 1층 + 슬롯 3층**: ① 이름 `이름 (가명 ID)` 한 줄(§5 이름 표기) — 항상 있음 ② 상태 태그 — **케이스 1개를 보는 화면(브리핑·기록)에서만 필수**, 허브처럼 케이스가 교차하는 화면은 생략 ③ 메타 한 줄(최대 1줄, 내용은 화면이 정함 — 브리핑=맥락, 허브=연락처) ④ 행동 버튼 최대 2개(§4-5, 화면이 정함, 없어도 됨). 목록·일정(대상 여럿)은 이 계약이 아니라 **행 카드 구성** 문제로 다음 세션에 별도 그릴링. 곁가지 2건(상담 기록 화면의 브레드크럼 제거·가명 ID 노출 정정)은 **CCC-11 완료 기준에 흡수** — 같은 머리 영역을 두 번 만지지 않는다. 상세 DESIGN.md §5 |
| D40 | 용어 개편 | **용어집 사람·조직 축 전면 대체(2026-07-28 그릴링)**: 조직→**기관**, 상담사→**실무자**, 담당자→**담당 실무자**, 참여자→**당사자**, 시스템 관리자+배정 책임자→**기관 관리자**(역할 통합 — PII 열람은 배정 업무 근거로 유지, 상담 내용은 자신이 담당인 케이스만 + 긴급 접근 예외). 화면·문서 표기만 바꾸고 코드 식별자(`beneficiaryId`·`organizationId` 등)·URL은 유지. D39·ADR-0016은 PR #5(CCC-28·29)가 선점해 건너뜀. 상세 ADR-0017 |
| D41 | 인테이크 정본 양식 | **인테이크 상담지를 Q 제공 질문지 v1로 확정(2026-07-28 그릴링)**: 4부 구성(신청·기본정보 / 현재 생활상황 / 필요한 도움과 자원 / 정리와 후속관리), 전 항목 필수+무응답 원칙. ① 1-1 기본정보(PII)는 화면에는 표시하되 저장은 **당사자 등록(pii_vault)으로만** — 세션 기록에 PII 미저장(R3 유지), 생년월일·주소·성별은 금고 스키마 확장 ② AI 항목 재구성(D29)은 정기 상담만 — **인테이크는 고정 폼** ③ '긴급도'(일반/주의/긴급/즉시 개입)는 리스크 플래그와 **별개 필드**, 실무자 직접 판단·AI 제안 없음(R5) ④ 원본의 '대상자·상담자'는 D40 용어로 정규화. 정본: `PRD/intake-questionnaire-v1.md`. §8 '항목 풀 확정'의 인테이크 부분이 해소됨(정기 상담 항목 풀은 여전히 미결) |
## 10. 조직 시크릿 접근 (bss-infra)

조직(SSB) 시크릿이 필요하면 이 경로만 쓴다 (Cloudflare 시크릿·AES 키 등 2단계부터 해당):

- 값의 원본은 **Infisical**. 사람용 금고(Bitwarden)와 그 마스터 비밀번호는 절대 조회·요청하지 않는다.
- 에이전트 인증(읽기 전용 Machine Identity, 이 Mac에 배치 완료):
  ```bash
  set -a; source ~/.config/infisical-agent/credentials; set +a
  infisical run --projectId=<ID> --env=<env> -- <명령>
  ```
  값은 주입만 한다 — `secrets get` 등으로 stdout·채팅·로그에 출력 금지.
- **값이 stdout에 닿는 명령은 실행 자체 금지**: `infisical secrets`/`export`/`get`, `--plain`, 자격증명 파일 열람. 이름 확인이 필요해도 값 섞인 출력을 만들지 않는다. (과거 실제 노출로 전 키 로테이션을 치른 규칙 — `~/.claude/hooks/secret-dump-guard.sh` 훅이 결정론 차단)
- .env는 이름만 커밋(`.env.example`), 값 커밋 금지. 절차 SSOT: `~/DEVELOPER/PROJECTS/BSS/bss-infra/docs/machine-access.md` (레포 `SocialSolidarityBank/bss-infra`).
- 시크릿 값이 채팅·커밋에 노출되면 즉시 보고하고 해당 키 로테이션을 안내한다.

## Agent skills

### Issue tracker

GitHub Issues(`SocialSolidarityBank/CCC`)를 `gh` CLI로 사용한다. 상세: `docs/agents/issue-tracker.md`.
**구현 실행 티켓은 Linear**(2026-07-25~): 워크스페이스 **CCC**(bss-ccc), Composio 백엔드로 접근(절차·함정: `~/developer/tools/portwright/services/linear.md`). 스펙은 GitHub 이슈, 실행·선행 관계는 Linear의 blocks. **티켓 완료 규약**: Linear 티켓에 쉬운 한글 완료 보고 코멘트 + Done 처리 → Linear Slack 연동이 팀 채널로 전달한다.

### 티켓 스레드 규칙 (#ccc-tickets, 2026-07-26 Q 확정 — 위반 시 게시 금지)

에이전트가 `#ccc-tickets`에 올리는 티켓 소식은 **반드시 스레드**로 올린다. 채널 본문에는 부모 글 1개만 두고 상세는 전부 댓글로 밀어 넣는다(티켓 1건 = 스레드 1개, 묶음 금지).

1. **부모 글** = `티켓 번호 · 개발 용어 없는 쉬운 한 줄`. Linear 제목을 그대로 복사하지 않는다
2. **첫 댓글** = 비개발자가 30초에 읽는 분량으로 ① 무엇이 달라지나 ② 왜 하나 ③ 나에게 무슨 영향인가. 파일 경로·함수·마이그레이션 번호·ADR/D번호·완료 기준은 **두 번째 댓글부터**
3. **강조는 이모지 또는 인라인 코드**로, 한 글에 최대 2개(🚨 확인 필요 / ⚠️ 방식 변경 / ✅ 완료 / 🔵 착수 / 🧭 방향 결정)
4. 부모 글과 첫 댓글은 **`humanize-korean` 스킬로 윤문 필수** — 짧고 쉽게 만드는 것이 목적
5. 올리기 전 점검 5개(본문 1개 / 쉬운 한 줄 / 첫 댓글 자족 / 강조 2개 이하 / 윤문) 전부 통과해야 게시한다

전문·예시·나쁨↔좋음 대조: `docs/agents/ticket-thread-protocol.md`.
**채널 분리(2026-07-26 Q 결정)**: Linear 연동 봇의 게시 형식은 바꿀 수 없으므로(설정 자체가 없음) 채널을 갈랐다 — `#ccc-tickets`는 **이 규칙대로 만든 스레드만**, `#ccc-linear`(공개)는 Linear 자동 알림 전문. **Linear에는 채널 변경 기능이 없다**(2026-07-26 API 전수 확인 — 생성·삭제 뮤테이션뿐, 채널은 Slack 인증 화면에서 고르는 값). 기존 연결(팀 CCC → #ccc-tickets)은 `integrationDelete`로 삭제하고 `#ccc-linear`로 재연결 완료(**2026-07-26 분리 운영 시작**). 채널을 다시 만들 일이 있으면 **공개**로 만든다(Slack에 비공개→공개 전환 API가 없고 Enterprise 전용이라, 비공개면 채널을 새로 파야 한다). 채널·연동의 구체 ID와 검증 함정은 `~/developer/tools/portwright/services/linear.md`(레포 밖 — 공개 레포에 운영 ID를 두지 않는다).

### 미리보기 환경 (CCC-6, 2026-07-25~)

팀원 피드백용: https://ccc-preview.account-855.workers.dev — 링크+지정 코드(Infisical 보관)만으로 열람, **가상 시드 전용**(운영 PII·운영 키 미연결), main 머지마다 자동 재배포. 상세·프로비저닝: `docs/ops.md` "미리보기 환경 (CCC-6)".

### Triage labels

기본 5개 역할(needs-triage/needs-info/ready-for-agent/ready-for-human/wontfix) 그대로 사용. 상세: `docs/agents/triage-labels.md`.

### Domain docs

단일 컨텍스트: 루트 `CONTEXT.md` + `docs/adr/`. 상세: `docs/agents/domain.md`.

### 문서 배치 규칙 (2026-07-25 Q 확정)

- HTML 시안·용어집 등 **아티팩트는 루트 `artifacts/`** (구 docs/artifacts 폐지)
- **PRD·제품 정의 문서는 루트 `PRD/`**
- 폐기 문서는 `docs/archive/` (폐기 헤더 필수)
