# 히스토리에 남은 알려진 발견 1건. 여기 있는 항목은 "괜찮다"가 아니라
# "이미 알고 있고 별도 티켓으로 처리 중"이라는 뜻이다.
#
# 이 예외가 없으면 히스토리 스캔(`pnpm guard:secrets:history`, CI)이 영구히 실패한다.
# 그러면 다음 사람이 실패를 당연하게 여기고 진짜 새 유출을 그냥 지나치게 된다 —
# 그게 이 레포에서 실제로 일어났던 실패 방식이다(룰 0개짜리 설정이 모든 스캔을
# "no leaks found" 로 통과시켰고, 레포 전환의 근거로 적힌 문장도 그 스캔이었다).
#
# 2026-07-28 전환 스냅샷 커밋 2cc840c 가 시드 툴 상수에 Cloudflare Access 서비스
# 토큰의 Client ID(자격증명의 공개 절반)를 그대로 옮겨 왔다. 이 PR 에서 HEAD 는
# 가명으로 바꾸지만 스냅샷 커밋에는 값이 남는다.
# 실제 통제 수단은 토큰 로테이션이며 Linear CCC-35 로 추적한다.
#
# **CCC-35 로 토큰을 재발급한 뒤에는 이 줄을 지운다** — 그때 이 값은 죽은 값이 되고,
# 예외를 남겨 두면 같은 모양의 진짜 유출을 가려 준다.
2cc840cc923a7e3eada31955d32196b8fb80a5e3:scripts/seed/preload-data.ts:cloudflare-access-service-token-client-id:44
