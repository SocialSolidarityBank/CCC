# 히스토리에 남은 알려진 발견 1건 — **이미 폐기된 값**이다.
#
# 2026-07-28 전환 스냅샷 커밋 2cc840c 가 시드 툴 상수에 Cloudflare Access 서비스
# 토큰 `ccc-pipeline-laptop` 의 Client ID(자격증명의 공개 절반)를 그대로 옮겨 왔다.
# 같은 날 오후 그 토큰을 재발급했으므로 커밋에 박힌 값은 더 이상 어떤 정책도
# 가리키지 않는다. HEAD 는 가명으로 바꿨지만 히스토리 재작성은 하지 않기로 했으므로
# 스냅샷 커밋에는 문자열이 남는다.
#
# 그래서 이 예외는 "나중에 처리할 것" 이 아니라 **종결된 항목의 기록**이다.
# 지우면 히스토리 스캔(`pnpm guard:secrets:history`, CI)이 영구히 실패하고,
# 그러면 다음 사람이 빨간 게이트를 당연하게 여겨 진짜 새 유출을 지나치게 된다.
#
# 범위는 좁다(실측 확인): 지문은 커밋+파일+줄 단위라 **같은 문자열이 다른 파일이나
# 새 커밋에 들어오면 그대로 검출된다.** 이 줄이 가리는 것은 아래 한 좌표뿐이다.
#
# 이 줄을 지워도 되는 때: 히스토리를 재작성해 값 자체를 없앨 때.
2cc840cc923a7e3eada31955d32196b8fb80a5e3:scripts/seed/preload-data.ts:cloudflare-access-service-token-client-id:44
