# 동의 트랙 결정 기록과 구현 게이트 v1

- 작성: 2026-07-29 (Claude Code, Q 결정 반영)
- 입력: `legal-risk-assessment-v1.md`(ORANGE 4건) → Q 결정 4건(아래 1장)
- 출력: 지금 개발 착수 가능한 게이트 G1~G5와, 법률 검토 회신까지 잠긴 게이트 L1~L4의 구분

## 1. 결정 기록 (2026-07-29, Q)

| # | 쟁점 | 결정 | 비고 |
|---|---|---|---|
| 결정1 | ① 개인정보 동의의 지위 | **하드 게이트 + 긴급 예외**: 등록 시 ① 필수 체크. 급박한 위기 개입은 "긴급 등록" 플래그(사유 기록 필수, 기한 내 동의 보완 알림, 전건 감사)로 예외 | D44의 "① 게이트 아님"을 부분 대체(D15의 ②·③ 미동의 경로는 불변). 법 제15조 제1항 제5호 근거 상정 — 단 제5호의 전제 요건("사전 동의를 받을 수 없는 경우") 충족 여부와 보완 기한은 자문(질문 8)으로 확정 |
| 결정2 | 보존 기한 상한 | **상담 종결 후 총 5년**(활성 1년 + 아카이브 4년), 도래 시 파기. 다른 법령의 더 긴 보존 요구·선행 파기 사유는 예외 | "법정 최대 권장 기간"은 존재하지 않음(법의 원칙은 최소 보유) — 금전 지원 증빙 5년 관행에 정렬. 법령 근거 매칭은 자문(질문 4) 확인 조건부 |
| 결정3 | 민감정보 게이트 | **문안 보강 + 마스킹 확대**: ③ 동의 문안에 민감정보 고지 추가(법적 게이트) + Mac Mini 2차 마스킹에 질병명·진단명 카테고리 추가(기술 게이트, 구체 병명만 치환해 상담 맥락은 보존) | D2 확장. 게이트 위치: 법적 게이트는 기존 ③ 동의 체크(파이프라인 진입점), 기술 게이트는 API 전송 직전 Mac Mini |
| 결정4 | 문안 보정 범위 | 3절 "서비스 개선" 목적 **삭제**(통계만), 만 14세 미만 문구 **추가**. 5절 Anthropic·미국 명시는 **자문 결과에 따라**(선반영 보류) | 문안 v0.3에 반영 완료 |

### CLAUDE.md 9장 반영용 D46 후보 행 (Q 확인 후 붙여넣기)

```
| D46 | 동의 게이트·보존 상한 | **D44의 ① 지위 부분 대체(2026-07-29 결정, D15 미동의 경로는 불변)**: ① 개인정보 동의는 등록의 하드 게이트로 승격(②·③ 미동의 경로는 불변) + "긴급 등록" 예외(사유 기록·기한 내 보완 알림·전건 감사, 제15조 제1항 제5호 상정). 보존은 종결 후 총 5년 상한(활성 1년+아카이브 4년, D32 실행 보완) 도래 시 파기. D2 마스킹을 질병명·진단명 카테고리로 확대(구체 병명만 치환). 문안 v0.3 반영, 법령 근거 확정은 법률 검토(패킷 v1.1 질문 4·8·9)와 함께 |
```

## 2. 지금 개발 착수 가능한 게이트 (자문 회신 불요)

자문이 뒤집을 수 있는 부분은 "설정값"으로 빼서, 구조는 지금 만들고 숫자·문구만 나중에 바꾼다.

### G1. ① 하드 게이트 + 긴급 등록 예외

- 등록 화면(`/participants/new`): ① 체크 필수화. 미체크 시 제출 차단 + "긴급 등록" 전환 버튼
- 긴급 등록: 사유 텍스트 필수 입력, `support_cases`에 긴급 플래그·보완 기한(기본 14일, **설정값** — 자문 질문 8로 확정), 만료 전 담당 실무자 알림
- API: `createSupportCase`에 ① 검증 추가(긴급 플래그 없으면 `consent_privacy` 필수)
- **기존 당사자의 추가 참여 사업 생성 흐름에도 동일 적용**: D44에 따라 두 번째 사업은 동의 2종(D49)이 미체크로 시작하므로, 해당 UI에도 ① 필수 체크·긴급 등록 버튼을 동일하게 배치(서버 검증만으로 막으면 화면에서 원인 없는 실패로 보임)
- 감사: 긴급 등록 생성·보완·만료 전건 `record_consent` 계열로 기록(D14)
- 테스트로 고정: ① 없음 + 긴급 아님 → 거부 / 긴급 → 허용 + 기한 생성
- 완료 기준: 기존 데이터 중 ① 미기록 케이스 목록화(마이그레이션 대상 아님, 보완 대상 리포트만)

### G2. 보존 상한 5년

- `support_cases`에 파기 예정일 계산 로직: `closed_at + 5년`(자문 반영용 **전역 설정값** — 기관별 변동은 허용하지 않음. 허용하려면 문안 3절을 "기관 규정에 따르되 최장 5년"식으로 먼저 재기술해야 함)
- 아카이브 전환(기존 D32 cron)에 더해, 상한 도래 케이스를 "파기 검토 큐"로 올리는 배치 추가 — 자동 삭제가 아니라 기관 관리자 확인 후 파기(기존 단건 파기 경로 재사용)
- 다른 법령 보존·법적 보류(litigation hold) 예외 플래그 1개
- 완료 기준: 상한 도래 → 큐 적재 → 관리자 파기 → pii_vault 삭제 + 가명 기록 보존이 테스트로 고정

### G3. 민감정보 마스킹 확대

- Mac Mini 2차 마스킹(NER)에 질병명·진단명 카테고리 추가: 사전(한국 질병명·정신건강 용어) + NER 병행, 치환 토큰은 `[질환]` 계열
- 원칙: 구체 병명만 치환, 상담 맥락 문장은 보존(브리핑 품질 유지)
- 마스킹 로그(무엇을 몇 건 치환했는지 집계만, 원문 저장 금지)로 검증 가능하게
- 완료 기준: 병명 포함 테스트 문장 셋에서 치환율 측정 + 문안 v0.3의 "구체적인 질병명 등" 문구는 이 게이트 배포 후에만 사용(문안 ⟦구현 연동⟧ 주석과 연동)

### G4. 문안 v0.3 화면 반영

- 동의가 나타나는 4곳(등록·당사자 정보·인테이크 읽기 전용·PR #5 자기 가입 폼)의 아코디언 문안을 v0.3으로 교체
- 단, v0.3도 DRAFT — 화면에는 "법률 검토 전 초안" 배지 유지, G3 배포 전에는 5절의 질병명 문구 제외한 변형 사용
- 완료 기준: 4곳 문안 단일 소스(하드코딩 4벌 금지 — 상수/마크다운 1곳에서 읽기) + **표시본 빌드**: ⟦법률 검토⟧·⟦구현 연동⟧ 내부 주석과 버전 이력·DRAFT 머리말을 제거한 화면용 본문을 생성하는 단계 포함(원본 md를 그대로 렌더하면 내부 주석이 당사자 화면에 노출됨)

### G5. 운영 준비 2건 (Q 액션, 코드 아님)

- 개인정보 처리방침 존재 여부 확인 → 없으면 작성 과제 등록(질문 3 고지 갈음 경로의 전제)
- 서면 동의 원본 보관 규칙 초안(등록 후 N일 내 원본 확인 등) — 자문 질문 7 회신과 함께 확정

## 3. 법률 검토 회신까지 잠긴 게이트

`red` **2026-07-31 정정 (D57 · ADR-0027)**: 아래 L4 의 수령자는 **Anthropic 이 아니라 OpenAI** 다. 코드는 이 문서 작성 이전부터 OpenAI 를 부르고 있었고, Q 결정으로 OpenAI 를 정본으로 확정했다. **수령자가 단일하게 특정되는 구조는 그대로**이므로 L4 의 질문 자체는 바뀌지 않는다 — 이름만 바뀐다. 중계형 사업자(OpenRouter)는 수령자를 특정할 수 없어 같은 결정에서 기각했다. **L1·L3·L4 동결은 이 정정으로 풀리지 않는다.**

| # | 잠긴 것 | 여는 조건 |
|---|---|---|
| L1 | 동의서 확정본(DRAFT 배지 제거) | 패킷 v1.1 질문 1·2·3·4·8·9·12·13 회신(문안 각 절이 걸린 질문 전부 — L4 선행 포함) |
| ~~L2~~ | ~~PR #5 머지(당사자 초대·자기 가입 배포)~~ | **2026-07-29 Q 결정으로 해제 — 아래 '개방 기록' 참조** |
| L3 | 전자서명·당사자 직접 작성 구현 착수 | 질문 6 회신(본인 확인·기록 보존 요건) |
| L4 | 5절 수령자(**OpenAI**·미국) 명시 여부 확정 | 질문 3 회신(고지 갈음 경로·기재 위치) |

### L2 개방 기록 (2026-07-29, Q 결정)

**결정**: L2를 Q 권한으로 해제하고 PR #5(당사자 초대·자기 가입)를 법률 회신 전에 머지한다.

**근거(Q)**: 서비스 출시 계획이 아직 없고 지금 단계의 목표는 기능 완성이다. 현재 검토된 수준으로 내부 운영에는 충분하다.

**이 결정이 성립하는 사실 조건** — 아래가 깨지면 재검토 대상이다:
- 배포된 것은 **화면 흐름 프로토타입**이다(D39): 실제 인증·이메일 발송이 없고 가입 데이터는 가상 시드 전용이다.
- **미리보기에 공개 표면이 없다**(ADR-0016 개정, PR #5 자체 커밋): 미리보기 환경에서 `/join` 공개 경로는 차단되고 팀에게는 실무자 화면(초대 링크 발급)만 보인다.
- 화면의 동의 문안은 여전히 **DRAFT 배지**를 달고 있다(L1 미해제).

**그대로 잠겨 있는 것**: L1(동의서 확정본)·L3(전자서명·당사자 직접 작성 구현)·L4(수령자 명시)는 회신 전까지 유효하다. **실제 당사자를 대상으로 한 운영 개시는 이 결정의 범위가 아니다** — 그때는 L1이 선행해야 한다.

**남은 선행 액션**: ~~검토 패킷 v1.1을 자문에 전달하는 것은 여전히 Q 액션이며, 회신이 오면 문안·설정값(보완 기한·보존 상한·수령자 문구)을 반영한다.~~ **2026-07-30 아래 '법률 검토 트랙 종료 기록'으로 대체.**

### 법률 검토 트랙 종료 기록 (2026-07-30, Q 결정)

**결정**: 동의서 법률 검토(자문 의뢰)를 **MVP 범위에서 진행하지 않는다**. 현행 문안(v0.2/v0.3 초안)으로 MVP 구현에는 충분하다고 판단, 검토 의뢰 작업을 닫는다.

**이 결정의 효과**:
- 패킷 v1.1(`legal-review-packet-v1.md`)은 **전달하지 않고 보존**한다 — 향후 검토 재개 시 그대로 쓴다.
- G1~G4는 자문 회신을 기다리지 않고 **설정값 기본값(보완 기한 14일·보존 상한 5년·현행 문구)으로 구현·운영**한다. 설정값 분리는 유지 — 나중에 검토가 재개되면 숫자·문구만 바꾼다.
- **L1·L3·L4는 해제가 아니라 동결이다**: 동의서 확정본(DRAFT 배지 제거), 전자서명·당사자 직접 작성, 5절 수령자 명시는 여는 조건이 사라진 것이 아니라 **재개 조건이 바뀐 것** — 실제 당사자 대상 운영 개시 전에 법률 검토를 재개해야 열린다(L2 개방 기록의 "운영 개시는 L1 선행" 원칙 유지).
- 화면의 **DRAFT 배지는 유지**한다(문안이 확정본이 된 것이 아니다).

**성립 조건**(L2 개방 기록과 동일): 서비스 출시 계획 없음, 화면 흐름 프로토타입 단계, 가상 시드 전용. 실제 당사자 운영을 시작하려는 시점에 이 기록이 재검토 트리거다.

## 4. 권장 착수 순서

1. **G1**(① 게이트) — 법적 공백을 매일 줄이는 항목, 다른 게이트와 독립
2. **G3**(마스킹 확대) — R1+R4 결합 리스크를 기술적으로 낮추는 항목
3. **G2**(보존 상한) — 구조만 먼저, 숫자는 설정값
4. **G4**(문안 교체) — G3와 문구 연동 주의
5. G5는 병행(Q 액션)

각 게이트 완료 시 커밋 단위를 분리하고, 자문 회신이 오면 설정값(보완 기한·보존 상한·문구)만 조정한다.

---

## 5. 구현 게이트 트랙 종료 기록 (2026-07-30, Q 결정)

**결정**: 동의 게이트 구현 트랙(G1~G5)을 **MVP 범위에서 지금 구현된 상태로 종료·완료 처리**한다.
남은 항목을 지금 채우지 않고, **실서비스 개시 준비 시점에 이 문서로 되돌아와** 재개한다.

법률 검토 트랙은 이미 §3에서 닫혔고(2026-07-30), 이 기록은 그 **구현 쪽 짝**이다.
즉 동의·보존 영역은 자문도 구현도 MVP 선까지만 하고 멈춘다.

### 종료 시점의 실제 상태

| 게이트 | 상태 | 남은 것 |
| --- | --- | --- |
| **G1** ① 하드 게이트 + 긴급 등록 예외 | **완료·머지** (PR #17, 마이그레이션 0028) | ① **만료 전 담당 실무자 알림 미구현** — `apps/api/src/notify.ts` 에 관리자용 `notifyAdmins` 하나뿐이고 실무자 개인에게 보내는 통로가 없다(어디로 낼지는 결정 사항) ② 보완 대상 리포트는 게이트웨이·API 까지만 — 보여 주는 화면이 없다 |
| **G3** 민감정보 마스킹 확대 | **완료·머지** (PR #18) | 사전은 병명만 잡는다 — 대화 속 제3자 인명은 NER 모델이 있어야 지워지고 로컬은 미설정(기존 상태). 문안 v0.3의 "구체적인 질병명 등" 문구는 이 게이트가 **배포된 뒤에만** 쓸 수 있다 |
| **G2** 보존 상한 5년 | **부분 — 구조·API 만** (브랜치 `feat/consent-g2`, 커밋 `262928c`, push 완료, **PR 없음**) | 완료 기준 테스트(상한 도래 → 큐 적재 → 보류 해제 → 관리자 파기 → 금고 삭제 + 가명 기록 보존). 그것이 없어 PR 을 올리지 않았다 |
| **G4** 문안 v0.3 화면 반영 | **미착수** | 동의 노출 4곳의 문안 교체 + 내부 주석 제거 표시본 빌드 |
| **G5** 운영 준비 2건 (Q 액션) | **미완** | 개인정보 처리방침 존재 확인 · 서면 동의 원본 보관 규칙 초안 |

### 이 결정으로 남는 리스크 (알고 받아들인다)

`red` **2026-08-16 정정 (ADR-0035 후속 실측 — 폐기된 서술 포함)**: 아래 첫 `yellow` 항목의 실측 2("`purge_due` 에 값을 넣는 코드가 레포 전체에 0건 / 보존 시계가 시작되지 않는다 / 파기는 일어날 수 없다")와 그 위에 선 결론, 그리고 '재개 트리거와 첫 3가지' 1·2번의 같은 전제는 **2026-08-16 재실측으로 반증됐다 — 폐기**. 마이그레이션 0006 의 DB 트리거가 케이스 종결 시 `purge_due` 를 설정하고(보존 시계는 시작된다), 매일 cron 과 관리자 수동 엔드포인트 두 경로가 D32·D46 이 요구하는 절차(아카이브 전환·검토 큐·확인) **없이** 실제로 파기한다. 즉 문제의 실체는 "파기가 일어날 수 없다"가 아니라 "절차 없이 파기된다"다. 현재 사실의 정본은 `docs/policy/deferred-blockers-v1.md` 1장(경로 A·B 실측과 D32·D46 6단계 대조표)이다. 아래 원문은 레포 관행대로 지우지 않고 당시 기록으로 남긴다.

- `yellow` **보존·파기 파이프라인 전체가 미구현이다.** — 2026-07-30 실측으로 확인·등급 하향. 이 자리에 있던 `red` "cron 이 여전히 즉시 파기한다 / 구동 여부 미확인"은 **두 군데가 사실과 달랐다.**
  - **실측 1 — Cloudflare 계정은 개설돼 있고 운영 워커는 배포돼 있다.** 계정 `account@bss.or.kr`, 워커 `ccc-api`(마지막 배포 2026-07-15), 파기 크론 `0 3 * * *` 설정 존재. "계정 미개설이라 안 돌 것"이라던 추측은 틀렸다.
  - **실측 2 — 그런데 파기는 일어날 수 없다.** ⟦폐기 — 위 2026-08-16 정정 참조, 정본은 `docs/policy/deferred-blockers-v1.md`⟧ `purge_due` 에 값을 넣는 코드가 레포 전체에 **0건**이다. `closeSupportCase` 도 설정하지 않고, 모든 쓰기가 `purge_due = NULL` 이다. 즉 D10 의 보존 시계가 **시작되지 않는다** — 크론이 돌아도 후보를 영영 찾지 못한다.
  - **실측 3 — 운영 D1 현재 상태**: `participant_pii_vault` 22행 전부 미파기, `purge_due` 보유 **0건**, 도래 **0건**, 종결 케이스 **0건**. 데이터는 2026-07-15~17 생성분(시연·시드 성격).
  - **결론**: 아카이브 전환이 없다는 사실은 그대로지만 **데이터가 지워질 위험은 현재 없다.** 남은 일은 "아카이브를 추가한다"가 아니라 **보존 시계 설정 + 아카이브 전환 + 5년 상한 검토 큐를 함께 만드는 것**이고, G2 의 법적 보류는 그 위에 얹는 안전장치다. 실서비스 개시 전 필수.
- `yellow` **운영 D1 이 마이그레이션 0012~0028(17개) 미적용**이다. 운영 워커는 Access·fail-closed 로 잠겨 실사용 경로가 아니지만, 재개 시 스키마 정합을 먼저 맞춰야 한다.
- 화면의 **DRAFT 배지 유지**, **L1·L3·L4 동결 유지** — §3 그대로다. 실제 당사자 대상 운영 개시는 여전히 L1(문안 확정) 선행이다.
- 설정값 3종은 기본값으로 굳는다: 보완 기한 14일 · 보존 상한 5년 · 현행 문안. **설정값 분리는 유지**했으므로 재개 시 숫자·문구만 바꾼다.

### 재개 트리거와 첫 3가지

**트리거**: 실제 당사자를 대상으로 서비스를 열려고 하는 시점(§3 법률 검토 종료 기록과 같은 트리거).

재개하면 순서는 이렇게 된다.

1. **보존 파이프라인 설계·구현** — 위 `yellow` 항목. 세 조각을 한 묶음으로 본다: ① **보존 시계 시작**(케이스 종결 시 `purge_due` 설정 — 지금 아무 데서도 하지 않는다 ⟦폐기 — 마이그레이션 0006 트리거가 이미 설정한다. 위 2026-08-16 정정과 `docs/policy/deferred-blockers-v1.md` 참조⟧) ② **아카이브 전환**(D32) ③ **5년 상한 파기 검토 큐**(D46). G2 브랜치(`262928c`)의 법적 보류를 살릴지 다시 설계할지는 이 묶음을 정한 뒤 판단한다.
2. **G2 완료 기준 테스트 + 머지** — 착수 함정: 금고(`participant_pii_vault`)의 UPDATE 가드가 엄격해 테스트에서 `t.db` 직접 UPDATE 로는 `purge_due` 를 넣을 수 없다. **그리고 `closeSupportCase` 도 `purge_due` 를 설정하지 않으므로 게이트웨이 경로로도 못 만든다** ⟦폐기 — 설정 주체는 게이트웨이 함수가 아니라 마이그레이션 0006 의 DB 트리거다. 위 2026-08-16 정정 참조⟧ — 위 1번의 보존 시계를 먼저 구현해야 이 테스트의 픽스처가 성립한다(선행 관계).
3. **G4 문안 반영** — G3 가 배포된 뒤에야 질병명 문구를 쓸 수 있다. 그리고 **등록 화면 폴더(`participants/new/`)를 UI 수정 레인 B 와 함께 지나므로 순서를 맞춰야 한다**(등록 화면을 두 번 뜯지 않게).

**성립 조건**: §3과 동일 — 서비스 출시 계획 없음, 화면 흐름 프로토타입 단계, 가상 시드 전용. 이 조건이 깨지는 순간 이 기록이 재개 트리거다.
