# 배포 이력과 버전 규칙

> 2026-08-11 Q 결정으로 시작한 기록이다. 그전까지 이 레포에는 버전 표기가 없었고 "지금 몇 버전인가"에
> 답하려면 매번 배포 로그를 뒤져야 했다. 이 문서가 그 답을 갖는다.

## 지금 운영에 있는 것

**v0.2.1** (커밋 `af26587`, 2026-08-19 배포)

## 규칙

**1. 번호가 사는 곳은 네 군데이고 값은 항상 같다.**

| 자리 | 무엇 |
| --- | --- |
| `package.json` | 레포 전체 |
| `apps/web/package.json` | 웹 |
| `apps/api/package.json` | API |
| git 태그 `v<번호>` | 그 번호로 운영에 나간 커밋 |

세 `package.json` 이 어긋나면 `pnpm deploy:production` 의 사전 점검이 배포를 막는다. 문서에만 적은
규칙은 지켜지지 않으므로 검사에 넣었다.

**2. 번호는 운영에 나가는 코드가 바뀔 때만 올린다.**

- 화면이나 기능이 새로 생기면 가운데 자리: `0.1.0` → `0.2.0`
- 고침과 다듬기만이면 끝자리: `0.1.0` → `0.1.1`
- 문서, 개발 도구, 스킬만 바뀐 배포는 **번호를 올리지 않는다.** 아래 이력에 행만 더한다

**3. 올리는 시점은 배포 직전이 아니라 PR 안이다.** 운영에 나갈 변경을 담은 PR 이 세 `package.json` 을
함께 고치고, 배포가 성공한 뒤에 태그를 단다.

```bash
git tag -a v0.1.0 <커밋> -m "운영 배포 v0.1.0" && git push origin v0.1.0
```

**4. `1.0.0` 은 실서비스 개시와 함께 붙인다.** 지금 열려 있는 조건은 보존과 파기 파이프라인
(`CLAUDE.md` 8장)과 D46 이다. 그 둘이 닫히기 전에는 `0.x` 로 둔다. 배포가 초록인 것과 실제 당사자
정보를 넣어도 되는 것은 다른 문제다.

## 이력

| 버전 | 커밋 | 날짜 | 무엇이 나갔나 |
| --- | --- | --- | --- |
| v0.2.1 | `af26587` | 2026-08-19 | D69 후속 3장: 검토 화면을 기관 관리자도 연다(CCC-105, D7 정합), HERO 상태 태그 톤과 검토 화면 라벤더(CCC-106, D61 ② 개정), 검토 화면 실측 하니스 등록(CCC-104) |
| v0.2.0 | `1f7107b` | 2026-08-19 | D69(ADR-0036) 구현 일괄: 호출 ① 대조 3종과 재료 다중화(마이그레이션 0034·0035), 목표·메모 마스킹 배선, 검토 화면을 실승인 화면으로(승인·반려·재생성). 운영 D1 에 0032~0035 적용(기록: `artifacts/prod-migration-2026-08-19/`) |
| v0.1.0 | `ef86dd5` | 2026-08-11 | 번호를 붙이기 시작한 기준선. 이 배포 자체가 실어 나른 화면 변경은 인테이크 화면 가로 넘침 수리 하나다(CCC-93) |

`yellow` **v0.1.0 이전 배포는 번호가 없다.** 2026-07-15 첫 배포부터 2026-08-10 까지 여러 번 나갔고,
그 기록은 GitHub Actions 의 `Deploy Production` 이력에 있다. 소급해서 번호를 매기지 않는다 — 없던
번호를 지어내면 이 표가 기록이 아니라 창작이 된다.
