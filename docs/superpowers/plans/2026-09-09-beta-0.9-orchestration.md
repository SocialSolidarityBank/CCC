# CCC 베타 0.9 실행 오케스트레이션

2026-09-09 Q 실행 승인. 총괄은 제품 코드를 직접 수정하지 않고 작업 인계, 파일 소유, 통합 순서와 검증 증거를 관리한다. 워크트리는 필요한 책임 경계만큼 만들거나 기존 공간을 재사용한다. merge, 배포, 인증서 변경, 새 외부 의존성은 별도 승인이다. 진행 상태는 기존 티켓과 커밋에 남기며 STATUS 문서는 만들지 않는다.

Q는 이 대화의 사용자이며 총괄은 MacBook의 현재 오케스트레이터 세션이다. 첫 배치의 공통 기준은 fetch로 확인한 `origin/main@4352d32`로 고정한다. 기준이 바뀌면 총괄이 다음 인계 커밋을 지정하며 각 레인이 임의로 서로 다른 최신 기준을 소비하지 않는다. 승인과 지시는 이 대화, 작업자 지시와 응답은 지정 Herdr pane, 완료 증거는 기존 실행 티켓과 커밋에 연결한다.

같은 실행 중 Q는 검증을 통과한 DESIGN/BACKEND 커밋을 총괄이 지정해 FRONTEND 작업 브랜치로 로컬 통합하는 것을 승인했다. 원본 브랜치는 보존하며 공개 push, origin/main 병합과 배포는 포함하지 않는다. 또한 기존 설정 후보가 사용하는 `@supabase/supabase-js@2.116.0` 한 건의 client 의존성 추가를 승인했다. 실제 계정 인증과 시크릿 작업, 다른 패키지 도입은 이 승인에 포함하지 않는다. 공유 lockfile 작성 슬롯은 BACKEND 체크포인트 또는 명시적 hash 동결 확인 뒤 FRONTEND로 넘긴다. 동결만으로 미검증 backend 코드를 소비하지 않으며, 나중에 통합된 lockfile은 한 작성자가 재생성하고 검증한다.

## 목표와 우선순위

최종 범위는 Community Cloud, Local Single, Local Office다. 첫 사용자 여정은 설치/로그인 → 기관 초기 설정/사업 확인 → 당사자 등록 → 일정 → 인테이크/수기 기록 → 15초 페이지다. 녹음/가림/AI 승인, 리포트, 공개 요청, 복원과 모드별 출고 검증을 제외하지 않는다. 수기 경로를 먼저 연결하는 것은 안전 관문을 우회하거나 베타 전체 완료로 보고하기 위한 축소가 아니다.

프런트엔드 원 계획은 MacBook `/Users/seongqkim/DEVELOPER/PROJECTS/CCC-new/.worktrees/frontend/docs/superpowers/plans/2026-09-09-apps-client-foundation.md`다. P0부터 P10까지 전 범위와 30 route, 추가 D86/D87/D88 요구를 유지한다. 각 기능의 계약과 완료 명령은 [실행 마스터 계획](../../../CCC_OPEN_PILOT_PLAN.md)의 해당 E 티켓과 `docs/specs/` 정본을 따른다. P7의 첫 로그인/기관 초기 설정/사업 확인 최소 경로는 P3 저장 검증보다 앞서 제공한다. 임시 성공 API나 구 동의 자동 승격은 허용하지 않는다. P7 나머지 관리 기능과 P10 최종 6영역 통합을 이 최소 경로와 혼동하지 않는다. 공개 사이트의 정본 목적지는 E2-6의 `apps/site`이며 기존 web의 공유 자산 독립과 source cutover는 P9, 운영 runtime 은퇴는 별도 E6-6c다.

Community Cloud의 업무 실행 경계는 Q가 선택한 [ADR-0048 D89](../../adr/0048-independent-cloud-api-runtime.md)를 따른다. Supabase DB/Auth/Storage는 유지하고 관리자 키 없는 독립 업무 API 환경을 설계한다. 현재 후보의 테스트는 이 새 배포 구조의 검증 증거가 아니다.

## 역할과 실행 상한

- MacBook ORCHESTRATOR: 기존 beta-0.9-orchestrator, orchestrate/beta-0.9. GPT-6 Astra. 제품 코드 수정 금지.
- MacBook FRONTEND: 기존 frontend 워크트리와 실행 세션을 재사용하고 frontend/beta-0.9-client에서 작업한다. apps/client에서 stt-trial 제외. 기존 계획은 보존한다. 첫 P0 인계 분석은 Q가 승인한 GPT-6 Astra로 수행하고 계약이 확정된 일반 구현은 GLM-5.3 배정 후보로 둔다.
- MacBook DESIGN: Q가 연 DESIGN 워크트리, design/work, Claude Sonnet 4.6. 공용 부품/CSS/표현과 실화면 검수. 새 기능의 경쟁 시안이나 미세한 디자인 취향 변경은 후순위다. 접근성, 필수 상태 구분과 화면 넘침 검사는 유지한다.
- Mac mini BACKEND: 재사용한 `/Users/barq/DEVELOPER/PROJECTS/CCC/.worktrees/BACKEND`에서 작업한다. 원본 feat/settings-backend@71778f3은 보존하고 integrate/beta-0.9-backend는 4352d32에서 시작한다. 기준 전환 후 나타난 기존 미추적 `supabase/`는 내용 조회/수정/커밋에서 제외해 보존한다. GPT-6 Astra, 무하위에이전트로 첫 통합을 수행한다.
- PRIVACY/STT: 기존 두 머신의 원본과 증거를 인계받기 전 새 구현을 배정하지 않는다. mini의 fix/ner-long-input@2a53f7b과 MacBook e5-4의 미커밋 증거는 별개 입력으로 보존한다.
- MacBook VALIDATION: Q가 승인한 검증 전용 WIP를 Git으로 받아 `verify/beta-backend`에서 PostgreSQL 계약과 실제 HTTP 스모크만 수행한다. 소스 수정은 BACKEND에 돌려보내고 제품 코드를 직접 고치지 않는다. 기존 DESIGN 에이전트와 교대하며 워크트리/자료는 지우지 않는다. GLM-5.3을 사용한다.

초기 동시 상한은 MacBook 총괄 1+작업자 2, mini 작업자 1, 합계 4다. 하위 에이전트도 이 수에 포함한다. 각 작업자는 임의로 하위 에이전트나 머신 간 복사본을 만들지 않는다. 무거운 모델 측정/통합 빌드는 머신별 한 번에 하나이며 필요하면 DESIGN/검수와 교대한다. 기존 외부 프로세스를 종료하지 않는다. 실제 실행 모델과 readiness는 세션 시작 결과로 확인하며 모델 카탈로그만으로 인증 성공을 주장하지 않는다.

mini의 공유 Docker 실패와 부족한 내장 디스크 여유 때문에 새 VM이나 공유 환경 재시작 대신 MacBook의 정상 Docker를 사용한다. Q는 이를 위한 미검증 표식의 로컬 WIP 보존 커밋과 Git 전송을 별도 승인했다. 이 예외는 검증용이며 FRONTEND/main 통합, 공개 push, 배포 승인이 아니다. 테스트는 같은 pinned PostgreSQL 이미지와 기존 harness를 쓰고, DB·신원·권한 검증을 mock이나 native 대체 결과로 바꾸지 않는다.

2026-09-09 검증 결과: MacBook VALIDATION에서 backend 소스 `e717ad9`가 PostgreSQL 계약 5개 파일 59개 테스트와 실제 loopback HTTP + 제한된 `ccc_api` 스모크를 통과했다. parity manifest는 harness가 생성한 `8440161`, BACKEND 정리 체크포인트는 `004a97d`다. FRONTEND는 이를 `9ca0b2c`로 로컬 통합했으며 client typecheck/test/build와 API typecheck를 통과했다. 이 상태는 pre-D89 소스 결합이며 hosted 인증, D89 실행 환경, 화면 완료가 아니다. 다음 인계 기준 커밋은 `9ca0b2c`이고 원본 브랜치들은 보존한다.

2026-09-10 후속 검증: 기관 준비 계약 `8d4b109`를 MacBook VALIDATION에 fast-forward한 뒤 PostgreSQL 계약 5파일 59개 테스트가 통과했다. 별도 임시 스모크는 제한된 `ccc_api` 연결로 `getInstitutionReadiness`를 직접 실행하여 최초 관리자 영수증 연결, Auth subject 변경 시 연결 거부, 다른 기관 사용자 비노출을 확인했다. hosted Auth는 검증하지 않았으며 임시 파일은 실행 후 제거했다. FRONTEND에 이 커밋의 로컬 통합을 허용했고 해당 레인은 `9a8ee05`로 병합한 뒤 P3 구현을 진행한다. BACKEND는 D89 실행 환경과 설치 초대 구현 경로를 조사한다. 사용자 결정은 주 오케스트레이터 대화에서만 받고 각 레인에 전달한다.

2026-09-10 Q는 DESIGN의 좁은 export 수정 권한을 이번 베타에 필요한 월간 달력, 업무 바, 당사자 HERO 공용 부품까지 확대했다. `client-surface.ts`의 해당 export 줄은 DESIGN 단독 작성이며 FRONTEND는 검증된 커밋만 통합한다. STT 화면, API와 활성화 동작은 변경하지 않고, 프런트엔드에 같은 부품을 복제하지 않는다.

2026-09-10 Q는 Windows DPAPI 연결에 MIT 라이선스 `@primno/dpapi@2.0.1`을 고정하고 네이티브 임시 평문 메모리 삭제와 오류 경로 정리를 보완하여 사용하는 방식을 승인했다. 공개 배포 바이너리를 그대로 신뢰하지 않고 보완한 소스를 직접 빌드하며 Windows 실제 검증을 요구한다. 이는 의존성 및 소스 보완 승인이고 운영 키 접근, 계정 권한 변경이나 검증 전 Windows 완료 승인이 아니다. 패치와 빌드 출처는 BACKEND가 관리한다.

검증 후속: `b3dcf35`의 독립 API 경계는 PostgreSQL 포함 7파일 85개 테스트를 통과했고, 실제 Deno 실행에서 설치 정보 누락의 503 거부를 확인했다. `3230f0b`의 당사자 및 배정 계약은 7파일 75개 테스트와 제한 PostgreSQL의 목록, 허브, 배정 요청, 관리자 승인, 자기 승인 거부, 재승인 409 스모크를 통과했다. 로컬 어댑터는 검수에서 늦은 타이머의 과거 시각 전달과 손상 원음의 삭제 누락을 발견해 BACKEND에 돌려보냈으며, 수정 `50150ba`는 동결 설치 후 AudioStore 33개와 Scheduler 4개 테스트를 통과했다. Windows/NTFS, 실제 hosted 인증 및 세 모드 실행 완료를 뜻하지 않는다.

2026-09-10 Q는 기존 Windows 시험 PC에 공식 Microsoft C++ Build Tools 설치를 승인했다. 읽기 전용 점검에서 Windows 11 Pro 64-bit, Node 24.19.0, Git, 관리자 실행 권한과 C: 여유 240.2GiB를 확인했고 기존 STT 예약 작업은 Ready였다. 공식 winget 패키지 `Microsoft.VisualStudio.2022.BuildTools` 17.14.40의 C++ workload를 `--quiet --wait --norestart`로 설치해 exit 0과 `vswhere`의 VC.Tools.x86.x64 존재를 확인했다. 별도 임시 디렉터리에서 x64 C++ 실행 파일을 컴파일하고 실행해 exit 0을 확인한 뒤 임시 파일을 제거했다. 기존 STT 작업, 계정 권한과 전역 인코딩은 변경하지 않았고 재부팅하지 않았다. DPAPI 자체 검증은 별도다.

2026-09-10 Q는 기존 `account@socialsolidaritybank.onmicrosoft.com` 구독(활성, 소유자 역할)에 한국 중부의 최소 Community Cloud 업무 API 실행 환경 생성과 베타 배포를 승인했다. 범위는 Container Apps 사용량 요금제, 최대 1개 실행 인스턴스, 기본 이미지 저장소와 월 30달러 예산 알림, 필요한 공급자 등록과 이미지 읽기 권한이다. 예산 알림은 결제 상한이 아니며 할인 미적용 시 예시 상시 가동 비용은 약 월 25.3달러다. 기존 운영 데이터베이스, STT 리소스, 계정 권한과 새 유료 구독은 만들지 않는다. Supabase 관리자 자격은 업무 실행 환경에 주입하지 않으며 실제 격리 증거는 배포 뒤 검증한다.

## 통합 순서와 단일 작성자

1. P0: frontend가 기존 계획을 최신 main 및 settings 후보와 대조해 route별 재사용/이전/신규/선행 대기를 고정한다. 디자인은 필요한 공용 export를 확인한다.
2. BACKEND: 원본 settings 후보를 보존하고 최신 main 위의 전용 통합 브랜치에서 충돌을 해결한다. SQLite 0051/0052, PostgreSQL 0007~0009 중복 번호는 이미 적용된 main 파일을 변경하지 않고 새 forward migration으로 통합한다. 번호는 실제 통합 시점에 배정한다.
3. gateway.ts, request-handler.ts, runtime.ts 및 backend 소유 계약의 첫 통합 작성자는 BACKEND 하나다. privacy 후속도 이 파일에 동시 배정하지 않는다.
4. 잠긴 wire 컴포넌트/CSS/기존 디자인 하니스 변경은 BACKEND가 적용하지 않는다. 필요한 원본 후보 diff와 소비 계약을 DESIGN에 인계한다. apps/client 소스도 FRONTEND가 적용한다. 공유 파일까지 포함한 원본 커밋 전체 cherry-pick은 금지한다.
5. 루트 package.json/pnpm-lock.yaml, contracts package exports, migration parity/schema snapshot 등 공용 파일은 패킷별로 작성자와 슬롯을 먼저 정한다. 첫 backend 범위의 실제 소비 파일은 BACKEND가 소유하되 FRONTEND/DESIGN은 동시에 수정하지 않는다.
   첫 배치 BACKEND의 허용 범위는 `packages/core/**`, `packages/http-api/**`, `packages/contracts/**`, backend adapters, `apps/community-cloud/**`, `apps/api/**`, DB schema와 migration/parity, 해당 회귀다. 루트 manifest/lockfile의 첫 수정 슬롯도 BACKEND에만 배정하되 신규 외부 의존성은 별도 승인이다. FRONTEND/DESIGN 첫 배치는 P0와 공용 부품 인계 문서만 수정하며 제품 코드/공유 manifest/lockfile/검사는 건드리지 않는다. `client-surface.ts`의 STT export 소유는 변경하지 않고 필요한 변경을 별도 인계한다.
   Q는 2026-09-09 이 실행 중 `apps/web/app/components/wire/client-surface.ts`의 `WireItem`/`WireItemProps`, `Icon`/`IconName`, `WireRadioGroup` export 세 건만 DESIGN 레인에 인계하도록 별도 승인했다. 파일 전체의 소유권 이전이나 STT 시험 화면/API/엔진/활성화 변경은 아니다. 이 좁은 export 변경은 DESIGN이 단독 작성하고 다른 레인은 같은 파일을 수정하지 않는다.
6. backend 계약과 공용 export를 받으면 frontend가 P1/P2 및 초기 기관 준비를 연결하고 P3/P4/P5 상담 흐름을 검증한다. 이어 P6/P7/P8과 P9/P10을 기존 의존관계대로 닫는다.
7. 개인정보 생산자/코어 소비자의 버전과 근거 검증을 통합한다. 실제 NER 실패와 장문 누락은 안전 문제이며 통과를 꾸미지 않는다. 제품 STT 기본 off, sttEngine=null, 무자동전환과 활성화 승인 조건을 유지한다.

## 첫 실행 인계

FRONTEND는 P0의 완료 대응표와 P1 소비 계약을 준비한다. settings 후보의 client 소스를 재구현하지 말고 경계를 분석하되 backend API를 추측해 성공 화면을 만들지 않는다. 실제 구현은 인계된 계약에 한해 자기 client 파일에서 진행한다.

BACKEND는 먼저 TAKEOVER_READY로 머신/경로/브랜치/HEAD/추적 파일 상태와 보존된 미추적 경로, 원본 후보를 자기 Herdr pane에 보고한다. 총괄이 인수를 확인한 뒤 별도 구현 지시를 받아 backend-owned slice를 통합한다. hosted Supabase 작업, 시크릿 조회, provider 호출, 모델 활성화, migration 운영 적용, push/merge/deploy는 하지 않는다. 실제 호스팅 검증은 후속 단계의 별도 승인 뒤 수행하며 이 첫 배치의 로컬 증거로 대체하지 않는다. 디자인/client 변경 없이 성립하지 않는 컴파일 경계는 담당 레인 인계로 해결한다.

## 완료 증거

코드 존재, 계약 확정, 합성 fixture, 로컬 실제 HTTP, 실제 호스팅 인증, 사용자 브라우저 동작, 운영 배포를 구분한다. 각 인계에는 변경 경로, 기준/결과 커밋, 소비 DTO와 거부 코드, 실행한 명령과 결과, 실제 화면/저장 증거, 미완료 관문이 있어야 한다. 작업 도중 여러 레인이 공용 검사와 빌드를 동시에 돌리지 않으며 총괄이 검증 슬롯을 조정한다. 원문/키/토큰은 어떤 보고에도 넣지 않는다.

정리 전에 원본 경로/파일 목록/hash, 보존 커밋 또는 복사본의 일치, 실행 프로세스와 소유자의 인계를 확인한다. 워크트리 이름 변경이나 서버 종료를 소스 보존 증거로 간주하지 않는다. 기존 원본 삭제/reset/clean/stash와 브랜치 삭제는 첫 실행 범위가 아니다.

## 실제 런타임 검증 1차 (2026-09-10 11:3x)

합성 하네스가 아니라 실제 업무 런타임으로 확인했다. 구성은 실제 `handleRequest`, 실제 PostgreSQL 컨테이너에
`migrations/postgres/*.sql` 전부 적용, 실제 `apps/client` production 빌드, HTTPS 세 origin이고, 신원 공급자만
합성이다(hosted Supabase Auth 미프로비저닝). 통합 tip은 `105b151`이다.

- 통과: 로그인과 2단계 인증, 업무 셸 라우팅, `GET /me` 200, `/programs` 200, `/program-options` 200(admissionState ready),
  `/participants` 200(실제 빈 목록), `/settings/retention-policy` 200, `/settings/counseling-memory` 200,
  `/pii-retention/reviews` 200, `/staff-invites` 200, `/audit-log` 200.
- 막힌 것: `GET /me` 의 `institution.consentCopy.status` 가 `provider_registry_unavailable` 이고 여섯 영역 전부
  `disclosureAvailable:false` 다. 그래서 `initialSetupState` 가 `not_set_up` 이고 실제 당사자 등록 화면이 거부한다.
- 원인: `consent_provider_registry_snapshots` 에 운영 writer가 없다. 저장소 전체에서 마이그레이션(sqlite 0051,
  postgres 0007)과 테스트 픽스처 `seedProviderRegistry` 뿐이다. 합성 하네스가 이 표를 대신 채워 가려져 있었다.
- 배정: API 레인이 설치·기관 초기 설정 경로의 실제 writer와 계약 테스트를 구현한다. PostgreSQL 에서 `ccc_api` 는
  이 표에 SELECT 만 있으므로 신뢰 설치 경로에서 써야 한다.

### 같은 런타임에서 통과한 쓰기 흐름

- 실무자 초대 발급: 관리자 화면에서 이메일과 역할을 골라 만들면 서버가 1회용 토큰을 한 번만 보여 준다.
- 초대 수락: 공개 `/staff/join` 이 주소에서 토큰을 지우고 실제 기관 이름과 역할, 만료를 서버에서 받아 보여 준다.
  이름과 초대 이메일로 수락하면 가입이 끝난다.
- 소비 확인: `GET /staff-invites` 가 같은 초대를 `status:"used"`, `usedAt` 채워진 상태로 돌려준다. 전부 실제
  PostgreSQL 행이다.
- 읽기 화면: 일정(기관 시간대 Asia/Seoul), 실무자 초대, 당사자 초대, 내 정보, 기관 준비가 실제 응답으로 렌더된다.

### PWA 확인 범위 (2026-09-10)

실제 빌드가 `/manifest.webmanifest`(이름 CCC 사례관리, display standalone, start_url `/schedule`)와 `/sw.js`
(200, `text/javascript`)를 제공한다. 브라우저의 워커 등록 자체는 자체 서명 인증서 때문에 `SecurityError` 로 막힌다.
Community Cloud 매니페스트가 https 를 요구해 http 로 내려서 우회할 수 없으므로, 워커 등록의 실제 증거는 신뢰된
인증서를 쓰는 호스팅 배포에서 확인한다. 등록 로직 자체는 `apps/client` 단위 검사 64건(11파일)이 통과한다.

## 실제 런타임 검증 2차 (2026-09-10 12:2x)

API 레인이 `145d364` 로 동의 provider registry 설치 writer를, `89a1b9a` 로 통합 tip의 라우트 단언 3건을 닫았다.
통합 체크아웃에서 `routes.test.ts` 82건과 새 설치 계약 8건을 합쳐 90건이 통과한다.

같은 실제 런타임에 설치 writer를 태우고 화면으로 이어서 확인했다.

- 동의 문안: registry 설치 뒤 `consentCopy.status` 가 `available` 로 바뀌고 여섯 영역이 전부 발행 가능해진다.
- 기관 초기 설정: 관리자 화면에서 기관 이름과 첫 사업 이름을 저장하면 초기 설정이 `완료` 가 되고 첫 사업이 실제로 생긴다.
- 사업 도입 확인(D87): 저장 위치 기본형과 처리 경로 외부 허용을 고르고 관리자 확인을 저장하면 `확인 완료` 가 되고
  확인 시각이 남는다.
- 당사자 등록 화면: 잠금 안내가 사라지고 실제 사업과 실제 실무자 디렉터리, 서버가 발행한 여섯 영역 고지가 렌더된다.
- 막힌 것: 등록 제출이 `409 provider_scope_mismatch` 로 거부된다. 클라이언트가 여섯 영역 전부에
  `retentionDuration:"default_temporary_d85"` 를 보내는데, 게이트웨이는 `voice_original_retention_period` 에만
  그 값을 허용하고 나머지는 `null` 을 요구한다. 화면 문구도 이 코드에 맞지 않는 낙관적 동시성 문구를 쓴다.
  FRONTEND 레인에 정확한 요청 본문 증거와 함께 배정했다.

## 실제 런타임 검증 3차 (2026-09-10 12:5x, 통합 tip 35bfbf2)

FRONTEND가 세 결함을 닫았다. `72806ef` 는 보유기간을 음성 원본 영역에만 싣고 409 문구를 가른다.
`c537e0d` 는 허브·인테이크 응답 키 계약을 실제 서버 모양에 맞춘다. `1c4727b` 는 인테이크 일정을 세부 목표로 보내고
담당이 아닐 때의 403을 정확히 안내한다.

같은 실제 런타임에서 한 사람의 업무 흐름을 처음부터 끝까지 통과시켰다.

1. 기관 초기 설정 저장과 D87 사업 도입 확인.
2. 당사자 등록: 여섯 영역 동의와 함께 `201`, 가명 ID `swallow-001` 생성.
3. 당사자 허브: 이름, ID, 연락처, 참여 사업, 담당 실무자, 여섯 영역 동의 현재값이 실제 행에서 렌더된다.
4. 상담 일정 등록: 인테이크 회차가 `201`, 일정 화면 주간 보기에 오전 10시 인테이크로 뜬다.
5. 15초 페이지: 전체 목표 입력 자리, 세 영역, 미해결 액션 카드가 실제 응답으로 렌더된다.
6. 인테이크 기록: 4단계 질문지를 채워 `201` 저장.
7. 상담 기록 목록과 전체 상담 리포트: 저장된 근거만 읽어 회차, 상황 변화, 목표와 방향, 위험 신호 구획을 만든다.
   근거 없는 구획은 "자료가 없어 빠진 구획" 으로 표시한다.

남은 것은 Windows Local Single 실행 증거와 신뢰 인증서를 쓰는 호스팅 배포다.

## 실제 런타임 검증 4차 (2026-09-10 13:2x)

- 정기 상담 기록 저장: `201`, 목록과 15초 페이지 회차 정리에 수기 배지로 뜬다.
- AI 정리 검토 화면: 설치가 AI를 끄고 있어 "AI 처리가 꺼져 있습니다" 와 "이 회차에는 AI 초안이 없습니다" 를
  보여 준다. D77 기본값 그대로다.
- 공개 요청 링크: `PUBLIC_SIGNUP_ENABLED` 를 켠 설치에서 실무자가 링크를 발급하면 `201` 과 1회용 토큰이 오고,
  로그인 없이 연 `/join` 이 기관 이름, 만료, 여섯 영역 고지를 서버에서 받아 렌더한다. 제출은 `201` 로 새 당사자와
  사업을 만든다. 서버는 이미 쓴 링크를 `status:"used"` 로 돌려준다.
- 남은 화면 지적 2건은 FRONTEND에 배정했다. 스위치가 꺼진 설치의 404를 전송 오류처럼 안내하는 문구와,
  이미 쓴 링크를 다시 열었을 때 방금 접수된 것처럼 보이는 완료 화면이다.

2026-09-10 Q는 베타 Cloud 실행을 위해 서울 리전 Supabase 프로젝트를 새로 만들고 그 값을 전달하기로 했다.
D76·D82대로 Supabase Auth와 MFA, RLS를 그대로 쓴다. 계정 로그인과 프로젝트 생성은 사람이 하고, 총괄은 값을
승인된 주입 경로로만 받아 쓰며 화면과 기록에 남기지 않는다. 승인된 Container Apps 예산 밖의 유료 등급은 만들지
않는다.

## Cloud 컨테이너 확인 (2026-09-10 15:4x)

API 레인이 `d2fedd6` 로 Community Cloud 실행 이미지를 만들었다. 총괄이 통합 체크아웃에서 직접 확인한 것은
`community-cloud-runtime` 19건 통과, `docker build` 성공, 설정 없이 띄우면 `installation_unavailable` 한 줄만
남기고 종료, 그리고 평문 PostgreSQL 접속 문자열을 준 실제 기동에서도 같은 fail-closed 로 거부한다는 점이다.
런타임이 `verify-full` 을 강제하므로 TLS 없는 데이터베이스로는 뜨지 않는다. 이미지 push 와 배포는 하지 않았다.

## Windows Local Single 실행 증거 (2026-09-10 16:15)

번들 `e7-local-service-v20`(SHA `158d330888858b283f88cc62d9a1dc6534f646ef4cd34f195681485a7766a65a`)이 실제
Windows 11 시험 PC에서 14단계를 모두 통과하고 exit 0으로 끝났다. 단계는 validate, setup, copy-source,
verify-copy, install, build-native, dpapi-tests, dpapi-protect, dpapi-store, build-adapters, encrypted-sqlite,
audio-store, scheduler, identity, cleanup이다.

여기까지 오면서 실제 제품 결함 여섯 개를 잡았다. 번들의 macOS 확장 속성 항목, 하드코딩된 패키지 관리자 실행
방식, 잠긴 stdout·stderr 리다이렉션, 존재하지 않는 native export 가정, strip-only 런타임이 못 읽는 TypeScript
문법과 확장자 없는 상대 import, Windows가 지원하지 않는 디렉터리 fsync, 그리고 한글 사용자 경로에서 깨지던
디렉터리 생성이다. 마지막 두 개는 기관 PC가 한글 계정을 쓰는 한 실제 운영에서 그대로 터졌을 결함이다.

남은 것은 hosted Supabase Auth와 MFA를 쓰는 Cloud 실행, 그리고 신뢰된 인증서 아래의 워커 등록 확인이다.

## 통합 상태 (2026-09-10 16:5x)

BACKEND의 Windows 수정을 통합하면서 공유 계약 하나를 되돌렸다. 그 브랜치는 `packages/contracts` 의 export를
`./src/*.ts` 에서 `./dist/*.js` 로 바꿨는데, 그러면 api·web·client 해석과 CI가 한 번에 깨진다. 통합 tip은 소스
export를 유지하고 build 스크립트만 받았으며, 번들은 자기 쪽에서 컴파일하도록 되돌려 보냈다. 그 뒤 BACKEND가
`packages/contracts/src` 의 확장자 없는 상대 import를 전부 명시로 바꾸고 가드를 넓혔다.

통합 tip 검증: api·client·community-cloud 타입검사 통과, 가드 3종 통과, client 단위 72건 통과,
Cloud 런타임·리포트·동의 registry 계약 31건 통과, audio-file과 scheduler-node 어댑터 5건 통과.
실제 런타임 재확인도 통과했다. 기관 초기 설정, D87 확인, 여섯 영역 동의 등록, 허브 렌더가 그대로 동작한다.

검증용 실행기는 `apps/api/test/tools/real-backend-preview.mjs` 로 옮겼다. `apps/api/tools/` 에 두면
`guard-db-gateway` 가 게이트웨이 우회로 잡는데, 그 지적이 맞다.

## Cloud 실행 환경 생성 (2026-09-10 18:0x)

Q가 Aside 브라우저로 Azure CLI 로그인을 마쳐 승인된 구독(`Azure subscription 1`, 소유자 계정)에 접속했다.
디바이스 코드 흐름은 AADSTS530035로 막혔고 대화형 브라우저 로그인은 통과했다. 조건부 액세스 정책은 없었고
포털은 라이선스 부족 안내만 보였다.

승인 범위 안에서 만든 것은 셋이다. 리소스 그룹 `ccc-beta-krc`(한국 중부), 컨테이너 레지스트리
`cccbetakrc0470`(Basic), Container Apps 환경 `ccc-beta-env`(로그 대상 없음). 업무 API 이미지
`ccc-community-cloud:0.9.0`을 레지스트리에서 빌드했다. 구독 예산 알림 `ccc-beta-monthly`를 월 30달러
실제 사용액 80% 기준으로 걸었다. 예산 알림은 결제 상한이 아니다.

아직 앱은 만들지 않았다. 업무 런타임은 `CCC_DATABASE_URL`이 TLS `verify-full`로 붙는 실제 PostgreSQL을
요구하므로 Supabase 프로젝트 값이 오기 전에는 기동하지 않는다.

## P9 공유 자산 독립 (2026-09-10 18:2x)

DESIGN이 브라우저 중립 Wire UI를 `packages/wire`(`@ccc/wire`)로 뽑고, FRONTEND가 `apps/client`의 소비를
그 패키지로 옮겼다. 이제 업무 클라이언트는 Next 앱 패키지를 하나도 참조하지 않는다(`@ccc/web` 참조 0건).

통합에서 두 가지가 깨져 돌려보냈고 둘 다 닫혔다. 첫째, 패키지가 `.tsx` 를 그대로 내보내 `@ccc/api`
타입검사가 `TS6142` 로 무너졌다. 둘째, `token-audit` 이 옛 위치의 CSS 정본을 읽어 `apps/web` 마크업 클래스
189개를 "스타일 없음" 으로 잡았다. 감사 CSS 출처는 새 패키지와 클라이언트 전용 CSS 둘 다 읽도록 합쳤다.

현재 통합 tip 증거: 타입검사 4종 오류 0, token-audit 0, align-audit 0, 위계 감사 통과, client 72건,
routes와 Cloud 런타임 101건, 실제 런타임 브라우저 완주 재확인.
