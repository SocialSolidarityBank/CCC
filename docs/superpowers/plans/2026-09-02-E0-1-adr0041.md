# E0-1 ADR-0041 and v0.3 Documentation Implementation Plan

> **For agentic workers:** This documentation ticket is complete in one pass. The parent session owns repository-wide validation after this branch is committed.

**Goal:** Publish the Q-approved ADR-0041 decision and synchronize the root development contract and S1 specification template with the CCC Open Pilot v0.3 plan.

**Architecture:** ADR-0041 is the policy source for D76-D83. `CLAUDE.md` carries a compact operational index while retaining the existing D31, D38, and ADR-0042/D84 entries. The specification template separates contract closure from implementation evidence so `확정` can gate implementation tickets without requiring adapters to exist.

**Tech Stack:** Markdown, ADR records, `CLAUDE.md`, CCC Open Pilot v0.3 documentation contracts.

**Spec:** `CCC_OPEN_PILOT_PLAN.md` E0-1, `inbox/0041-one-core-three-deployment-modes.md`, and the published `docs/adr/0041-one-core-three-deployment-modes.md`.

## Global Constraints

- The approval date is 2026-09-02 and the decision maker is Q.
- Community Cloud, Local Single, and Local Office are all formal implementation modes.
- STT is `off` immediately after installation in all three modes; no engine is auto-selected before STT-G1~STT-G3 and Q approval.
- Community Cloud original audio is temporary data in institution-owned Supabase private Storage.
- Original audio is retained through the next business day’s first Agent processing opportunity, deleted immediately after processing, and deleted with an administrator incident if still unprocessed 24 hours after first Agent availability.
- SG6 has exactly seven fail-closed codes and no external AI call is made in those states.
- Goal-model review belongs to E2-8.
- The runtime has seven ports: `Database`, `AudioStore`, `Identity`, `SecretStore`, `Scheduler`, `STTProvider`, and `AIProvider`.
- D65, D67, and D68 are historical decisions superseded by ADR-0041 D76-D81; D53’s safety rules remain and its engine/default decision points to D77 and STT-G1~STT-G3.
- SQL/driver boundary ownership is `guard-db-gateway`; platform import ownership is E1-6’s `guard-core-imports`.
- The PII vault key is `PII_ENC_KEY` from `SecretStore`, held as a Cloud Edge secret in Community Cloud and in Local DPAPI.
- The ADR separately names Notion ADR-0040 §8.2/§14 and the former PR #210/current ADR-0042 Workers relay and 30-day policy as superseded while preserving D84’s read-only preflight.
- S1’s corresponding canonical contract file is `docs/specs/S1-database-sql-subset.md`; E3-1a and E3-1b own implementation artifacts. S1 completion criteria describe document contents, not artifact or test existence.
- `CLAUDE.md` D84 remains the ADR-0042 read-only Supabase preflight decision and is not renumbered or overwritten.
- This branch changes documentation only. No implementation files, PR state, Linear state, or external inbox files are changed.

---

### Task 1: Publish ADR-0041

**Files:**
- Create: `docs/adr/0041-one-core-three-deployment-modes.md`

**Interfaces:**
- Consumes: E0-5a approval and E0-5b number allocation from `CCC_OPEN_PILOT_PLAN.md`.
- Produces: approved D76-D83 policy for modes, STT, core ports, database, client boundary, privacy/audio, consent/identity, and installation/release.

- [ ] Set status to approved on 2026-09-02 by Q.
- [ ] Record all five approved decisions without retaining the rejected faster-whisper default or upload-plus-24-hour clock.
- [ ] Enumerate the seven SG6 fail-closed codes and their fixed screen copy.
- [ ] Define SG8 audio timing, Supabase private Storage ownership, deletion evidence, and administrator incident behavior.
- [ ] Move the goal-model review open item to E2-8 and keep STT-G1~STT-G3 as the engine decision gate.
- [ ] Mark D65, D67, and D68 as historical and superseded by the appropriate ADR-0041 decisions, while preserving D53 safety rules.
- [ ] Name Notion ADR-0040 §8.2/§14 and former PR #210/current ADR-0042 Workers relay and 30-day policy as superseded, while preserving D84 read-only preflight.
- [ ] Keep ADR-0042/D84 as the separate read-only Supabase preflight decision.

### Task 2: Synchronize CLAUDE.md

**Files:**
- Modify: `CLAUDE.md` sections 2, 4 R1, 5, 8, and 9

**Interfaces:**
- Consumes: published ADR-0041 D76-D83.
- Produces: compact operational guidance and D76-D83 decision rows before the preserved D84 row.

- [ ] Replace the obsolete D1/R2/R2-era deployment stack summary with the three modes and their storage/client boundaries.
- [ ] Update R1 to the `packages/core` gateway facade and narrow Database port, assigning SQL/driver checks to `guard-db-gateway` and platform import checks to E1-6 `guard-core-imports`.
- [ ] Update the AI pipeline to STT `off`, Agent processing opportunity, immediate deletion, seven fail-closed codes, OpenAI `store:false`, and E2-8 ownership.
- [ ] Replace superseded STT and Cloud audio questions in section 8 with the remaining gates and implementation evidence.
- [ ] Mark D65, D67, and D68 superseded and point D53 to D77 and STT-G1~STT-G3.
- [ ] Insert D76-D83 in numerical order immediately before the existing ADR-0042 D84 row. Do not alter D31 or D38.

### Task 3: Move and revise the specification template

**Files:**
- Create: `docs/specs/SPEC-TEMPLATE-and-S1-example.md`

**Interfaces:**
- Consumes: inbox template and v0.3 plan paths and commands.
- Produces: common S1-S15 template with contract-first status lifecycle.

- [ ] Define statuses as `초안 → 검토 → 확정 → 구현 검증 완료`.
- [ ] Require interface, rule table, mode differences, fixture definition, verification commands, and expected verdict for `확정`; do not require implemented adapters.
- [ ] Define `구현 검증 완료` as implementation plus actual runtime/test evidence owned by the related E ticket.
- [ ] Make `docs/specs/S1-database-sql-subset.md` the only canonical S1 output path and assign implementation artifacts to related E tickets. The S1 related-ticket list is exactly E1-2, E3-1a, E3-1b, E3-2, E3-3, and E3-4.
- [ ] Keep S1 §4 criteria document-contract-only: define interfaces, rules, fixture inputs/expected outcomes, parity rules, and verification commands without requiring gateway, migration, or test artifact existence.
- [ ] State in the template that each corresponding `docs/specs/S{n}-*.md` file, together with its GitHub Issue, is canonical; this template only provides the common form and S1 example.
- [ ] Correct S1 paths to `migrations/sqlite`, `migrations/postgres/0001_baseline.sql`, and the canonical S1 spec path.
- [ ] Correct S1 verification commands to the v0.3 `pnpm test:contracts --db=...`, `pnpm guard:sql-dialect`, and `pnpm guard:migration-parity` names.

### Task 4: Record the ticket-local plan and commit

**Files:**
- Create: `docs/superpowers/plans/2026-09-02-E0-1-adr0041.md`

- [ ] Keep the plan limited to documentation owned by E0-1.
- [ ] Commit ADR-0041, the CLAUDE.md synchronization, the specification template, and this plan together.
- [ ] The parent session runs focused and project-wide validation after all parallel tickets land; this worker runs no validation commands.
