# DESIGN-RULES.md, 화면 작업 전에 읽는 규칙

**여기서 새로 정하는 것은 없다.** `DESIGN.md` 와 pen 의 규칙을 옮겨 적은 것이라 어긋나면 원문이 맞고 이 파일이 오류다(색값은 pen, 색 밖은 DESIGN.md). 장 제목의 `§N-M` 이 원문 자리다.

뺀 것은 원문에서 본다. 레이아웃 §4, **카드와 컴포넌트 계약 §5**, 모션 §6, 접근성 예외 §9, 문안 §10, 다크 모드 §11.

## 0. 낱말 둘

화면이 답답해 보이는 원인은 거의 이 둘을 헷갈린 것이다.

| 낱말 | 뜻 | 판정 |
| --- | --- | --- |
| **눌린 쌓임** | 중요도가 다른 값이 **같은 옷**(크기, 굵기, 색이 셋 다 같다)으로 세로로 쌓인 것 | 위반. 고친다 |
| **나열** | 본래 대등한 것이 나란한 것(목록 항목, 표의 행, 형제 구획) | 정상. 둔다 |

## 1. 위계 표 (§2-2 규칙 3)

카드 안에서 쓸 수 있는 단은 아래 표가 전부다. **표를 임의로 늘리지 않는다.** 표 밖 조합(예: 16/400 `--sub`)은 위반이다.

⑤ 는 2026-08-25 Q 지시로 난 자리 하나짜리 단이다. 일정 기간 이름은 누를 수 없는 읽는 값인데 양옆이 전부 컨트롤이라, 본문 단으로 두면 컨트롤보다 세게 읽힌다. **다른 자리에 퍼뜨리지 않는다.** ⑥은 같은 날 Q가 공통 입력값을 14px로 통일해 추가했다.

| 단 | 이름 | 크기/굵기/색 | 서는 자리 |
| --- | --- | --- | --- |
| ① | **제목** | 16/600 `--ink` | 카드 제목(`.wire-card-title`), 항목 제목(`WireItem`) |
| ② | **라벨** | 14/600 `--sub` 또는 계열 deep | 구획 라벨(`WireCardSection`), 입력칸 라벨, 사이드바 메뉴 묶음 머리(2026-08-30 Q 3차) |
| ③ | **본문** | 16/400 `--ink` | 읽는 값, 문단 |
| ④ | **설명** | 14/400 `--sub` | 메타(`.panel-meta`), 자동 저장 상태 |
| ⑤ | **기간 값** | 14/500 `--ink` | 일정 기간 이름(`.schedule-period-label`) 하나뿐 |
| ⑥ | **입력값** | 14/400 `--ink` | 공통 input·select·textarea |
| ⑦ | **입력칸 도움말** | 13/400 `--sub` | `.wire-form-hint`(2026-09-04 Q, 구 14) |
| ⑧ | **폼 묶음 제목** | 16/600 민트 deep | `.wire-fieldset>legend`(2026-09-04 Q) |
| ⑨ | **상담 기록 작성 안내** | 12/400 `--sub` | 작성 화면의 `.record-writing-help`만. 입력 도움말, 설명, 빈 상태 안내, notice와 `goalSection` 안내 포함(2026-09-07 Q) |

②가 ③보다 작은 것은 의도다. 크기가 겹치면 위계가 사라진다.

**이 표는 읽는 줄의 계약이다.** 누르는 것(버튼, 탭, 토글, 단계 버튼)은 자기 면과 테두리로 이미 구분되므로 표를 따로 쓴다(원문 §2-1 '누르는 것은 표를 따로 쓴다'). 값이 아직 없을 때(`.is-empty`)와 비활성은 새 단이 아니라 **상태**다 — 크기와 굵기는 그대로 두고 색만 `--sub` 로 물러선다.

- **연속 금지.** 이웃한 두 줄이 크기, 굵기, 색 셋 다 같으면 위반이다. 최소 한 축을 바꾼다. 나열은 예외다.
- **셋 이상이면 장치를 쓴다.** 한 묶음에 줄이 3개 이상이면 다음 장치 중 하나를 반드시 쓴다. 가로선(`.wire-card-divider`), 카드 분리, 상태 낱말을 배지로, 2열로 가르기.
- **상태 낱말은 배지로 올린다.** '미기록', '3회차' 같은 분류와 상태는 `WireBadge`로 낸다. 단, HERO는 상태도 라벨/값으로 표시하는 §5 계약이 우선하며 승인된 배지 예외는 없다(2026-09-08 Q).
- **그 배지는 제목 글자 바로 뒤에 붙는다**(2026-09-04 Q 전역 기준, 구 카드 머리 양끝 배치 대체). 카드 제목·접힘 카드 제목·입력칸 라벨 모두 같다. 오른쪽 끝으로 미는 것은 행동 버튼과 꺽쇠뿐이다. HERO에는 배지를 두지 않는다.
- **배지와 버튼은 여백을 바꾸지 않는다**(같은 날). 배지가 설 수 있는 줄은 배지 유무와 무관하게 높이 22(`--badge-height`)를 예약한다.
- **중요한 값을 민짜 본문으로 두면 위반이다**(§2-3). 강조 어휘는 배지, 굵기 600, 계열 컬러 셋뿐이다.
- **긴 읽는 본문은 14/400 `--ink`(⑥ 조합 재사용)다**(2026-08-28 Q). 문장으로 읽는 본문은 회차 핵심 한 줄, 회차 정리, 수기 메모, 일반 세션 목표, 전체 목표 문구, 불릿 목록(`.wire-bullets`), 일반 미해결 액션 내용, 당사자 초대 안내 문단이다. 상담 기록 좌측 레일의 세션 목표와 미해결 액션 본문은 §5가 정한 14/600 예외다. 짧은 값과 제목은 ③① 그대로 16이다. 섹션 패널(`.record-section`)이 카드 안 카드로 폭을 좁힌 것이 계기였으나, 같은 값이 한 벌로 서도록 패널 밖 같은 값에도 적용한다.
- **당사자 초대 화면의 기준은 한 문장이다**: 실무자에게 하는 안내 본문은 14, 입력칸 도움말 `.wire-form-hint`는 전역 규칙대로 13, 당사자에게 보여 주는 값은 16이다. 링크 주소·QR·이메일 문안과 그 라벨이 당사자에게 보여 주는 값이다. 스코프는 `.participant-invite-stack`이고 실무자 초대·공개 가입 화면의 읽는 값은 16을 유지한다.
- **사이드바 메뉴 묶음 머리만 그라데이션 글자다**(2026-08-30 Q 4차, `.navigation-section-title` 14/600 + `--gradient-deep` `background-clip:text`). 셸 묶음 머리 한 자리 밖으로 퍼뜨리지 않는다. 대비는 AA 미달이고 §9 에 예외로 기록했다.
- **읽기 전용 정보 줄과 당사자 정보 허브의 내용 값은 14/400이다**(2026-08-31 Q, 구 일부 16). 이름과 카드 제목은 자기 제목 단을 유지한다. 동의 체크 라벨은 등록, 당사자 정보, 자기 가입, 종결 화면 모두 14/600 `--ink`이며 `line-height:normal`과 `cursor:pointer`를 공용 `.consent-checkbox`가 갖는다. 동의 전문 제목은 14/600 `--sub`다.
- **인테이크 작성 선택지와 인테이크 조회 문답은 14px이다**. 선택지는 누르는 값이라 14/600이고 `WireChoice`의 체크박스 또는 라디오와 텍스트만 쓴다. 선택지 행에는 별도 아웃라인과 알약을 쓰지 않는다. 조회 값은 14/400이다. 상담 기록 작성 레일의 글자 크기와 굵기는 §5 전용 계약이 정한다. 카드 제목과 짧은 핵심 값은 기존 16 단을 유지한다.
- **`WireDataRows`의 선은 ROW 사이에만 둔다**(2026-09-03 Q). 첫 ROW 위와 마지막 ROW 아래에는 선이 없고 위아래 내용 여백이 같아야 한다.
- **인테이크 작성과 조회의 단계 레일은 `IntakeStepRail` 하나다**(2026-09-03 Q). 레일 제목은 두 모드 모두 `인테이크 4단계` 16/600이고 번호는 배지 없는 14/600 텍스트다. 버튼은 번호 `2ch`·제목 유동·건수 `4ch` 3열이며 열 사이는 8px이다. 조회와 수정 모두 건수를 `완료/전체` 비율로 표시한다. 질문 밖 필수인 상담일, 부채 표 첫 행, 연계 기관 표 첫 행, 종합의견도 분모에 포함하므로 단계별 전체는 11, 21, 8, 6이다. 조회는 `11/11`, 수정은 현재 진행값을 `6/21`처럼 표시한다. 번호와 건수는 줄지 않고 제목만 줄어든다. 단계별 별도 배경색은 쓰지 않고 기존 현재 단계 블루와 필수 누락 리스크 상태만 유지한다.
- 인테이크 본문의 현재 단계 제목 줄은 조회의 `전체 열기`·`전체 닫기` 버튼 유무와 관계없이 `--pill-height` 32px을 예약한다. 작성, 수정, 조회에서 첫 카드 시작 위치가 같아야 한다.
- 인테이크 조회 아코디언의 소절 제목은 `role="heading"`과 `aria-level="3"`을 가져 단계 h2 아래의 제목 탐색 구조를 유지한다.

## 2. 부품 2종 (`app/components/wire/wire-section.tsx`)

카드 안 구획과 항목을 **손으로 쌓지 않는다.** 아래 둘이 위계 표를 부품에 박아 둔 것이다.

- **`WireCardSection`** = 구획 하나. 라벨(②) + 내용. **형제 구획이 이어지면 구분선이 저절로 붙어** 위 '셋 이상이면 장치'의 가로선을 대신 만족시킨다. `tone`은 `sub`(기본), `mint`, `lavender`, `discrepancy`다. 구획 전체 행동은 `action` 슬롯으로 카드 오른쪽 끝에 둔다.
- **`WireItem`** = 항목 하나. `title`(①) + `description`(④) + `status`(배지) + `action`(14/600 계열색). 개별 항목 행동은 항목 텍스트 뒤에 두고 카드 끝으로 밀지 않는다.
- **둘 다 `className` 슬롯이 없다.** 바깥에서 크기와 굵기와 색을 정하는 길이 곧 계약을 비켜 가는 길이다. 모양이 모자라면 자기 클래스를 만들지 말고 부품을 고친다.

## 3. 형태 어휘 3종 (§3-1)

| 형태 | radius | 무엇이 이 형태인가 |
| --- | --- | --- |
| **사각** | 6 (`--radius-control`) | 입력하는 것과 장소 이동. 입력칸, 선택창, 내비 항목 |
| **알약** | `--radius-pill` | 글자 버튼과 읽기 전용 상태. 버튼 전 종류, 배지, 칩, 상태 태그. **선택지 행은 알약이 아니다**(2026-09-04 Q 2차 폐기) |
| **원** | 원형 | 아이콘 버튼, 라디오. 셀 아이콘 버튼과 일정 기간 이동 버튼은 32(`--pill-height`) |

**전역 버튼 개정(2026-08-25 Q):** 모든 `.wire-button`은 알약이다. 입력·선택창과 내비
항목은 사각 6을 유지한다. 일정 이동 화살표는 32px 원형 아이콘 버튼이다.

**버튼 높이 단일(2026-08-28 Q):** 전 버튼 높이는 **32 하나**다(구 2026-08-27 "md 40 / sm 32
자리별 2단" 폐지). HERO·폼 행동이 40 이라 툴바·목록 32 옆에서 홀로 커 보이던 것을 없앴다.
`WireButton` 의 `height` 축과 `data-height` 는 제거됐다. 예외 하나 — 입력칸에 붙은 조작(달력
토글, 인라인 값 편집 트리거)은 `.wire-button` 이 아니라 자기 클래스로 **입력 높이 정렬
계약**(40, `--control-height`)을 따른다. kit 페이지의 전시용 버튼은 감사 대상이 아니다.

**버튼 아이콘(2026-09-06 Q, 2026-09-07 위저드 추가):** 등록·초대·저장류 행동 버튼(당사자 등록,
상담 등록, 당사자 초대, 가입 링크 만들기, 등록하기, 폼 확정 저장(기록지·세션 목표·기본정보·동의서),
수정 완료, 위저드 마지막 단계의 `완료`/`저장`(일정 등록·인테이크))은 `WireButton icon` 슬롯에
사이드바 `NavIcon` 또는 `Icon check` 16px을 단다. 같은 목적지는 사이드바와 같은 아이콘이다(당사자
등록 `participant-add`, 상담 등록 `calendar-plus`, 초대 `invite`. 2026-09-09 Q D88이 구 `calendar` 대체,
어휘 표는 §9). HERO 행동 3개(당사자 정보·상담 기록
확인·상담 기록), 이동·보기(neutral) 버튼, 위저드의 `이전`/`다음`, 인라인 편집 짝의 저장·취소,
danger 버튼에는 달지 않는다.

**공용 아웃라인(2026-08-25 Q):** 카드·버튼·배지는 `--wire-outline-width: 1px`와
`--wire-outline-color: var(--line)`을 같은 시작점으로 쓴다. `--line`은 라이트 `#D7D3CE`,
다크 `#514A5B`로 대비를 한 단계 높인다. 선택 카드와 세컨더리 버튼은
기존 `--gradient-brand` 아웃라인, 의미 톤 배지는 자기 면과 같은 톤, 위험 행동은
`--risk` 1.5px 예외다. 화면별로 중립 카드선을 진하게 덮어쓰지 않는다.

카드와 패널, 배너, 모달은 radius 12(`--radius-card`)로 이 셋 밖에 선다. 카드의 경계와 그림자, 카드 안 카드 금지, 버튼이 서는 자리는 §5가 갖는다. **카드 안 카드 금지의 예외 둘**은 ① 섹션 패널(`.record-section`, 2026-08-28) ② **반복 행 카드**다. 허브의 참여 사업 행, 최신 일정 링크, 동의 묶음, 등록 동의 텍스트 상자는 컨테이너 카드 안에서도 낱개 카드다. 허브 목표 트리의 전체 목표와 세부 목표 구획은 **위아래 두 행**이고 문구는 한 줄 말줄임으로 폭을 지킨다(2026-08-31 Q, 구 가로 2열 대체). 브리핑 카드 구획, 회차 카드 산출물 항목, 등록 동의의 자세히 읽어보기 아코디언도 같은 반복 행 카드다. 상자는 공용 `.wire-repeat-card`가 만들고 등록 동의 상자만 radius 6을 쓴다.

## 4. 색 계열 의미 (§5 D58, §1-4)

계열의 뜻은 **고정이다.** "여기만 예쁘게" 고르지 않는다.

| 계열 | 뜻 |
| --- | --- |
| **블루** | 채움형 배지는 시간 축만: 오늘, 날짜, 주차, 기한 |
| **민트** | 사람, 소속, 진행, 담당 |
| **라벤더** | AI 산출, 승인 대기, 주의, 필수 표식 |
| **코랄, 시안, 라이트마젠타, 앰버, 라임** | 여러 형제 배지의 구분 variation. 기본 배정 순서는 민트, 라벤더, 코랄, 시안, 라이트마젠타(#D96BC8)이고 라임과 앰버는 최후순위 폴리백(2026-08-24 Q 결정). 상담 유형은 기본 상담=민트, 인테이크=라벤더. 성공, 경고, 우선순위를 뜻하지 않음. 예외로 상담 기록 레일의 세부 목표와 지난 상담 날짜만 접근 가능한 `--lime-deep` 글자를 쓴다 |
| **리스크 핑크** | 확인된 경고 전용(D9). 다른 어디에도 쓰지 않는다 |

- 블루·민트·라벤더는 **base(게이지·막대 면), deep(일반 계열 글자·채움형 배지 면), tint(옅은 배경)** 3단이고 섞지 않는다. **base 를 글자에 쓰지 않는다.** `--lime-deep`은 상담 기록 레일의 세부 목표와 지난 상담 날짜만 쓰며, 새 색값이 아니라 승인된 고대비 lime 값을 재사용한다.
- **deep 글자는 곁다리로 읽는 칩에 쓴다.** 색상 배지는 전용 `--badge-*` 면을 쓰고, 전경은 테마 고정 `--on-badge`로 라이트는 밝게 다크는 어둡게 반전한다. 라이트마젠타만 전경이 `--on-badge-light-magenta`다.
- 색상 배지는 라이트에서 Pen deep 면, 다크에서 base 면과 같은 아웃라인을 함께 쓴다. 외곽선은 별도 색 대비를 만드는 선이 아니라 채움의 경계를 고정하는 계약이다. neutral 배지는 `--ink` 글자를 유지한다. 일반 리스크 영역은 `--risk-tint-solid`를 유지한다.
- 브리핑의 **내용 불일치**는 판단이나 안전 리스크가 아니라 확인할 기록 차이다. 제목은 전용 코랄 글자 `--discrepancy`, 건수 배지는 코랄 variation으로 표시하고 `--risk`와 리스크 배너 면은 쓰지 않는다.
- 새 색과 새 그라데이션을 만들지 않는다. 배지 variation은 승인된 코랄, 앰버, 라임, 시안, 라이트마젠타 다섯 가지뿐이다. 라이트마젠타는 승인 hex #D96BC8 하나만 쓰고 다크 별도 음영을 만들지 않으며, 그 면 위 글자만 전용 `--on-badge-light-magenta`를 써 두 테마 모두 대비 6.29로 통과한다(그라데이션 어휘는 §3-3).
- blue 채움 배지는 `TimeAxisBadge`만 만든다. `WireBadge tone="blue"`를 화면에서 직접 쓰지 않는다.
- 정보형 `WireCallout`의 blue tint는 배지가 아닌 안내 표면이다. 이 예외를 배지로 확장하지 않는다.
- **HERO는 모든 정보를 라벨/값으로 표시한다**(2026-09-08 Q). 진행 상태와 AI 검토 상태도 예외가 아니다. 승인된 HERO 배지 예외는 없으며, 필요하면 Q에게 제안하고 승인받기 전에는 적용하지 않는다. 목록과 일반 카드의 배지 규칙 및 별도 리스크 배너는 유지한다.
- HERO 배지 금지는 운영 화면의 기본 계약이다. **배지를 포함한 시안 또는 향후 Q가 승인한 예외의 배지 행은 라벨/값 정보 전체 아래**에 둔다. 이름 옆이나 정보 오른쪽은 금지다. 현재 배지 후보는 시안용이며 운영 예외는 승인되지 않았다. 이 규칙을 이유로 `stageTags` API를 복원하거나 운영 배지를 추가하지 않는다(§3-1).
- 일반 화면의 `전체 목표`·`세부 목표`·`세션 목표` 라벨은 14/600 민트 deep으로 통일한다. 목표 문구는 색을 받지 않는 14/400 `--ink`다(2026-08-31 Q, 구 세부 목표 16 대체). 상담 기록 좌측 레일의 세부 목표와 지난 상담 날짜는 §5가 정한 13px lime 예외다.
- 일정 등록 스텝(`.schedule-wizard-steps`)의 **완료는 무채색**이다. `--ink` 면 + `--on-badge` 글자(라이트 다크 배지, 다크에서는 `--ink` 가 밝아 화이트 배지. 2026-08-30 Q). 현재 스텝은 활성 채움(`--gradient-action`)에 **프라이머리 버튼과 같은 `--line-action` 1px 아웃라인**이다(2026-08-30 Q 2차, 구 그라데이션 아웃라인 대체), 대기는 `--line` 아웃라인이다. 기본 `WireSteps` 의 완료 민트(진행 축)는 다른 자리가 생기면 그대로다.
- **단계 표시를 탭으로 그리지 않는다**(2026-09-09 Q, D88). 순서가 있는 흐름은 `WireSteps`가 그리고 `role="tablist"`를 쓰지 않는다. **이미 지나온 단계 칩만 버튼**이고 현재와 앞 단계는 누르지 못하는 읽기 항목이며 `aria-current="step"`은 그대로다. 탭으로 그리면 앞 단계가 눌릴 것처럼 보이고 보조기기는 "선택 안 됨"으로만 읽어 막힌 이유를 알리지 못한다.

## 5. 안내는 13px, 배지와 기록 작성 안내와 월간 격자 셀은 12px을 쓴다

표준 읽는 계단은 `--text-2xl` 28 / `--text-xl` 24 / `--text-lg` 18 / `--text-md` 16 / `--text-sm` 14다. `--text-detail` 13px은 상담 기록 레일의 세부 목표와 지난 상담 날짜, 입력칸 도움말, **읽는 화면의 안내 문구와 예시 문구**(`.report-description`, 2026-09-09 Q D88)가 쓴다. `WireBadge`와 당사자 정보 `전문 보기` 배지형 버튼, **월간 격자 셀의 날짜 숫자와 일정 이름**(`.month-date`·`.calendar-event-title`, D88)은 `--text-badge` 12px을 쓴다. 기본 배지는 높이 **22**(`--badge-height`), 좌우 패딩 8, 굵기 400이다. `전문 보기` 알약만 높이 20을 유지한다.

입력칸에 붙는 조작 버튼은 상자 밖 오른쪽이다(2026-09-04 Q). `.wire-field-with-action`이 입력칸과 32px 버튼을 한 줄에 세우고 버튼은 입력 상자 세로 중앙에 앉는다. 상자 안에 남는 조작은 값 자체를 고르는 것(달력 토글, 인라인 값 편집)뿐이다.
선택지 행은 알약을 쓰지 않는다. 체크박스와 라디오가 한 어휘이고, **컨트롤과 글자 사이는 전역 6**이다(2026-09-05 Q, 구 4). 선택지 행, 동의 체크 행, 상담 기록 레일 채움 표시, 레거시 `.field` 선택지 줄이 같은 값을 쓴다. 보기 사이 열 간격은 24다.

**모바일 라디오 균형 배치**(2026-09-08 Q, §5): 뷰포트 767px 이하에서 보기의 자연 너비와 열 간격 24px을 합쳐 한 줄에 들어가면 그대로 둔다. 줄바꿈이 필요하면 보기 순서를 유지하면서 행별 개수를 고르게 나눈다. 기본은 **5개 `3+2`, 4개 `2+2`, 3개는 가능한 한 한 줄**이며 마지막 한 개만 남는 행을 가능한 한 피한다. 긴 라벨과 더 많은 보기는 320px과 390px에서도 가로 넘침이나 라벨 잘림 없이 읽을 수 있게 나눈다. 컨트롤과 글자 사이 6px, 열 간격 24px은 유지하며 데스크톱과 체크박스 그룹은 바꾸지 않는다.
**행만 나누고 선택지는 내용 너비로 왼쪽 정렬한다**(2026-09-08 Q 후속). 보기 사이는 24px이며 균등 너비 칸이나 `space-between`으로 남는 가로 공간을 채우지 않는다.

재배치해도 보기 순서, 라벨 연결, 네이티브 키보드 이동과 선택 동작, 입력값과 제출 의미는 유지한다. 폭 변경으로 컨트롤을 다시 마운트하거나 선택값과 포커스를 잃지 않는다. 보기 추가와 삭제, 라벨 변경, 글꼴 로드 뒤에도 현재 폭에 맞게 다시 배치하며 중복 라디오 입력은 만들지 않는다.

폼의 선택 표시는 두지 않는다. 필수는 `WireBadge tone="lavender"`를 재사용한 `.wire-required-marker`로 표시하고 라벨과 8px 띄운다. 표식은 높이 22px, 좌우 패딩 8px, radius pill, 12/400이다. 배경은 없고 테두리와 글자에 승인된 `--lavender-deep`을 함께 쓴다. 기존 배지 면 `--badge-lavender`는 흰 배경 대비가 2.41:1이라 작은 글자에 쓰지 않는다. 카드 제목과 개별 항목에 중복하지 않고 실제 필수 입력 항목에만 붙인다.

**15초 페이지 배치 6건**(2026-09-08 Q, 같은 날 6차 개정까지 반영):
- **줄글로 읽는 본문은 행간 1.6**(`--leading-relaxed`)이다. 회차 핵심 한 줄과 미해결 액션 내용처럼
  줄바꿈되는 문장에만 쓰고, 한 줄 값과 라벨과 배지는 기존 행간을 유지한다.
- **회차별 정리와 미해결 액션은 형제 사이를 `--line` 1px 가로선으로 가른다.** 선은 카드 본문 폭
  전체를 가로지르고, 행 안쪽 위아래 여백은 카드 패딩과 같은 24다. 목록 음수 마진 -24가 맞물려
  카드 위 첫 글자, 선 위, 선 아래, 카드 아래 마지막 글자의 여백이 모두 24로 같다. 호버 면은 그
  전폭 띠를 그대로 쓴다.
- **미해결 액션 한 건은 회차 행과 같은 어휘다**(2026-09-08 Q 2차, 구 배지 아래 출처 회차 버튼).
  행 전체가 출처 회차 링크이고 오른쪽 끝에 이동 꺽쇠만 둔다. 배지와 버튼을 연달아 세우지 않는다.
  내용이 첫 층, 담당과 기한 배지가 둘째 층이며, 기한은 시간 축이라 `TimeAxisBadge`를 유지하고
  날짜는 공용 한국어 표기를 쓴다. 같은 클래스를 쓰는 종결 화면은 한 줄 배치 그대로다.
- **리스크 인용과 세션 목표의 행동은 내용 폭에 맞춰 배치한다**(2026-09-08 Q 데스크톱 후속). 뷰포트 768px 이상이고 해당 리스크 항목 또는 세션 목표 구획의 내부 폭이 600px 이상이면 텍스트는 왼쪽, 행동 버튼은 같은 행 오른쪽에서 세로 중앙을 공유한다. 세션 목표 라벨은 그 위 전폭을 유지한다. 그보다 좁거나 모바일이면 기존 세로 배치와 내용 아래 12px 간격을 유지한다. 전체 목표 미설정 안내는 AI 제안 제목 바로 아래에 두고 닫기나 숨김 상태는 사용하지 않는다.
- **나머지 본문 행동 버튼은 데스크톱 오른쪽, 모바일 왼쪽 세로 배치다.** 저장·취소와 불일치 처리에 적용한다. 버튼의 내용 너비와 높이 32는 유지한다. HERO의 이름·행동과 여닫기 도구 모음, 다른 화면은 바꾸지 않는다.
- **AI 제안 항목은 `div` 가로선 섹션이다**(2026-09-08 Q 7차, 구 낱개 상자 + 라벤더 tint 면 +
  `근거 회차 보기` 버튼 + `근거 인용 보기` 접힘). 목록 첫 항목 위, 형제 사이, 마지막 항목 아래에
  `--line` 1px 전폭 가로선을 둔다. 각 행 전체는 호버와 키보드 포커스에서 `--gradient-hover` 면을 쓰며,
  항목마다 테두리 상자를 얹지 않는다. 상자를 두 겹 이상 겹치면 모바일 글줄이 눌린다
  (실측: 390px에서 139px까지 좁아졌다). 행 전체가 근거 회차 링크이고 오른쪽 끝에 이동 꺽쇠만 둔다.
  계열색은 제목 글자에만 두며 제목은 라벨 단 14/600 라벤더 deep, 확인 이유는 14/400 `--sub`다.
  **근거 인용은 싣지 않는다.** 제목과 이유에 내용이 겹치므로 원문은 클릭해서 그 회차 기록에서
  읽는다. 행 위아래 패딩은 12이고 목록 위아래 경계선 두께는 각각 1px이다.
- **회차별 정리의 날짜와 유형 배지와 수기 배지 사이는 767 이하에서 8이다**(2026-09-08 Q 7차,
  미해결 액션의 담당·기한 배지 사이와 같은 값. 구 고정 칸 112·64 + 간격 8은 죽은 자리를 만들었고
  Q 6차의 4는 좁았다). 모바일에서만 칸을 내용 폭으로 풀며, 데스크톱의 고정 칸 정렬(본문 시작 x
  고정)은 그대로다. 세션 목표에 병기하는 `세부 목표` 라벨은 좁은 폭에서도 두 줄로 쪼개지지 않는다.
- **현재 맥락 요약은 15초 페이지에 표시하지 않는다**(2026-09-08 Q 후속). 이 페이지의 메모리 조회와 전달도 제거하며 별도 기억 화면과 저장 기능은 바꾸지 않는다.

**나란히 서는 두 글자 덩어리는 옷이 달라야 한다**(2026-09-08 Q). 정보 종류가 다르면 크기나 색이나
굵기 중 최소 하나를 바꾼다. 세션 목표에 병기하는 세부 목표는 라벨이 민트 deep 14/600,
문구가 14/400 `--sub`이고, 목표 본문 14/400 `--ink`와 색으로 갈린다. AI 제안 구획에서 빈 상태
안내와 전체 목표 미설정 안내가 나란히 서므로 색으로 가른다. 둘 다 §5가 정한 13px 안내
대역(`.report-description`)이고 빈 상태는 `--sub`, 미설정 안내는 `--ink`다(2026-09-09 Q D88, 구
14/400 두 벌 대체). 크기가 같으니 가르는 것은 색이다.
안내 글은 다른 구획 내용과 같은 전체 폭을 쓴다. 전체 목표가 없을 때만 표시하며 AI 제안을 차단하지 않는다.

일정 기간 이동 꺽쇠는 12px SVG와 1.5px 획을 32px 공용 원형 버튼 안에 둔다.
버튼 면과 기간 이름 사이는 `--space-3`만큼 띄우며 일간·주간·월간 이름은 자기 글자 폭만
차지한다. 접힌 날짜 제목과 카드 밖 구획 제목은 카드 본문 시작선에서 왼쪽 정렬한다.

일정 날짜 아코디언의 상태 표시는 32px 원 안에서 닫힘=아래 화살표, 열림=위 화살표로 바꾼다.
원은 별도 버튼이 아니라 전체 summary 클릭 영역 안의 상태 표시다.

일정 업무 바는 두 줄이다(2026-09-08 Q). 1행은 기간 네비, 2행은 `오늘`과 보기 선택창과 `상담 등록`이며
두 줄 모두 가운데 정렬이고 어느 폭에서도 줄바꿈하지 않는다. 당사자 등록은 사이드바가 장소로 갖는다.

주간 일정의 날짜, `오늘` TimeAxisBadge, 건수, 당사자 이름은 두 폭 모두 한 줄에서 세로 중앙을
공유한다. 좁아지면 줄을 늘리지 않고 이름 목록만 말줄임으로 줄어든다. 날짜 제목은 16/600이고
건수와 이름 목록은 14/400 `--ink`다(2026-09-08 Q, 구 16/400). 건수와 이름 사이는 공용 메타 세로선
어휘를 그대로 써 `--line-control` 1px과 좌우 12px으로 가르며, 채운 면 위에서는 `--line-on-action`
으로 넘어간다. 배지 면을 움직이지 않고 `.wire-badge-label`을 면 정중앙에 둬 모든 TimeAxisBadge의
위아래 여백을 같게 유지한다.

767 이하 일정 카드의 상담 유형과 상태 배지는 라벨/값 정보 전체 아래 줄로 내려가 이름과 같은
왼쪽 시작선에 선다(2026-09-08 Q, HERO 배지 하단 배치 규칙을 목록 카드에 적용). 768 이상 데스크톱과
당사자 목록 카드는 우상단 자리를 유지한다.

회차별 기록과 인테이크 조회 아코디언을 펼치면 summary 안 배지는 `--panel` 면과 `--ink` 글자를 쓰고 외곽선은 없앤다(2026-09-05 Q). 외곽선이 차지하던 사방 1px을 패널 면으로 바꿔 높이 24px, 좌우 패딩 9px로 표시한다.
접히면 높이 22px과 기존 색상 면, 계열 외곽선, `--on-badge` 글자로 돌아온다.
펼친 summary의 제목 줄은 22px을 최소로 예약하되 24px 배지가 있으면 내용 높이를 따른다. 바깥 summary 패딩과 카드 사이 간격은 바꾸지 않는다.
`기록됨`은 접힌 상태에서 계열 상태가 아니라 완료된 기록의 존재 표시이므로 neutral `--ink` 아웃라인을 쓴다.
15초 페이지 회차 행의 고정 칸은 내용 실측값이다(2026-09-04 Q): 날짜 112, 유형 배지 64, 수기 배지 44, 칸 사이 8. 배지는 칸 폭으로 늘리지 않고 자기 글자 폭만 차지해 좌우 패딩 8 계약을 지킨다. 본문 시작 x는 그대로 고정된다.
회차 카드 폭이 600px 이하이면 핵심 한 줄은 두 번째 행 전체 폭으로 내려가고 최대 두 줄로
자른다. 768px 데스크톱 셸의 좁은 본문에서도 한글이 한 음절씩 세로로 찢어지지 않아야 한다.

당사자 이름은 자리 두 단이다(2026-08-27 Q, 구 전역 21 한 벌 대체): 목록·일정 카드(`row`)는
카드 내용과 같은 16/600, 당사자 정보·HERO(`hero`·`hub`)는 카드 제목과 같은 18/600이며
767 이하도 같다. 전용 토큰 `--text-participant-name`은 폐기했고 계단 토큰만 쓴다.
`PageTitle`이 화면의 유일한 `h1`이고 `ParticipantHeroCard`의 당사자 이름은 그 아래 `h2`다(2026-09-03 Q). 시각 크기는 그대로 18/600이다.
H2는 태그 이름만으로 한 높이를 쓰지 않고 원 프리뷰의 역할 두 단을 따른다. 카드 여러 장을 묶는 섹션 H2는 18/600, `--leading-snug`, 실측 높이 24.3px이다. 카드 안 H2는 16/600, 같은 행간, 실측 높이 21.6px이다. HERO의 이름 H2는 18/600, `--leading-tight`, 실측 높이 22.5px이다. 같은 역할 안에서는 배지 유무가 높이를 바꾸면 안 된다.
`/participants/:id/**`는 예외 없이 `ParticipantHeroCard`를 단다(2026-09-04 Q, 기본정보 수정의 화면 단위 예외 폐기). 공개 계약은 `name`, `beneficiaryId`, `nameSize`, `actions`, `className`과 선택적 `details`다. `details`는 `ParticipantHeroDetail`의 `label`, `value`, `tone?` 필드를 유지한다. 구 `stageTags`와 `ParticipantHeroStageTag`는 폐지한다(2026-09-08 Q). 정보가 있는 데스크톱 HERO는 최소 높이 177px을 갖되 내용이 늘면 자라며, 모바일은 내용 높이를 따른다. 구분선 위에는 이름과 행동만 두고, 아래 `.participant-hero-info`에는 `.participant-hero-details`의 라벨/값 격자만 둔다. 열 수는 바로 아래 폭 표가 정한다. 오른쪽 배지 열이나 하단 배지 행은 두지 않는다. 정보가 없으면 구분선과 하단 영역을 만들지 않는다.
**HERO 정보 격자의 열 수는 폭이 정한다**(2026-09-09 Q D88, 구 "15초 페이지 네 항목만 4열" 대체). HERO 내부 폭 960px 이상이면 4열, 760px 초과 960px 미만은 3열, 760px 이하는 80px 라벨 행이다. 항목 수는 열 수를 정하지 않는다. 당사자 정보 허브는 당사자 ID, 연락처, 이메일, 생년월일, 기록 현황, 최근 상담, 진행 상태 일곱 항목을 싣는다(구 세 항목 대체). 진행 상태는 배지가 아니라 격자 항목이며 이는 §4의 HERO 배지 금지와 같은 방향이다. 이름과 행동 버튼의 배치는 바꾸지 않는다.
HERO의 라벨은 값의 뜻을 구체적으로 밝힌다. 기록 작성은 `이번 상담`과 `기본 상담 N회차`, 기록 조회와 종결은 `진행 상태`와 `진행 중` 또는 `종결`, 인테이크는 `인테이크`와 `작성 중` 또는 `수정 중`, `완료`, AI 검토는 `AI 검토 상태`와 `검토 대기` 또는 `승인됨`, `반려됨`으로 표시한다. 브리핑의 최신 상담 유형과 회차는 기존 최신 상담일과 혼동되지 않는 라벨을 쓴다. 회차는 기존 데이터의 의미를 유지하고 미확정 건수는 추정하지 않는다. `15초 페이지` 같은 화면 이름을 정보로 반복하거나 구 태그를 일괄 `상태` 라벨로 옮기지 않는다.
인테이크 수정 HERO 우측에는 프라이머리 `수정 완료` 버튼을 둔다. 이 버튼은 현재 입력을 저장한 뒤 조회 화면으로 돌아가며, 하단 `저장`과 같은 `complete` 경로를 사용한다.
가명 ID는 목록·일정 카드에서는 이름 옆 14/400 `--sub` 조각이다. 당사자 정보 HERO에서는
이름 아래 정보 격자의 첫 항목으로 내려가 `당사자 ID` 라벨과 14/400 `--ink` 값으로 보인다
(2026-09-02 Q A안, 구 HERO 이름 옆 조각 대체). 이름이 없으면 ID가 제목 폴백이 되므로 정보
격자에서는 반복하지 않는다. 정보 격자의 열 수는 앞의 폭 규칙이 정한다. 빈 항목은 자리를 남기지 않는다.
이메일은 D24 역할 검사를 통과한 당사자 정보 허브와 당사자 목록이 각각 전용 허브 응답과 목록 응답으로 받는다(2026-09-09 Q D88, 구 "이메일은 전용 허브 응답에서만" 대체). 생년월일은 허브 응답만 받고 목록에는 싣지 않는다. 계좌와 주소는 어느 쪽에도 올리지 않는다. 감사는 화면 조회 1건 = 감사 1행을 유지하고 실은 항목을 그 행의 `fields`에 더한다(D24). HERO에 넘기는 `details`는 각 화면이 응답으로 받은 값만 담는다.
카드의 이름과 ID 묶음은 정보 행과 같은 80px 라벨 격자에 올려 ID 시작선을 값 열과 맞춘다. 767 이하
목록 카드는 정보 행이 한 줄로 합쳐져 값 열이 없으므로 이름·ID도 글자 폭 + 10으로 붙인다(2026-09-07
Q 결정. 일정 카드는 값 열이 남아 80px 유지).
HERO의 `전화번호`와 `이메일` 라벨은 사람과 연락 정보이므로 mint deep, `상담일` 라벨은 시간 정보이므로 blue deep을 쓴다. 값은 모두 14/400 `--ink`다. `휴대전화번호` 표기는 `전화번호`로 통일한다.
HERO 왼쪽 라벨은 값의 위쪽에 정렬한다. 패드와 모바일에서 값이 두 줄 이상이어도 라벨은 첫 행의 위쪽에 맞추며 세로 중앙으로 내리지 않는다(2026-09-07 Q 후속 수정).

767 이하 모바일 배치 3건(2026-09-06 Q, 가로 넘침이 아니라 세로 나열을 줄이는 개정): ① 당사자 목록 카드의
참여 사업·연락처는 한 줄(라벨 글자 폭), 일정 카드는 상담 일시가 길어 2행 유지 ② 브리핑 회차 행은
첫 행 날짜·유형·수기, 둘째 행 핵심 한 줄 전폭(꺽쇠 열 앞까지) ③ 생활 6영역 카드 제목은 이름과
직전 상태 배지가 한 줄. 구획 라벨(`WireCardSection` h3)은 줄바꿈하지 않고 안내가 든 action
슬롯만 아래 전폭으로 내려간다. 폼 입력칸 세로 스택은 그대로다.

일반 폼 입력은 기본적으로 보이는 라벨을 위에 둔다. 당사자 관련 폼에서 카드 제목과 바로 아래 입력 라벨이 같은 입력을 같은 말로 가리키는 경우에는 아래 동어반복 제거 계약이 우선한다. 일정·당사자 **업무 바**는 압축된 조작면 예외이며,
`WireToolbarField`가 화면에 숨긴 접근성 라벨과 입력 셸을 함께 소유한다. 호출부가
`wire-input-box`나 검색 셸을 손으로 복사하지 않는다.
**카드 제목과 입력 라벨이 동어반복이면 제목만 남긴다**(2026-09-08 Q). `담당 실무자 의견`처럼 같은 입력을 같은 말로 가리키는 아래 라벨만 화면에서 제거하고, 제목 아래에 무엇을 적어야 하는지 돕는 안내를 둔다. 입력의 접근성 이름은 제목 연결 또는 숨긴 라벨로 보존하며 자리표시자나 안내문으로 대신하지 않는다. 서로 다른 입력을 구분하는 라벨과 제목과 다른 뜻의 라벨은 유지한다. 안내는 해당 화면의 기존 글자 규칙을 따르며 상담 기록 작성 화면에서는 `.record-writing-help`를 쓴다. 입력값, 필수 여부, 검증, 제출과 초안 복원 계약은 바꾸지 않는다.

표준 읽는 글자의 하한은 14px이다. 예외는 상담 기록 레일 세부 목표와 지난 상담 날짜의 `--text-detail` 13px, **입력칸 도움말 `.wire-form-hint` 13px**, **읽는 화면의 안내 문구와 예시 문구 `.report-description` 13px**(2026-09-09 Q D88), 배지와 배지형 상태 표시의 `--text-badge` 12px, **상담 기록 작성 안내 `.record-writing-help` 12px**, **월간 격자 셀의 날짜 숫자와 일정 이름 12px**(D88)이다. 작성 안내 12/400은 2026-09-04 Q 결정 그대로이며 13px 안내와 자리가 다르다. 월간 12px은 셀 안에서만 살고 요일 줄과 `+N건`으로 퍼지지 않는다. 자리 전용 토큰은 다른 본문, 라벨, 메타로 퍼뜨리지 않는다. 가명 ID는 표준 `--text-sm` 14 계단을 쓴다. 코드에 px 숫자를 직접 쓰지 않고 토큰을 사용한다.
금액 단위 `원`은 앞말과 띄어 쓴다. `100만 원`, `1,200만 원`, `30만 원`이 맞고 `100만원`은 쓰지 않는다.

상담 기록지 체크리스트의 채움 표시는 읽기 전용 체크박스 모양을 쓴다(2026-09-03 Q). 끔은 `--panel` 면과 `--gradient-deep` 외곽선, 켬은 `--gradient-action` 면과 체크 표시로 일반 체크박스와 같은 옷이다. 체크리스트 라벨은 14/400이다. 이번 상담 목표와 미해결 액션 본문은 각각 세부 목표와 출처 날짜보다 먼저 읽히도록 이 자리에서만 14/600 `--ink`를 쓴다.
상담 기록 좌측 레일의 세션 목표와 미해결 액션은 의미 순서를 갖는 `ol`이고, 각 항목 첫 줄 왼쪽에 Enclosed Alphanumerics 원문자 `① ② ③`을 18px로 둔다. 원문자는 면, 테두리, radius가 없는 글리프이며 버튼이나 배지로 그리지 않는다. 본문은 `p`를 유지한다. 세부 목표는 다음 줄에 `세부 목표:` 13/600 `--lime-deep` 라벨과 13/400 `--lime-deep` 문장을 이어 쓰며 배지를 쓰지 않는다. 미해결 액션의 `지난 상담 <날짜>`도 13/400 `--lime-deep` 글자로 본문보다 물러난다. 두 목록의 형제 행 사이 `--line`은 카드 본문 너비를 끝까지 가른다.

### 상담 기록 작성 전용 배치 (2026-09-07 Q)

아래 네 결정은 `/participants/:id/programs/:caseId/records/new` 작성 화면에만 적용한다. 단, `WireRepeatActions`의 공용 버튼 순서는 `+`, `-`로 통일한다(2026-09-08 Q). 상담 기록 조회, 인테이크, 일정 등록의 배치와 다른 호출부의 버튼 표시 조건은 바꾸지 않는다.

1. 체크리스트 항목은 데스크톱과 모바일 모두 **2열**이다. 상담 기록 보기와 저장 행동 둘은 `수기 메모 하나만 채워도 저장됩니다. 저장 버튼을 여러 번 눌러도 같은 기록이 두 번 만들어지지 않습니다.` 문단 **아래**에 둔다. 읽기 전용 채움 표시와 14/400 라벨은 유지한다.
2. `오늘 확인할 질문` 카드의 설명은 구분선 위아래 모두 **카드 안 전체 폭, 왼쪽 정렬, 좌우 각각 24px 패딩**이다. `72ch` 등 별도 읽기 폭 제한이나 한쪽 추가 들여쓰기를 두지 않는다. 질문 체크박스는 실제로 선택할 수 있어야 한다.
3. `이번 상담에서 확인할 것` 제목은 카드에 **한 번만** 표시한다. 데스크톱과 모바일 모두 각 행은 **번호 없이 입력칸, `+`, `-` 순서**로 가로 배치한다(2026-09-08 Q 후속 수정). 모든 행에 두 버튼을 상시 표시하고 같은 조작 폭을 확보해 입력칸 너비를 동일하게 유지한다. `+`는 해당 행 바로 뒤에 새 행을 넣고, `-`는 해당 행을 삭제한다. 마지막 한 행의 `-`는 숨기지 않고 비활성화한다. `WireRepeatActions`를 사용하며 다른 호출부의 기존 표시 조건은 유지한다. 입력칸마다 고유한 접근성 이름을 제공하되 제목과 번호를 화면에 반복하지 않는다. 모든 행은 제출과 초안 복원에서 순서대로 보존하고, 기존 단일 `sessionGoalNote` 필드에 줄바꿈으로 직렬화한다. 줄바꿈을 포함한 전체 200자 제한을 행별 한도로 잘못 표시하거나 값을 잘라 저장하지 않는다.
4. 작성 화면의 **모든 설명, 가이드, 빈 상태 도움말**은 `.record-writing-help`가 소유한 **12/400 `--sub`, `--leading-normal`**이다. `goalSection` 슬롯, 방향 수정 안내, 미해결 액션 없음 안내, 케이스 목표 설명, 일정 버전 제출 안내와 notice도 포함한다. 해당 자리에서는 공용 도움말 13px보다 이 규칙이 우선한다. 제목, 라벨, 입력값과 실제 기록 내용 및 읽는 목표 문구는 기존 위계를 유지한다. 페이지 전체 글자나 모든 `p`를 일괄 축소하지 않는다.

검사는 `.record-writing-help`의 정확한 12/400 `--sub` 조합만 허용한다. 일반 12px 설명 단을 열거나 baseline에 위반을 추가해 숨기지 않는다.

## 6. 굵기 3단 (§2-1)

**400** 본문 / **500** 셸 기본(사이드바, 헤더) + 일정 기간 값(§1 ⑤) / **600** 강조. **700 은 없다.**

**크기, 굵기, 행간 중 하나라도 정하는 요소 선택자는 셋을 같이 정한다.** 비워 두면 브라우저 기본 700 이 산다.

## 7. 여백 4단 (§3-4, 2026-09-09 Q D88이 구 3단 대체)

| 자리 | 값 |
| --- | --- |
| 페이지 세로 스택(구획 카드, HERO, 배너) | **32** `--section-gap` |
| 읽는 목록 안 카드(브리핑 회차, 기록, 검토 큐) | **20** `--space-5` |
| 고르는 목록 안 카드(당사자 관리, 일정 등록 후보) | **8** `--space-2` (2026-09-09 Q D88). 훑어 하나를 고르는 목록만 해당하며 새 토큰은 없다 |
| 행 카드 스택(접힌 회차 줄, 당사자 행, 브리핑 목록) | **16** `--space-4` (2026-08-28 Q, 구 12. 맨 텍스트 행이 빽빽해 전역 개정). 브리핑 회차 줄은 2026-09-04 Q로 행마다 위아래 8을 넣어 36px 호버 면을 만들고 목록 gap을 0으로 둔다. 실제 글자 사이는 그대로 16이다 |

읽는 목록인지 고르는 목록인지는 D59의 카드 성격 기준과 같다. 글을 읽는 목록을 8로 좁히면 카드 경계가 글줄 사이 여백처럼 읽혀 한 건의 끝이 흐려진다.

- 카드 안 **좌우는 24 하나다.** 세로만 역할이 정한다(패딩 4종은 §3-4).
- **4의 배수만** 쓴다. 하프스텝은 2, 6, 10, 14 넷뿐이고 그 밖의 값은 앞줄에 `/* optical: 사유 */` 가 필요하다.
- **동의 묶음은 실제 글자 간격을 맞춘다.** 등록 안내문과 첫 체크, 긴급 등록 체크와 바로 이웃한 설명, 일반 체크끼리는 모두 14±1px이다. 긴급 등록 사유 칸이 열리면 설명이 체크와 직접 이웃하지 않으므로 기본 행 간격을 유지한다. 당사자 정보 fieldset의 내부 gap은 브라우저 익명 상자에 있어 바깥에서 덮지 못한다. 따라서 20px 전문 보기 버튼으로 줄어든 4px을 둘째 체크 행과 `마지막 기록`의 위 여백에 각각 보태 실제 글자 간격을 모두 20px로 맞춘다. 접힘과 펼침 모두 바깥 카드의 위아래 실제 여백은 같다(2026-09-03 Q 픽셀 실측).
- 같은 사이드바 메뉴 묶음 안 항목 사이는 2px `--space-0-5`다(2026-09-03 Q). 묶음 머리와 목록 사이는 4px `--space-1`, 묶음 사이는 16px `--space-4`다(2026-09-04 Q "사이간격이 너무 넓어서 벙짐", 구 8px과 24px). 버튼 내부 패딩은 바꾸지 않는다.
- **입력칸 한 벌의 세로 간격은 12다**(2026-09-04 Q, 구 8). 라벨 → 컨트롤 → 도움말 세 층이 `--space-3`을 쓰고, 라벨 행 안 조각 간격(글자 ↔ 배지) 8은 그대로다. 선택지 행만 예외다: 40px 누름 면적을 만드는 위아래 8 때문에 **보이는 체크박스·라디오 윗선은 라벨에서 22**이고, 음수 마진으로 12에 맞추지 않는다.
- **배지와 버튼이 여백을 바꾸지 않는다**(2026-09-04 Q 전역 기준). 배지가 설 수 있는 줄은 배지 유무와 무관하게 높이 22(`--badge-height`)를 예약한다: `.wire-form-label`, `.wire-card-head`, `.wire-card-summary>.wire-card-title`, `.wire-title-with-badge`.
- **임시본 복원 버튼 사이는 8px**이다(2026-09-08 Q). `DraftRestorePrompt`의 `이어쓰기`와 `새로 시작` 묶음만 `--space-2`를 쓰며 일반 콜아웃과 행동 묶음의 12px(`--space-3`) 간격은 유지한다. `이어쓰기`는 secondary, `새로 시작`은 neutral 아웃라인이다. 상담 기록, 인테이크, 세션 목표 수정의 공용 호출부 모두 같으며 복원과 폐기 동작은 바꾸지 않는다.

### 셸 spacing v2

| 자리 | 값 |
| --- | --- |
| 데스크톱 헤더 | 높이 **64**, 좌우 패딩 **32** |
| 사이드바 첫 메뉴 시작 | 헤더 아래 **32** |
| 사이드바 선택·호버 상자 | **248 × 32**(2026-09-04 Q, 구 40), 좌우 여백 **16**, 아이콘 시작선 **25** |
| 페이지 컨테이너 | `max-width` **1440**, 데스크톱 패딩 **48 / 32**, 모바일 **32 / 16** |
| 뒤로 버튼 아래 제목 | 데스크톱 **40**, 모바일 **32** |

- `1440`은 배치 폭이다. 폼은 `520` 또는 `420`, 여러 줄 설명은 `max-width: 72ch`로 읽기 폭을 따로 제한한다. **예외 둘**: 당사자 등록 카드 안내문(`.register-card .schedule-form-hint`, 2026-08-30 Q)과 상담 기록 작성 `오늘 확인할 질문` 카드의 구분선 위아래 설명(2026-09-07 Q)은 카드 안 전체 폭을 쓴다. 후자는 좌우 24px과 왼쪽 정렬을 유지한다.
- 뷰포트 브레이크포인트는 `max-width: 767px` 하나다. `768px`은 데스크톱 셸을 유지한다. HERO는 별도로 자신의 내부 폭 760px 이하에서 세로 정보 배치로 전환한다. 셸 전환이 아닌 기존 컨테이너 질의 방식이다.
- 767 이하는 페이지·HERO·업무 바 행동을 내용 크기의 가로 묶음으로 두고 줄바꿈한다. 행동 버튼은 전부 높이 32이며(2026-08-28 Q — 구 2026-08-26 '전부 40'을 전 버튼 32 단일로 대체) 카드 폭에 맞춰 늘리지 않는다.

## 8. 꺽쇠 한 벌 (§3-4)

모든 꺽쇠는 `Chevron`의 **12×12 SVG 슬롯 한 벌**을 쓴다(2026-09-02 Q). 경로는
`M3.3 4.65 6 7.35 8.7 4.65`, 획 1.5px, 둥근 끝과 둥근 연결이다. 보이는 외곽은 약
6.9×4.2px이고 경로 중심은 슬롯 정중앙 `(6, 6)`이다. 글자 크기를 따라 CSS 테두리 사각형을
키우던 em 배수와 `--chevron-box`, `--chevron-stroke`는 폐지했다. 위, 아래, 왼쪽, 오른쪽은
이 SVG 한 개를 회전해서 만든다.

### 화살표 어휘 (2026-09-02 Q)

방향이 기능을 말한다. **왼쪽 꺽쇠 = 뒤로**, **오른쪽 꺽쇠 = 페이지 이동 전용**
(행 끝, 버튼 안), **아코디언은 닫힘 아래, 펼침 위**다. 아코디언에 오른쪽 꺽쇠를 쓰면
이동 링크와 혼동되므로 금지다.

꺽쇠 형태는 **버튼형과 일반형 둘뿐**이다. 버튼형 `.wire-chevron-button`은 일정 이전·다음을
정본으로 삼은 32px 원, `--panel` 면, `--gradient-brand` 1px 아웃라인과 같은 호버·포커스 링을
쓴다. 카드가 열려도 테두리 색을 바꾸지 않는다. 기본 카드 아코디언 상태 표시가 버튼형이다.

일반형은 테두리와 면이 없는 12px 슬롯이다. 좁은 이력 줄, 뒤로 버튼, 등록 동의
`자세히 읽어보기`, 근거 인용, 세부 목표 줄, 당사자 정보 `전문 보기`가 일반형을 쓴다.
24px 소형 원은 폐지했다. 뒤로 버튼은 좌 12px, 꺽쇠와 글자 사이 6px, 우 16px으로 실제
잉크 여백을 맞춘다. `전문 보기` 알약은 좌 10px, 글자와 슬롯 사이 6px, 우 8px이다.
일정 보기 선택창은 96×32px이고 좌 12px, 우 10px이다.

일반 폼 선택창의 12px 꺽쇠 슬롯은 오른쪽 10px `--space-2-5`에 둔다. 일정 보기 선택창의 전용 오른쪽 10px 계약과 같은 여백이다(2026-09-03 Q `align-check`).

모션은 `--motion-fast`와 `--ease-standard` 한 쌍이다. 화살표 있는 summary는 네이티브
마커를 숨긴다. SVG path 중심과 슬롯 중심 오차는 0.5px 안이어야 하고,
`pnpm design:align`의 `chevron-xy`가 뒤로, 닫힘, 펼침, 390px, 일정 업무 바를 함께 실측한다.

### 펼침 채움 (2026-08-28 Q)

**펼친 카드 아코디언의 제목 줄은 전부 `--gradient-action` 채움이다**(구 브리핑 3영역·세션
목표 수정·일정 날짜 제외 폐지). 예외는 위기·안전 아코디언(`.is-crisis`) 하나 — 리스크
레드 독점(D9) 위에 다른 채움을 얹지 않는다. 채운 면 위 글자와 선은 `--on-action`과
`--line-on-action`을 쓴다. summary 안 상태 배지는 `--panel` 면과 `--ink` 글자를 쓰고
외곽선을 없앤다. 패널 면은 사방 1px씩 넓혀 높이 24px, 좌우 패딩 9px로 표시한다.

## 9. 월간 격자, 아이콘 어휘, 번호, 좁은 화면 카드 행동 (2026-09-09 Q D88)

### 월간 격자

월간 본문은 **우리가 그린 7열 격자**이고 달력 라이브러리를 쓰지 않는다.

- 셀 높이는 148 고정, 767 이하는 88이다. 날짜는 숫자만 쓰고 `일` 같은 접미사를 붙이지 않는다.
- 요일 줄은 월요일부터 일요일까지 이어지는 `--gradient-brand` 면 하나다. 그 위 글자는 **두 테마 고정 `--on-action`** 이다. `--ink`를 쓰면 다크에서 흰 글자가 밝은 파스텔 면에 얹힌다.
- 토·일·공휴일 코랄은 셀 안 날짜 숫자가 가진다. 요일 줄에는 계열 색을 쓰지 않는다.
- 셀에 넘치는 일정은 `+N건`으로 접고 누르면 그 날짜의 일간 보기로 간다. 팝오버는 만들지 않는다.
- 종일 일정만 색 면(승인된 다섯 배지 색)을 받고 시간 있는 일정은 면 없는 글자 행이다. 두 종류의 줄 높이는 같다.
- 셀 글자 12px 예외는 §5가 정한다.

### 아이콘 어휘

**같은 대상의 보기와 등록은 같은 글리프를 쓰고 더하기로 가른다**(2026-08-30 Q의 사람 / 사람 + 더하기 어휘를 일정에도 적용).

| 행동 | 아이콘 |
| --- | --- |
| 일정 보기 | 달력 `calendar` |
| 일정 등록하기 | 달력 + 더하기 `calendar-plus` |
| 상담 기록하기 | 펜 `record` |
| 당사자 목록 | 사람 `participants` |
| 당사자 등록 | 사람 + 더하기 `participant-add` |
| 당사자 초대 | 고리 `invite` |

더하기는 달력 몸통 안에 넣는다. 옆에 붙이면 16px에서 테두리와 겹친다. 화살표는 방향만 말하므로 기록 행동에 쓰지 않고, 시계 `upcoming`은 일정 메뉴에서 쓰지 않는다. 같은 행동이 사이드바와 카드 버튼에서 다른 글리프를 쓰지 않는다.

### 구획 번호

**번호는 부품이 만든다.** 제목 문자열에 `01. `을 박지 않는다. 구획 목록의 순서에서 뽑아 두 자리로 채우고, 마크업은 제목 요소 안에 `.report-index` + 제목 두 조각이다. 번호는 제목과 **같은 계단**을 쓰고 색만 `--sub`로 물러선다. 자리는 `2ch` 고정에 `font-variant-numeric: tabular-nums`라 번호 자릿수가 달라도 제목 시작 x가 같다.

### 좁은 화면 카드 행동

767 이하에서 **당사자 카드의 행동 버튼 셋**(당사자 정보 보기, 일정 등록하기, 상담 기록하기)은 줄이 남는지와 무관하게 **세로로 쌓고 카드 폭 전체로 편다.** 버튼 글자를 시각적으로 숨겨 아이콘만 남기지 않는다. 순서는 데스크톱 가로 순서와 같다. 다른 카드와 HERO의 행동 배치는 기존 계약 그대로다.

## 디자인 미리보기의 기본 상태 (§7)

**모든 필드를 가상 데이터로 채운 시안이 기본**이다(2026-09-08 Q). 입력값, 읽는 값, 배지, 반복 항목과 조건부 구획까지 내용이 있는 모습을 보여 준다. 실제 당사자 정보나 운영 데이터는 쓰지 않는다. 빈 상자나 자리표시자만 있는 화면을 기본 시안으로 내지 않는다.

빈 상태와 조건별 상태는 채워진 기본 시안에 **추가**한다. 서로 양립할 수 없는 조건은 별도 상태로 보여 주며, 빈 상태나 조건부 구획을 숨긴 화면만으로 전체 시안을 대신하지 않는다. 미승인 요소는 시안용 후보로 구분한다. HERO 배지 후보도 정보 아래에 두되, 시안에 보였다는 이유로 운영 예외가 승인되지는 않는다.

## 검사가 잡는 것과 못 잡는 것

| 검사 | 무엇을 보나 |
| --- | --- |
| `pnpm guard:tokens` | **값** 한 축씩. 계단 밖 크기, 미선언 굵기, 토큰 밖 색, 인라인 스타일. 클래스는 **양방향** 대조다 — CSS 만 있고 마크업이 안 쓰면 dead-class, 마크업만 있고 CSS 가 없으면 unstyled-class(2026-08-30 신설 — 규칙 증발 사고 2건이 계기. 스타일 없는 훅은 `MARKUP_HOOKS` 에 사유와 함께 등재) |
| `pnpm guard:align` | **기하 정렬과 상자 여백**. 단일행 컨트롤과 공용 동의 행의 `align-items:center` + `line-height:normal`, 세로 손보정 금지, 묶음 상자의 승인 패딩 쌍을 검사한다. |
| `pnpm guard:hierarchy` | **조합**. 한 요소에 최종적으로 적히는 크기+굵기+색이 위 1장 표 밖인가 (2026-08-10 신설, CCC-80). 커밋 훅에도 걸려 있다 |
| `pnpm design:hierarchy` | **렌더 결과**. 실제 부품으로 렌더한 정적 HTML 을 브라우저에 물려 계산값을 읽는다. 화면 9개 × 폭 1280·767·390, 레인 둘이다. ① **이웃 관계** 세로로 붙은 두 줄이 같은 옷인가(1장 '연속 금지') ② **기하** 자리 예약이 살아 있는가, 붙박이가 켜지는가, 화면 밖으로 나가는가(2026-09-02 갱신, CCC-91) |
| `pnpm design:align` | **실측 정렬과 크기**. 중심 공유 x, y, 정중앙 xy와 확장 `center-y-each`, `no-overlap-x-each`, `bullet-y`, `chevron-xy`, `gap-pair`, `text-gap-pair`, `inset-x-ink`, `inset-y`, `inset-right`, `overflow-x`, `width`, `height`, `size`를 실제 부품 하니스에서 잰다. `height`는 선택적으로 외곽선 두께와 좌우 패딩도 함께 단언한다. 접힌 배지 22px과 펼친 배지 24px, 구획 전체 행동의 오른쪽 끝과 H2 역할별 높이를 함께 살핀다. 하니스 CSS는 `RootLayout`의 `shellStyles` 순서로 조립하며 그 순서가 바뀌면 실패한다. |
값이 셋 다 합법 토큰인데 조합이 표 밖인 자리가 있다. 그래서 `guard:tokens` 위반 0 인데도 화면이 눌린 채로 나왔다. 조합은 이제 `guard:hierarchy` 가 막지만, **이웃 관계는 여전히 CSS 파일에 없다** — 렌더 결과를 봐야 알 수 있어 하니스와 검수가 맡는다. 그 앞단을 막는 것이 2장의 부품이다.

기존 위반은 `scripts/design/hierarchy-baseline.json` 에 담겨 있고 새 위반만 막는다. 고쳤으면 기준선에서도 지운다(안 지우면 실패한다). 기준선을 다시 만드는 명령은 `pnpm guard:hierarchy:baseline` 이고, **새 위반을 기준선에 넣어 통과시키는 용도가 아니다.**

실측 쪽 기준선은 레인마다 따로다. 이웃 관계는 `hierarchy-adjacency-baseline.json`, 기하는 `geometry-baseline.json` 이고 둘 다 `pnpm design:hierarchy -- --update-baseline` 이 함께 다시 쓴다. **기하 기준선은 지금 비어 있다** — 여기 적히는 것은 승인된 예외가 아니라 아직 안 고친 결함이므로, 고치면 반드시 지운다.

가로 넘침은 **넘침이 시작되는 바깥 테두리만** 센다. 부모가 이미 넘쳤으면 자식은 따라 나간 것이라 새 사실이 아니고, 자손까지 세면 판정이 글꼴을 타 리눅스 CI 와 macOS 가 갈린다(2026-08-11 실측: 같은 결함이 16건 대 129건).
