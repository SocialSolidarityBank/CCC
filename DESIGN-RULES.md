# DESIGN-RULES.md, 화면 작업 전에 읽는 규칙

**여기서 새로 정하는 것은 없다.** `DESIGN.md` 와 pen 의 규칙을 옮겨 적은 것이라 어긋나면 원문이 맞고 이 파일이 오류다(색값은 pen, 색 밖은 DESIGN.md). 장 제목의 `§N-M` 이 원문 자리다.

뺀 것은 원문에서 본다. 레이아웃 §4, **카드와 컴포넌트 계약 §5**, 모션 §6, 접근성 예외 §9, 문안 §10, 다크 모드 §11.

## 0. 낱말 둘

화면이 답답해 보이는 원인은 거의 이 둘을 헷갈린 것이다.

| 낱말 | 뜻 | 판정 |
| --- | --- | --- |
| **눌린 쌓임** | 중요도가 다른 값이 **같은 옷**(크기, 굵기, 색이 셋 다 같다)으로 세로로 쌓인 것 | 위반. 고친다 |
| **나열** | 본래 대등한 것이 나란한 것(목록 항목, 표의 행, 형제 구획) | 정상. 둔다 |

## 1. 위계 4단 (§2-2 규칙 3)

카드 안에서 쓸 수 있는 단은 넷뿐이다. **다섯 번째 단을 만들지 않는다.** 표 밖 조합(예: 16/400 `--sub`)은 위반이다.

| 단 | 이름 | 크기/굵기/색 | 서는 자리 |
| --- | --- | --- | --- |
| ① | **제목** | 16/600 `--ink` | 카드 제목(`.wire-card-title`), 항목 제목(`WireItem`) |
| ② | **라벨** | 14/600 `--sub` 또는 계열 deep | 구획 라벨(`WireCardSection`), 입력칸 라벨 |
| ③ | **본문** | 16/400 `--ink` | 읽는 값, 문단 |
| ④ | **설명** | 14/400 `--sub` | 메타(`.panel-meta`), 도움말(`.wire-form-hint`) |

②가 ③보다 작은 것은 의도다. 크기가 겹치면 위계가 사라진다.

**이 넷은 읽는 줄의 표다.** 누르는 것(버튼, 탭, 토글, 단계 버튼)은 자기 면과 테두리로 이미 구분되므로 표를 따로 쓴다(원문 §2-1 '누르는 것은 표를 따로 쓴다'). 값이 아직 없을 때(`.is-empty`)와 비활성은 새 단이 아니라 **상태**다 — 크기와 굵기는 그대로 두고 색만 `--sub` 로 물러선다.

- **연속 금지.** 이웃한 두 줄이 크기, 굵기, 색 셋 다 같으면 위반이다. 최소 한 축을 바꾼다. 나열은 예외다.
- **셋 이상이면 장치를 쓴다.** 한 묶음에 줄이 3개 이상이면 넷 중 하나를 반드시 쓴다. 가로선(`.wire-card-divider`), 카드 분리, 상태 낱말을 배지로, 2열로 가르기.
- **상태 낱말은 배지로 올린다.** '미기록', '3회차' 같은 분류와 상태는 본문 글자로 두지 않고 `WireBadge` 로 낸다.
- **중요한 값을 민짜 본문으로 두면 위반이다**(§2-3). 강조 어휘는 배지, 굵기 600, 계열 컬러 셋뿐이다.

## 2. 부품 2종 (`app/components/wire/wire-section.tsx`)

카드 안 구획과 항목을 **손으로 쌓지 않는다.** 아래 둘이 위계 4단을 부품에 박아 둔 것이다.

- **`WireCardSection`** = 구획 하나. 라벨(②) + 내용. **형제 구획이 이어지면 구분선이 저절로 붙어** 위 '셋 이상이면 장치'의 가로선을 대신 만족시킨다. `tone` 은 `sub`(기본), `mint`, `lavender`, `blue`.
- **`WireItem`** = 항목 하나. `title`(①) + `description`(④) + `status`(배지) + `action`(14/600 계열색).
- **둘 다 `className` 슬롯이 없다.** 바깥에서 크기와 굵기와 색을 정하는 길이 곧 계약을 비켜 가는 길이다. 모양이 모자라면 자기 클래스를 만들지 말고 부품을 고친다.

## 3. 형태 어휘 3종 (§3-1)

| 형태 | radius | 무엇이 이 형태인가 |
| --- | --- | --- |
| **사각** | 6 (`--radius-control`) | 누르고 입력하는 것. 버튼 전 종류, 입력칸, 내비 항목 |
| **알약** | `--radius-pill` | 읽기만 하는 것. 배지, 칩, 상태 태그 |
| **원** | 원형 | 32px 아이콘 버튼, 라디오 |

카드와 패널, 배너, 모달은 radius 12(`--radius-card`)로 이 셋 밖에 선다. 카드의 경계와 그림자, 카드 안 카드 금지, 버튼이 서는 자리는 §5 가 갖는다.

## 4. 색 계열 의미 (§5 D58, §1-4)

계열의 뜻은 **고정이다.** "여기만 예쁘게" 고르지 않는다.

| 계열 | 뜻 |
| --- | --- |
| **블루** | 일정, 시간, 유형, 정보 |
| **민트** | 사람, 소속, 진행, 담당 |
| **라벤더** | AI 산출, 승인 대기, 주의 |
| **리스크 핑크** | 확인된 경고 전용(D9). 다른 어디에도 쓰지 않는다 |

- 각 계열은 **base(면), deep(글자), tint(배경)** 3단이고 섞지 않는다. **base 를 글자에 쓰지 않는다.**
- **deep 은 곁다리로 읽는 칩에만 쓴다.** 자기 tint 위 대비가 2.0 안팎이다(§9).
- 새 색과 새 그라데이션을 만들지 않는다(그라데이션 어휘는 §3-3).

## 5. 크기 계단 (§2-1), 닫혀 있다

`--text-2xl` 28(페이지 제목, HERO 실명) / `--text-xl` 24(767 미만 제목) / `--text-lg` 18(섹션 제목) / `--text-md` 16 / `--text-sm` 14 / `--text-btn` 15(핵심 버튼 전용).

**14 가 하한이고 예외가 없다**(12px `--text-xs` 는 2026-08-10 폐지). `font-size: <숫자>px` 를 쓰지 않는다. 계단 밖 크기는 당사자 카드 이름 하나다(`calc(var(--text-md) + 1px)`).

## 6. 굵기 3단 (§2-1)

**400** 본문 / **500** 셸 기본(사이드바, 헤더) / **600** 강조. **700 은 없다.**

**크기, 굵기, 행간 중 하나라도 정하는 요소 선택자는 셋을 같이 정한다.** 비워 두면 브라우저 기본 700 이 산다.

## 7. 여백 3단 (§3-4)

| 자리 | 값 |
| --- | --- |
| 페이지 세로 스택(구획 카드, HERO, 배너) | **24** `--section-gap` |
| 같은 목록 안 카드 사이 | **20** `--space-5` |
| 행 카드 스택(접힌 회차 줄, 당사자 행) | **12** `--space-3` |

- 카드 안 **좌우는 24 하나다.** 세로만 역할이 정한다(패딩 4종은 §3-4).
- **4의 배수만** 쓴다. 하프스텝은 2, 6, 10, 14 넷뿐이고 그 밖의 값은 앞줄에 `/* optical: 사유 */` 가 필요하다.
- **여백은 부품이 만들지 않고 묶음의 `gap` 이 만든다.** `margin-bottom` 으로 띄우면 이중이 된다.

## 8. 꺽쇠 배수 (§3-4)

꺽쇠는 px 이 아니라 **em** 이라 옆 글자를 따라 커지고 작아진다. 상자 **`.5625em`**, 획 **`.125em`** 하나다(2026-08-10 통일). 예외인 동의 요약 줄은 크기를 다시 박지 않고 기준 글자를 `.7em` 으로 낮춘다.

## 검사가 잡는 것과 못 잡는 것

| 검사 | 무엇을 보나 |
| --- | --- |
| `pnpm guard:tokens` | **값** 한 축씩. 계단 밖 크기, 미선언 굵기, 토큰 밖 색, 인라인 스타일 |
| `pnpm guard:hierarchy` | **조합**. 한 요소에 최종적으로 적히는 크기+굵기+색이 위 1장 표 밖인가 (2026-08-10 신설, CCC-80). 커밋 훅에도 걸려 있다 |
| `pnpm design:hierarchy` | **렌더 결과**. 실제 부품으로 렌더한 정적 HTML 을 브라우저에 물려 계산값을 읽는다. 화면 8개 × 폭 1280·767·390, 레인 둘이다. ① **이웃 관계** 세로로 붙은 두 줄이 같은 옷인가(1장 '연속 금지') ② **기하** 자리 예약이 살아 있는가, 붙박이가 켜지는가, 화면 밖으로 나가는가(2026-08-11 신설, CCC-91) |

값이 셋 다 합법 토큰인데 조합이 표 밖인 자리가 있다. 그래서 `guard:tokens` 위반 0 인데도 화면이 눌린 채로 나왔다. 조합은 이제 `guard:hierarchy` 가 막지만, **이웃 관계는 여전히 CSS 파일에 없다** — 렌더 결과를 봐야 알 수 있어 하니스와 검수가 맡는다. 그 앞단을 막는 것이 2장의 부품이다.

기존 위반은 `scripts/design/hierarchy-baseline.json` 에 담겨 있고 새 위반만 막는다. 고쳤으면 기준선에서도 지운다(안 지우면 실패한다). 기준선을 다시 만드는 명령은 `pnpm guard:hierarchy:baseline` 이고, **새 위반을 기준선에 넣어 통과시키는 용도가 아니다.**

실측 쪽 기준선은 레인마다 따로다. 이웃 관계는 `hierarchy-adjacency-baseline.json`, 기하는 `geometry-baseline.json` 이고 둘 다 `pnpm design:hierarchy -- --update-baseline` 이 함께 다시 쓴다. **기하 기준선에 지금 적혀 있는 5건은 승인된 예외가 아니라 아직 안 고친 결함이다**(인테이크 화면이 390 에서 12px 밖으로 나간다, CCC-91 실측 · 고치는 것은 CCC-93).

가로 넘침은 **넘침이 시작되는 바깥 테두리만** 센다. 부모가 이미 넘쳤으면 자식은 따라 나간 것이라 새 사실이 아니고, 자손까지 세면 판정이 글꼴을 타 리눅스 CI 와 macOS 가 갈린다(2026-08-11 실측: 같은 결함이 16건 대 129건).
