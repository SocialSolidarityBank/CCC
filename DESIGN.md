# CCC 디자인 시스템 — SSOT

> **이 파일은 근거와 이력 보관소다 (2026-08-10, ADR-0033).** 화면을 만들기 전에는 `DESIGN-RULES.md` 를 읽는다. 그쪽은 지금 지켜야 하는 것만 추린 요약본이고, **결정 권한은 여기 그대로 있다.** 둘이 어긋나면 이 파일이 맞고 요약본이 옮겨 적기 오류다(색값은 여전히 pen 이 SSOT).
>
> 이 파일은 도구 계측 59,532토큰이라 화면 하나 만들 때 통째로 읽히는 일이 실제로 없었고, 그래서 규칙이 날짜별 개정 문단 안에 묻혀 매번 같은 위반이 났다. 나눈 것은 **읽는 자리**지 권한이 아니다. 여기 없는 규칙은 요약본에도 없다.
>
> **규칙을 바꾸면 `DESIGN-RULES.md` 도 같은 커밋에서 고친다.** 요약본이 뒤처지면 없느니만 못하다.

> **버전: CCC-design-system V0.1 (2026-07-26 Q 확정)**
> 첫 확정본이다. 토큰·컴포넌트 계약·문안 규칙이 pen 문서와 일치하는 상태를 기준선으로 삼고, 이후 변경은 이 기준선과의 차이로 관리한다. 태그 `design-system-v0.1`.

> **색 토큰의 정본(SSOT)은 pen.dev 문서의 '색 토큰' 페이지다** (2026-07-26 Q 확정).
> 색값이 이 문서와 pen 페이지에서 어긋나면 **pen 페이지가 맞다**. 이 문서는 그 값을 반영하고 규칙을 붙이는 자리다.
> **색 외 토큰(간격·형태·타이포·컴포넌트 계약)의 SSOT는 이 파일이다** (ADR-0011). Stitch·Figma·코드가 어긋나면 이 파일이 맞다.
> 기계 소비용 토큰 파일: `design/tokens.css`.
> pen 원본: `~/.pencil/documents/d3a3c6d6-9556-4bb6-8ef3-cb8b2ec1a082/pencil-welcome-desktop.pen`
>
> **v3.2 (2026-08-07 · D61)** — 디자인 규칙 리팩터. **색값은 한 개도 바꾸지 않았다**(pen SSOT 불변). ① 버튼 전 종류를 알약에서 **직사각형 radius 6** 으로(§3-1 — 입력칸·카드와 같은 사각 어휘. 알약은 배지·상태 태그 전유물, 원형은 32px 아이콘 버튼만) ② 배지 레시피 8종 복사본을 **`.wire-badge`(WireBadge) 단일 계약**으로 통합(§5 — risk 톤 신설, HERO 상태 태그도 `.wire-status-tag` 하나로) ③ 제목과 본문의 **짝 계약** 신설(§2-1 — 카드 안 구획 제목은 14/600 라벨(`--sub` 또는 계열 컬러), 제목 아래 설명은 14/400) ④ 카드 패딩 4종과 **구분선 단일화**(§3-4 — `.wire-card-divider` 하나) ⑤ **콜아웃(WireCallout)·인용(WireQuote) 컴포넌트 확정**(§5 — 톤 3종, 손 안내 상자 금지) ⑥ 그라데이션 어휘 요약표 승격(§3-3) ⑦ 화면 문자열의 긴 대시·구분자 가운뎃점 제거를 `pnpm guard:tokens` 검사로 강제(§10). 상세 ADR-0031.
>
> **v3.1 (2026-07-31)** — 토큰 리팩터. **색은 한 값도 건드리지 않았다**(pen이 SSOT이고 값은 Q 결정이다). 색 외 축에서 비어 있던 자리를 채웠다: ① 크기·행간 토큰 신설(§2-1 — 크기 토큰이 **하나도 없었다**) ② 광학 하프스텝 4종 토큰화 + 카드 안 패딩 3종 확정(§3-4) ③ 버튼의 일반↔강조 구분을 테두리에서 면·깊이로 옮기고 호버·누름 상태 신설(§5) ④ 체크박스·라디오·고른 행의 켬을 **면 채움**으로(§5, Q 결정) ⑤ `pnpm guard:tokens` 신설 — 이 문서의 계약 중 기계로 잴 수 있는 것을 결정론으로 옮겼다. 함께 고친 결함 1건: `--blue-base`는 **정의된 적 없는 토큰**이라 달력에서 선택한 날짜에 면이 칠해지지 않았다. 상세 ADR-0025.
>
> **v3 (2026-07-26)** — Q가 pen에서 색 토큰을 직접 정리한 결과를 그대로 반영했다. 변수명을 SSOT 이름(blue/mint/lavender)으로 통일, 폐기 3개 삭제, 파스텔·리스크 색값 전면 조정, 높이 변수 4종 추가. **색값은 Q 결정이므로 임의 변경 금지다.** 대비 수치는 전부 이 문서 작성 시점에 실측해 갱신했고, 미달 항목은 §9에 있다.
> **v2 (2026-07-25)** — 근거는 D34 / ADR-0012. Pen에서 Q가 정밀화한 '상담 준비 브리핑' 화면에서 값을 역추출해 규칙화했다.
> v1(무채색 전용, D30/ADR-0008)은 색 규율 부분이 D34으로 대체 — 타이포·간격·액센트 띠 금지 규칙은 그대로 살아있다. git 이력 참조.
>
> **관찰(Observed)과 추론(Extrapolated)을 구분해 표기했다.** 화면 1개(브리핑)에서만 뽑았으므로, `[추론]` 표시된 항목은 아직 시안으로 확인되지 않았다 — Stitch·코드가 이를 관찰값처럼 취급하면 안 된다.

## 0. 정체성 한 줄

상담 5분 전에 열어 15초 안에 훑는 조용한 내부 업무 화면. 바탕은 웜 뉴트럴이고, **파스텔 3색은 장식이 아니라 정보 분류 신호**다. 그 위에서 **리스크 핑크는 화면의 유일한 경고색**으로, 파스텔 3계열에 없는 색상환 위치(H328 마젠타핑크)로 구분된다(D9). 깊이는 옅은 이중 그림자로 만들고, 카드 경계는 파스텔 1px 그라데이션 테두리가 함께 잡는다.

## 1. 색 토큰

### 1-1. 중립 (관찰)

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--canvas` | `#FAFAF9` | 화면 바탕 (웜 오프화이트) |
| `--panel` | `#FFFFFF` | 카드·패널·입력칸 표면 |
| `--muted` | `#F5F5F4` | 보조 표면·플레이스홀더 배경 |
| `--ink` | `#3D3445` | 기본 텍스트 (캔버스 위 11.32 · 흰 위 11.83) — 순검정이 아니라 보라 기운을 넣은 웜 잉크 |
| `--sub` | `#534E57` | 보조 텍스트·불릿·비활성 아이콘 (흰 위 8.09) |
| `--line` | `#E7E5E4` | 장식 보더 1px — 로고 자리·필 버튼 등 (흰 위 1.26) |
| `--line-control` | `#E3E3E3` | **입력칸·컨트롤 경계** (흰 위 1.28) |
| `--track` | `#E0E0E0` | 추이 막대의 무채색 구간 (흰 위 1.32) |

### 1-2. 파스텔 3색 — 각 색은 base / deep / tint 3단 (pen '색 토큰' 페이지 값)

`base`는 **면**(게이지 채움·추이 막대)에만, `deep`은 **글자·아이콘**에만, `tint`는 **배경**에만 쓴다.

| 계열 | base (면) | deep (글자·아이콘) | tint (배경) | deep 대비: tint 위 / 흰 위 |
| --- | --- | --- | --- | --- |
| 블루 | `#93C1F5` | `#67A9F0` | `#E7EEF8` | 2.11 / 2.47 |
| 민트 | `#8ED9B8` | `#56CC99` | `#E4F4EC` | 1.76 / 2.00 |
| 라벤더 | `#CBA8E2` | `#C890F0` | `#F2E7F7` | 2.02 / 2.41 |

> **색값은 Q가 pen에서 직접 정한 것이며 임의로 바꾸지 않는다(2026-07-26).** deep 3색은 v2보다 밝아져 자기 tint 위 대비가 4.40/2.33/3.78에서 **2.11/1.76/2.02로 내려갔다**. 세 값 모두 WCAG AA(4.5)는 물론 large text 기준 3:1에도 미달하므로, **글자를 키우거나 굵게 해도 기준을 통과하지 못한다**. 영향 범위와 완화 방법은 **§9 알려진 접근성 예외**에 있다.

### 1-3. Semantic (관찰)

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--risk` | `#F071B4` | **확인된 리스크 플래그 전용 — 화면의 유일한 경고색** (흰 위 2.72 · 배너 배경 위 2.39) |
| `--risk-tint` | `rgba(240,113,180,0.05)` | 리스크 배너 배경 틴트 |
| `--risk-tint-solid` | `#F7EEF2` | 위 틴트의 불투명 등가값 — 그라데이션 테두리에 필요 |

> **2026-07-26 개정(Q 결정)**: `#B52573`(H328 · S66 · L43)에서 **`#F071B4`(H328 · S81 · L69)** 로 밝아졌다. 색상(H328)은 그대로라 마젠타핑크 자리는 유지되지만, **밝기가 올라 흰 배경 위 대비가 6.04에서 2.72로 떨어졌다**. 리스크 배너의 제목과 경고 아이콘이 이 색이므로 **경고 텍스트가 WCAG 기준 아래에 있다** — §9에 기록했다. 이 수치를 알고 유지하는 것이 Q의 결정이다.

### 1-4. 색 규율 (위반 = D34 락 위반)

1. **파스텔 3색은 의미에 고정한다.** 아래 배정표를 벗어나 "여기만 예쁘게" 색을 고르지 않는다.
2. **리스크 독점은 유지된다.** `--risk`는 확인된 리스크 플래그·위기 상태·오류에만 쓰고, 다른 어떤 요소도 이 색을 쓰지 않는다.
   > **구분 근거가 2026-07-26에 바뀌었다.** v2에서는 "리스크만 채도가 파스텔의 2배"라는 채도 격차가 D9를 강제하는 장치였다. 현재 값은 블루 S83 · 라벤더 S76 · 리스크 S81로 **채도가 거의 같아 그 장치가 더는 작동하지 않는다.** 남은 구분은 색상 하나뿐이다 — 리스크 H328(마젠타핑크)은 블루 H211·민트 H154·라벤더 H275 어디에도 없는 자리다. 따라서 락의 내용도 바뀐다: **파스텔 3계열에 핑크·마젠타 계열(H300~H345)을 새로 들이는 것이 락 위반**이다. 특히 라벤더 deep(H275)과 리스크(H328)가 나란히 놓이는 검토·승인 화면에서 둘의 거리가 가장 가까우므로, 그 화면에서는 색만으로 구분을 맡기지 않고 아이콘과 문구를 함께 둔다.
3. **base는 글자에 쓰지 않는다.** 면·게이지·막대 전용. 글자·아이콘은 반드시 deep. deep도 **14px 이상·굵기 600**에서만 쓴다. 다만 현재 deep 값들은 크기를 키워도 대비 기준을 통과하지 못하므로(§9), 이 규칙은 대비 확보가 아니라 **최소한의 읽기 부담 관리**로만 작동한다.
4. **감정 추이는 무채색만**(R4). `--track` 그레이 스케일. 파스텔도 리스크 마젠타도 쓰지 않는다.
5. **주의·대기·미완료**는 라벤더 tint 배지로 표시한다(v1의 검정 반전 배지를 대체 — D34). 노랑·주황은 여전히 금지.
6. **액센트 띠 금지는 유지된다**(ADR-0008). 한쪽만 두꺼운 색 띠로 강조하지 않는다. 카드 헤더/본문 사이 1px 그라데이션 선은 **구조적 구분선**이라 이 규칙의 대상이 아니다(ADR-0008의 리스트 행 구분선 예외와 동일 취급). **2026-08-06 Q: 카드 구분선은 회색 `--line` 풀블리드로 바뀌었다**(§5 카드/패널). 그라데이션 구조선의 남은 사용처는 인용 블록 세로선과 탭 활성 밑줄이다.
7. **새 색 추가 절차**: 쓰일 배경 위 WCAG AA(4.5) 대비를 먼저 계산 → ADR-0012 개정 → 이 파일 → `design/tokens.css`. 계산 결과가 미달이면 **§9에 예외로 기록**한 뒤에만 쓴다 — 계산을 건너뛰는 것은 금지고, 미달을 아는 채로 쓰는 것은 결정이다.

### 1-5. 파스텔 3색 배정표 (관찰에서 도출)

| 계열 | 의미 | 관찰된 사용처 |
| --- | --- | --- |
| **블루** | 시간·상태·진행 | 활성 내비 항목, **기본값(AI 상태가 아닌) HERO 상태 태그**('15초 페이지' — 2026-08-08 Q 개명, 구 '상담 준비'), 액션 아이템 기한, 전체 열기/닫기 토글 라벨 |
| **민트** | 사람·소속 | 당사자 정보 라벨(연락처·비상연락처·거주지), 참여 사업 칩, **새 가입 배지**(CCC-26 — 인테이크 전 케이스로 담당자가 아직 확인하지 않은 당사자. 목록 카드와 사이드바 '참여자' 메뉴의 숫자 두 곳) |
| **라벤더** | AI·승인 대기 | '승인 대기 N건' 배지 (D5), **AI 초안 검토 화면의 HERO 상태 태그**(검토 대기·승인됨·반려됨 3종 — `stageTagTone="lavender"`, 2026-08-19 D61 ② 개정·CCC-106) |
| 3색 회전 | 구분 전용 | GAS 게이지 — 목표 순서대로 블루→민트→라벤더. **의미 없음**, 목표 3개를 서로 구분하기 위한 회전이다. GAS 점수의 좋고 나쁨을 색으로 표시하지 않는다(D6·R4) |

## 2. 타이포그래피 (관찰)

- 서체: **Pretendard Variable** (npm self-host, CDN 금지 — PII 앱). 폴백: Apple SD Gothic Neo, Noto Sans KR, system-ui.
- 굵기는 **400·500·600 세 단계**다(2026-08-04 Q — 사이드바 기본 굵기로 500 신설, 강조(활성·선택·기관명)만 600. 구 2026-08-03 '400·600 두 단계'를 대체하며 본문 400 은 그대로다. 그 전 역사: 700 이 작은 화면에서 뭉개져 2026-08-03 에 강조 단을 600 으로 내림. `pnpm guard:tokens` 가 강제).
- 트래킹: 기본(한글·혼용) `-0.01em`. **숫자만 단독으로 서는 곳**(GAS 점수·ID 번호·날짜·기한)만 `-0.03em`.
- **한글 광학 보정** `--nudge-hangul` = **0 확정**(2026-08-04 실측 — 구 0.045em·0.02em 잠정값 폐기): canvas TextMetrics 로 재 보니 Pretendard 한글 잉크 중심은 line-height 1 컨트롤에서 이미 기하 중심 ±0.4px 안이다(16px '상담 등록' 기준 −0.18px). "한글은 위로 떠 보인다"는 전제가 이 폰트에서는 성립하지 않아, 양수 보정값이 두 번 모두 "아래로 치우침"으로 관찰됐다. 토큰은 단일행 컨트롤 전용 보정 축(계약)으로 남고 값만 0 이다 — Q 가 눈으로 덮어쓸 수 있다.
- 이탤릭·디스플레이 세리프 금지(한글 대응 서체 부재로 기각된 축 — ADR-0008 유지).
- **줄바꿈 전역 계약**(2026-08-07 Q 9차): 본문은 `word-break:keep-all`(어절 경계에서만 꺾음) + `overflow-wrap:break-word`(어절보다 긴 통짜 값 안전핀) + `text-wrap:pretty`(마지막 줄이 낱말 하나로 남는 극단 방지), 제목·구획 머리(h1·h2·h3·legend)는 `text-wrap:balance`(두 줄이 되면 길이를 고르게). body 에 걸려 상속되므로 화면별로 다시 선언하지 않는다.

### 2-1. 크기 계단 — **닫혀 있다** (2026-07-31 신설)

크기는 아래 다섯 토큰뿐이고 **이 계단 밖의 `font-size`는 위반**이다. 코드에서는 px을 직접 쓰지 않는다.

| 토큰 | 값 | 이 크기가 서는 자리 |
| --- | --- | --- |
| `--text-2xl` | 28px | 페이지 제목 · HERO 실명 |
| `--text-xl` | 24px | 좁은 화면(767 미만) 페이지 제목 · GAS 점수 |
| `--text-lg` | 18px | 섹션 제목(카드 여러 장을 묶는 h2 — 카드 제목은 2026-08-07 Q 로 16 에 내려갔다) |
| `--text-md` | 16px | 본문 · 행 강조 · 입력값 · 가명 ID |
| `--text-sm` | 14px | 배지·칩·라벨·메타 · 이동·보기 조작 알약(neutral·ghost 버튼, 달 라벨) · 입력칸 아래 도움말(`.wire-form-hint`·`.schedule-form-hint`) — **본문 계열의 하한이며 예외가 없다**(2026-08-10 Q 로 도움말 예외가 닫혔다) |
| ~~`--text-xs`~~ | ~~12px~~ | **폐지**(2026-08-10 Q): 마지막 사용처였던 `.wire-form-hint` 가 14 로 올라가 사용처가 0 이 됐고, 그것으로 2026-08-07 "작게 표시해야 할 설명 문구는 12px 전역 통일"이 완전히 걷혔다. 14 로 재정의해 이름만 남기지 않은 것은 `--text-sm` 과 값이 같은 두 번째 이름이 되기 때문이다. 지운 쪽이 결정론으로 막힌다: `token-audit` 이 `var(--text-xs)` 를 `undefined-token` 으로, `font-size:12px` 을 `raw-font-size` 로 잡는다. 12 가 다시 필요하면 토큰을 되살리기 전에 Q 에게 묻는다 |
| `--text-btn` | 15px | **버튼 전용 하프스텝**(2026-08-06 Q): 핵심 버튼(프라이머리·세컨더리·위험)만. 본문 16 과 라벨 14 사이의 광학 값이라 버튼 밖에서는 쓰지 않는다 |

> **왜 역할이 아니라 계단으로 이름을 붙였나.** 이 표의 왼쪽 열을 `--text-badge` · `--text-body` 처럼 역할로 지으면, 14px 하나가 배지·칩·라벨·도움말·메타 다섯 역할에 쓰이는 현실(코드 89곳)에서 자리마다 "이건 무슨 역할인가"를 판단해야 하고 그 판단이 곧 드리프트가 된다. 역할 대응은 이 표가 갖고, 토큰은 계단만 센다.
>
> **신설 전 상태**: 크기 토큰이 **하나도 없었다**. 전부 원시 px이었고 계단 밖 값 12px·13px이 네 곳에 새어 있었다. 그중 둘(`consent-upload-slot-state` 12px 라벤더 deep · `register-program-fixed-label` 13px 민트 deep)은 §9가 못 박은 "미달 색은 **14px 이상·굵기 700에서만**"을 정면으로 어기고 있었다 — 네 곳 모두 14px로 올렸다.

줄 높이도 크기와 짝으로 토큰화한다(크기만 이름을 주고 행간을 원시값으로 두면 같은 크기가 자리마다 다른 리듬으로 앉는다): `--leading-none` 1 · `--leading-tight` 1.25 · `--leading-snug` 1.35 · `--leading-normal` 1.5 · `--leading-body` 1.55 · `--leading-relaxed` 1.6.

**계단 예외 1건**(2026-08-06 Q "이름 볼드 + 폰트 1px"): 당사자 카드의 이름은 계단 16 위에 광학 +1px, 즉 `calc(var(--text-md) + 1px)` 로 선다(§5 '당사자 카드'). 기준값이 계단 토큰이라 계단이 움직이면 함께 움직인다. 이 밖의 계단 밖 크기는 여전히 위반이다.

**굵기는 토큰으로 치환하지 않는다.** 400·500·600은 그 자체로 읽히는 값이라 이름을 붙여도 읽는 사람이 얻는 것이 없고, 막아야 할 것은 "계단 밖 굵기(700 같은)가 새로 들어오는 일"인데 그건 이름이 아니라 검사가 막는다 — `pnpm guard:tokens`가 원시값을 직접 단언한다.

**굵기는 반드시 선언한다**(2026-08-09 신설). 700이 새는 길은 "700이라고 쓰는 것" 말고 하나 더 있었다 — **아무것도 안 쓰는 것**이다. `h1`·`h2`·`strong`은 크기와 행간만 적히고 굵기가 비어 있어 브라우저 기본값 `bold`(700)가 그대로 살았고, 검사는 선언된 값만 보므로 이 경우를 못 잡았다. 실제로 700으로 서 있던 자리: 로딩·오류·빈 상태의 페이지 제목 6곳, 온보딩·관리자·가입 완료의 `h2`, 상담 기록의 '기록 오류' 강조. 이제 세 선택자가 600을 선언하고 `guard:tokens`가 그 선언의 존재를 단언한다. **크기·굵기·행간 중 하나라도 정하는 요소 선택자는 셋을 같이 정한다.**

아래 역할표는 **계약이다**(2026-08-02 D58 승격 — 이전에는 관찰 기록이었다). **이 표 밖의 크기-역할 조합은 위반**이다. 큰 묶음은 넷: 캡션·라벨·메타 = `--text-sm`(14) / 본문 = `--text-md`(16) / 카드·섹션 제목 = `--text-lg`(18) / 헤딩 = `--text-xl`·`--text-2xl`(24·28).

| 단계 | 크기 | 굵기 | 줄 높이 | 용도 |
| --- | --- | --- | --- | --- |
| HERO 실명 | `--text-2xl` | 600 | `--leading-tight` | 당사자 실명 (D31) |
| GAS 점수 | `--text-xl` | 600 | `--leading-none` | 게이지 중앙 숫자 (트래킹 -0.03em) |
| 섹션 제목 | `--text-lg` | 600 | `--leading-snug` | 카드 여러 장을 묶는 h2(회차별 기록·상담 전 꼭 봐야할 내용) |
| 카드 제목 | `--text-md` | 600 | `--leading-snug` | 카드 헤더(`.wire-card-title` — 2026-08-07 Q "폰트 크기 줄일 것", 구 18 대체). **예외**: 당사자 허브의 전폭 카드 3장(참여중인 사업·최신 일정·동의서)은 섹션 역할이라 `--text-lg` 18 이다(같은 날 Q 8차, `.participant-hub-card`) |
| 행 강조 | `--text-md` | 600 | `--leading-normal` | 목표명·액션 제목·정보 값·리스크 항목 |
| 셸(사이드바·헤더) | `--text-md` (기관명 `--text-lg`) | 500 (활성·선택·기관명 600) | `--leading-normal` | 내비 라벨·'사업' 라벨·사업명·하단 묶음 — **셸 글자 하한은 16**(2026-08-04 Q, 구 14/600). 기관명은 18/600. 2026-08-05 헤더 신설로 기관명·'사업' 라벨·사업명은 데스크톱에서 헤더에 서지만 타이포 계약은 같다 |
| 본문 | `--text-md` | 400 | `--leading-body` | 브리핑 본문·주소 |
| 여러 줄 입력 | `--text-md` | 400 | `--leading-relaxed` | textarea |
| 배지·칩 | `--text-sm` | **400**(2026-08-06 Q — 구 600: 뱃지 글자는 강조가 아니라 분류 표시라 본문과 같은 굵기다) | `--leading-normal` | 상태 태그·배지·칩·기한·토글 |
| 라벨 | `--text-sm` | 600 | `--leading-normal` | 정보 라벨(민트 deep)·**카드 안 구획 제목**(2026-08-07 D61: h3 소제목도 라벨이다 — 14/600, 색은 `--sub` 또는 계열 컬러(상담 기록 구획은 민트, 2026-08-06 Q)) |
| 메타·설명 | `--text-sm` | 400 | `--leading-normal` | 제목 아래 설명 줄(`.panel-meta`), 자동 저장 상태, 도움말(2026-08-07 D61 분리: 구 600 대체. 짝에서 제목이 600 을 갖고 설명은 400 으로 물러선다) |
| 핵심 버튼 | `--text-btn` | 600 | normal | 프라이머리·세컨더리·위험 버튼 라벨(2026-08-06 Q). 이동·보기 조작(neutral·ghost)은 14/400 — 강조 제외 400 기본 원칙 |
| 가명 ID | `--text-md` | 400 | `--leading-none` | **D59(2026-08-04): 가명 ID 는 화면에 표시하지 않는다** — 이름이 없는 두 경우(무응답 등록·파기 후)에만 이름 자리 폴백으로 나온다. §5 '이름 표기' 참조. **2026-08-06 Q 부분 재개정: 당사자 카드(일정·당사자 목록)의 정보 칸에는 이름 옆에 16/400 `--sub` 로 표시한다**(§5 '당사자 카드') |

> v0.1 표에 있던 "목록 본문 16/400/1.3" 행은 지웠다 — 코드가 한 곳도 구현한 적이 없어 계약이 아니라 미실현 초안이었다.

**제목과 본문의 짝 계약** (2026-08-07 D61): 한 덩어리 안에서 제목과 본문이 나뉘면 아래 두 짝 중 하나만 쓴다. 크기, 굵기, 색을 자리마다 새로 조합하지 않는다.

| 짝 | 제목 | 본문·설명 |
| --- | --- | --- |
| 카드 제목 + 본문 | 16/600 `--ink`(2026-08-07 Q — 구 18/600) | 16/400 `--ink` |
| 소제목(안내줄·항목) + 설명 | 16/600 `--ink` | 14/400 `--sub` |

카드 안 구획 제목(h3 소제목)은 제목이 아니라 **라벨**이다: 14/600 (`.wire-card-section>h3` = `--sub`, `.record-block>h3` = 민트 deep — 상담 기록 구획 라벨은 계열 컬러, 2026-08-06 Q). 본문 16 과 크기가 겹치면 위계가 사라지기 때문에 본문보다 작게 두고 색으로 물러선다. 정보 필드 라벨도 민트 deep 이다(사람·소속 축, D34).

**누르는 것은 표를 따로 쓴다** (2026-08-10 Q 신설, CCC-84). 위 역할표는 **읽는 줄**의 표다. 컨트롤은 자기 면과 테두리를 가져 §2-2 규칙 1 의 '한 축'을 이미 만족하므로, 읽는 줄과 같은 잣대로 재면 멀쩡한 자리가 걸린다. 실제로 `guard:hierarchy` 를 처음 돌렸을 때 잡힌 14건 중 6건이 컨트롤이었다.

| 컨트롤 | 크기/굵기 | 색 | 자리 |
| --- | --- | --- | --- |
| 핵심 버튼 | 15/600 | `--ink` · `--risk` · 계열 deep | 프라이머리·세컨더리·위험 |
| **작은 컨트롤** | 14/600 | `--ink` · 계열 deep · `--risk` | 작은 버튼(`data-height="sm"`) · 인테이크 단계 버튼 · 동의 토글 요약 · 완료 버튼 · 눌린 시간 슬롯 |
| 이동·보기 조작 알약 | 14/400 | `--ink` · `--sub` | neutral·ghost |
| **본문 크기 컨트롤** | 16/600 · 16/400 | `--ink`(선택) · `--sub`(비선택) | 탭 · 그 자리에서 고치는 값 표시 버튼 |

이 표는 읽는 줄의 표를 **대신하지 않고 더한다.** 갈아치우면 라벨 차림으로 조용히 서 있는 토글(`.briefing-history>summary` 14/600 `--sub`)이 새로 걸린다. 그리고 읽는 줄에는 이 칸을 열지 않는다 — 14/600 `--ink` 를 전역으로 열면 구획 라벨을 `--sub` 대신 `--ink` 로 쓴 것까지 합법이 되어, 이 표가 막으려던 드리프트가 그대로 들어온다.

**상태는 색만 물러선다** (같은 날 신설). 아래 둘은 새로운 단이 아니라 상태다. 크기와 굵기가 계약 안이면 색이 `--sub`·`--muted` 로 내려가는 것을 위반으로 세지 않는다.

- **비활성** — `:disabled` · `[aria-disabled]`
- **값이 아직 없음** — `.is-empty`. 목표 카드 세 곳(`.briefing-goal-text` · `.briefing-goal-display` · `.record-goal-text`)이 값이 있으면 16/600 `--ink`, 비면 16/400 `--sub` 로 똑같이 물러선다

`yellow` **`16/400 --sub` 는 조합으로 열 수 없다.** 킷 페이지의 '고치기 전' 반례가 정확히 그 조합이라, 표에 넣는 순간 반례가 반례가 아니게 된다. 그 크기로 물러서야 하는 자리(HERO 연락처, 당사자 카드 보조 칸)는 검사에서 **자리를 지정한 예외**로 둔다(`hierarchy-audit.mjs` 의 `ALLOW`, 2026-08-10 Q).

### 2-2. 줄이 쌓일 때 — **위계 쌓임 규칙** (2026-08-09 Q 신설)

위 역할표와 짝 계약은 **한 줄, 또는 제목+본문 두 줄**까지만 답한다. 실제 화면에서 어긋난 자리는 전부 **셋 이상이 쌓인 곳**이었다: 상담 일정 등록의 `상담 유형 / 기본 상담 / 안내문 / 버튼` 4줄, 인테이크 1-3의 `8회 / 다음 상담 방법`, 상담 기록 작성의 6영역 구획. 크기가 비슷한 줄이 이어지면 무엇을 먼저 읽어야 할지가 사라진다. 아래 넷은 **계약이다**.

1. **연속 금지.** 세로로 이웃한 두 줄이 *크기·굵기·색* 셋 다 같으면 위반이다. 최소 한 축은 달라야 한다. 목록 항목처럼 **본래 같은 위계인 형제**는 예외다(`.wire-bullets>li`, 표의 행) — 형제는 쌓임이 아니라 나열이다.
2. **셋 이상이면 장치를 쓴다.** 한 묶음에 줄이 3개 이상 쌓이면 아래 넷 중 하나를 반드시 쓴다. 그냥 쌓지 않는다.
   ⓐ 가로선(`.wire-card-divider`) ⓑ 카드 분리 ⓒ 상태 낱말을 배지로 올리기(규칙 4) ⓓ 2열로 가르기
3. **위계 4단.** 카드 안에서 쓸 수 있는 단은 넷뿐이다. 다섯 번째 단을 만들지 않는다.
   | 단 | 크기/굵기/색 | 자리 |
   | --- | --- | --- |
   | ① 카드 제목 | 16/600 `--ink` | `.wire-card-title` |
   | ② 구획 라벨 | 14/600 `--sub`(또는 계열 deep) | `.wire-card-section>h3` · `.record-block>h3` · 입력칸 라벨 |
   | ③ 값·본문 | 16/400 `--ink` | 읽는 값·문단 |
   | ④ 설명·메타 | 14/400 `--sub` | `.panel-meta` · 입력칸에 붙는 도움말 `.wire-form-hint`(2026-08-10 Q 지시로 14, 구 12) · 구획 머리에 서는 안내 문단 `.schedule-form-hint`(2026-08-09 Q 지시로 14). 이 단에 12 는 없다 |
4. **상태 낱말은 배지로 올린다.** '미기록'·'무응답'·'확인 필요'·회차처럼 **분류·상태를 나타내는 낱말**은 본문 글자로 두지 않고 `WireBadge` 로 낸다. 톤은 §5 계열 의미를 그대로 따른다(블루=일정·시간·유형 / 민트=사람·상태 / 라벤더=AI·대기 / risk=경고). 배지는 자기 면과 테두리를 가지므로 규칙 1의 '한 축'을 저절로 만족한다.

> **왜 검사로 안 막나.** 규칙 1은 정적 검사로 잡을 수 있는 모양이 아니다 — 이웃한 두 줄의 계산된 크기·굵기·색을 알아야 하고, 그건 CSS 파일이 아니라 렌더 결과에 있다. 그래서 이 규칙의 강제는 **하니스 실측**(앱 CSS 를 그대로 실은 정적 HTML 에서 계산값을 읽는다)과 `/design-review` 가 맡는다. 검사가 막는 것은 그 아래 단계다 — 계단 밖 크기·미선언 굵기·인라인 스타일(`pnpm guard:tokens`).
>
> **2026-08-10 개정(CCC-80): 규칙 3 은 검사로 넘어갔다.** 위 문단은 규칙 1 에 대해서는 그대로 맞지만, "위계 4단 밖 조합"까지 사람 몫으로 둔 것은 과했다. 한 요소에 최종적으로 적히는 조합은 CSS 만 보고도 세울 수 있다 — 같은 요소를 겨냥한 규칙들을 모아 크기·굵기·색을 합치면 된다. `pnpm guard:hierarchy`(`scripts/design/hierarchy-audit.mjs`)가 그 병합을 하고 §2-1 역할표와 대조한다. guard:tokens 가 못 보던 자리를 이것이 본다: 세 값이 전부 합법 토큰인데 조합이 표 밖인 경우(실제 첫 수확이 15초 페이지 AI 제안의 16/400 `--sub`).
>
> 검사가 여전히 못 보는 것은 둘이다. **이웃 관계**(규칙 1)는 DOM 순서를 알아야 하고, **세 축이 다 선언되지 않은 자리**는 값이 요소 종류나 조상에서 온다. 둘 다 하니스가 잰다(`pnpm design:hierarchy`). 미확정 자리 목록은 `pnpm guard:hierarchy -- --unresolved` 로 본다.
>
> **하니스가 봐주는 자리 하나를 여기 적어 둔다.** 이어지는 본문 문단(양쪽 40자 이상)은 나열로 보고 세지 않는다. 한 덩어리의 글이라 원래 대등하기 때문이다. 구조로는 가를 수 없어서 길이로 근사했다 — 킷 반례의 라벨·값 쌍도, 동의 문안의 문단 쌍도 둘 다 이름 없는 `<p>` 형제이고 갈리는 것은 길이뿐이다(5~22자 대 60자 이상). 근사이므로 봐준 쌍도 `measure.json` 의 `prose` 에 그대로 남는다.
>
> 하니스가 재는 범위는 화면 7개(상담 일정 등록은 2단계 기준) × **폭 3종 1280·767·390** 이다(2026-08-10 CCC-86 으로 넓혔다, 구 1280 하나).
>
> **좁은 폭을 따로 재는 이유는 실측이 답했다.** 15초 페이지가 1280 에서 0건인데 767·390 에서 걸렸다 — 회차 줄의 상담일과 핵심 한 줄이 넓은 폭에서는 가로로 나란히 서서 규칙 1 의 대상이 아니었다가, 열이 하나로 접히면서 위아래로 붙고 둘 다 16/400 `--ink` 가 된다. **가로로 나란하던 것이 세로로 쌓이는 순간이 규칙 1 이 처음 적용되는 자리**이므로 넓은 폭 0건은 좁은 폭 0건을 뜻하지 않는다. 이 자리는 기준선에 담아 두었고, 고치려면 상담일을 배지나 라벨로 한 단 올리면 된다(§2-2 규칙 4).
>
> 기존 위반은 `scripts/design/hierarchy-baseline.json` 에 담아 두고 새 위반만 막는다(2026-08-10 Q: 한 번에 차단하면 작업이 멈추고 결국 검사를 끄게 된다). 래칫은 양방향이다 — 새 위반이 생겨도 실패하고, 기준선에 있는 항목이 고쳐졌는데 목록에서 안 빠져도 실패한다.

### 2-3. 중요 정보 강조 의무 (2026-08-09 Q 신설)

상태, 분류, 경고값처럼 훑을 때 먼저 잡혀야 하는 값은 **강조 어휘로 반드시 처리한다**. 강조 어휘는 셋이다: 배지(`WireBadge`), 굵기 600, 계열 컬러. 지금까지는 "강조한다면 이렇게"라는 방법 규칙만 있었고 강조 자체를 의무로 세운 문장이 없어, 중요한 값이 민짜 본문(16/400 `--ink`)으로 남아도 걸리지 않았다. **중요한 값을 민짜 본문 글자로 두면 위반이다.**

- 어느 값이 어느 어휘를 입는지는 기존 계약이 정한다. 분류와 상태 낱말은 §2-2 규칙 4(배지로 올린다)를, 계열 배정은 §5 'D58 신설 계약'의 고정 의미(블루=일정, 민트=상태, 라벤더=AI와 승인 대기, 리스크=확인된 경고 전용)를 그대로 따른다. 이 절은 배정을 새로 만들지 않고 의무만 세운다.
- 본문 문장 안 강조는 색이 아니라 굵기 600이다(D58). 크기 키우기, 밑줄, 새 색 같은 강조 수단을 새로 만들지 않는다.
- 강제는 §2-2와 같은 길이다. "이 값이 중요한가"는 정적 검사가 판단할 수 없으므로, 하니스 실측과 `/design-review` 검수가 잡는다.

## 3. 형태·깊이·간격

### 3-1. 라운드 (pen 값, 2026-07-26 정정 1건 + 2026-08-07 D61 개정)

- **버튼 전 종류**(2026-08-07 D61, 구 pill 대체)·입력칸·내비 항목·리스크 항목 행: **6px** (`--radius-control`)
- 카드·패널·배너·모달: **12px** (`--radius-card`)
- 배지·칩(읽기 전용)·**상태 태그**(2026-08-06 Q 알약 재개정)·원형 아이콘 버튼(정사각 32)·라디오·불릿 점: **9999px** (`--radius-pill`)
- 체크박스: **4px** / 추이 막대: **2px**

> 2026-07-26 정정: v2는 HERO 상태 태그를 pill로 정규화했으나, **pen은 6px을 유지하고 있어 pen 값으로 되돌렸다.** 구분 기준은 형태가 아니라 성격이다 — 눌러서 상태를 바꾸는 태그는 컨트롤(6px), 읽기만 하는 배지·칩은 pill.
>
> **2026-08-06 (Q) 재개정: 상태 태그('상담 준비'·'진행 중' 류)도 알약이다.** 화면의 상태 태그는 실제로 읽기 전용이라 위 성격 기준으로도 pill 쪽이고, 뱃지 계열과 모양이 갈려 두 벌로 보이던 것을 통일했다(구 6px 대체 — `.participant-hero-stage`·`.wire-status-tag`).
>
> **2026-08-07 (D61) 버튼 직사각화.** 버튼(프라이머리, 세컨더리, 일반, 고스트, 위험과 크기 2단 전부)이 알약에서 **직사각형 + radius 6** 으로 바뀌었다. 입력칸(6), 카드(12)와 같은 사각 어휘로 묶어 조작 요소가 한 계열로 읽힌다. 형태 어휘는 셋으로 닫힌다: **누르고 입력하는 것 = 사각 6 / 읽기만 하는 배지·칩 = 알약 / 32px 원형 아이콘 버튼(헤더 계정 행동, 드로어 손잡이) = 원**. `pnpm guard:tokens` 의 pill 허용목록이 이 계약을 강제한다.
>
> **예외 1건(2026-08-07 Q 9차): 동의 '전문 보기'는 배지형 버튼이다.** Q 지시로 기본 배지 레시피(높이 24 · `--sub` 외곽선 · 알약 · 14/400)를 빌린 조작 요소가 됐다(`.consent-detail[data-inline]` 의 summary — 허용목록 등재). 다른 조작에 이 형태를 새로 만들지 않는다. **자리는 체크 라벨 바로 우측, 세로 가운데다**(2026-08-08 Q, 구 '라벨 아래 줄' 대체): 동의 항목 한 줄 = 체크 + 이름 + 전문 보기이고, 펼친 전문만 그 아래 줄을 통째로 쓴다(`.consent-item` 격자). 좁은 화면에서는 이름이 줄바꿈하고 알약은 같은 줄에 남는다.

### 3-2. 깊이 — 이중 그림자 + 파스텔 테두리

pen은 그림자 **색 3개**(`--shadow-mint` · `--shadow-lavender` · `--shadow-blue`)를 따로 두고 노드에서 좌우로 조합한다. CSS는 `box-shadow` 한 줄로 합쳐야 하므로 합성값을 함께 둔다. **pen의 3색 구조를 정본으로 삼는다(Q 결정)** — `--shadow-blue`는 현재 어디에도 쓰이지 않지만 pen에 있으므로 남긴다.

```css
--shadow-mint:     rgba(86, 204, 153, 0.15);
--shadow-lavender: rgba(200, 144, 240, 0.15);
--shadow-soft: -2px 2px 8px var(--shadow-mint), 2px 2px 8px var(--shadow-lavender);
--shadow-modal: 0 8px 32px rgba(61, 52, 69, 0.18);
```

민트와 라벤더를 좌우에서 각각 15% 투명도로 겹친 **양쪽 그림자**다. 모달만 위 `--shadow-modal`을 추가로 쓴다.

> **2026-08-05 (ADR-0030) — 본문 카드는 그림자를 쓰지 않는다.** 카드·HERO·리스크 배너·안내줄 등 본문 흐름 안의 표면은 **아웃라인**(비선택 = 회색 `--line` 1px / 선택·활성 = `--gradient-brand` 1px)만으로 선다 — Infisical·Cloudflare·Vercel·Supabase 대시보드의 공통 문법이다(Q 지시 "그림자 최소화, 아웃라인으로 대체"). `--shadow-soft` 는 **본문 흐름 밖으로 떠 있는 층**(팝오버·드롭다운·스티키 헤더)에만 남는다. 본문 카드가 그림자를 나눠 가지면 정작 떠 있는 층이 뜨지 않는다. *(구 2026-07-26 "깊이는 그림자·경계는 테두리" 분담을 대체.)* **2026-08-06 후속 종결: 프라이머리 버튼의 그림자도 뺐다.** 버튼도 본문 흐름이고, 일반과 강조의 구분은 면(채운 그라데이션 vs 흰 면 + 아웃라인)이 이미 만든다(§5 버튼 5종).

무채색 보더 1px은 **두 토큰을 구분해 쓴다**: 장식 경계(필 버튼·리스크 항목 행)는 `--line`, **입력칸·컨트롤 경계는 `--line-control`**. 다만 `--line-control`은 1.48에서 **1.28로 내려가** ADR-0008이 못 박은 입력 경계 가시성 기준 아래로 왔다. 색값은 Q 결정이므로 유지하되, 입력칸은 경계선 하나에만 기대지 않고 **라벨을 항상 위에 두는 규칙**(§5)으로 형태를 알린다.

### 3-3. 그라데이션 — 5개만

| 이름 | 값 | 쓰이는 곳 |
| --- | --- | --- |
| `--gradient-frame` | `linear-gradient(90deg, #93C1F5 0%, #8ED9B8 50%, #CBA8E2 100%)` | **셸 프레임 가로 구분선 1px** — 데스크톱 헤더 아래 · 모바일 바 아래 (2026-08-05 Q — 구 `--gradient-sidebar` 면 배경 대체: "그라데이션 배경 빼고 모두 라인으로 영역 구분". 3색 축은 그 배경 그대로, 값만 1px 선에서 보이는 base) |
| `--gradient-frame-v` | 위와 같되 `180deg` | **셸 프레임 세로 구분선 1px** — 사이드바 오른쪽 경계 |
| `--gradient-brand` | `linear-gradient(90deg, #8ED9B8 0%, #CBA8E2 100%)` | 카드 테두리 1px · 카드 헤더/본문 구분선 1px · 리스크 배너 테두리 1.5px |
| `--gradient-brand-v` | 위와 같되 `180deg` | 인용 블록 2px 세로선 · GAS 카드 테두리 |
| `--gradient-deep` | `linear-gradient(180deg, #56CC99 0%, #C890F0 100%)` | 체크박스 테두리 (deep 쌍) |
| `--gradient-action` | `linear-gradient(135deg, #A8E3C6 0%, #D1B3E3 100%)` | 프라이머리 버튼 배경 · **켜진 체크박스·라디오의 면** · **고른 리스트 행의 면** (뒤 둘은 2026-07-31 Q 결정) |
| `--gradient-hover` | `linear-gradient(90deg, #E7EEF8 0%, #F2E7F7 100%)` | 리스트 행 호버 배경 — tint 쌍 (2026-08-03 Q 결정. 선택의 action 그라데이션보다 연해 '지나가는 중'과 '고른 것'이 갈린다) |

이 표 밖에 그라데이션을 만들지 않는다 (2026-08-03 `--gradient-hover` 추가 · 2026-08-05 `--gradient-sidebar` 폐지 + 프레임 라인 2종 추가로 **7개**).

**그라데이션 어휘 한 줄 요약** (2026-08-07 D61 승격, 값 변경 없음 — 어느 자리에 어느 그라데이션인지 반복 규칙으로 못 박는다):

| 어휘 | 그라데이션 | 색 구성 |
| --- | --- | --- |
| 셸 프레임 라인(헤더 아래·사이드바 오른쪽 1px) | `--gradient-frame`(`-v`) | **3색** 블루→민트→라벤더 base |
| 선택·활성·세컨더리 아웃라인·탭 밑줄·인용 세로선 | `--gradient-brand`(`-v`) | 2색 민트→라벤더 base |
| 지금 정한 것(프라이머리 면·켠 체크박스와 라디오·고른 행) | `--gradient-action` | 2색 밝은 파스텔 |
| 컨트롤 테두리(체크박스·라디오) | `--gradient-deep` | 2색 deep |
| 지나가는 중(행·카드 호버 채움) | `--gradient-hover` | 2색 tint |

3색은 셸 프레임 전용이고 본문 컴포넌트에 쓰지 않는다. 본문에서 그라데이션은 "선택했거나(brand) 정했거나(action) 지나가는 중(hover)"의 세 가지 뜻만 갖는다.

> **2026-08-02 (D58·ADR-0028) 허용 범위 개정 — 형태로 가른다.** ① `--gradient-brand` 사용처에 **세컨더리 버튼 1px 아웃라인**이 추가된다. 알약(pill)은 그라데이션이 기본(중요=`--gradient-action` 채움, 일반=`--gradient-brand` 아웃라인)이고, **카드(radius 12)는 현행 유지**(회색 기본 + 선택·활성만 그라데이션) — 알약과 카드는 형태가 달라 신호가 섞이지 않는다. ② **리스크 배너 테두리는 브랜드 그라데이션에서 `--risk` 단색 1.5px 로 바뀐다**(2026-07-26 변경 ① 재개정) — 위험 버튼과 같은 "빨강 테두리 = 위험" 축으로 모아 배너의 유일성을 지킨다. ③ **탭 활성 밑줄**(2px)이 `--ink` 단색에서 `--gradient-brand` 로 바뀐다(2026-08-03 Q). ④ 리스트 행 호버가 `--gradient-hover`(신규 tint 쌍)를 쓴다.

> **2026-08-06 (Q) 카드 구분선은 그라데이션에서 회색으로.** 카드 헤더/본문 구분선(WireCard, WireCardDetails 펼침 제목 밑, 당사자 카드 행 사이)은 `--gradient-brand` 1px 에서 **회색 `--line` 1px 풀블리드**(카드 아웃라인까지 가로지른다)로 바뀌었다. 그라데이션 3색은 구조선이 아니라 정보 표시(계열 뱃지, 컬러 글자) 몫이다. `--gradient-brand` 의 남은 사용처는 카드 테두리(선택·활성), 세컨더리 버튼 아웃라인, 탭 활성 밑줄, 인용 블록 세로선(`-v`)이다.

> 2026-07-26 변경 3건. ① `--gradient-risk`(리스크 색 테두리)는 **없어졌다** — 리스크 배너 테두리도 브랜드 그라데이션을 쓴다. ② 프라이머리 버튼 배경이 55% 반투명 조합에서 **불투명 로고 계열 두 색**(`#A8E3C6` → `#D1B3E3`)으로 바뀌었고, 아웃라인도 그라데이션이 아니라 **`--line-action`**(잉크 50% = `#3D344580`) 1px이다. ③ base 쌍과 deep 쌍을 구분해 쓴다 — 면 경계는 base, 체크박스처럼 작고 또렷해야 하는 곳은 deep.

**그라데이션 테두리는 `border-image`로 만들지 않는다** — 브라우저가 `border-image`를 쓰는 요소의 `border-radius`를 무시해서 모서리가 사각으로 렌더된다(시안의 리스크 배너가 radius 12를 지정하고도 각져 보인 원인). 대신 배경 2겹으로 만든다:

```css
.risk-banner{
  border: 1.5px solid transparent;
  border-radius: var(--radius-card);
  background:
    linear-gradient(var(--risk-tint-solid), var(--risk-tint-solid)) padding-box,
    var(--gradient-brand) border-box;
}
```

안쪽 채움에 `--risk-tint`(5% 반투명) 대신 `--risk-tint-solid`(`#F7EEF2`, 같은 색을 캔버스 위에 합성한 불투명값)를 쓰는 이유는, 반투명이면 아래 깔린 테두리 그라데이션이 배너 안쪽까지 비쳐 보이기 때문이다.

> 2026-08-02 이후 리스크 배너 자체는 `--risk` 단색 테두리로 바뀌어 이 기법이 필요 없어졌지만(위 D58 개정 참조), **배경 2겹 기법 자체는 그라데이션 테두리를 쓰는 모든 자리의 표준**으로 유효하다 — 선택·활성 카드, 세컨더리 버튼 아웃라인이 그 사용처다.

### 3-4. 간격 (관찰)

- **4의 배수만**: 4·8·12·16·20·24·32·40 (`--space-1` ~ `--space-10`).
- 광학 보정 하프스텝은 **2·6·10·14 넷뿐**이고 전부 토큰이 있다(2026-07-31): `--space-0-5`(배지 세로 패딩) · `--space-1-5`(접힘 줄·범례) · `--space-2-5`(배지·칩 좌우) · `--space-3-5`(버튼 sm 좌우). **이 넷 외의 4 미만 값은 위반이다.** 이름을 준 이유는 값을 새로 들이려는 것이 아니라, 허용된 하프스텝인지 눈대중 px인지 구분할 방법이 없던 상태를 없애기 위해서다.
- 위 어느 쪽에도 안 맞는 값은 **바로 앞줄에 `/* optical: 사유 */`를 달아야** 통과한다(`pnpm guard:tokens`). 현재 면제는 하나뿐이다 — 위기·안전 칸의 패딩 23.5 사방(구 손잡이 막대 간격 3 은 2026-08-05 2차에 햄버거 글리프가 사이드바 아이콘 원형 버튼으로 바뀌며 규칙째 사라졌다)(2026-08-09, 구 15/19 대체: 이 칸만 테두리가 1.5px이라 24를 그대로 두면 글자 시작선이 옆 카드보다 0.5px 밀린다. 1.5 + 23.5 = 25 로 다른 카드의 1 + 24 와 맞춘다. 값은 `--card-pad` 로 한 번만 적고 펼친 제목 줄의 풀블리드가 그것을 되읽는다).
- 본문 패딩 40 / 섹션 간 24 / 카드 사이 20 / 사이드바 패딩 24·항목 간격 4·블록 간격 32.
- **카드-섹션 여백 3단** (2026-08-05 ADR-0030 — 전면 카드화와 함께 간격을 세 값으로 닫는다. 이 밖의 카드 간격을 새로 만들지 않는다): ① 페이지 세로 스택(구획 카드·HERO·배너 사이) = **24**(`--section-gap`) ② 같은 목록 안 카드 사이(그리드·스택) = **20**(`--space-5`) ③ 행 카드 스택(접힌 회차 줄·당사자 행) = **12**(`--space-3`). **④ 카드 안 구획 제목과 그 내용 사이 = 본문 gap 하나(20, 2026-08-07 Q "여백을 맞춰야지 — 규칙이 없니")** — 한 카드에 구획이 여럿일 때 제목마다 다른 간격 경로(카드 제목 구분선 마진 vs 본문 gap)를 타지 않게, 구획 제목·필드 묶음을 전부 본문 직계 자식으로 두고 body gap 하나가 간격을 만든다.
- **가로선(구분선) 중심 대칭** (2026-08-07 Q 9차 "카드 아웃라인과 가로선을 중심으로 세로 정렬"): 카드 안 풀블리드 가로선의 세로 여백은 **그 카드의 세로 패딩과 같은 값**이다 — 각 구획이 아웃라인과 가로선 사이 정중앙에 선다. `.wire-card-divider` 기본 24(카드 패딩 24), 세로 패딩이 다른 카드는 `--divider-gap` 으로 제 패딩을 알린다(당사자 카드 20). HERO 의 행 gap(24)·펼친 접힘 카드 제목 밑 여백(24)·카드 안 행 구분선 12/12 도 같은 계약이다. 새 카드가 가로선 여백을 따로 정하지 않는다.

**카드 안 패딩은 4종뿐이다** (2026-07-31 정리, 2026-08-07 D61 개정 — 카드 컴포넌트(WireCard)가 24 사방으로 서 있던 실태를 계약으로 승격하고 본문 20/24 를 대체했다):

| 종류 | 값 | 쓰는 곳 |
| --- | --- | --- |
| 머리·행 | **16 / 24** | 카드 헤더, 리스트 행, 접힌 회차 줄 |
| 본문 카드 | **24 사방** | WireCard·HERO·모달·폼 카드·펼친 회차 본문·리스크 배너(2026-08-07 회차 본문과 배너를 20/24 에서 통일) |
| 낱개 행 카드 | **20 / 24** | 당사자 카드, 참여 사업 카드(2026-08-06 Q 조정값 유지) |
| ~~좁은 보조 패널~~ | ~~16 / 20~~ | **폐지(2026-08-09)** — 근거는 폭이었다: 레일이 200px이던 시절 좌우 24를 주면 안쪽 글 폭이 152로 떨어졌다. 상담 기록 작성 레일이 격자 4칸(1120 기준 약 360)으로 넓어지고 접힘 칸이 WireCardDetails로 옮겨 가면서 이 값을 쓰는 자리가 없어졌다. 셋을 쓰던 자리는 전부 '본문 카드 24 사방' |

**카드 안 선 규칙** (2026-08-07 D61): 가로 구분선은 **`.wire-card-divider` 하나**다(회색 `--line` 1px, 카드 아웃라인까지 풀블리드 — 구 `.participant-card-divider` 는 같은 선언의 복사본이라 삭제). 세로선은 `--line` 1px 에 콘텐츠 높이만큼만(헤더의 기관 | 사업 구분선). 카드 안에서 새 선 스타일을 만들지 않는다.

> **좌우는 24로 모은 것이 이 정리의 핵심이다.** 이전에는 사방 20 또는 사방 24가 섞여 있어 같은 페이지의 카드끼리 글자 시작선이 4px씩 어긋났다. 세로는 역할(머리 16 / 본문 20)이 정하고, 가로는 24 하나다 — **2026-08-09부터 예외가 없다**(폭을 이유로 남겨 두었던 '좁은 보조 패널'이 그 폭 조건과 함께 사라졌다).
- 컨트롤 높이: **핵심 버튼(프라이머리·세컨더리·위험)·내비 항목·입력칸 40px**. 배지·칩 **24px**. **조작 알약(neutral·ghost — 뒤로·시간순·달 이동·취소류)·sm 버튼 32px**(2026-08-06 Q "같은 글자 크기의 버튼은 같은 크기" — 구 '뒤로만 32' 를 조작 전체로 통일). 꺽쇠 크기는 **em 으로 글줄 폰트 크기를 따라간다**(2026-08-07 Q 8차 전역 계약. 같은 날 "등록하기 '>' 가 너무 크다"의 버튼 px 개별 보정을 일반화): 기본 `.wire-chevron` .625em(16px 글줄에서 10) · 핵심 버튼 .5333em(라벨 15 에서 8) · 조작 버튼 .5em(라벨 14 에서 7) · 카드 화살표(`.wire-card-arrow`·`.briefing-card-arrow`) .5625em(16px 에서 9). 획도 .125em 이다. 새 화면이 꺽쇠 크기를 px 로 다시 박지 않는다. 달 라벨은 이 줄의 값이라 크기 +1(15)·600 광학 예외(§2-1 형식). **동의 요약 줄('자세히 읽어보기'·'전문 보기')의 꺽쇠만 기준 글자를 `.7em` 으로 낮춘다**(2026-08-08 Q "꺽쇠 크기 더 줄이기", 구 '전문 보기도 .5625em' 을 대체한다): .5625em 은 16px 글줄에서 잡은 비율이라 14px 글줄에서는 글자에 비해 컸다. 크기를 px 로 다시 박는 대신 기준 글자를 줄여 상자·획·광학 보정이 한 비율로 함께 작아진다. **`.7em` 은 위 넷과 달리 꺽쇠 자신의 기준 글자를 줄이는 값이라 위 목록과 바로 견주면 안 된다.** 글줄 대비로 환산하면 `.5625 × .7 = .39375em` 으로 목록에서 가장 작다(14px 글줄에서 상자 7.88 이 5.5 가 된다).
- 아이콘: 내비·화살표 **16px**, 섹션·카드 헤더 **18px**. 단색 라인 아이콘만.

## 4. 레이아웃 (신규 결정 — D37/ADR-0015, 2026-07-27)

이 절의 숫자는 **시안에서 잰 값이 아니라 1440 캔버스에서 역산해 정한 값**이다. 구 "전체 1280px"은 그날 시안 캔버스가 1280이었다는 사실이 규칙이 된 것이라 폐기했다. 전체 스펙·실측·구현 함정: `docs/superpowers/specs/2026-07-27-layout-contract-design.md`.

### 4-1. 가로 프레임

| 층 | 값 |
| --- | --- |
| 기준 캔버스 | **1440** (시안도 이 폭에 그린다) |
| **상단 헤더** | **56**(`--header-height`) · **화면 전체 폭**(셸 그리드 1행 — 사이드바 위까지) · `position:sticky` + `--z-sticky` (2026-08-05 Q 신설, Infisical 레퍼런스 — 구 락 8 '헤더 띠 금지' 대체. "z-index 최상위로 확장"). 배경은 **캔버스 그대로**(면 배경 없음)이고 아래와는 **하단 1px `--gradient-frame` 라인으로만** 갈린다(같은 날 Q 지시 연쇄 — 그라데이션 면 → 라인 → 3색 그라데이션 라인). 좌우 패딩 24 라 **기관 마크가 사이드바 메뉴 아이콘과 같은 좌측선(24)에 선다**('다가오는 일정' 정렬 — Q 지시 ②). 내용 = **기관 선택창**(마크 + 기관명 16/600 + 상하 꺽쇠 — 2026-08-05 Q 2차, 구 '기관명=홈 링크' 형태 대체. 크기는 2026-08-06 Q ⑤ 로 사업명과 같은 16, 구 18 대체: Infisical·OpenAI 처럼 여러 기관 전제. D50 홈 배선은 목록 안으로 — 기관을 고르면 그 기관 홈 `/`) · 1px 세로 구분선 · **사업명 텍스트 + 상하 꺽쇠**(상자·'사업' 라벨 없음 — Q 지시 ③, 꺽쇠는 2026-08-05 2차에 아래 단방향 → 상하로) · 우측 계정 행동 3개(설정·테마·로그아웃, 32px 원형 아이콘 버튼). **768 미만에는 없다** — 모바일 바(§4-4)가 담는다 |
| 사이드바 | **280** (`--sidebar-width`, 2026-08-02 D58 개정 — 구 240) · **캔버스 배경 + 오른쪽 1px `--gradient-frame-v` 라인**(2026-08-05 Q — 구 `--gradient-sidebar` 면 배경 폐지: "모두 라인으로 영역 구분") · 화면 **왼쪽 끝 고정** · 헤더 아래 1행(`top:56; height:calc(100dvh - 56px)`) · **뷰포트 고정**(본문이 스크롤해도 제자리, 메뉴가 넘치면 사이드바 안에서만 스크롤) · 데스크톱 내용은 **메뉴뿐**(기관·사업·하단 묶음은 헤더로 이동, 드로어에서만 전부 산다) · 위 패딩 20 (첫 메뉴 항목 윗변 = '뒤로' 알약 윗변 = 76) · **메뉴 항목 높이 = '뒤로' 알약과 같은 32**(2026-08-06 Q — 구 40. 윗변 정렬과 짝: 높이까지 같아야 한 리듬으로 읽힌다) |
| 페이지 컨테이너 | **max-width 1280**(`--page-max`, 2026-08-09 Q — 구 1120, D37 부분 개정) · `margin-inline:auto` |
| 컨테이너 패딩 | **좌우 40** (768 미만 세로 24 · 가로 16) |
| **글 폭** | **최대 1200** — max-width 가 닿는 광폭 기준. 1440 뷰포트에서는 1080 |

**2026-08-23 CCC-82 결정: 컨테이너 좌우 패딩은 40을 유지한다.** Material 3의 24px 안은 채택하지 않았다. 페이지 장폭을 1280으로 올린 뒤에도 40px은 본문 시작선을 안정적으로 잡고, 최대 글 폭 1200을 만드는 계약의 일부다. 24px로 줄이면 같은 화면에서 본문만 32px 더 넓어져 장폭 개정과 별개의 폭 변경이 된다.

**`box-sizing:border-box`이므로 1280에 패딩 40이 포함된다.** **2026-08-09 Q 장폭 개정(1120 → 1280)**: 넓은 모니터에서 공간이 남아 장폭을 전체적으로 올렸다. 1440 에서는 사이드바 밖 본문 영역이 1160 이라 max-width 가 안 닿고 컨테이너가 1160 을 채우며(글 폭 1080), **1560 이상에서 1280 으로 고정**되고 잔여가 좌우로 남는다. 늘어난 폭은 상담 기록 작성의 우측 바로가기 목차 같은 **광폭 전용 셋째 열**의 자리다(컨테이너 질의 ≥1150 에서만 선다 — §4-2 와 같은 원리로 화면 폭이 아니라 컨테이너 폭으로 판단). 가운데 정렬은 2026-08-05 셸 개편에서도 유지가 확정됐다(Q 선택 — 사이드바 붙임안 기각).

허용 폭은 **셋뿐이다** — **1280**(페이지 본문, 2026-08-09 개정) · **520**(모달·폼, §5) · **420**(단일 검색칸). **`.narrow`(960)는 2026-08-05 폐지** — Q "특별한 이유가 없으면 장폭은 가장 넓은 페이지에 맞춰 고정". 폼·읽기 화면도 1280 하나를 쓰고, 읽기 폭이 필요한 폼은 페이지가 아니라 폼 자신이 좁힌다. 새 폭 도입은 ADR 수정 사항이다.

### 4-2. 카드 그리드, 열로 설명하고 숫자로 고정하지 않는다

**새 화면의 공간을 설명할 때는 폭 숫자보다 열 수를 먼저 말한다.** 좁으면 1열, 넓으면 2열, 아주 넓으면 3열이다. 이 어휘는 화면을 계획하고 검수할 때 무엇이 나란히 서는지를 말하는 기준이다. 새 브레이크포인트를 만드는 규칙이 아니며, 실제 접힘은 아래 최소 폭과 컨테이너 질의가 결정한다.

`repeat(auto-fit, minmax(min(<최소폭>,100%), 1fr))` · gap 20. **`repeat(<숫자>, …)`로 열을 직접 지정하지 않는다** — 최소 폭이 열 수를 만든다.

| 그리드 | 최소 폭 | 1440에서 | 접히는 지점 |
| --- | --- | --- | --- |
| 표준 (아코디언·정보 카드) | **420**(`--grid-min`) | 2열 · 각 510 | 1열 = 화면 1180 미만 |
| 조밀 (GAS 게이지·정보 필드) | **280**(`--grid-min-dense`) | 3열 · 각 333 | 1열 = 화면 1200 미만 |

- **조밀 그리드는 3열 아니면 1열이다.** 2열이면 3개짜리 묶음(D33 세부 목표)이 둘 + 외톨이 하나로 앉는다. `@container (max-width:879px)`로 접는다.
- 최소 폭은 취향이 아니라 위 열 수(표준 2 / 조밀 3)에서 역산한 결과다. **열 수를 바꾸려면 본문 폭을 고치지 최소 폭을 건드리지 않는다.**
- 넓은 카드는 `grid-column: span 2`, 해제는 **컨테이너 질의**(`@container (max-width:859px)`)로 한다. 화면 폭으로 풀면 1열인데도 span이 살아남아 열 수를 2로 되끌어올린다.
- 아코디언이 나란할 때: **펼친 카드 `align-self:stretch`**(서로 높이가 같아짐) · **접은 카드 `align-self:start`**(요약 줄만 남음). 접으면 행이 실제로 줄어든다.

### 4-3. 세로 리듬

**40 / 24 / 16 / 20** — 페이지 헤더 아래 40 · 섹션 사이 24 · 섹션 제목↔내용 16 · 카드 gap 20. 섹션 제목은 **카드 밖 h2 18/600**이고, 카드 안 14/600 민트 deep 라벨은 필드 이름 전용이다.

### 4-4. 브레이크포인트 — `max-width: 767px` 하나

앱 안 5벌(1120·900·768·640·359)을 하나로 수렴시킨다. **768px은 데스크톱이다.** 열 수는 최소 폭이 접으므로 화면 폭 분기는 셸이 바뀌는 지점 하나면 된다.

768 미만: 사이드바 → **드로어로 숨김**(상단 56px 모바일 바 + 폭 280 패널 + 스크림). 가로 줄로 눕히면 기관명·사업 전환기·메뉴가 한 줄에 밀려 아무것도 안 읽힌다. **상단 헤더(§4-1)는 데스크톱 전용이다** — 768 미만에서는 **모바일 바가 곧 헤더**다: **좌측 = 기관·사업 선택창**(헤더와 같은 상자 없는 텍스트 + 상하 꺽쇠), **우측 = 원형(32) 사이드바 버튼**(2026-08-05 Q 2차 — 같은 날 ④ '메뉴 버튼만·좌측 햄버거' 대체: 여러 기관·사업을 고르는 흐름이라 바에서도 기관·사업이 보이고 골라져야 한다). 바 선택창의 목록은 상자가 아니라 **바 전폭 시트**로 떨어진다(좌우 16 — 어느 쪽 닻이든 좁은 화면에서는 이름이 잘린다). 바 좌측 패딩 16 = 본문 컨테이너 패딩과 같은 좌측선(기관 마크 · '뒤로' 알약 · 페이지 제목이 한 줄 — Q 2차 ⑥). 경계는 데스크톱 헤더와 같은 `--gradient-frame` 1px 라인. **드로어는 오른쪽에서 나온다**(2026-08-06 Q ① — 여는 버튼이 바 오른쪽 끝이라 패널도 같은 쪽. 구 왼쪽 대체. 본문과 만나는 왼쪽 모서리에 `--gradient-frame-v` 1px). **드로어 내용 = 계정 행동 상단 줄 + 메뉴뿐이다**(2026-08-06 Q ② — 기관명·사업 전환기 블록 제거, 그 맥락은 바가 전담. 구 '전부 담는다' 대체): 상단 줄 = **드로어 버튼(좌) + 설정·테마·로그아웃(우)**(2026-08-06 2차 — 구 '계정 행동 좌 + 닫기 X 우' 반전. 닫기는 X 가 아니라 여는 버튼과 같은 사이드바 아이콘·같은 32 원형 — 한 버튼이 여닫는 토글로 읽힌다), 높이 56 — **아래 1px 구분선(y 56)이 본문 바의 프레임 라인과 같은 높이**다(2026-08-05, 선의 주인은 머리 줄). **드로어 안 모든 아이템은 좌 24 · 우 24 한 줄에 선다**(2026-08-06 Q ③ — 드로어 버튼·활성 메뉴 상자 좌변, 계정 행동 묶음·메뉴 상자 우변. 메뉴 알약의 -12 좌측 보정은 데스크톱 전용으로 남는다). 여닫음 전환은 **모션 토큰**(`--motion-base` 200ms · `--ease-standard`)을 쓰고, **스크림은 늘 마운트된 채 같은 리듬으로 페이드**한다(2026-08-06 Q "부자연" — 조건 마운트면 닫는 순간 어둠이 뚝 끊긴다. 닫힘 상태는 투명 + 클릭 통과). 셸 크롬은 OS 탭 하이라이트를 끈다(§6 밖 어휘 — 둥근 파란 플래시가 그림자 잔상으로 읽힌다).

### 4-5. 셸 내부

- **화면의 모든 글자는 카드 안에 있다.** HERO도 카드다. 예외는 카드 여러 장을 묶는 섹션 제목뿐.
- 페이지 헤더(HERO): 좌측 이름 묶음 + 우측 **행동 버튼 최대 2개**(세컨더리 → 프라이머리). 축은 **사이드바 = 장소 / 우상단 = 행동**(D35). **예외 1건(2026-08-06 Q): 브리핑 HERO 는 3개다** — '전체 상담 기록'이 맨 아래 링크에서 행동 줄(당사자 정보 옆)로 올라왔다. 프라이머리는 여전히 오른쪽 끝 1개다. **같은 날 후속: 당사자 HERO 는 2행 골격이다** — 1행 = 이름·태그(좌) + 버튼(우), 회색 `--line` 풀블리드 구분선 아래 2행 = 정보 행(가명 ID·연락처·메타 — 2026-08-07 위계 개편으로 연락처·ID 도 이 행이다, §5 ParticipantHeroCard). 정보 행에 넣을 것이 없으면 1행만 남는다. 좁아지면 1행이 접혀 버튼 묶음이 이름 아래로 내려가고, 767 미만은 버튼 세로 스택 + 메타 조각 세로 배열(세로선 해제)이다.
- **셸 프레임은 두 층이다**(2026-08-05 Q · Infisical 레퍼런스): **헤더 = 맥락(기관 · 사업 — 둘 다 선택창, 2026-08-05 2차) + 계정 행동(설정·테마·로그아웃)** / **사이드바 = 장소(메뉴)**. 계정 행동 3개는 32px 원형 아이콘 버튼(드로어 닫기 X 와 같은 세컨더리 옷 — 그라데이션 1px 테두리 + `--panel` 채움)이고 라벨은 aria-label + title 이 갖는다. 설정이 현재 화면이면 블루 tint 채움 + 블루 아이콘(내비 활성과 같은 신호).
- **기관·사업 전환기는 헤더에서 상자 없는 텍스트 + 상하 꺽쇠다**(2026-08-05 Q ③ — '사업' 라벨도 화면에서 뺀다. 접근 이름("기관 <기관명>"·"사업 <사업명>")은 aria-labelledby 로 유지. 꺽쇠는 2026-08-05 2차에 아래 단방향 → **상하**로: 단방향은 '펼침', 상하는 '고르는 값'). 기관명·사업명 모두 16/600(2026-08-06 Q ⑤ — 구 기관명 18 대체. 현재 워크스페이스 = 셸 강조 굵기). 동작 계약은 그대로다 — **1개여도 선택창**(2026-08-03 Q, 기관도 동일 원칙), 드롭다운 폭 상한 `min(60vw, 480px)`(데스크톱, 바에서는 §4-4 전폭 시트). 빈 패딩 마스크 트릭(이름 뒤 28 빈 구간)은 **헤더 전용**이다 — 바에서는 이름이 꺽쇠 앞 28px 에서 먼저 사라진다(2026-08-06 Q ④ '겹침'). **드로어(768 미만)에는 전환기가 없다**(2026-08-06 Q ② — 기관·사업 맥락은 모바일 바가 전담. 구 '드로어는 구 사이드바 구성 그대로' 대체).
- **하단 묶음(설정·테마·로그아웃)의 자리는 헤더 우측이다**(2026-08-05 — D58 ⑤ '사이드바 발치 상시 노출'의 자리 부분 대체. 상시 노출 원칙은 유지된다: 헤더가 sticky 라 본문 스크롤과 무관하게 항상 보인다). 드로어에서는 같은 묶음(32px 원형 아이콘 버튼, 2026-08-05 Q 2차 "웹화면과 통일")이 **상단 줄 우측**에 산다 — 좌측은 드로어 버튼(닫기, 2026-08-06 2차 반전 — 구 발치 자리·'좌측 버튼 3개+우측 X' 대체).
- **'뒤로' 알약은 본문 컨테이너와 같은 좌측선에 선다**(2026-08-06 Q "뒤로, 제목, 시간순, 카드까지 좌측정렬" — 구 2026-08-05 '사이드바 안쪽선(24) 고정·프레임 크롬이라 제목과 일부러 안 맞춘다'를 대체). 컨테이너 기하(장폭 1120 + auto 마진 + 좌우 패딩)를 그대로 받아 알약·페이지 제목·툴바·카드의 왼쪽 끝이 한 선이다. 옷은 **일반(neutral) 그레이 아웃라인**이고(§5 버튼 5종, 구 그라데이션 테두리 대체. 테두리는 2026-08-07 부터 카드와 같은 `--line`) 화살표와 글자 사이는 6 이다(구 8, 같은 날 Q "조금 좁혀"). 세로: 헤더 아래 20 + 알약 32, 첫 메뉴 항목 윗변과 같은 높이(76). **768 미만은 좌측 16** — 모바일 바 내용·본문 컨테이너 패딩과 같은 좌측선에 선다(2026-08-05 Q 2차 ⑥ "메뉴 - 뒤로 - 상담 일정 좌측 정렬").
- z-index 4층: 본문 0 · 스티키 10(`--z-sticky`) · 드롭다운 30(`--z-dropdown`) · 스크림·모달 100(`--z-modal`). **숫자를 직접 쓰지 않는다** — 층은 이름으로만 부른다. 드로어 스크림만 `calc(var(--z-modal) - 1)`인데, 이는 5번째 층이 아니라 "모달 바로 아래"를 식으로 표현한 것이다(드로어 패널이 `--z-modal`에 서고 스크림이 그 뒤에 깔린다).

### 4-6. 구현 규칙 — main 의 2026-07-26 셸 작업에서 흡수 (디자인 결정이 아니라 하드하게 얻은 사실)

D37 이 값을 정하기 전에 main 에서 셸을 세우며 확인된 것들이다. **값은 D37 이 이기지만, 아래 네 줄은 값과 무관하게 계속 유효하다.**

1. **폭을 정하는 클래스는 `.page-content` 와 `.narrow` 둘뿐이다.** 화면에서 `max-width` 를 새로 쓰지 않는다(§7 락 9).
2. **`.page-content` 에는 `width:100%` 가 반드시 있어야 한다.** `.app-shell` 의 그리드 아이템이고 auto 마진을 갖는데, 폭을 명시하지 않으면 박스가 트랙을 채우지 않고 **내용 크기로 줄어든다** — 2026-07-26 이전에 같은 클래스인데도 화면마다 컬럼이 429~1200px 로 달랐던 원인이 이것 하나였다.
3. **섹션 사이 여백은 페이지 그리드의 `gap` 이 준다.** 화면에서 `margin-bottom` 으로 섹션을 띄우지 않는다(이중이 된다).
4. **`auto-fill` 은 금지다.** 빈 트랙을 남겨 카드가 1장일 때도 절반만 차지한다. `auto-fit` 만 쓴다.

> **2026-08-05 셸 개편 실측**: 상단 헤더 신설 + `.narrow` 폐지 반영본은 `artifacts/layout-measure/d60-shell/` — 8화면 × 5폭 = 40건 위반 0. `layout-measure.py` 는 헤더 높이(데스크톱 56 · 767 미만 0)를 단언 항목에 추가했고 narrow 분기(960 상한)를 걷었다.
>
> **반영 완료 (2026-07-27)**: `design/tokens.css` 와 앱 화면이 위 값으로 돌아간다. 실측은 `scripts/design/layout-measure.py` 가 **실제 앱 화면**을 1440·1280·1024·767·390 다섯 폭에서 재고 계약과 다른 숫자를 보고한다 — 화면 8개(기본 폭 6 + `.narrow` 2) × 폭 5개 = **40건 위반 0**(증거 `artifacts/layout-measure/d37/`). 이 스크립트는 폭·패딩·간격뿐 아니라 **§4-2 표의 열 수(1440 에서 표준 2 · 조밀 3)도 단언한다** — 값만 보면 그리드가 통째로 1열로 주저앉아도 통과하기 때문이다. 시안 검증 시트(`artifacts/layout-frame-v1/sheet.html`)와 역할이 다르다 — 시트는 **시안이** 계약과 맞는지, 이 스크립트는 **코드가** 계약을 지키는지 본다.
>
> 반영하며 계약이 답을 주지 않아 정한 것 2건. ① **레일이 붙은 화면(상담 기록)의 접힘은 컨테이너 질의로 한다** — 브레이크포인트를 767 하나로 모으면 사이드바 240 이 살아 있는 768~900 구간에서 본문이 228px 로 짓눌린다(2026-07-26 에 900 으로 잡아 뒀던 바로 그 결함). `@container (max-width:643px)` = 본문 최소 420 + gap 24 + 레일 200. ② **`.page-content` 에 `container-type:inline-size`** 를 둬서 열 수·span 해제의 기준을 화면이 아니라 컨테이너로 옮겼다 — 화면 폭으로 재면 사이드바 유무에 따라 같은 폭에서 다른 결과가 나온다.
>
> **화면 구성이 계약을 따라 하나 바뀌었다 (2026-07-27 Q 결정)**: **GAS 게이지가 아코디언 밖 전폭 섹션이 됐다.** 아코디언 안(폭 510)에서는 조밀 그리드 최소 280 으로 3열이 물리적으로 안 나오고 계약이 2열을 금지하므로(세부 목표 3개가 둘 + 외톨이 하나로 앉는 것을 막는 규칙) 게이지가 세로로 쌓였다. `CLAUDE.md` 6장 원문이 이미 "GAS 는 HERO 아래 가로 배치"라 전폭(1040)으로 올렸고 3열 각 333 이 나온다. 접히지 않는 것도 의도다 — `CLAUDE.md` 6장의 아코디언 4종은 브리핑·질문·액션·개인정보이고 게이지는 '5분 전에 훑는' 대상이다. 브리핑 아코디언은 6개에서 **5개**가 됐다.
>
> **768 미만 드로어도 구현됐다 (2026-07-27)**: 손잡이 바 56px + 패널 280(max 82vw) + 스크림. **별도 모바일 내비를 두지 않고 같은 사이드바가 변한다** — 구 `.mobile-navigation` 은 메뉴만 렌더해 좁은 화면에서 기관·사업 전환기가 통째로 빠져 있었다. 닫는 길 3개(스크림·**상단 X**·Esc — 2026-08-04 Q, 구 하단 '메뉴 닫기' 텍스트 버튼을 기관명 오른쪽 X 아이콘으로 대체), 이동 시 자동 닫힘, 열린 동안 본문 스크롤 잠금, 초점 이동·복귀. 실측은 `scripts/design/drawer-check.py` 15항목(2026-08-06 — 오른쪽 슬라이드·좌우 시작선 24 단언 반영).


## 5. 핵심 컴포넌트 계약

### 관찰된 것

- **프라이머리 버튼**: `--gradient-action` 배경 + **`--line-action` 1px 아웃라인**(잉크 50%), radius pill, 높이 40, 패딩 0 16, 라벨 16/600 `--ink`. 배경이 불투명 파스텔이라 무채색 보더로도 경계가 서고, 잉크 계열 아웃라인이 라벨과 같은 축이라 버튼이 한 덩어리로 읽힌다. **그림자는 2026-08-06 에 뺐다**(D60 후속 종결, §3-2).
- **카드/패널**: **아웃라인 카드**(2026-08-05 ADR-0030 — 구 `--shadow-soft` 폐지) — 흰 배경 · radius 12 · **비선택 = 회색 `--line` 1px / 선택·활성 = `--gradient-brand` 1px**. 헤더(패딩 16/24: 아이콘 18 + 제목 18/600 + 우측 배지/화살표)와 본문(패딩 20/24)은 **회색 `--line` 1px 풀블리드 선**으로 나눈다(2026-08-06 Q, 구 `--gradient-brand` 1px 대체. 카드 아웃라인까지 가로지른다, §3-3).
  > **2026-08-05 (ADR-0030) — 본문 구획은 전부 카드 컴포넌트다.** D59 의 "읽기 구획은 플랫" 규칙을 대체한다(Q: "그라데이션 라인과 그레이 라인으로 모두 카드 div 컴포넌트로 — 컴포넌트화를 안 해서 여백 관리가 어려움"). 브리핑 3영역·미해결 액션·전체 목표 줄·설정 구획·전체 일정 날짜 묶음이 전부 **WireCard/WireCardDetails**(아웃라인 카드)로 돌아온다 — 답답함의 원인이던 그림자는 뺐으므로 상자 나열로 읽히지 않는다. 손으로 카드 div 를 그리지 않는다 — 여백은 컴포넌트와 여백 3단(§3-4)이 정한다. **카드 안에 카드를 두지 않는다**는 유지 — 카드 안 하위 묶음(동의 fieldset 등)은 상자 대신 위 `--line` 1px 구분선 하나로 가른다(D59 ③ 유지. ② 가명 ID 폐지는 2026-08-06 부분 재개정으로 당사자 카드 정보 칸에는 표시한다, §5 '이름 표기'). 상세 ADR-0029(구)·ADR-0030(현).
  > **테두리에 그라데이션을 두르지 않는다.** pen 의 `C · 카드`·`C · 리스트 행`·`C · 빈 상태`·`C · 모달`은 **stroke 가 아예 없고** 이중 파스텔 그림자만으로 뜬다(2026-07-26 실측). 그라데이션 stroke 를 가진 컴포넌트는 **`C · 리스크 배너` 하나뿐**이다. 모든 카드에 두르면 두 가지가 동시에 망가진다 — 화면이 촌스러워지고(Q 지적), **리스크 배너가 다른 카드와 구별되지 않아 D9 가 깨진다.** 캔버스(#FAFAF9) 위 흰 카드는 그림자만으로 약해 회색 1px 을 기본으로 두되, **선택·활성일 때만** 그라데이션 테두리로 바꾼다(`[data-selected="true"]`·`[aria-current="true"]`).
- **호버·선택 테마 규칙** (2026-08-05 ADR-0030): ① 카드·행의 **면 호버** = 자기 테마의 tint 쌍 `--gradient-hover`(다크 값은 tokens.css). ② **선택** = 두 테마 공통 — 카드는 `--gradient-brand` 아웃라인, 행·컨트롤은 `--gradient-action` 면 + `--on-action` 글자(채움이 두 테마에서 같은 밝은 파스텔이라 글자도 늘 어두운 잉크). ③ **반전 호버**(내비류 강조 호버) = 라이트 **어두운 면(`--ink`) + 그라데이션 글자** ↔ 다크 **그라데이션 면(`--gradient-brand`) + `--on-action` 글자** — 테마가 바뀌면 면과 글자가 서로 자리를 바꾼다(Q: "컬러 반전을 잘해야 함").
- **리스크 배너** (D9): 확인된 플래그 있을 때만, HERO 바로 아래, 접힘 불가. 배경 `--risk-tint-solid`, **`--risk` 단색 1.5px 균일 테두리**(2026-08-02 D58 — 구 `--gradient-brand` 1.5px 을 대체. 세컨더리 버튼이 브랜드 그라데이션 아웃라인을 갖게 되어, 배너는 위험 버튼과 같은 "빨강 테두리 = 위험" 축으로 옮겼다), radius 12, 패딩 20/24. 헤더는 18px 경고 아이콘 + 16/600 `--risk` 제목. 항목은 흰 배경 행(radius 6 · `--line` 1px — 2026-08-05 그림자 대체)에 체크박스 + 16/600 `--ink` 텍스트.
  > 테두리는 다른 카드와 같은 브랜드 그라데이션이고, **리스크 색은 배너 안에서 경고 아이콘과 제목 두 곳에만 쓴다**(D9). 즉 배너를 눈에 띄게 만드는 것은 테두리가 아니라 위치(HERO 바로 아래)와 배경 틴트와 아이콘이다. 다만 §9에 적은 대로 현재 `--risk`의 대비가 2.72라 **제목만으로 경고를 전달하지 않고 아이콘을 반드시 함께 둔다.** 액센트 띠 금지는 유효하다 — 이 테두리는 네 변이 균일하다.
- **상태 배지·칩**: 높이 24 · 패딩 0 10 · 라벨 14/600 `--ink`. **화면 전체에서 `.wire-badge`(WireBadge 컴포넌트) 하나가 유일한 배지 계약이다**(2026-08-07 D61 통합 — 같은 레시피를 복사하던 `.status` 계열, `.briefing-badge`, `.record-kind`, `.record-owner`, `.record-ai-source`, `.settings-user-status`, `.onboarding-step`, `.consent-upload-slot-state` 8종을 전부 대체했다. 배지를 새로 그리지 않고 tone 만 고른다). 톤 5종: **neutral**(배경 없음, `--sub` 1px 테두리) / **blue**(일정·유형·정보) / **mint**(진행·상태·담당) / **lavender**(AI·승인 대기·주의) / **risk**(확인된 리스크·오류 상태 전용, D9 허용 자리). 계열 톤은 tint 배경 + 계열 deep 흐린 외곽선 1px + `--ink` 글자(2026-08-05 재규정). **HERO 상태 태그**도 `.wire-status-tag` 하나를 쓴다(2026-08-07 `.participant-hero-stage` 복사본 삭제. 형태는 2026-08-06 Q 재개정대로 알약 400 — 배지와 같은 레시피, 블루 tint + 블루 deep 외곽선 + `--ink` 글자). **2026-08-19 D61 ② 개정(CCC-106)**: 상태 태그는 배지와 같은 방식으로 색 계열을 고를 수 있다 — `ParticipantHeroCard` 의 `stageTagTone`(기본 blue) 이 `.wire-status-tag` 의 `data-tone` 을 정한다. 기본값이 blue 라 태그를 넘기지 않으면 기존 화면은 변화가 없다. AI 산출·승인 대기 계열 낱말(D58 ④)에만 라벤더를 연다 — AI 초안 검토 화면의 검토 대기·승인됨·반려됨 3종이 첫 적용 자리다(`.wire-status-tag[data-tone="lavender"]`, 라벤더 tint + 라벤더 deep 외곽선 + `--ink` 글자, 배지 lavender 톤과 같은 레시피). 톤 5종 전부를 미리 열지 않고 쓰는 것만 연다.
- **GAS 게이지**: 96px 원형. 트랙은 해당 목표 계열의 tint, 채움은 같은 계열의 base, 중앙 점수 24/600(`--ink`, 점수 없으면 `--sub`). 아래 목표명 16/600, 그 아래 미니 추이 — 16×N px 막대(높이 12/18/24로 단계 표현, radius 2), 과거는 `--track`, **최신 1개만** 계열 base.
- **액션 아이템 행**: 18px 체크박스 + 제목 16/600 + 기한 14/600 블루 deep, 행 간격 16.
- **정보 필드**: 라벨 14/600 민트 deep(위) + 값 16/600 또는 16/400 `--ink`(아래), 간격 4.
- **불릿 목록**: 6px 원형 `--sub` 불릿 + 16/400 텍스트, gap 8. **불릿은 항목이 2개 이상일 때만이다**(2026-08-07 Q 규칙 신설 — 한 항목뿐이면 목록이 아니라 문장이므로 불릿 없는 본문 한 줄로 그린다. WireBullets 부품이 가르므로 이 부품을 쓰는 모든 카드에 자동 적용된다).

### pen `컴포넌트 원본` 페이지에서 확정된 것 (2026-07-26)

브리핑 화면에 없어 규칙이 없던 조각을 상담 일정·당사자 등록·검토·승인 3화면 시안에서 정한 뒤, pen 문서의 **`컴포넌트 원본` 페이지에 재사용 컴포넌트로 안착**시켰다. 이제 이 표는 제안이 아니라 확정 계약이고, **컴포넌트를 고치면 화면 전체에 전파된다.** 원본 위치는 pen `컴포넌트 원본` 페이지 하나뿐이며, `컴포넌트` 페이지는 인스턴스만 모은 보기용 사본이다.
초기 시안 참고: `artifacts/design-system-v2/screens/` (index.html이 목록).

| 조각 | 계약 | 쓰인 화면 |
| --- | --- | --- |
| **입력칸** | 높이 40 · radius 6 · `1px var(--line-control)` · 배경 `--panel` · 글자 16/400. 라벨은 **항상 위** 14/600 `--sub`, 자리표시자가 라벨을 대신하지 않는다. 필수는 라벨 옆 `--risk` 별표. **select는 네이티브 화살표를 끄고 꺽쇠를 직접 그린다**(우측 12px 여백 + `padding-right:36px` — 네이티브는 테두리에 붙어 다른 입력칸과 안 맞는다) | 당사자 등록 |
| **입력 오류** | 테두리 `1.5px var(--risk)` + 아래 14/600 `--risk` 메시지. 색만으로 알리지 않도록 메시지를 반드시 함께 둔다 | 당사자 등록 |
| **포커스** | `outline: 2px solid var(--blue-deep)` + `outline-offset: 2px`. 모든 조작 요소 공통, `:focus-visible`만. **상자형 입력칸은 링이 상자 하나뿐이다** — 내부 컨트롤의 전역 링을 끈다(이중 링, 2026-08-03). overflow:clip 컨테이너 안(회차 summary)은 안쪽 링(-2px) | 전 화면 |
| **버튼 5종** (구 4종) | **radius 6 직사각형**(`--radius-control`, 2026-08-07 D61 — 구 radius pill 대체. 입력칸·카드와 같은 사각 어휘) · 기본 높이 40 · 패딩 0 16 · 라벨 16/600 · **내용은 가운데 정렬이 기본**(`justify-content:center`, 2026-08-02 — 구 왼쪽 정렬 기본은 버그였다. 왼쪽·양끝 정렬은 명시 옵션으로만). 프라이머리(`--gradient-action` 배경 + `--line-action` 1px. 그림자는 2026-08-06 폐지, §3-2) / 세컨더리(흰 배경 + **`--gradient-brand` 1px 아웃라인**(배경 2겹 방식 §3-3, 2026-08-02 D58 — 구 `--line-action` 1px 을 대체) + `--ink` 글자) / **일반(neutral)**(흰 배경 + **카드 아웃라인과 같은 `--line` 1px**(2026-08-07 Q "뒤로·시간순·달 이동 아웃라인을 카드 div 아웃라인과 맞출 것" — 구 `--line-action`(잉크 50%) 대체) + `--ink` 글자 — **2026-08-06 Q 위계 재편으로 신설**: 뒤로, 시간순, 달 이동 같은 이동·보기 조작이 쓴다. 컬러는 중요 행동의 어휘다: 세컨더리 = 컬러 아웃라인, 프라이머리 = 그라데이션 면. D58 ① "알약 일반 = 브랜드 아웃라인" 부분 개정) / 고스트(배경·테두리 없음, `--sub` 글자) / **위험**(흰 배경 + `1.5px var(--risk)` + `--risk` 글자 — 되돌리기 어려운 행동에만). **일반과 강조의 구분은 테두리가 아니라 면과 깊이다**(2026-07-31): 세컨더리 테두리를 `--line-control`(흰 위 1.28)에서 프라이머리와 **같은** `--line-action`(잉크 50%)으로 올렸다 — 1.28짜리 테두리는 흰 카드 위에서 사실상 안 보여 버튼이 버튼으로 읽히지 않았고, 입력칸과 달리 버튼에는 라벨이 형태를 알려 주는 장치(§9 완화)가 없다. 이제 둘의 테두리는 같고 프라이머리만 그라데이션 면을 갖는다(그림자는 2026-08-06 폐지). *(2026-08-02 D58 부분 개정: 세컨더리 테두리가 다시 `--gradient-brand` 아웃라인으로 바뀌었다 — 구분의 1차 축이 면·깊이라는 원칙은 유지되고, "두 버튼의 테두리가 같은 세기"라는 계약이 "두 버튼 모두 브랜드 축의 테두리"로 넓어진 것이다.)* **상태**: 호버 = 잉크 6% 워시(프라이머리는 `brightness(.96)`, 위험은 `--risk-tint-solid`), 누름 = `translateY(1px)`, 비활성 = `--muted` 면. 호버는 `@media (hover:hover)` 안에 둔다(터치 기기에서 탭한 버튼에 호버가 남아 눌린 채 굳은 것처럼 보인다). **줄높이는 `--leading-none`(1)** — 본문 1.55 를 상속하면 줄 상자가 24.8px 처럼 분수가 되어 브라우저·배율에 따라 상하 반올림이 어긋난다(2026-08-03). 상하 정렬은 flex `align-items:center` + 정수 줄 상자가 강제한다. **라벨은 줄바꿈하지 않는다**(`white-space:nowrap`) — 한글은 어디서나 끊길 수 있어 칸이 좁아지면 `당사자 정 / 보` 처럼 낱글자로 쪼개진다(2026-07-30 390px 실측). 버튼은 한 번에 읽히는 한 덩어리라 넘칠지언정 쪼개지지 않는 쪽이 옳고, 좁은 화면의 자리는 위 '모바일' 행의 세로 배치가 만든다 | 전 화면 |
| **버튼 크기 2단** | **종류와 크기는 별개 축이다**(2026-07-26 Q 결정). 크기는 **높이·패딩·라벨만** 바꾸고 색·테두리·라운드 규칙은 두 크기가 완전히 같다. md = 높이 40(`--control-height`) · 패딩 0 16 · 16/600 / **sm = 높이 32(`--pill-height`) · 패딩 0 14 · 14/600**. 32px을 별도 컴포넌트로 만들지 않는 이유는 버튼 규칙이 4종에서 8종으로 늘어나는데 실제로 다른 것은 크기뿐이기 때문이다. 코드에서는 `height="sm"` | 검토·승인(기록에 추가·넘어가기·전사 원문 보기 등) |
| **체크박스** | 18px · radius 4 · **끔 = 흰 면** · **`--gradient-deep` 1px 테두리**(민트 deep → 라벤더 deep 세로). **켬 = 면이 `--gradient-action`으로 칠해지고** 그 위에 `--ink` 체크 표시가 얹힌다(2026-07-31 Q 결정 — 프라이머리 버튼과 같은 그라데이션이다. 새 색도 새 그라데이션도 아니고, "지금 내가 정한 것"이라는 뜻을 버튼과 나눠 갖는다). 대비는 그라데이션 양끝 위 8.15 / 6.35로 둘 다 통과. **왜 바꿨나**: 이전에는 켬과 끔이 12px 체크 글리프 하나로만 갈렸는데, 18px 상자 안에서 그 차이는 훑을 때 잡히지 않는다 — 동의 체크박스(D23·D49)처럼 "표시했나 안 했나"가 곧 법적 기록인 자리에서 가장 위험한 종류의 모호함이다. 라디오도 같은 어휘다(가운데 `--ink` 점 + 같은 면). 라벨 16/600 + 설명 14/400 `--sub` 세로 배치. 옆 텍스트가 한 줄이면 세로 가운데 정렬, 여러 줄이면 첫 줄 라인박스 높이 래퍼 기준으로 가운데 정렬한다(전체 블록 기준 center 금지) | 당사자 등록·리스크 배너 |
| **체크박스 리스크 변형** | **테두리만 `--risk`로 바꾸고 나머지는 기본과 같다**(2026-07-26 Q 결정 — 켬 면이 `--gradient-action`이 된 2026-07-31 이후에도 이 문장 그대로다). 리스크 배너 안에서 리스크 독점을 담당하는 요소라 존재한다(§9 완화). 코드에서는 `data-tone="risk"`. 2026-07-31에 **체크 획만 핑크에서 `--ink`로 바뀌었다** — 채운 면 위에서 핑크 획은 대비 1.85·1.44라 아예 보이지 않는다. 리스크를 알리는 것은 획이 아니라 테두리이므로 신호는 잃지 않는다. **구현 주의**: 체크 표시는 `::after`가 아니라 `background-image`로 그린다 — `input`은 replaced element라 Firefox가 생성 콘텐츠를 렌더하지 않아 체크 표시가 조용히 사라진다. data URI 안에는 `var()`를 쓸 수 없어 획 색만 hex로 박히는데, 이 시스템에서 원시 hex가 불가피한 유일한 자리다(획이 `--ink` 하나로 모여 이제 그 자리는 한 값뿐이다) | 리스크 배너 |
| **리스트 행** | 높이 자유 · 패딩 16/24 · **호버 = `--gradient-hover`**(2026-08-03 Q 결정 — 구 `--muted` 단색. hover 미디어 안, 선택 행에는 안 겹친다) · **고른 행은 `--gradient-action` 면**(2026-07-31 Q 지시 "리스트도 채운다" — 체크박스 켬과 같은 어휘라 화면 어디서든 '내가 고른 것'이 한 가지로 읽힌다. 구 `--muted`는 호버와 **같은 값**이라 고른 것인지 지나가는 중인지 구분되지 않았다. 구현은 배경 2겹이다 — `--surface-fill` 변수로는 못 한다, `linear-gradient()`의 색 자리에 그라데이션을 넣을 수 없기 때문이다). 시간(16/600 고정폭) → 이름 묶음 → 우측 배지·화살표. 이름 묶음은 D59 이름 표기대로 `김미영 010-1234-5678`(실명 + 구분용 전화번호, ID 없음)이고, 그 아래 메타 줄에는 `대면` `3회차`만 남는다. **경계는 행이 놓인 맥락이 정한다**(2026-07-26 코드 적용 시 확정): 행이 한 컨테이너 안에 **붙어 있으면** 행 사이 `1px var(--line)` 구분선, 행이 gap 그리드에 **낱개로 놓이면** 카드 계약(아웃라인 카드, 2026-08-05 ADR-0030)을 쓴다. 낱개 행에 구분선만 주면 떠 있는 글자가 되고, 카드 안의 행에 카드 경계를 주면 테두리가 두 겹으로 겹친다. 일정·당사자 목록의 낱개 카드는 2026-08-06 부터 아래 '당사자 카드' 부품이 담당한다 | 설정 실무자 목록(구분선) · 상담 일정·당사자 목록(당사자 카드) |
| **당사자 카드** (2026-08-06 Q) | 일정(다가오는·전체)과 당사자 목록이 **같은 부품**을 쓴다(`ParticipantCard`, `components/wire/`). **두 화면 모두 2행 골격이다**(같은 날 3차 — 구 '목록은 한 줄 카드' 대체). **행 순서는 2026-08-07 Q 가 뒤집었다 — 이름이 위다**(사람이 단위인 카드라 이름 행이 먼저 서고, 날짜·참여 사업 정보는 가로선 아래로 내려간다. HERO 2행 골격과 같은 문법): 1행(공통) = 이름·가명 ID·연락처(D24 값 규칙). 2행 = 일정 화면은 날짜·시간 좌측 + 종류 뱃지 우측(블루, 지난 일정에는 무채색 상태 뱃지 추가), 당사자 목록은 참여 사업 N개 좌측 + 상태 뱃지 우측(진행 중=민트/종결=무채색). **칸은 앞 아이템을 따라 붙는 좌측정렬이다**(같은 날 5차 — 구 고정 폭 칸(날짜 104·이름 72·가명 ID 96) 대체: 가명 ID 가 길면 고정 칸 안에서 줄바꿈해 답답했고, 짧은 날짜 옆 시간이 멀리 밀렸다. 2차의 space-between 고른 펴기 폐지는 유지). 틈은 1행 12 · 2행(정보 칸) 20 — 값 사이 여백을 한 단 넓힌다. **정보 칸 사이는 `--line-control` 1px 세로선으로 가른다**(2026-08-07 Q "아이디·연락처가 오는 경우 세로선" — HERO 정보 행과 같은 문법. 호버 그라데이션 채움 위에서는 가로 구분선과 같은 규칙으로 카드 표면색으로 뒤집힌다). **이름만 강조: 600 + 계단 16 에 광학 +1px**(§2-1 계단 예외), 나머지는 16/400. **가명 ID 는 연락처(`--sub`)보다 한 발 옅다**(3차 — `color-mix(--sub 80%, --panel)`, 흰 위 4.78 AA 통과. 대조용 값이라 물러선다). 뱃지는 행 오른쪽 끝, 정보 값은 왼쪽 — 두 축이 섞이지 않는다. 행 사이 구분선은 **회색 `--line` 1px 풀블리드**(§3-3 2026-08-06 개정)이고, **호버 그라데이션 채움 위에서는 카드 표면색으로 뒤집힌다**(같은 날 5차 — 라이트=화이트. 다크는 표면색이 호버 tint 와 밝기가 겹쳐 `--line-control` 로 잇는다). 정보 3색 배분: 블루=일정(종류 뱃지), 민트=상태(진행 중 뱃지), 라벤더=참여 사업 N개(컬러 글자). **상세 화살표(>)는 없다**(같은 날 4차 — 구 '목록 카드 오른쪽 끝 화살표' 폐지): 카드 전체가 링크이고 호버 그라데이션 아웃라인이 눌림을 알린다, 일정 카드와 같은 문법. 좁은 화면에서는 정보 칸이 줄 단위로 접힌다(구 리스트 행의 모바일 접힘과 같은 원칙) | 상담 일정 · 전체 일정 · 당사자 목록 |
| **빈 상태** | 라인 아이콘 28px + 제목 16/600 + 설명 14/400(최대 34자폭) + 다음 행동 세컨더리 버튼. **무채색만** | 상담 일정·검토 |
| **모달** | 폭 520px · radius 12 · 흰 배경 · 스크림 `rgba(61,52,69,.4)` · 그림자 `--shadow-modal`. 헤더(제목 18/600 + 설명 16/400) · 본문 · 하단 버튼 줄 순. 하단은 오른쪽 정렬이고 **세컨더리(돌아가기)가 왼쪽, 프라이머리(승인)가 오른쪽 끝** | 검토·승인 |
| **탭** | 활성 = `--ink` 글자 + **하단 2px `--gradient-brand` 밑줄**(2026-08-03 Q — 구 `--ink` 단색). 활성 밑줄은 `::after` 절대 배치로 컨테이너 밑줄(1px `--line`)을 **가운데로 관통**한다(±0.5px, 2026-08-03 재개정 — 끝만 맞추면 2px 가 위로 돌출해 계단으로 보인다). 비활성 `--sub` | 검토·승인 · 관리자 2차 내비 |
| **인용 블록** | AI 제안의 근거 발언(D6·D9) 전용. **`--gradient-brand-v` 2px 세로선**(카드 구분선과 같은 어휘) + 왼쪽 패딩 12 + 14/400 `--sub` + 타임코드. 회색 세로선을 쓰지 않는다. **그라데이션 방향은 선이 흐르는 방향(위에서 아래)과 같아야 한다** — 폭 2px 쪽으로 흐르게 걸면 색이 섞일 거리가 없어 단색으로 보인다. pen에서는 `rotation: -180`, CSS에서는 `180deg`. 액센트 띠 금지 대상이 아니다(ADR-0008의 구분선 예외와 동일) | 검토·승인 |
| **AI 제안 행 상자** (2026-08-09 Q · CCC-77) | 15초 페이지 영역 ① AI 제안은 **행(제목 + 확인 이유 + 근거 회차 링크)마다 `--lavender-tint` 면 + radius 6(`--radius-control`) + 패딩 상하 12 좌우 16 상자**다. AI·승인 대기 축(D58 ④)을 면으로 편 것이다. **글자 색은 불변**(`--ink`/`--sub`): 라벤더 deep 글자는 tint 위 대비 미달이라 금지(§9) | 15초 페이지 |
| **이름 표기** | **D59(2026-08-04)가 구 "어디서나 `이름 (가명 ID)` 한 줄"(2026-07-26)을 대체한다.** 화면 표기는 **실명 하나**다 — 가명 ID 는 백엔드 전용 식별자로 회귀한다(D31 의 원래 정의로 복귀). **폴백 두 경우에만** ID 가 이름 자리에 나온다: ① 이름 무응답 등록(D41 무응답 원칙) ② 보존 기간 경과로 금고가 파기된 당사자(D10·D32) — 이때 ID 는 화면에 남는 유일한 식별자다. **동명이인 구분은 전화번호(전체 번호)가 맡는다** — 목록·일정 행에서 이름 옆 16/400 `--sub` 로 나란히 선다(연락처 칸이 이미 있는 카드에는 중복으로 더하지 않는다). 실명은 그 자리의 제목 크기(HERO 28 · 리스트 행 16. 허브 h2 단은 2026-08-09 Q 로 폐지, HERO 도 전 화면 28: CCC-77) `--ink` 600. **2026-08-06 Q 부분 재개정: 당사자 카드(일정·당사자 목록)의 정보 칸과 당사자 정보 허브 HERO 에는 가명 ID 를 이름 옆에 표시한다**(16/400 옅은 그레이 — 당사자 카드 ID 와 같은 `color-mix(--sub 80%, --panel)`, §5 '당사자 카드'. 허브는 같은 날 후속으로 추가). 백엔드 식별자 지위와 폴백 두 경우 원칙은 유지된다 | 전 화면 |
| **HERO 좌측 묶음** | 이름(D59 — 실명 하나)을 **한 줄로 두고**, 상태 태그나 승인 대기 배지를 그 오른쪽에 **간격 20으로 떨어뜨려** 세로 가운데 정렬한다(2026-07-26 Q 확정). 2행으로 나누면 HERO의 위아래 여백이 깨진다. 배지를 이름에 바로 붙이지 않는 이유는 이름과 한 단어처럼 읽히기 때문이다 | 브리핑·검토·승인 모달 |
| **ParticipantHeroCard** (D38 · D59 개정 · 2026-08-06 Q 2행 골격) | 당사자 중심 화면(`/participants/:id/**`)의 **공통 머리 부품** — `components/wire/`에 둔다. **2행 골격(2026-08-06 Q · 2026-08-07 위계 개편)**: 1행 = **이름·상태 태그만** 좌, 행동 버튼이 우. 회색 `--line` 풀블리드 구분선 아래 2행 = 정보 행 — 가명 ID·연락처(허브)·메타(브리핑·기록)가 `wire-meta-row` 세로선(§10)으로 갈라 선다. 연락처·가명 ID 를 1행에서 내린 것이 2026-08-07 개편이다(제목 줄이 좁고 답답했고 값이 이름과 같은 위계로 읽혔다 — 이름만 위계 위에 남긴다. 당사자 카드와 같은 문법). 정보 행에 넣을 것이 없으면 1행만 남는다. 슬롯 의미는 유지: ① 이름 — 실명 한 줄, **전 화면 hero 28**(2026-08-09 Q, D59 ③ 부분 개정·CCC-77. 구 "브리핑·기록 = hero 28, 허브 = h2 18" 대체) ② 연락처 — 이름 옆 16/400 `--sub` ③ 상태 태그 — 케이스 1개 화면에서만 ④ 행동 버튼(§4-6 상한 — 브리핑만 3개). 좁아지면 1행이 접혀 버튼이 이름 아래로 내려가고, 767 미만은 버튼 세로 스택 + 메타 세로 배열이다. 계좌·주소 등 추가 PII 는 어느 슬롯에도 올리지 않는다. 카드 계약(radius 12 · 아웃라인 `--line` 1px, 그림자 없음: ADR-0030)은 위 카드/패널 규칙을 따른다 — HERO 는 D59 성격 규칙의 '강조 컨테이너'라 카드로 남는다. **예외 1건(2026-08-07 Q): 기본정보 수정 화면은 이 부품을 쓰지 않는다** — 이름·저장 줄이 카드 밖 페이지 제목 줄(전체 일정의 제목+행동 줄 문법)이고, 본문은 기본 정보·추가 정보 카드 2장이다(같은 날 5차 — 구 '카드 한 장에 합침' 재개정) | 브리핑·당사자 허브·상담 기록(CCC-11) |
| **계열 칩 3종** (D47) | 배지 계약의 '계열 배지'를 화면 전체에서 쓸 수 있게 **축별로 못 박은 것**이다(2026-07-30). 형태는 배지와 같고(높이 24 · 패딩 0 10 · radius pill · 14/600) **배경만 계열 tint**다. ① **유형 칩 = 블루 tint**(시간·상태 축) — 상담 유형처럼 그 기록이 '무엇이었나'를 가르는 값. 인테이크도 **같은 블루**다, 유형은 전부 같은 축이라 하나만 다른 계열로 빼면 축이 깨지고 구분은 글자가 한다 ② **담당 칩 = 민트 tint**(사람·소속 축) — `당사자`·`실무자`·`기관` ③ **AI 출처 칩 = 라벤더 tint + `--lavender-deep` 글자**(AI·승인 대기 축) — `AI 제안`. **①②는 글자가 `--ink` 다**: deep 3색은 자기 tint 위 대비가 2.0 안팎이라 §9 예외의 '보조 정보 한정'에 걸리는데, 유형과 담당은 **읽어야 하는 값**이라 예외 대상이 아니다. deep 글자가 남는 곳은 출처처럼 곁다리로 읽는 칩뿐이다 | 상담 기록 |
| **활성 카드의 제목 줄 채움** (2026-08-09 Q) | 펼친 카드의 **제목 줄**이 고른 행과 같은 옷을 입는다 — `--gradient-action` 면 + `--on-action` 글자, 카드 아웃라인까지 풀블리드. 제목 줄은 구조적으로 행이라 위 호버·선택 규칙 ②를 고치지 않고 그대로 빌린다(카드는 여전히 `--gradient-brand` 아웃라인으로 활성을 알리고, 그 위에 행 어휘가 얹힌다). **카드 전체를 채우지 않는다** — 펼친 카드의 본문은 읽는 면이고, 회차 본문·기록지 아코디언 본문은 긴 글이라 파스텔 면 위에서 읽기가 나빠진다. **제외 2곳**: ① 브리핑 3영역(`.briefing-card`)은 `open` 이 기본값이라 '활성'이 아무 정보를 갖지 않는다 — 채우면 15초 페이지에 파스텔 띠 셋이 상시로 서서 조용한 화면(§0)과 리스크 배너의 유일성(D9)이 함께 흐려진다 ② 위기·안전 아코디언(`.is-crisis`)은 `--risk` 틴트·테두리 자리라 경고색 독점을 지킨다(D9). **채운 면 위 글자는 하나씩 `--on-action` 으로 덮는다** — 제목 줄 안 조각이 저마다 `--ink`·`--sub` 를 선언하고 있어, 두 테마에서 같은 밝은 파스텔인 채움 위에 다크의 밝은 `--ink` 가 그대로 얹히면 안 보인다(실측 대비 8.15 / 6.35 로 양끝 통과). 배지는 자기 tint 면을 가진 독립 표면이라 건드리지 않는다 | 전체 상담 기록 회차 카드 · 상담 기록 작성 아코디언 |
| **활성 카드 테두리** (D47) | 위 카드/패널 규칙의 "**선택·활성일 때만** 그라데이션 테두리"의 **첫 실사용처**다(2026-07-30). 펼친 회차 카드가 `--gradient-brand` 1px 을 두르고 접힌 줄은 회색 `--line` 1px 그대로라, "지금 보고 있는 것"이 색으로 잡힌다. 구현은 **배경 2겹**(`linear-gradient(--panel,--panel) padding-box, var(--gradient-brand) border-box`) — `border-image` 로 만들면 브라우저가 `border-radius` 를 무시해 모서리가 사각으로 렌더된다(D34 리스크 배너에서 이미 겪은 결함). **리스크 배너와 겹치지 않는다**: 배너는 1.5px 이고 전용 tint 배경·경고 아이콘을 함께 갖는다. **details 카드에는 `overflow:clip` 이 필수다**(2026-08-03) — Chromium 이 details 의 border-box 그라데이션 층을 하단 radius 로 못 잘라 아래 모서리가 각지고 링이 삐져나온다(레이어 승격으로도 안 고쳐짐, 킷 실측) | 상담 기록 |
| **인테이크 저장 안내줄** (CCC-31) | 인테이크 저장 직후 브리핑 진입에만 1회 표시(`?notice=intake_saved` — 컴포넌트가 마운트 직후 URL에서 제거). 블루 tint 배경(시간·상태 축, D34) + `--line` 1px + radius 12 + 패딩 16, 제목 16/600 `--ink` + 본문 14/400 `--sub` + 세컨더리 sm 버튼. 리스크 배너 어휘(그라데이션 테두리·전용 tint·경고 아이콘)는 확인된 리스크 전용이라 빌리지 않는다(D9). **2026-08-07 부터 아래 콜아웃 부품의 인스턴스다** | 브리핑 |
| **콜아웃 (WireCallout)** (2026-08-07 D61 신설) | 안내줄의 공용 부품(`wire-callout.tsx`). 구조는 제목 16/600 `--ink` + 본문 14/400 `--sub` + 행동 줄(세컨더리 이하만 — 프라이머리는 화면 주 행동 몫, §4-5) 순서로 고정. 톤 3종은 계열 의미를 따른다: **info**(블루 tint) = 시간·상태 안내 / **mint** = 사람·소속 / **lavender** = 주의·대기(색 규율 5). **risk 톤은 없다**(리스크 어휘는 배너 전용, D9). 손으로 안내 상자를 그리지 않는다 — 구 `.note` 회색 상자 폐지. 자동 저장 복원 안내(draft-notice)·인테이크 저장 안내·인테이크 중복 경고(상담 등록)가 쓴다 | 브리핑·기록 작성·상담 등록·/kit |
| **로딩 화면 (PageLoading)** (2026-08-09 Q) | 아래 `[추론]` 목록에 있던 '로딩'을 계약으로 올린 것이다. 규칙 셋: ① **제목은 로드된 화면과 같은 부품·같은 문자열**(`PageTitle`) — 구 로딩은 클래스 없는 `h1` 이라 UA 기본 굵기 700 으로 서다가 내용이 오면 600 으로 가늘어졌고, 767 미만에서는 크기까지 28 에서 24 로 뛰었다(`.wire-page-title` 에만 좁은 화면 분기가 있다) ② **모션 없음**(§6 — 스피너·시머는 장식용 무한 반복이라 금지, 로딩 진입 모션은 2026-08-04 '떠오름' 폐지로 함께 닫혔다). 회색 스켈레톤 블록도 두지 않는다: 움직이지 않는 회색 덩어리는 내용을 흉내 낼 뿐 "곧 온다"를 말하지 못한다 ③ 안내 한 줄은 **카드 안**에 선다(§4-5 "화면의 모든 글자는 카드 안에"). **오류·빈 상태도 같은 셸**을 쓴다 — 셋이 제각각이면 로딩에서 오류로 넘어갈 때 제목 자리와 여백이 함께 흔들린다. 오류 쪽 셸은 `PageError` 부품이다(2026-08-09 신설: `PageTitle` + 카드 안 `WireError` 92 예약 + 복귀 버튼 줄) | 15초 페이지 · 당사자 정보 · 기본정보 수정 · 인테이크 |
| **위저드 공용 클래스** (2026-08-09 Q) | 상담 등록·인테이크 두 위저드가 화면 전체를 **인라인 스타일 객체**로 그리고 있었다 — `pnpm guard:tokens` 는 `layout.tsx`·`wire-styles.ts` 두 파일만 훑으므로 인라인 값은 검사 밖이었고, 실제로 제목이 계단에 없는 20px(다른 화면 h2 는 18), 인테이크 기본정보 값이 **버튼 전용** 하프스텝 15px 로 서 있었다. 같은 스타일 블록이 두 파일에 복사돼 있어 한쪽만 고치면 두 화면이 갈라지므로 함께 옮겼다. 어휘 넷: `.wizard-stack`(세로 스택 20) · `.wizard-actions`(버튼 줄, 왼쪽부터 — 오른쪽 정렬인 `.wire-form-actions` 와 다르다) · `.wizard-field`(칸 하나의 세로 묶음 8) · `.wizard-form`(**입력 묶음 최대 폭 520**, §4-1 "읽기 폭이 필요한 폼은 페이지가 아니라 폼 자신이 좁힌다" — 장폭 1120 안에서 글줄 1040 짜리 textarea 는 눈이 되돌아올 자리를 잃는다). 폭을 다 써야 하는 것(후보 목록·참고 카드)은 이 래퍼 밖에 둔다. 제목·라벨·캡션·오류·입력칸은 새 클래스를 만들지 않고 이미 있는 계약을 쓴다: 전역 `h2`(18/600) · `WireFormField` · `.panel-meta` · `.wire-field-error` · `.wire-field-label`/`.wire-field-value` · `WireChoice` | 상담 등록 · 인테이크 |
| **인용 블록 컴포넌트 (WireQuote)** (2026-08-07) | 위 '인용 블록' 계약의 부품화(`wire-callout.tsx`). 마크업은 blockquote + 타임코드이고 모양은 `.wire-quote` 가 소유한다 | /kit(검토·승인 화면 대기) |
| **근거 인용 접힘 (WireSourceQuotes)** (2026-08-21 · D73) | 요약 주장, AI 제안, 리스크 플래그, 대조 항목 바로 아래에서 근거 인용을 기본 접힘으로 둔다. 펼친 내용은 `WireQuote`를 쓰고, 끝의 인라인 링크는 해당 회차 카드 `#record-{sessionId}`로 간다. 전사 전문은 이 부품에 싣지 않는다 | AI 초안 검토 · 15초 페이지 · 전체 상담 기록 |
| **빈 상태·오류 (WireEmpty / WireError)** (2026-08-09 Q 전수 정리) | 두 뜻이 옷 셋에 뒤섞여 있었다: `.empty` 17곳 중 **10곳이 `role="alert"`** 였고(같은 회색 줄이 "비었다"와 "못 불러왔다"를 함께 입었다), 같은 오류를 다른 화면은 리스크 배지(9곳)·`.schedule-form-error`(2곳)·인라인 검정 글자(2곳)로 그렸다. 이제 짝은 둘뿐이다 — **비었다 = `WireEmpty`**(14/400 `--sub` 한 줄, `role="status"`, `.empty`) · **실패했다 = `WireError`**(14/600 `--risk` 텍스트 한 줄, `role="alert"`, `.wire-error`). 입력칸 하나의 검증 오류는 여전히 `WireFormField` 의 `error` 슬롯이다(칸 수준 vs 화면 수준). 구 `min-height:92px` 자리 예약은 **켜는 것**으로 바뀌었다(`reserve`, 두 부품 공통) — 그 값이 늘 켜져 있어서 브리핑은 한 카드에 빈 줄이 둘일 때 184px 이 비어 보였고, 그래서 `.briefing-note` 라는 두 번째 빈 상태 규칙을 따로 갖고 있었다(삭제). **같은 날 정정(Q)**: 전수 정리가 골랐던 리스크 배지 옷은 물렸다. 배지는 inline-flex 알약이라 그리드나 카드 본문에 직접 서면 전폭으로 늘어난 세로 여백 0 의 알약 막대가 됐고, 알약은 짧은 라벨 전유물이다(§3-1). 문장 오류의 옷은 §9 "오류·경고 텍스트" 계약을 따르는 텍스트 줄이고, 입력칸 오류(`.wire-field-error` 14/600)와 같은 크기·굵기라 오류 어휘가 텍스트 하나로 모인다. 화면 전체가 실패한 자리는 이 줄을 맨 그리드에 세우지 않고 **`PageError` 셸**(위 로딩 행과 같은 셸: `PageTitle` + 카드 + 복귀 버튼 줄)에 담는다 | 전 화면 |
| **반복 칸 추가·삭제 (WireRepeatActions)** (2026-08-09 Q) | 같은 조작이 **네 벌**이었다('이 목표 삭제'·'이 질문 삭제'·'추가하기'·'이 줄 삭제'·'줄 추가'). 라벨보다 나빴던 것은 **자리**다 — 삭제는 칸 아래, 추가는 묶음 아래로 서로 다른 줄에 떨어져 한 쌍으로 읽히지 않았다. 이제 `+`/`-` 아이콘 버튼 한 쌍이 조작 대상 바로 아래 **오른쪽 정렬 한 줄**에 선다(간격 8 — 조작 줄 기본 12 보다 좁게 붙여 한 쌍으로 읽힌다). 형태는 조작 버튼 계약 그대로(neutral · 32 · radius 6)이고 문자 글리프가 아니라 SVG 아이콘이다(D58 ⑦). 접근성 이름은 호출부가 항목 이름으로 채운다(`목표 추가`·`이 목표 삭제`). 상한·마지막 한 칸 판정은 호출부가 슬롯을 비우는 것으로 표현한다 | 인테이크 반복 행 표 · 상담 일정 등록 목표·질문 |
| **모바일** (768px 미만) | 사이드바 → 상단 가로 스크롤 내비 · 본문 패딩 24/16 · h1 24px · 2열 그리드 1열 · **리스트 행은 시간+이름 / 메타 / 배지 3줄로 접힘**(한 줄 유지 시 이름이 세로로 쪼개짐 — 렌더에서 확인) · **페이지 헤더의 행동 버튼은 세로로 쌓이고 각자 헤더 폭을 온전히 쓴다**(`.page-actions` 를 `flex-direction:column` + `width:100%`, 헤더는 `align-items:stretch`). 가로로 둔 채 폭만 나누면 라벨이 길어질 때마다 결함이 돌아온다 — 변수는 버튼 **개수**가 아니라 **라벨 길이**다 | 상담 일정 · 브리핑 · 당사자 허브 |
| **사이드바 내비 항목** | 높이 40 · radius 6 · 라벨 **16/500·활성 600**(2026-08-04 Q — 구 14/600. 사이드바 글자는 하한 16·기본 500·강조 600, §2-1 역할표 '사이드바' 행). 알약은 좌우 −12 로 되밀어 **아이콘·글자가 사이드바 좌측선(패딩 24)에 선다**(기관 마크·'사업' 라벨·선택창 상자와 한 줄). 비활성 `--sub`. **활성 = `--blue-tint` 채움 + `--gradient-brand` 1px 테두리 + `--ink` 글자 + `--blue-deep` 아이콘**(2026-08-04 Q 테두리 추가 — `--blue-tint` 가 사이드바 그라데이션 최상단 색과 동일해 위쪽 메뉴에서 활성 상자가 안 보였다. 글자·아이콘은 2026-07-26 정정 유지). 활성 글자에 `--blue-deep` 를 쓰면 tint 위 대비가 **1.9** 라 라벨이 읽히지 않고, 비활성(`--sub`, 7.8)보다 오히려 흐려져 **위계가 뒤집힌다** — §9 의 대비 예외는 '보조 정보 한정'이므로 주 메뉴 라벨은 대상이 아니다. 블루 신호는 아이콘이 갖는다. **호버 = 어두운 면(`--ink`, 다크는 `--canvas`) + 그라데이션 글자**(2026-08-03 Q — 구 잉크 6% 워시 대체. 활성과 어휘가 완전히 갈려 '지나가는 중'과 '지금 있는 곳'이 안 섞인다). 활성 항목 위에서는 활성 표시가 이긴다. 호버 규칙은 `@media (hover:hover)` 안에 둔다 — 터치 기기에서는 탭한 항목에 호버가 남아 굳은 것처럼 보인다 | 전 화면 |
| **선택지(라디오·체크박스) 행** | 동그라미/네모와 라벨은 **같은 줄**이고 누를 면적은 컨트롤 높이(40) 만큼 준다. 라디오·체크박스에 텍스트 입력칸 규칙(`width:100%` · 테두리 · 패딩)을 상속시키지 않는다 — 동그라미가 칸을 가로질러 늘어나고 라벨이 아래 줄로 밀려 선택지가 세로로 쪼개진다(2026-07-26 실제 결함) | 상담 기록 작성·인테이크 |
| **페이지 제목** | 28/600 `--ink` · **왼쪽 정렬**(2026-07-26 정정, 이전 가운데 정렬 폐기). 가운데 정렬은 제목만 아래 콘텐츠 축에서 떨어져 나와, 페이지마다 제목 위치가 달라 보이는 원인이었다 | 전 화면 |

### D58 신설 계약 (2026-08-02 · ADR-0028)

- **클릭 가능성은 형태로 — 단독 클릭 요소는 전부 버튼이다.** 홀로 서 있는 클릭 요소(화면 하단 이동 링크 · 카드 안 처리 버튼 · 필터 등)는 버튼으로 만든다 — 중요하면 채움, 일반이면 아웃라인. **문장·표 안 인라인 참조 링크만** 밑줄 텍스트로 남는다. *(2026-08-07 D61 부분 개정: "전부 알약" 의 형태 부분을 직사각 radius 6 이 대체한다 — §3-1. 채움과 아웃라인의 위계 원칙은 그대로다.)* 예외 재확인: 사업 전환기는 행동이 아니라 값이므로 radius 6 컨트롤 그대로다(§4-5. HERO 상태 태그는 2026-08-06 Q 재개정으로 알약 배지 계열이 됐다 — §3-1).
- **계열의 의미는 고정이다.** 블루 = 일정·시간·유형·정보 / 민트 = 진행·상태·담당(사람·소속) / 라벤더 = AI 산출·승인 대기 / 리스크 핑크 = 확인된 경고 전용(D9). 데이터 나열의 구분은 글자색이 아니라 **계열 배지·칩**(tint 배경)이 맡고, 본문 강조는 색이 아니라 **굵기 600**이다. 위 계열 칩 3종(D47)의 축과 같은 표다 — D58은 그 축을 화면 전체 규칙으로 승격했다.

### 버튼 소속 규칙 (2026-08-09 Q 신설)

버튼은 **소속 묶음에만 선다**. 소속 묶음은 셋이다: **폼 액션 줄**(`.wire-form-actions`, `.wizard-actions`, 모달 하단 버튼 줄), **카드 액션 슬롯**(HERO 행동 버튼, 콜아웃 행동 줄, 빈 상태의 다음 행동), **페이지 제목 줄**(전체 일정, 기본정보 수정의 제목+행동 줄). 위 D58 신설 계약이 "단독 클릭 요소는 전부 버튼"으로 형태를 정했다면, 이 절은 그 버튼이 서는 자리를 닫는다.

- 본문 문단 사이나 카드 사이에 버튼이 혼자 떠 있으면 위반이다. 소속 묶음 없이 놓인 버튼, 주변 여백이 여백 3단(§3-4) 밖인 버튼은 검수에서 반려한다.
- §5 계약이 자리를 따로 명시한 조작은 그 계약이 곧 소속 묶음이다. 반복 칸의 WireRepeatActions 조작 줄, 동의 '전문 보기' 배지형 버튼(§3-1 예외)이 그 경우다. 이 목록 밖에서 새 자리를 만들려면 계약부터 세운다.
- 버튼 주변 여백은 버튼이 스스로 만들지 않는다. 소속 묶음의 gap이 만든다(§4-6 규칙 3과 같은 원리다).
- 새 화면 검수 체크에 여백 3단(§3-4) 준수 확인을 포함한다.

### `[추론]` — 아직 시안에 없는 것

토스트·인라인 알림, 테이블(정렬·페이지), 파일 업로드(녹음). 다음 사이클 대상.

> 2026-08-09에 이 목록에서 빠진 것: **로딩**(위 PageLoading 행). 스켈레톤은 목록에서 빠진 것이 아니라 **안 만들기로 정해진 것**이다 — §6 이 로딩 모션을 닫아 두었고, 정지한 스켈레톤은 내용을 흉내 낼 뿐이라 문장 한 줄보다 덜 알려 준다.
>
> 2026-07-31에 이 목록에서 빠진 것: **드롭다운·플로팅 메뉴**(D50 ② 사업 전환기), **날짜 선택기**(D48/ADR-0020), **다크 모드**(아래 §11).

## 6. 움직임 — 모션 2종 (2026-08-02 D58/ADR-0028 개정, 2026-08-04 떠오름 폐지 — 구 v1 대체)

모션은 아래 **2종뿐**이고, 이 밖의 모션은 위반이다. 그라데이션이 디자인의 핵심 정체성(Q)이므로 모션도 그라데이션이 주인공이다. **로딩·스크롤 진입 모션은 없다** — D58 이 신설했던 3종째 '떠오름'(스크롤 첫 진입 1회)은 2026-08-04 프리뷰 눈 튜닝에서 Q 결정으로 폐지했다(속도·이징을 조정해도 훑기 화면과 어울리지 않았다 — ADR-0028 개정 기록). 모션 트리거는 **사용자 입력(호버·클릭)뿐**이다.

| 이름 | 트리거 | 동작 | 대상 |
| --- | --- | --- | --- |
| **흐름** | 호버 | 그라데이션 위치가 좌→우로 흐르듯 이동 — `background-size` 200% + `background-position` 이동, **호버 동안만** | 그라데이션을 가진 요소: 프라이머리 버튼 채움 · 세컨더리 버튼 아웃라인 · 선택·활성 카드 테두리 |
| **눌림** | 클릭 | `translateY(1px)` (§5 버튼 상태의 현행 값을 승격 — 새로 만든 것이 아니다) | 모든 버튼 · 클릭 행 |

공통 계약:

- **시간 2단 + 이징 1종만** 쓴다: `--motion-fast`(120ms — 눌림·호버 워시), `--motion-base`(200ms — 흐름 진입), `--ease-standard`. 예외 하나: 흐름의 **반복 주기**만 `--motion-flow-period`(1600ms)를 따로 갖는다 — 120·200ms 로 반복하면 점멸로 보이고, 반복 주기는 전환 시간이 아니라 흐름의 정의 자체다. 이 넷 밖의 duration·easing 은 위반이다.
- 그라데이션이 없는 요소의 호버는 **잉크 6% 워시**(§5) 그대로다 — 워시는 상태 표시지 모션이 아니므로 3종에 세지 않는다.
- 애니메이션 속성은 `transform`·`opacity`·`background-position` 만 — 레이아웃을 움직이는 속성(width·margin 등) 금지.
- `prefers-reduced-motion: reduce`에서 전환 제거(tokens.css 의 시스템 계약 그대로).
- **장식용 무한 반복 금지는 유지**하되, 흐름(호버 동안 반복)만 명시 예외다 — 손을 올린 동안만 도는 것은 장식이 아니라 반응이다.
- 구 v1 의 "스크롤 트리거 모션 금지"는 사실상 복원됐다 — D58 이 상한부(1회·8px·200ms)로 열었던 예외(떠오름)를 2026-08-04 에 닫았기 때문이다. 스크롤·로딩 진입 모션을 되살리려면 ADR-0028 재개정이 필요하다.

## 7. 생성 도구(Stitch 등)에 주는 절대 규칙

1. 위 토큰 값 외의 색·서체·라운드를 만들지 않는다. 파스텔은 **블루·민트·라벤더 3계열뿐**이며 새 색조(노랑·주황·핑크 등)를 추가하지 않는다.
2. 파스텔 base는 면에만, deep은 글자에만, tint는 배경에만. 섞어 쓰면 대비가 깨진다.
3. 리스크 핑크(`--risk`)는 확인된 위험에만. 리스크 배너는 배경 `--risk-tint-solid` + **`--risk` 단색 1.5px 균일 테두리**(2026-08-02 D58)이고, 리스크 색은 배너 테두리·경고 아이콘·제목에만 쓴다.
4. 그라데이션은 3장 표의 5개 외에 만들지 않는다. 그림자는 `--shadow-soft`(모달만 `--shadow-modal` 추가) 외에 만들지 않는다.
5. 히어로 일러스트·스톡 이미지·이모지·장식 아이콘 배경 금지. 아이콘은 16/18px 단색 라인(stroke 1.5 · `currentColor` — 사이드바 SVG 시스템이 공용 원본, 2026-08-02 D58). **문자 글리프(⚠·✓·●·○·▸ 등)를 아이콘 대용으로 쓰지 않는다** — OS·폰트마다 렌더가 다르고 다크 모드에서 색 제어가 안 된다.
6. 밀도는 업무 도구 기준 — 마케팅 랜딩의 큰 여백·큰 디스플레이 타입을 쓰지 않는다.
7. 언어는 한국어, 실제 도메인 콘텐츠(당사자 실명·GAS 목표·액션 아이템·승인 대기 배지)로 채운다.

**레이아웃 락 (D37/ADR-0015 — 위 1~7이 색·형태만 다뤄 폭·거터·리듬 규칙이 0개였다. 형용사가 아니라 숫자로 못 박는다.)**

8. 셸은 **상단 헤더(전폭 56px) + 좌측 280px 사이드바** 둘이다(2026-08-05 Q 개정, Infisical 레퍼런스 — 구 "상단 헤더 띠를 만들지 않는다"를 대체). 헤더 내용은 기관(홈)·사업명·설정·테마·로그아웃뿐이고 **여기 메뉴를 늘리지 않는다**. **그라데이션 면 배경은 셸에 없다** — 헤더·사이드바 모두 캔버스이고, 영역 구분은 `--gradient-frame`(가로)·`--gradient-frame-v`(세로) **1px 라인**이 한다(같은 날 Q — 구 `--gradient-sidebar` 면 폐지).
9. 페이지 본문 컨테이너는 **max-width 1280 · 패딩 40**(768 미만 24/16) 하나다(2026-08-09 Q — 구 1120). 예외는 §4-1 허용 폭 2종(520·420)뿐이며(`.narrow` 960 은 2026-08-05 폐지) 새 폭 도입은 ADR 수정 사항이다.
10. 카드 그리드는 **gap 20 · 표준 최소 폭 420 · 조밀 최소 폭 280**이다. **열 개수를 직접 지정하지 않는다.**
11. 브레이크포인트는 **`max-width: 767px` 하나**다.
12. 세로 리듬은 **40 / 24 / 16 / 20** 고정이다. 원시 px 대신 `var(--space-N)`을 쓴다.
13. 시안은 **1440 캔버스**에 그린다.

14. 글자 크기는 **§2-1의 다섯 계단뿐**이고, 굵기는 **400·500·600 셋뿐**이다(2026-08-04 개정 — 500 은 사이드바 기본 굵기). 코드에 `font-size: <숫자>px`를 쓰지 않는다.
15. 간격·반경·z-index·색은 **토큰 이름으로만** 쓴다. 계단 밖 값이 필요하면 바로 앞줄에 `/* optical: 사유 */`를 단다 — 사유 없는 원시값은 위반이다.

> **기계 검사 (2026-07-31)**: 락 14·15와 미정의 토큰 참조는 **`pnpm guard:tokens`**(`scripts/design/token-audit.mjs`)가 잡는다 — 현재 위반 0. 락 8·9·10·11은 실제 앱 화면을 다섯 폭에서 재는 `scripts/design/layout-measure.py`가 담당한다(§4-6 반영 완료 주석 참조). `pnpm guard:layout`이라는 이름의 스크립트는 **아직 없다** — v0.1의 "작성 예정"은 여전히 예정이고, 위 두 도구가 그 자리를 나눠 맡고 있다.
>
> 6번 "밀도는 업무 도구 기준"이 지켜지지 않은 이유가 형용사였기 때문이므로, 이 여덟은 전부 숫자이거나 존재 여부다.

## 8. Pen 관찰값에서 버린 것 (재수입 금지)

| 버린 값 | 이유 |
| --- | --- |
| `--success: #5B42D6` (보라) | 이전 상태에서 남은 오염값. success 역할은 이 시스템에 없다(GAS 긍정은 색으로 표시하지 않음 — D6·R4) |
| `--primary #FF8400`, `--destructive #D93C15`, `--color-warning/info/*` 등 | Pencil 기본 템플릿 토큰 세트. 프로젝트 토큰과 한 파일에 공존했을 뿐 이 시스템과 무관 |
| `--strong: #003322` | 미사용 — D30 시절 다크 사이드바 배경 |
| 구 변수명 `accent` · `accent-secondary` · `accent-tertiary`(+ `-deep`) | 2026-07-26에 `blue` · `mint` · `lavender`로 통일. 한 계열이 두 이름을 쓰던 상태를 해소했다. **구 이름으로 되돌리지 않는다** |
| 원시 hex `#cccccc80`(버튼 보더) · `#e0e0e0`(추이 막대) | `--line` · `--track`으로 토큰화(값은 유지) |
| `font-weight: 500` | 400/700 2단계로 정규화 |
| `ink-deep: #0B1220`(버튼 라벨) | `--ink`로 통합 — 쿨한 남색이라 웜 뉴트럴 축에서 혼자 튄다 |
| 로고 팔레트(`logo-blue`·`logo-mint`·`logo-lavender`·`logo-stroke`) | 로고 시안 트랙 소유. 로고 확정 시 ADR-0008 로고 절에서 별도로 다룬다 |

## 9. 알려진 접근성 예외 (2026-07-26 갱신)

시안 톤을 우선해 **의도적으로 WCAG 기준 아래에 둔 값**이다. 모르고 지나친 것이 아니라 계산해서 알고 쓰는 것이며, 여기 없는 값이 미달이면 그건 결함이다. 아래 수치는 2026-07-26 pen 최종 색값으로 실측했다.

| 값 | 쓰이는 곳 | 실측 | 기준 |
| --- | --- | --- | --- |
| **`--risk` `#F071B4`** | **리스크 배너 제목과 경고 아이콘 (D9)** | 흰 위 **2.72** · 배너 배경 위 **2.39** | AA 4.5 (아이콘은 1.4.11 3:1) |
| `--risk` (같은 값) | **오류·경고 텍스트** — 입력 오류 메시지, `[role="alert"]`, 폼 오류 문구, 검색 결과 오류 | 흰 위 **2.72** | AA 4.5 |
| `--risk` (같은 값) | **위험 버튼**의 라벨과 1.5px 테두리 (되돌리기 어려운 행동 전용) | 흰 위 **2.72** (테두리는 1.4.11 3:1 대상) | AA 4.5 |
| `--risk` (같은 값) | **위기 영역 강조** — 기록지 위기·안전 아코디언, 인테이크 위기 패널의 균일 테두리와 제목 | 흰 위 **2.72** · 틴트 배경 위 **2.39** | AA 4.5 |
| 민트 deep `#56CC99` | 당사자 정보 라벨(연락처·비상연락처·거주지), 사업 칩 | tint 위 **1.76** · 흰 위 2.00 | AA 4.5 |
| 라벤더 deep `#C890F0` | 승인 대기 배지 (D5) | tint 위 **2.02** · 흰 위 2.41 | AA 4.5 |
| 블루 deep `#67A9F0` | 활성 내비, 상태 태그, 액션 기한, 전체 열기 토글 | tint 위 **2.11** · 흰 위 2.47 | AA 4.5 |
| `--line-control` `#E3E3E3` | 입력칸·컨트롤 경계 | 흰 위 **1.28** | 1.4.11 3:1 |
| `--track` `#E0E0E0` | GAS 추이 막대의 과거 구간 | 흰 위 **1.32** | 1.4.11 3:1 |

> **v2 대비 두 가지가 나빠졌다.** ① deep 3색이 2.11/1.76/2.02로 내려가 **글자를 키우거나 굵게 해도 large text 3:1조차 통과하지 못한다**(v2에서는 블루가 4.40으로 근소 미달이었다). ② **리스크가 새로 예외 목록에 들어왔다** — v2의 `#B52573`은 6.04로 통과했으나 현재 `#F071B4`는 2.72다. 이 항목만은 성격이 다르다. 나머지는 보조 정보지만 **리스크는 D9가 화면으로 강제하려는 바로 그 신호**이고, 위기 상황에서 급히 훑을 때 읽혀야 하는 글자다.

**영향 범위**: 본문·값·제목은 영향 없다(`--ink` 캔버스 위 11.32, `--sub` 흰 위 8.09). 전화번호·주소·액션 제목·브리핑 본문 등 실제 읽어야 할 내용은 전부 기준을 넘는다.

> **`--risk` 항목이 4행으로 늘어난 이유(2026-07-26 코드 적용)**: 스킨 패스에서 오류 텍스트·위험 버튼·위기 영역이 실제로 `--risk`를 쓰게 됐다. v2까지 이 색은 `#B52573`(6.04)로 기준을 통과했으므로 이 자리들은 예외가 아니었다. 값이 `#F071B4`(2.72)로 바뀐 이상 **전부 예외로 기록해야 한다** — 위 규율 7번대로 미달을 아는 채로 쓰는 것은 결정이고, 기록하지 않는 것이 결함이다. 네 자리 모두 §5 계약이 **색과 문구를 함께** 두도록 이미 강제하고 있다(오류는 메시지 텍스트, 위험 버튼은 라벨, 위기 영역은 제목, 배너는 아이콘+건수 문구). 고대비 모드를 켜면 네 자리가 한꺼번에 6.04로 올라간다.

**완화(구현 시 필수)**:
- **리스크는 색 하나에 절대 기대지 않는다.** 배너는 ① 경고 아이콘 ② "확인된 리스크 N건" 문구 ③ HERO 바로 아래 고정 위치 ④ 접힘 불가, 이 네 가지로 신호를 만든다. 색을 전혀 못 보는 상태에서도 이 배너가 무엇인지 알 수 있어야 한다(WCAG 1.4.1).
- 미달 색은 **14px 이상·굵기 600에서만** 쓴다. 12px나 400으로 내려가면 예외 범위를 벗어난다.
- 라벨을 그 색 **하나로만** 구분하지 않는다 — 위치(값 위)와 굵기가 함께 구분한다.
- 입력칸은 경계선에만 기대지 않는다. 라벨을 항상 위에 두고, 오류는 테두리 색과 함께 **메시지 텍스트**로 알린다.
- **고대비 모드**(사용자 설정)는 아래 6개 토큰만 교체하면 되고 마크업 수정이 필요 없다. 전부 실측 확인했다.

  | 토큰 | 기본 | 고대비 | 고대비 실측 |
  | --- | --- | --- | --- |
  | `--risk` | `#F071B4` | `#B52573` | 흰 위 6.04 · 배너 배경 위 5.31 |
  | `--blue-deep` | `#67A9F0` | `#496B9A` | tint 위 4.67 |
  | `--mint-deep` | `#56CC99` | `#3C775E` | tint 위 4.62 |
  | `--lavender-deep` | `#C890F0` | `#8256A3` | tint 위 4.61 |
  | `--line-control` | `#E3E3E3` | `#948E89` | 흰 위 3.24 |
  | `--track` | `#E0E0E0` | `#948E89` | 흰 위 3.24 |

**재검토 시점**: 외부 기관 온보딩(D27) 또는 공공 조달·접근성 심사가 걸릴 때. 그때는 위 고대비 값이 준비된 대안이다. 리스크 항목은 그보다 먼저, 실제 실무자가 배너를 쓰는 걸 관찰한 뒤 다시 볼 값이다.

## 10. UI 문안 규칙 (2026-07-26 Q 확정)

실제 화면에 노출되는 텍스트에는 **가운데 점(`·`) 사용을 자제하고, 긴 대시(`—`)는 쓰지 않는다.** 둘 다 AI가 쓴 티가 나는 문장 부호라는 판단이다.

- 항목을 나열할 때는 부호 대신 **별도 요소로 나누고 간격(gap 12)으로 띄운다.** 목록 행의 메타는 `대면` `3회차` 두 텍스트, 검토 화면 세션 메타는 `3월 12일 상담` `3회차` `녹음 42분` 세 텍스트가 각각 독립 노드다. **2026-08-06 Q 보강: 메타 줄(`.wire-meta-row`)의 조각 사이는 `--line-control` 1px 세로선으로 가른다** — 서로 다른 위계·성격의 값이 한 줄에 설 때 간격만으로는 경계가 안 읽힌다(브리핑 HERO 메타에서 확정, 전 사용처 공통).
- 예외는 **이름에 딸린 가명 ID**다. `김미영 (swallow-003)`처럼 **한 칸 띄우고 괄호**로 붙인다. 노드는 둘로 나누고 띄어쓰기는 간격 4로 만든다 — 한 덩어리로 읽히게 붙이면서도 ID가 실명만큼 진해지지 않게 하려는 것이다.
- 문장 안에서 부호가 꼭 필요하면 쉼표를 쓴다. 버튼 라벨처럼 짧은 곳은 괄호가 낫다(`맞음 (플래그 확정)`).
- **날짜·시각 표기는 한 계약이다**(2026-08-07 Q "표기 규칙이 제각각이다" — 회차 기록·당사자 정보·전체 일정이 `2026. 6. 21.`, `8월 7일 (금) 13:00`, `2026-06-21` 로 갈라져 있었다): 날짜 = `2026년 8월 7일`, 시각 = `오후 1:00`, 날짜+시각 = `2026년 8월 7일 오후 1:00`. 요일 표기·24시간제·`YYYY-MM-DD` 노출은 폐지한다(정렬·그룹핑 키는 표시가 아니므로 예외). 구현은 `lib/format-korean-date.ts` 의 `formatKoreanDate`/`formatKoreanTime`/`formatKoreanDateTime` 만 쓴다 — 화면마다 `Intl.DateTimeFormat` 을 새로 만들지 않는다. 시간대 기본은 기관 표준(Asia/Seoul)이고, 기관 시간대를 아는 화면은 그 값을 넘긴다(운영 런타임 기본 시간대는 UTC 라 시간대를 안 박으면 날짜가 하루 민다).
- **한 낱말 안에서 묶어 읽는 가운뎃점은 예외다.** `이름·연락처·계좌`, `수면·식사`, `브리핑·통계·보고서`처럼 한 호흡으로 읽히는 병렬은 한국어 표기 관행이므로 그대로 둔다. 금지 대상은 **서로 다른 정보를 잇는 구분자로 쓰인 가운뎃점**이다.
- ~~이 규칙은 **제품 UI 문자열에만** 적용된다~~ **2026-08-06 Q 개정, 범위를 산출물 전반으로 넓힌다**: 문서, 커밋 메시지, PR, 티켓, 코드 주석 등 이 레포에서 새로 쓰는 한국어 산문에서도 **긴 대시(`—`)는 쓰지 않고, 가운데 점(`·`)은 자제한다.** 한 호흡 병렬 예외는 그대로다. 이미 있는 문서를 소급 수정하지는 않는다.

## 11. 다크 모드 (2026-07-31 신설 — ADR-0026)

**켜는 방법: `<html data-theme="dark">`.** `data-contrast="high"`(§9)와 **같은 모양**이다 — 토큰만 갈아끼우고 마크업은 한 글자도 바뀌지 않는다. 둘은 겹쳐 켤 수 있고, 그 조합용 블록도 따로 있다.

**토글은 사이드바 발치에 있다** — 설정과 로그아웃 사이. 로그아웃과 **같은 서버 액션 폼**이고 이유도 같다: 쿠키(`ccc_theme`)를 서버가 써야 다음 렌더의 `<html data-theme>` 이 **첫 페인트부터** 맞는다. GET 링크로 두면 프리페치가 테마를 제멋대로 바꾼다.

> **localStorage 가 아니라 쿠키인 이유.** 테마는 `<html>` 속성이라 첫 페인트 전에 정해져 있어야 한다. localStorage 는 자바스크립트가 돈 뒤에야 읽히므로 어두운 화면을 기대한 사람에게 흰 화면이 한 번 번쩍이고, 그걸 막으려면 `<head>` 에 블로킹 인라인 스크립트를 넣어야 한다 — PII 앱에 인라인 스크립트를 들이는 것보다 서버가 쿠키를 읽어 속성을 박는 편이 낫다. 레이아웃은 이미 `headers()` 를 써서 동적이라 비용도 새로 생기지 않는다.
>
> **라벨은 가는 곳을 말한다**(`다크 모드` / `라이트 모드` — 조사 '로'는 2026-08-04 Q 지시로 뺐다, 짧아도 가는 곳으로 읽힌다). 현재 상태를 말하면 누를 때마다 무엇이 될지 한 번 더 생각해야 한다. 현재 상태는 `aria-pressed` 가 따로 알리고, 아이콘도 가는 곳(달/해)을 가리킨다.

`yellow` **`prefers-color-scheme`에 일부러 연결하지 않았다.** 팀 검수는 미리보기 환경에서 이뤄지는데, 자동 감지를 켜면 OS가 다크인 검수자에게 **Q가 아직 보지 않은 팔레트**가 나간다. 색값이 명시적으로 동결된 시스템(§1 머리말)에서 그건 사고다. 승인되면 다크 블록을 미디어 쿼리로 복제하는 한 수로 켤 수 있다.

### 11-1. 값의 지위는 `[도출]`이다

라이트 팔레트는 pen이 SSOT이고 값은 Q 결정이지만, **다크 값은 pen에 없다.** v0.1이 적어 둔 "Pen 파일의 Dark 테마 선언"은 Pencil 기본 템플릿이고 §8이 이미 폐기한 세트다(`--primary #FF8400` 등이 든 그 묶음). 그래서 이 절의 hex는 **잠정이고 Q가 덮어쓸 수 있다.** 오래 가는 것은 값이 아니라 **어느 토큰이 뒤집히고 어떤 계약이 바뀌는가**다.

### 11-2. 라이트를 그대로 뒤집으면 안 되는 자리 셋

1. **tint 층이 뒤집힌다.** 라이트에서 tint는 거의 흰 배경이고 deep이 그 위 글자다. 다크에서는 tint가 어두워지므로 **deep이 자기 tint보다 밝아야** 한다. 색상(hue)을 바꾸는 것이 아니라 base/deep/tint 계약을 다크에서 다시 도출하는 것이라, §7-1 "새 색조 금지" 위반이 아니다 — 세 계열의 H값(211·154·275)은 그대로다. **base는 안 바꾼다**: base는 게이지·막대의 *면*이라 어두운 배경 위에서도 그대로 선다.
2. **깊이 모델이 바뀐다** (§3-2 개정). 파스텔 이중 그림자는 15% 투명도라 어두운 캔버스 위에서 아무 일도 하지 않는다. 다크에서 깊이는 **면의 밝기 단계**(`--canvas` #17141B < `--panel` #201C27 < `--muted` #2A2532)가 만들고, 그림자는 검정으로 바꿔 경계만 거든다.
3. **`--gradient-action`은 두 테마에서 같다.** 채움을 어둡게 만들면 체크 표시가 깨진다 — 체크 획은 data URI 안에 hex로 박혀 있어 `var()`가 닿지 않는 유일한 자리이기 때문이다(§5 체크박스 계약). 그래서 **채운 면은 두 테마에서 같은 밝은 파스텔**로 두고, 그 위 글자·획은 `--ink`가 아니라 **`--on-action`**(항상 어두운 잉크)이 맡는다.
   - 따라 나온 토큰이 하나 더 있다: **`--line-on-action`**. 프라이머리 버튼의 아웃라인은 밝은 파스텔 면 위라 어두워야 하고, 세컨더리는 어두운 패널 위라 밝아야 한다. 규칙은 하나다 — *"자기 면 대비 잉크 50%"*. §5의 "두 버튼의 테두리는 같은 세기다"는 그대로이고 **값만 면을 따라간다**(라이트에서는 두 값이 같다).

### 11-3. 원시 색값은 하나씩 다시 선언해야 한다

`var()` 조합으로 만든 토큰(`--shadow-soft`)은 재료가 바뀌면 따라오지만, **hex·rgba·그라데이션으로 박힌 토큰은 따라오지 않는다.** 빠뜨리면 그 자리만 라이트로 남는다 — 예컨대 `--gradient-hover`를 빠뜨리면 어두운 화면의 리스트 행 호버만 밝게 켜진다.

`pnpm guard:tokens`의 **dark-parity 검사**가 이것을 기계로 잡는다: 라이트에서 원시 색값으로 선언된 토큰은 다크 블록에 대응이 있거나, 의도적 불변이면 허용목록(`THEME_INVARIANT`)에 사유와 함께 들어가야 한다. 현재 불변 10개는 `--on-action` · `--line-on-action` · base 3색 · `--gradient-action` · `--gradient-brand` · `--gradient-brand-v` · `--gradient-frame` · `--gradient-frame-v`(프레임 라인 — base 3색이라 어두운 면 위에서도 그대로 선다, 2026-08-05)이고 전부 위 11-2의 근거를 갖는다.

함께 토큰이 된 것 2개: **`--scrim`**(모달·드로어 뒤 어둠 — 화면에 rgba 리터럴로 두 번 박혀 있었고, 다크에서 그대로면 어두운 화면 위에 보라 장막이 한 겹 더 앉는다)과 위 `--on-action`·`--line-on-action`.

### 11-4. 실측 대비 — 다크가 라이트보다 낫다

`pnpm design:contrast`가 두 테마를 한 번에 잰다(손계산 폐지 — §9 표의 숫자를 사람이 계산해 왔고, 틀려도 아무도 모르는 상태였다).

| 짝 | 라이트 | 다크 | 기준 |
| --- | --- | --- | --- |
| 블루 deep / 블루 tint | **2.11** | **8.83** | 4.5 |
| 민트 deep / 민트 tint | **1.76** | **9.20** | 4.5 |
| 라벤더 deep / 라벤더 tint | **2.02** | **8.53** | 4.5 |
| 리스크 / 면 | **2.72** | **7.46** | 4.5 |
| 리스크 / 배너 배경 | **2.39** | **6.84** | 4.5 |
| 입력칸 경계 / 카드 | **1.28** | **3.02** | 3 |
| 추이 막대 / 카드 | **1.32** | **3.02** | 3 |
| 본문 / 캔버스 | 11.32 | 15.99 | 4.5 |
| 채움 위 글자(그라데이션 양끝) | 8.15 / 6.35 | 8.15 / 6.35 | 4.5 |

### 11-5. 확인한 자리

`/kit` 에 **그라데이션 테두리 3종** 절을 새로 뒀다(리스크 배너 · 펼친 회차 카드 · 위기 아코디언). 이 셋이 다크에서 가장 깨지기 쉬운 부품이다 — 배경 2겹(padding-box 채움 + border-box 그라데이션)으로 만들어져, 채움색 토큰이 다크에서 안 따라오면 테두리가 안쪽까지 번지거나 아예 사라진다. **눈으로 볼 자리가 없으면 회귀를 잡을 방법도 없어서** 킷에 상설로 뒀다(증거 `artifacts/design-system-v3/gradient-surfaces-{light,dark}.png`).

토글은 실제 클릭으로 확인했다 — 누르면 다크, 새로고침해도 유지, 다시 누르면 라이트로 돌아온다.

> **§9의 예외 7건이 다크에서는 전부 해소된다.** 어두운 면 위에서는 밝은 파스텔이 유리하기 때문이고, 다크 값은 Q 동결 대상이 아니라 기준을 넘도록 고를 수 있었기 때문이다. 즉 **§9 '알려진 접근성 예외'는 라이트 전용 표**다 — 다크에는 예외가 없다.
> 다크+고대비를 함께 켜면 값이 한 번 더 올라간다. 라이트용 고대비 값은 **밝은 배경 기준**이라 어두운 면 위에서는 오히려 대비가 떨어지므로, 조합 전용 블록을 따로 뒀다.
