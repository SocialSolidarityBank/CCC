Settings foundation wave: explicit user scope expansion
User selected '여기서 기반부터 이어서 구현' after being told that signed-in business client, login and missing settings services are absent. Main now owns these foundation prerequisites as well as the 19 settings modules. Previously excluded foundation areas may be implemented in the two settings worktrees, not by editing other worktrees. No commit/push/deployment/live secret use authorized. Preserve the verified profile PATCH work.

Baseline: feca8606df82607f8f5fb294d2be6a16d2e095cf. Authoritative specification: docs/specs/S2-auth-capability-manifest.md, ADR-0041 and ADR-0044, current canonical packages/contracts/runtime.ts and install-manifest.ts. Retain all 19 modules in scope; this wave implements prerequisites, not completion of the whole task. UI refinement remains later in design lane.

Ownership and exact interfaces
A. settings-backend owns NEW adapters/identity-supabase/package.json and src/index.ts plus private verifier source if justified, and apps/api/test/identity-supabase.test.ts. No edits to gateway.ts, request-handler.ts, contracts/runtime.ts, root/package lock or existing Access adapter. Main owns those shared integration files.
Export createSupabaseIdentity(env: GatewayEnv, config: SupabaseIdentityConfig): Identity.
SupabaseIdentityConfig contains issuer:string, jwksUri:string, optional fetch:typeof globalThis.fetch, optional now:()=>number (epoch milliseconds). These are trusted server installation inputs, not JWT/bootstrap-derived. Fixed aud authenticated, ES256/RS256 only, <=3600s credential lifetime, 60s skew, 3600s JWKS maximum cache, global 60s unknown-kid refresh cooldown with bounded negative cache and single-flight fetch. Validate configured issuer/JWKS same HTTPS Supabase project; prohibit URL userinfo/query/fragment and follow redirects=false. No arbitrary token-jku network requests. Existing Access behavior is unchanged; do not create a generalized JWT framework or mutable test-only global hooks.
Main supplies gateway export resolveDirectoryActorByAuthSubject(env: GatewayEnv, subject:string, authn: IdentityActor['authn'], credentialIssuedAt:string): Promise<IdentityActor|null>. It matches users.auth_subject, requires active human, checks actor/session revocations, returns canonical roles+team supervision without PII. It never resolves by token email/role. Main supplies contracts/runtime.ts export MfaRequiredError extends Error; HTTP maps to 403 mfa_required.
Adapter calls the gateway resolver only after cryptographic JWT verification and aal2 enforcement. Errors: ActorAuthenticationError401, MfaRequiredError403 for cryptographically valid aal1, ForbiddenError403 for missing/inactive/revoked directory identity, IdentityStoreUnavailableError503 for JWKS/directory/revocation outages. Do not convert outages to invalid credentials or log provider error bodies. revokeAll/revokeSession call existing gateway revocation operations. Export types via the new package root. No direct SQL/driver usage in adapter.

B. settings-design owns apps/client/src/business/**, apps/client/src/main.tsx, apps/client/package.json and apps/client/src/vite-env.d.ts if needed. No edits to existing STT trial implementation/vite proxy, backend/core/contracts, root pnpm-lock, or 3113 static prototype. Reuse existing @ccc/web/wire browser-safe exports and shared CSS, current labels/layout; no visual redesign.
Implement real Community Cloud login+MFA via official Supabase JS client with persistSession:false and detectSessionInUrl:false; no tokens/cookies/localStorage/sessionStorage/IndexedDB persistence and no client-invented role. Only signed manifest supplies Supabase origin/public key. Keep a single auth session/client per loaded installation, clear all loaded business data on logout/session change. Password/refresh/access tokens never logged or placed in URL. Validate manifest and bootstrap with existing contract functions before constructing SDK/network targets. Public signing keys are trusted build-time public config, not fetched alongside an unverified manifest; choose VITE_CCC_INSTALL_SIGNING_KEYS as a JSON public-key map. Missing trust config must fail closed with a real configuration error, not dummy content.
A business transport takes verified installation + access-token getter and only accepts relative API paths constrained beneath verified effective apiBase. Fetch uses Authorization Bearer, credentials:omit, cache:no-store, redirect:error; no silent retries for writes. Preserve safe server error codes and 401/403/409/503 distinctions; never surface arbitrary provider error body text. Validate /capabilities decoder + mode and X-CCC-Installation-Id before further business reads.
Mount the new signed-in business entry on /settings and /login; retain the current STT-trial entry on other paths for now (Main owns later overall route cutover). Initial real connected modules: own profile GET /me, institution GET/PATCH /organization/profile with expectedOrgName and 409 input preservation, institution memory GET/PUT /settings/counseling-memory using the current exact contracts/counseling-memory.ts. Roles are the GET /me roles union, not URL flags or preview roles. Show only actually connected functions in this wave, keep the 19-menu target in the plan; never render dummy operational values or pretend remaining modules are done. Main will connect remaining modules after their real services exist.
Cloud MFA must support selecting an existing TOTP factor and challenge/verify, and first TOTP enrollment when no verified factor exists. Enrollment QR/secret are short-lived auth UI only, cleared on cancel/logout; do not print them to terminal/browser logs. SDK errors must be mapped to safe codes. Local Single/Office login remains a subsequent mode-specific wave, not a fake cloud fallback. Display an honest unsupported-current-runtime reason for those modes until Main adds real local integration.

Validation coordination
Both workers skip ALL test/build/lint/formatter/install/browser/service/commit commands while concurrent. Write behavior tests where failure would defend the specified cryptographic/auth/request-origin or conflict contracts; do not add plumbing/mock echo tests. Main supplies shared exports, installs combined dependencies once, runs tests/typecheck and actual HTTP/browser smoke centrally, and integrates fixes. State exact changes and blockers, no claimed runtime success without Main output. Do not spawn additional agents or panes.
