Organization profile transaction review
Scope: assigned feca860 profile section; gateway.ts:10631-10682, Database contract, D1/PostgreSQL batch implementations, new settings route describe. Auth/HTTP owned by the other reviewer. Source-only review; Main owns fixes and smoke. No source/port/schema edits or commands executed.

P1 T01: PostgreSQL cannot infer the nullable CAS parameter type.
packages/core/src/gateway.ts:10663-10665 has an independent `? IS NULL` binding. packages/contracts/src/sql.ts:79-81 translates it to `$6 IS NULL`, distinct from the column-typed `$5` in `org_name = $5`. db-postgres/src/index.ts:70-74,219-222 forwards raw null/string bindings without a SQL type annotation using extended protocol (line 18). In the supported initial-name path expectedOrgName=null, $6 has no type context. [INFERENCE, not executed] PostgreSQL rejects this statement with 42P18 (could not determine data type of parameter $6), so the initial profile write fails instead of committing. The UPDATE fails before any audit insert; this is availability/parity failure, not partial commit. Minimal profile-only correction: `CAST(? AS TEXT) IS NULL` for the isolated binding, or equivalent portable typed null-safe predicate. No port/schema change needed. Main should exercise null and non-null expectations through the actual PostgreSQL adapter. Reported 15 D1 route tests and API typecheck do not cover this: support/d1.ts:180-188 explicitly uses Miniflare D1.

P2 T02: Concurrent-identical regression does not force the contested CAS path.
apps/api/test/settings-routes.test.ts:183-189 starts Promise.all but never ensures both requests finish the initial SELECT before either batch. It can pass [200,409] solely through gateway.ts:10648 (second request reads the already-changed name), without exercising a losing UPDATE followed by the audit EXISTS/NOT EXISTS branch at 10671-10676. A missing/broken audit NOT EXISTS could therefore escape this test depending on scheduling. Missing adversarial invariant: gate both initial reads at the same version, then release batches; require one successful write, one conflict, exactly one version increment, and exactly one audit attributed to the winning actor. Distinct admin actors with identical old/new names ensure the audit dedup key remains operation/version based, not actor based. Exercise on both adapters; keep instrumentation outside the production port.

Transaction assessment (source reasoning, not runtime proof)
- D1 adapter:253-258 delegates both ordered statements to native d1.batch and preserves RETURNING rows. PostgreSQL adapter:320-344 executes each statement sequentially inside pool.begin, including actor-scoped execution via :250-267; result():113-125 preserves RETURNING rows. Neither splits UPDATE and audit into independently committed calls.
- For identical contenders reading version v, the winning UPDATE locks/changes the row to v+1 and inserts its audit in the same transaction. Under normal PostgreSQL READ COMMITTED visibility, the loser waits, returns no UPDATE row, then sees the committed winner audit. Its identical detail includes nextVersion and orgName (gateway:10654-10656), so NOT EXISTS suppresses a duplicate even for a different actor. D1 serialized batches have the same intended outcome. The post-batch ConflictError is safe here because the losing batch has no mutation. No additional atomicity bug found in this sequence.
- A thrown audit insert error rolls the UPDATE back with the batch. Existing trigger regression :202-210 covers restored name and no audit. Strengthen it to check restored version and updated_at too, then successful retry: no consumed version after failure. Same-value no-op :220-225 should likewise preserve version/updated_at, not merely audit count.
- Database contract :1-19 already exposes ordered results via batch; no new port or schema is required for the correction or these invariants. This review does not request changes to Foundation's reserved boundaries.

Disposition: one concrete PostgreSQL correctness finding and one concurrency-proof gap. Main's reported test/typecheck passes accepted as given, not rerun. No tests, builds, linters, formatters, services, browser, commits, or backend edits performed. Only this requested report created.

Resolution addendum: T01/T02 corrective re-review
Fresh sources: gateway.ts #BFBA; settings-routes.test.ts #F435. Only the two original findings inspected.

T01: RESOLVED IN SOURCE. gateway.ts:10664 now uses CAST(? AS TEXT) IS NULL. This supplies the isolated PostgreSQL placeholder's missing type context, including when expectedOrgName is null. Version CAS, nextVersion-bearing audit detail and NOT EXISTS remain present at :10655-10679. No port/schema change is required. This closes the identified SQL typing defect by source inspection, not PostgreSQL execution.

T02: ORIGINAL BARRIER/ATTRIBUTION GAP RESOLVED IN TEST SOURCE. settings-routes.test.ts:188-199 creates the once() promise before either arrival and waits on it in the injected batch wrapper until the second arrival. Since the profile pre-read precedes DB.batch, neither profile batch can execute before both requests finish their pre-reads. The already-registered shared promise avoids a missed synchronous ready event. :163-167 forwards the injected env, and :203-207 uses distinct actors and asserts exactly one audit belonging to the response winner, with statuses [200,409]. This now targets the losing CAS plus audit-suppression path instead of relying on Promise.all scheduling.

Remaining verification detail, not a newly discovered implementation defect: the test still does not explicitly assert one version increment; the earlier recommendations to check version/updated_at on audit rollback and no-op also remain unimplemented in the inspected cases. The original scheduling and winning-actor attribution defects are closed; this addendum does not claim every suggested invariant was added.

Runtime boundary: no tests/builds or runtime checks performed by this reviewer. Main reports PostgreSQL unavailable (Colima Docker endpoint EOF, default endpoint absent, no local Homebrew PostgreSQL executables); no PG execution claimed or retried. Main reports the D1 real loopback save/read-back/stale409/audit1 smoke passed before the latest strict-auth change, with final smoke being rerun. That earlier result is not represented here as verification of the final source.

Only this report appended. No source edits, new findings outside scope, port/schema changes or production-readiness claim.
