# STATUS — 비영리 사례관리 프로그램

> **이 레포는 2026-07-28 전환으로 새로 시작했습니다.** 옛 히스토리와 상세 이력은 비공개 아카이브 `SocialSolidarityBank/CCC-archive`에 보존돼 있습니다. 이 문서의 과거 기록은 아카이브의 STATUS.md가 정본이며, 여기에는 개인정보가 포함된 원문을 싣지 않습니다. 옛 이슈·PR을 인용할 때는 `CCC-archive#NN` 형식을 씁니다.

<!-- 갱신 형식 (2026-07-31 Q 승인) — Last updated 는 **한 문장짜리 도장**이다.
     서술은 History 맨 위에 행으로만 넣고, 여기에 "앞선 작업:" 으로 이어 붙이지 않는다.
     이유: 한 줄에 누적하면 동시에 일하는 두 세션이 100% 충돌한다(PR #31·#33 이 겹쳐 두 번 손으로 해소).
     History 는 행 단위라 자동 병합된다. 상세 규칙은 ~/.claude/HARNESS.md §3. -->
- **Last updated**: 2026-07-31 (Claude Code — STT 안전장치 3종을 넣었다: 무음 경계 분할 · 반복 붕괴 검사 · 엔진 등록소. 상세는 History 맨 위 행.)

## Next Actions

1. `green` **(완료 — 2026-07-31) STT 안전장치 3종 구현.** D53·ADR-0024 대로 `chunking.py`(무음 경계 분할 — ffmpeg silencedetect, 경계 계산은 순수 로직) · `repetition.py`(반복 붕괴 검사 — 접어서 경고, **지우지 않는다**) · `transcribe.py`(엔진 등록소 + 조각 순회·시각 되돌리기·반으로 잘라 1회 재시도)를 넣고 워커에 연결했다. 설정 4개 신설(`CCC_STT_ENGINE`·`CCC_STT_MAX_CHUNK_SECONDS`·`CCC_STT_MIN_CHUNK_SECONDS`·`CCC_STT_REPEAT_THRESHOLD`). 게이트: `python3 -m unittest discover` **74개 전부 통과**(신규 30개 — 분할 10 · 반복 11 · 오케스트레이션 9, ML·ffmpeg 없이 가짜 엔진으로). `yellow` **구현 중 결함 1건을 잡았다**: 접힌 경고 줄이 그대로 두면 **당사자 발화로 섞여 감정 점수와 역할 추정에 들어간다** — `Segment.warning` 표시를 신설해 `estimate_roles`·감정 집계·전사 라벨 세 곳에서 걸러냈다(R4·D11). `yellow` **실측(G1~G3)은 Q 지시로 뒤로 미뤘다** — 지금 코드는 **실제 녹음으로 검증되지 않았고**, 엔진 기본값도 현행 Whisper 그대로다. 조각 길이 180초·반복 임계 4회는 조사 문서 기반 초기값이며 실측에서 조정한다.
2. `yellow` **다음 덩어리는 LLM 어댑터다 — 설계는 끝났고 구현이 남았다(D51·ADR-0022).** 순서는 의존 관계가 정한다: ① **동의 배선**(화면 D49 ② 체크 → `pilot_text_ai_consent_evidence` 자동 기록) — 이게 없으면 뒤의 모든 초안 저장이 DB 트리거에서 거부된다 ② **텍스트 일감 표 + 계약 확장**(마이그레이션 1개, `docs/api-contract-pipeline.md` 에 상태·엔드포인트 추가, 무폴링 감시 두 큐 합산) ③ **처리 장비의 LLM 어댑터**(호출 2벌 — 승인감/불일치, 프롬프트 버전은 `ai_draft_versions.prompt_version` 에) ④ **STT 가드**(무음 경계 분할 + 반복 검사 + 엔진 추상화, D53·ADR-0024). ①~③ 은 처리 장비 없이도 만들고 테스트할 수 있고, ④ 의 실측(G1~G3)만 장비 앞 세션이 필요하다. `yellow` **Q 액션 1건**: 녹음 캡처 표준화(조용한 상담실 + 테이블 중앙 근접 마이크) — 조사가 CONFIRMED 로 판정한 가장 큰 품질 레버인데 코드가 아니라 운영 지침이다.
2. `green` **(완료 — 2026-07-30) 미리보기 관리자 코드 활성화.** `PREVIEW_ADMIN_ACCESS_CODE` 를 미리보기 워커에 넣었고 관리자 경로가 실제로 열린 것을 실측으로 확인했다(`/preview/unlock` 200 → `GET /me` 가 `role: "admin"` · `account@bss.or.kr` 반환 · 당사자 **22명** 조회, 틀린 코드는 401). 코드 변경 0 — PR #28 이 만들어 둔 스위치를 켠 것뿐이다. `yellow` **Q 액션 2건**: ① 생성된 코드 값을 Infisical 의 기존 `PREVIEW_ACCESS_CODE` 옆에 보관한 뒤 스크래치패드 파일 삭제 ② 외부 테스터에게 **어느 코드를 줄지 결정** — 화면 대부분(D45 브리핑·D47 기록)은 실무자 시점 기준으로 설계됐으므로 관리자 코드만 주면 정작 보여줄 화면을 못 본다. 시점 전환은 주소창에 `/preview` 를 다시 열어 다른 코드를 넣는 방식이고 화면에 전환 버튼은 없다.
   - `green` **(해결) 빈 첫 화면 — 시드 일정을 다시 폈다.** 예정 16건이 전부 과거라 '다가오는 일정'이 양쪽 시점 모두 비어 있었다. `status='scheduled'` 16건만 2026-07-30~08-06 에 하루 2건씩(KST 10:00·13:00) 재배치했다 — `completed` 64건은 **건드리지 않았다**(미래로 옮기면 이미 있는 상담 기록과 어긋난다). 결과: 관리자 오늘 2·다가오는 **16**·당사자 22, 실무자 오늘 2·다가오는 **12**. `yellow` **시드 날짜가 하드코딩 절대값이라 시간이 지나면 또 빈다** — 재보정 방법과 근본 해결(기준일 상대 계산)은 `docs/ops.md` §시드 일정.
2. `green` **UI 수정 스윕 완료 — 다음 덩어리를 고를 차례다.** 레인 A·B·C 는 머지됐고 레인 D 는 1군(생년월일 2곳, PR #25)과 2군(상담 일시 3곳·액션 기한 1곳, **D48**·ADR-0020)을 같은 날 둘 다 끝냈다 — **훑기 결함 R6 은 6곳 전부에서 닫혔다.** 2군은 Q 결정(달력 + 직접 입력 병행)에 따라 공개 부품 `react-day-picker`(MIT, v10)를 달력 알맹이로 쓰며 **이 레포의 첫 화면 라이브러리**다 — 겉모습은 기존 D34 토큰 조합만 쓴다(새 색·반경·그림자 0개). 근거·기각 이유: `artifacts/ui-revision-sweep-v1/lane-d-prep.md` · `docs/adr/0020-date-picker-calendar.md`. 다음 후보는 아래 2·3번이고 2번은 착수 전에 결정이 먼저다.
   - (완료 기록 — 레인 D 1군, PR #25) 착수 당시 배경: 훑기 R6(날짜가 `mm/dd/yyyy` 로 나옴)과 Q 의 '생년월일 한국어화' 요청이 **같은 원인**이다: 네이티브 `<input type="date">` 의 표기는 **보는 사람의 브라우저 UI 언어**를 따르고 페이지 로케일을 무시한다 — 즉 팀원마다 다른 것을 본다. 로케일 설정으로는 안 되고 **부품 교체**여야 한다. 대상 파일이 7개(`wire-form-field.tsx` 포함)로 흩어져 어느 레인에 넣어도 충돌하므로 단독 레인이다(`work-design.md` §3). **2026-07-30 Q 지시: 직접 만들기부터 시작하지 않는다 — 다른 날짜 입력 부품으로 갈아타거나 한국어 사이트에서 실제로 쓰는 방식을 찾아서 넣는다.** 순서: ① 한국 은행·공공·복지 신청 화면의 생년월일 입력 방식 조사(연·월·일 3칸 / `YYYY.MM.DD` 마스크 / 한글 달력 — 생년월일은 달력을 넘겨 찾는 값이 아니라 아는 값을 적는 값이다) ② 공개 부품 비교(표기 안정성 · 한국어 월·요일 표기 · 키보드만으로 입력 · **라이선스 표기 — 미표기 저장소 금지**, 번들 크기, D34 토큰에 얹히는가) ③ 직접 제작은 마지막 선택 ④ 고른 근거와 기각 이유를 착수 문서에 남긴다(되돌리는 비용이 크다). 상세 기준: `artifacts/ui-revision-sweep-v1/lane-b-prep.md` §9-3.
3. `green` **동의 2종 통합 완료(D49 · ADR-0021) — 남은 것은 머지와 사후 확인 2건.** 결정·구현·게이트를 같은 세션에 끝냈다(브랜치 `feat/consent-two-kinds`). `yellow` 남은 사후 확인: ① **화면 실측 스크린샷을 찍지 않았다** — 변경이 체크박스 3→2개와 문안이라 웹 테스트(DOM 단정 228개)로 대체했다. 등록·자기 가입·동의 수정 허브 3화면을 눈으로 볼 필요가 있으면 `ia-shots.py` 를 돌린다 ② **구 3종 데이터의 표시 규칙**(둘 중 하나라도 찍혀 있으면 ② 동의)은 테스트로 고정했으나 운영 D1 에는 아직 그런 행이 없다(마이그레이션 0012~0028 미적용 상태 그대로).
4. `green` **(완료 — 2026-07-30) 테스트 파일 정리 — `afterEach(cleanup)` 누락을 7곳에서 닫았다.** 착수해 보니 **10개가 아니라 7개**였다: `intake-draft` · `record-draft` · `intake-saved-notice` 세 파일은 이미 `afterEach(() => { cleanup(); ... })` 형태로 정리하고 있었고, 앞선 실측이 `afterEach(cleanup)` 이라는 **글자 그대로**만 찾아서 이 형태를 놓쳤다. 실제로 손댄 7개: `settings/page` · `intake-wizard` · `life-area-fields` · `open-action-resolutions` · `risk-banner` · `admin-sidebar` · `list-row`. 그리고 목록을 믿지 않고 `render(` 를 쓰는 테스트 전체를 다시 훑어 `cleanup` 없는 파일이 **0건**임을 확인했다 — 이것이 '전부 닫혔다' 의 근거다. 게이트: `pnpm typecheck` · `pnpm test` 전부 통과(웹 228개 · API 394개), **종료코드 0**. `globals: true` 로 근본 해결하는 별건 판단은 그대로 남는다. 아래는 착수 당시 배경 — 원래 12개였고 `register-form` 은 레인 B 에서, `edit/basic-info-form` 은 **레인 D 1군에서** 해소했다(각 레인이 그 파일을 만졌으므로 그 자리에서 닫았다). 남은 10개는 실측으로 확인했다(전부 아직 없음): `settings/page` · `intake-wizard` · `intake-draft` · `life-area-fields` · `open-action-resolutions` · `record-draft` · `risk-banner` · `intake-saved-notice` · `admin-sidebar` · `list-row`. 근거: 이 누락이 있으면 파일이 끝난 뒤 jsdom 이 내려가는 동안 React 가 남은 작업을 돌려 `window is not defined` 가 터지고 — **테스트가 전부 통과해도 종료코드가 1** 이 된다(CI 는 그 숫자만 본다). 지금까지 '플레이크' 로 넘긴 실패가 실은 이것이다. `apps/web/vitest.config.ts` 에 `globals` 가 없어 자동 정리가 안 걸리므로 파일마다 한 줄이 필요하다 — **`globals: true` 로 근본 해결하는 쪽은 다른 동작에도 영향이 있어 별건 판단**이다. 레인 C 가 새로 만든 테스트 파일들은 처음부터 넣었다.
- `yellow` (대기열) **CCC-42 사후 리뷰의 미반영 잔여 2건** — LOW 2건은 이번 수정에서 뺐다: ① 서버 액션 오류 문구가 원인 불명 실패에도 "권한" 탓으로 표기(`apps/web/app/actions.ts` catch 범위) ② PUT 응답이 `resolvedBy` 포함 전체 객체 반환(브리핑은 의도적으로 미탑재 — 비대칭). 다음에 그 파일을 만질 때 함께. (기존 red 항목 중 **브랜치 보호는 2026-07-30 해소** — `main` 에 `verify`·`pipeline-test`·`secret-scan` 3종을 필수 검사로 걸고 강제 푸시·브랜치 삭제를 막았다. 최신 상태 강제(strict)는 **끄고**, 관리자 우회는 **허용**했다: 전자는 레인 병렬 작업에서 모든 PR 을 매번 다시 맞추게 만들고, 후자는 CI 가 인프라 문제로 멈췄을 때 아무도 머지 못 하는 상황을 막는다. `yellow` 필수 검사는 **잡 이름으로 고정**돼 있어, 워크플로에서 잡 이름을 바꾸면 GitHub 가 없어진 검사를 영원히 기다린다 — 이름을 바꿀 때 이 설정도 같이 고칠 것. **2026-07-30 실측으로 알게 된 것 하나**: PR 의 base 를 바꾸면 `pull_request` 이벤트가 다시 발생해 필수 검사가 **재실행**되고, 그동안은 머지가 막힌다(스택 PR 을 풀 때 겪는다). Actions secret 재등록은 미완이면 유지. CCC-35 는 2026-07-28 로테이션 완료로 Linear Done 확인됨.)

## 전환 요약 (2026-07-28)

- **왜**: 공개 레포의 git 히스토리에 실직원 이메일 커밋이 SHA 직접 조회로 접근 가능했다(과거 가명화가 새 커밋으로만 덮고 히스토리 재작성은 안 함).
- **어떻게**: 구레포를 `CCC-archive`로 이름 변경 + private 전환(무인증 접근 차단) → `origin/main`의 깨끗한 스냅샷을 새 `CCC` 공개 레포로 이전.
- **제외**: STATUS.md(이 파일)·`docs/handoffs/`는 실 이메일·로컬 경로 포함이라 스냅샷에서 제외하고 가명화해 재생성.
- **검증**: 실직원 개인 이메일 0건 · gitleaks no leaks · 첫 커밋 author는 조직 계정 · 무인증 차단 404(아카이브) / 공개 200(새 레포).
- **함정**: 이름 재사용으로 옛 클론의 origin URL이 새 공개 레포로 해석된다 — 모든 옛 클론은 origin을 `CCC-archive.git`으로 재지정하거나 **새로 clone**해야 한다(옛 클론에서 새 CCC로 push 금지).

## Blockers

- 처리 장비 파이프라인(Part B)과 **STT 실측 게이트 G1~G3**(D53)은 해당 하드웨어 위 세션 필요.
- ~~Claude 대조 프롬프트는 상담 템플릿 스펙 확정(D29) 선행~~ `green` **해소(2026-07-31 D52)** — 기록지가 고정 폼이 되어 D29가 보류됐다. 대조 3종·핵심 한 줄·AI 제안·내용 불일치는 항목 풀과 무관하므로 **지금 착수 가능**하다. D29에 매여 있던 것은 상담 템플릿 초안(`aiSchema`) 하나뿐이었고, 그것도 이제 보류다.

## History

- 2026-07-31 (Claude Code): **STT 안전장치 3종 구현 — 무음 경계 분할 · 반복 붕괴 검사 · 엔진 등록소**(D53·ADR-0024, PR #34). 지금까지 `transcribe.py` 는 17줄이었고 오디오를 통째로 Whisper 에 던졌다 — 그런데 이 레포 조사에 실측된 실패가 있다: whisper large-v3 가 34분 대화의 **48%를 같은 문장 254번 반복**으로 잃고 녹음에 없던 문장을 지어냈으며, 같은 구간을 3분으로 잘라 넣으니 반복이 사라졌다(통짜 입력 자체가 원인의 일부). 그 산출물이 승인 화면의 '음성에 없는 내용'에 오르면 **승인=정합성 검증(R2)의 전제가 무너진다**. **반복 구간은 지우지 않고 접어서 경고를 남긴다** — 그 시간대에 엔진이 무너졌다는 사실 자체가 실무자에게 필요한 정보다(판단은 하지 않는다, R5). `yellow` **구현 중 결함 1건**: 접힌 경고 줄이 그대로 두면 **당사자 발화로 섞여 감정 점수와 역할 추정에 들어간다**(화자 분리가 전사 뒤에 돌기 때문) — `Segment.warning` 표시를 신설해 세 곳에서 걸러냈다(R4·D11). 게이트: `python3 -m unittest discover` **74개 전부 통과**(신규 30개 — 분할 10 · 반복 11 · 오케스트레이션 9, ML·ffmpeg 없이 가짜 엔진으로). `yellow` **실측(G1~G3)은 Q 지시로 뒤로 미뤘다** — 지금 코드는 실제 녹음으로 검증되지 않았고 엔진 기본값도 현행 Whisper 그대로다. 조각 180초·반복 임계 4회는 조사 기반 초기값이며 실측에서 조정한다. `yellow` **번호 충돌 1건**: 작업 중 main 이 D50(셸 내비게이션)을 선점해 이 세션의 결정 3건을 D51~D53 으로 밀었다.
- 2026-07-31 (Claude Code): **STATUS 갱신 형식 변경 — Last updated 를 한 문장 도장으로 줄였다**(Q 승인). `Last updated` 한 줄에 "앞선 작업:" 을 계속 이어 붙이는 관행 때문에 **동시에 일하는 두 세션이 100% 충돌**했다 — 오늘 PR #31·#33 이 겹치며 두 번을 손으로 풀었다. 조심해서 피할 수 있는 종류가 아니라 형식이 충돌을 보장했다. 이제 서술은 **History 행에만** 넣는다(행 단위라 자동 병합된다). 버린 체인 ~10,000자는 전부 History 에 이미 있던 사본이며 4건을 대조해 확인했다 — 이번 세션 서술만 History 행으로 옮겼다. 형식 규칙은 STATUS.md 머리의 주석과 `~/.claude/HARNESS.md` §3 양쪽에 박았다. 같은 날 §2 에 "변경 표면에 비례한 검증" 도 함께 넣었다 — 시드 스크립트만 바꾼 PR #32 에 앱 전체 스위트를 두 번(로컬 19분) 돌리고 CI 가 같은 것을 또 30분 돌린 일이 근거다.
- 2026-07-31 (Claude Code): **가상 시드 데이터셋을 20명 → 4명으로 줄였다**(브랜치 `claude/cleanup-fake-datasets-edbfdb`, 커밋 `ff6d5f9`). Q 용도가 "미리보기에서 MVP 확인"이라 20명 규모가 필요 없다. 남긴 4명은 **전원 ai00 담당**이다 — 미리보기 고정 데모 실무자(`PREVIEW_ACTOR_EMAIL`)가 ai00 이고 실무자는 자신이 담당인 케이스만 열 수 있어(D7), 다른 상담사에게 배정하면 화면이 빈다. 기능 커버리지는 유지: 목표 교체(#3) · confirmed 플래그(#4 debt — 리스크 배너 D9 의 유일한 시연) · 미해결/해결 액션 · 추이 3종. 가상 상담사 3명은 배열을 비웠다(담당 0건 이름만 남으므로). 카운트가 전부 `PARTICIPANTS.length` 파생이라 코드 수정은 1줄뿐(방출 문장 수 하한 600 고정 → 참여자당 비례), 나머지는 주석·RUNBOOK 정정. 검증: 시드 생성+재생 검증 스위트 green(방출 180 문장 · 세션 15 · vault 4행 복호화 일치). **Q 지적 2건을 실측으로 갈랐다**: ① "동일 인물 여러명" — 프리뷰 실측 결과 김서준(`crane-001`) 한 명에게 일정이 **9건**(시드는 5건) 붙어 있고 그중 4건이 07-25 이후 **팀 테스트로 생긴 것**이었다. 일정 화면은 일정 1건당 한 줄이라 한 사람이 아홉 번 나온다 — 데이터 손상이 아니라 테스트 흔적이다. 화면에서 직접 등록된 당사자 2명(`crane-002`·`dolphin-002`)도 있다. 그래서 예정 일정을 **사람당 정확히 1건**(08-03·04·05·06)으로 맞췄다 ② "답변·질문도 옛날거 그대로" — 4명의 메모를 사람마다 다른 상황·말투로 다시 쓰고 맞춤형 질문을 1개에서 4명 전원으로 늘렸다. **프리로드 SQL 산출물 신설**: `seed.sql` 은 계정을 담지 않아(게이트웨이 방출 쓰기만) 빈 DB 에 그대로 얹으면 로그인 신원이 없어 화면이 안 열린다 — `preload.sql` 을 먼저 적용하는 경로를 만들고 RUNBOOK 5장(미리보기 초기화)을 신설했다. `green` **프리뷰 D1 초기화·재주입 완료**(Q 승인). 롤백 북마크 `0000006a-00000000-000050b8-94222ebba6f4f099150177a93488a5fd`. 적용 후 실측 — 당사자 4 · 참여사업 4 · 세션 15 · 일정 19(예정 4, **사람당 1건**) · 확인된 플래그 1 · users 8(담당 0건이던 가상 상담사 3명 소멸) · 마이그레이션 27개 전부 적용(운영 D1 보다 앞선다). 새 데이터가 프리뷰 키로 복호화되는 것까지 확인. `yellow` **중간에 오진 하나** — 시드를 만들 키를 찾다가 `PII_ENC_KEY`(운영) 만 보고 "Infisical 프로젝트에 닿지 못한다" 고 판단했는데, Q 가 시크릿 이름을 알려줘 **`PREVIEW_PII_ENC_KEY` 가 같은 프로젝트에 따로 있다**는 것이 드러났다. 접근이 아니라 **이름** 문제였다. 함정·판별법을 `~/developer/tools/portwright/services/infisical.md` 에 기록(값 노출 없이 복호화 성공 여부로만 키를 가리는 절차 포함). `yellow` **부분 삭제는 영원히 불가능**하다(동의·감사 append-only 트리거 → 참조된 당사자도 삭제 불가) — 프리뷰 정리는 앞으로도 초기화 경로뿐이고, 초기화하면 `audit_log` 1,564행도 함께 사라진다(가상 데이터라 무해). `yellow` 남긴 4명은 전원 ② 동의라 미동의 수기 폴백(D5) 경로는 프리뷰에서 안 보인다.
- 2026-07-31 (Claude Code): **셸 내비게이션 5건 — 홈·로그아웃·사업 전환기·뒤로가기·'고정' 제거**(브랜치 `feat/sidebar-nav-controls`, **D50**·ADR-0014 개정 2~4). ① Q 요청 5건을 D35 축(사이드바=장소 / 우상단=행동)을 깨지 않고 넣었다 — 셋은 '장소'이고 뒤로가기는 축 바깥의 '되돌리기'다. ② **가장 값진 발견은 착수 전 조사에서 나왔다**: 이 앱에는 **나가는 길이 아예 없었다**(`로그아웃`·`logout` grep 0건). 그리고 `/preview` 가 셸 안에서 렌더돼 **코드 입력 화면에 사이드바가 그대로 떠 있었다** — 로그아웃을 만들어도 나갔는데 안 나간 것처럼 보였을 것이라, 같이 고쳤다. ③ **리뷰가 잡은 HIGH 1건**: `.narrow` 는 `--page-max` 를 `.page-content` **자기 자신에** 얹으므로 형제인 뒤로가기 줄이 물려받지 못한다 — 좁은 화면(960 vs 1120)에서 '뒤로'가 제목보다 **80px 왼쪽**에 섰고 대상이 10개 화면이었다. `:has(+ .page-content.narrow)` 로 맞추고 브라우저에서 두 값이 960 으로 같아진 것을 실측했다. ④ **리뷰가 잡은 MEDIUM 2건도 반영**: (가) 사업 전환기를 `role="listbox"`/`option` 으로 적었다가 **되돌렸다** — ARIA 는 option 안에 링크를 금지하고 listbox 는 화살표 이동을 기대하게 만드는데 구현하지 않았다. 역할을 참칭하지 않고 `aria-current` 로 알린다 (나) 뒤로가기가 히스토리 없을 때 홈으로 가게 했는데, `/` 는 **리다이렉트** 경로라(히스토리를 갈아치운다) 링크로 연 사람은 같은 화면으로 되돌아와 아무 일도 없는 것처럼 보인다 → **버튼 자체를 그리지 않는다.** ⑤ 검증: `pnpm typecheck` ✓ · **웹 241 통과**(신규 13) · `pnpm build` **종료코드 0** · 헤드리스 실측 — 홈 버튼(`/settings`→일정) · 뒤로가기(일정→설정→뒤로) · 좁은 화면 정렬(둘 다 960, 글자 좌표 동일) · `/preview` 셸 없음 · 모바일 드로어 정상. `yellow` **못 한 실측 1건**: 히스토리가 1인 상태(링크를 새 탭에서 직접 열기)는 브라우저 창이 항상 빈 탭을 먼저 만들어 재현되지 않았다 — 단위 테스트로만 덮었다. ⑥ 곁가지: `ccc_preview` 문자열이 세 곳에 흩어져 있어(rename 하면 로그아웃이 조용히 깨진다) 의존성 없는 `app/lib/preview-cookie.ts` 로 모았다.
- 2026-07-31 (Claude Code): **LLM 데이터 처리 방식 · 녹음 전사 STT 그릴링 — D51~D53 확정, 코드 변경 0.** 워크트리 `ccc-llm-stt-implementation`. 착수 전 실측 3건이 논의의 형태를 바꿨다: ① **LLM 거버넌스 층은 이미 다 있다** — 마스킹 스냅샷·사업자 등록·초안 버전·승인·근거 검증·핵심 한 줄·구조화 제안·불일치 저장이 구현돼 있고 **LLM 사업자를 부르는 코드만 0건**이다(`purge_due`·`consent_text_ai_at` 과 같은 모양) ② **동의 확인 장치가 서로 모르는 채 둘 있다** — 화면 D49 2종은 녹음 업로드만 막고, AI 초안을 막는 것은 별도 표(0026 트리거)라 **지금 호출을 붙이면 동의를 다 받은 케이스도 저장이 거부된다** ③ **상담 기록지는 이미 고정 폼**이라 D29 보류가 문서 정리로 닫힌다. 결정: **D51** AI 호출 위치 = 처리 장비(R3 2차 마스킹이 거기에만 있고 불일치 검출은 수기 메모 저장 시점에도 돈다) + 텍스트 일감 표 신설 + 동의 배선 연결 + 호출을 승인 단위로 둘로. **D52** 기록지 고정·D29 보류 → §8 항목 풀 미결 해소. **D53** STT 는 엔진 교체보다 가드 먼저(실측된 반복 붕괴 48%·254회와 지어낸 문장이 승인 화면에 오르면 R2 전제가 무너진다), 엔진 확정은 G1~G3 실측으로 유예. 용어 **처리 장비** 신설 — 문서는 "Mac Mini", 실제는 WSL2 노트북으로 갈라져 있었고 용어집에 그 장비를 가리키는 말이 없어 여태 안 걸렸다. 산출물: ADR-0022·0023·0024, CONTEXT.md(처리 장비·일감·동의 근거 신설 + 세션·전체 목표·상담 템플릿·항목 풀 정정), CLAUDE.md §2·§5·§8·§9, ADR-0006 보류 헤더, `docs/api-contract-pipeline.md`·`docs/ops.md` 용어 정리.
- 2026-07-31 (Claude Code): **심사위원용 미리보기 마무리 — 빈 화면 해결 + 전체 목표 편집을 관리자에게 개방**. ① **빈 첫 화면(Q 보고 "안 불러와진다")의 원인은 코드가 아니라 날짜였다** — 예정 상담 16건이 전부 과거라 '다가오는 일정'이 비었다. `status='scheduled'` 16건만 **2026-07-31~08-21 에 걸쳐** 앞으로 폈고 `completed` 64건은 **손대지 않았다**(미래로 옮기면 이미 있는 상담 기록과 어긋난다). **한 주가 아니라 3주로 편 이유**: '다가오는 일정'이 8일 창이라 촘촘히 몰아 두면 그 주가 지나는 순간 또 빈다 — 심사 날짜를 모르므로 뒤엣것이 차례로 창에 들어오게 뒀다(현재 오늘 1·다가오는 8·당사자 22). 스키마 CHECK 가 `scheduled` 행에는 세션이 붙어 있지 않음을 보장해서 딸려 오는 것이 없다. `yellow` **트리거 함정**: `counseling_schedules_update_guard` 가 `OF` 절 없이 모든 UPDATE 에 걸려 있어 `version=version+1` 을 같이 쓰지 않으면 거부된다. ② **심사위원에게 코드 하나만 주기 위해 D45 를 개정했다**(Q 결정) — 조사 결과 관리자 시점은 실무자 화면을 **거의 전부 포함**하는데(사이드바 동일·브리핑·기록 전부 열림·당사자 22명, 거기에 설정·`/admin`·온보딩이 추가) 딱 하나, **전체 목표 카드의 입력·수정 버튼**만 `role === 'counselor'` 로 막혀 있었다. 그 한 줄 때문에 코드를 둘로 나눠 줘야 하는 상황이라, 편집 권한을 **담당 실무자 + 기관 관리자**로 넓혔다(`canEditOverallGoal` + 쓰기 가드 2곳). 층으로 봐도 이 편이 일관된다 — 같은 화면의 '내용 불일치' 처리 권한이 이미 담당 실무자·기관 관리자다. ③ 기존 테스트 2개(admin 은 403·`canEdit=false`)를 **지우지 않고 새 동작으로 뒤집었다**(admin PUT 200·`canEdit=true`), 뒤 단정이 값을 이어 쓰므로 각각 원복 단계를 넣었다. ADR-0018 에 개정 절, CLAUDE.md §6 3번에 반영. ④ `yellow` **시드 날짜는 하드코딩 절대값이라 또 낡는다** — 재보정 절차와 근본 해결(기준일 상대 계산 + `generate.ts` 의 `UPCOMING_FROM` 동시 수정)을 `docs/ops.md` §시드 일정에 남겼다.
- 2026-07-30 (Claude Code): **미리보기 관리자 코드 활성화 — 외부 테스터에게 건넬 준비**(브랜치 `claude/ccc-external-tester-setup-219296`). ① **요청은 "테스터 계정과 코드를 새로 만들어 달라"였지만 만들 것이 없었다** — PR #28 이 관리자 시점을 코드까지 다 만들어 배포해 뒀고 `wrangler secret list --env preview` 실측 결과 **시크릿 값 하나만** 빠져 있었다. 그래서 **코드 변경 0**, 스위치를 켜는 작업이 됐다. ② **"기존 계정에 관리자 권한 추가"는 하지 않았다** — 시드 실무자(`ai00@ggbss.or.kr`)의 role 을 바꾸면 팀원이 쓰는 실무자 시점이 사라진다(PR #28 이 코드를 둘로 가른 이유가 그것이다). ③ **착수 전 실측이 위험 하나를 지웠다**: 관리자(`8fd733ce…`)의 `support_case_assignees` 배정이 **0건**이라 화면이 빌 것을 의심했는데, 게이트웨이가 admin 을 배정 검사에서 **면제**한다 — `assertSupportCaseAccess`(`gateway.ts:6417`) 조기 반환, 일정(`:8882`)·당사자 목록(`:7834`)·D36 허브 게이트(`:7650`) 전부 admin 분기에 배정 JOIN 이 없다. 실제로 **22명 전부** 조회됐다(실무자는 9명). ④ **값이 stdout 에 닿지 않게 넣었다**(HARNESS §2): 스크래치패드 파일로 생성 → `wrangler secret put ... < 파일` stdin 주입. 검증도 상태 코드와 `role` 만 봤다. ⑤ 검증: 시크릿 3개 등록 확인 · 관리자 코드 언락 200 · `GET /me` → `role: "admin"` · 당사자 22명 · **틀린 코드 401**. **재배포는 필요 없었다** — `PREVIEW_ADMIN_ACTOR_EMAIL` 은 `[env.preview.vars]` 값이라 배포 때 실리는데, `/me` 가 `account@bss.or.kr` 를 돌려준 것이 그 값이 **이미 워커에 살아 있다는 증거**다(파일에 있다는 사실이 아니라 이 응답이 근거다). `yellow` **실무자 코드는 시험하지 못했다 — 이것은 열린 확인 항목이다.** Infisical 주입(`infisical run`)으로 값을 보지 않고 시험하려 했으나 CLI 가 대화형 로그인을 요구했고(머신 아이덴티티 자격증명이 이 셸에서 안 먹었다), 우회하려면 값을 stdout 에 내는 플래그가 필요해 **중단했다**(§시크릿 규칙). 실무자 코드 **경로의 코드는 이번에 바뀌지 않았고** `resolveSigningCode` 가 그 코드를 먼저 본다 — 다만 그 함수는 이제 매 언락마다 관리자 시크릿도 함께 읽으므로 **"구조적으로 회귀 불가"라고까지는 말할 수 없다.** Q 가 기존 코드로 한 번 들어가 보는 것이 확인 방법이다. ⑥ **곁가지 발견 — 시드 일정이 전부 과거다**: `counseling_schedules` 80건의 최신이 `2026-07-28` 이라 '다가오는 일정'이 **양쪽 시점 모두** 빈다. 데이터가 아니라 날짜 문제이고(당사자·기록은 그대로) D1 업데이트만으로 고쳐진다 — 외부에 건네기 전 권고 사항으로 `docs/ops.md` §알려진 제약에 남겼다. ⑦ 문서: `.dev.vars.example` 에 관리자 변수 2개 추가(이름만) · `docs/ops.md` 프로비저닝 4번에 관리자 시크릿 + stdin 주입법 추가.
- 2026-07-30 (Claude Code): **테스트 정리 — `afterEach(cleanup)` 누락을 7곳에서 닫았다**(브랜치 `fix/test-cleanup-sweep`). ① **개수가 10이 아니라 7이었다**: 앞선 실측이 `afterEach(cleanup)` 이라는 **글자 그대로**를 찾는 방식이라, 같은 일을 하는 `afterEach(() => { cleanup(); window.localStorage.clear(); })` 형태를 못 봤다. `intake-draft` · `record-draft` · `intake-saved-notice` 세 파일이 그 형태로 이미 정리하고 있었다 — **손댈 필요가 없었다**. ② 실제로 고친 7개: `settings/page` · `intake-wizard` · `life-area-fields` · `open-action-resolutions` · `risk-banner` · `admin-sidebar` · `list-row`. 각 파일에 `afterEach` · `cleanup` import 를 더하고 import 블록 바로 아래에 `afterEach(cleanup);` 한 줄을 넣었다(레포의 기존 패턴 그대로 — `record-list.test.tsx` 등). ③ **왜 필요한가**: 이 한 줄이 없으면 파일이 끝난 뒤 jsdom 이 내려가는 동안 React 가 남은 작업을 돌려 `window is not defined` 가 터지고, **테스트가 전부 통과해도 종료코드가 1** 이 된다(CI 는 그 숫자만 본다). 지금까지 '플레이크' 로 넘긴 실패의 정체다. ④ **목록을 믿지 않고 다시 셌다**: 물려받은 목록이 이미 3건 틀렸으므로, `render(` 를 쓰는 테스트 파일 전체에서 `cleanup` 이 없는 파일을 훑었다(`grep -rl "render(" apps/web/app apps/api --include="*.test.ts*"` → 각 파일에 `cleanup` 유무 확인) — **결과 0건**. 이것이 '전부 닫혔다' 의 근거다. ⑤ 게이트: `pnpm typecheck` 통과 · `pnpm test` 전부 통과(웹 30파일 228개 · API 31파일 394개, **종료코드 0**). `yellow` 다만 **이 통과가 고침의 효과를 증명하지는 않는다** — 원래 증상이 간헐적이라(그래서 '플레이크' 로 읽혔다) 고치기 전 실패 기준선이 없다. 근거는 메커니즘과 레포의 기존 관행이다. ⑥ 남는 별건: `apps/web/vitest.config.ts` 에 `globals: true` 를 켜서 파일마다 한 줄 없이 자동 정리되게 하는 근본 해결은 **다른 동작에도 영향이 있어 별도 판단**으로 남겨 뒀다.
- 2026-07-30 (Claude Code): **미리보기에 관리자 시점 추가**(PR #28, 브랜치 `feat/preview-admin-tester`). ① **문제**: Q 가 관리자 화면을 볼 테스터 계정을 요청했는데, 미리보기 시드에는 **관리자 계정이 이미 있었다**(`account@bss.or.kr`, org `bss` — 이름이 비어 있어 `기관 관리자 (테스터)` 로 채웠다). 없던 것은 계정이 아니라 **들어갈 길**이다 — 코드 게이트가 지정 코드 하나로 모두 고정 데모 실무자(`PREVIEW_ACTOR_EMAIL`) 시점을 주기 때문이다. 그 값을 관리자로 바꾸면 팀원 피드백용 실무자 화면이 사라진다. ② **해법**: 코드를 하나 더 두고 **어느 코드로 들어왔는지로 신원을 가른다**. `red` **토큰 형식은 안 바꿨다** — 세션 토큰은 원래부터 발급에 쓰인 코드를 HMAC 키로 서명하므로, 리졸버가 코드별로 갈라 검증하면 신원이 갈린다. 토큰에 역할을 적어 넣으면 위조 표면이 는다. ③ **안전장치 3개**: **(가)** 코드와 이메일이 **둘 다** 있어야 열린다 — 반쪽 설정에서 열어 주면 실무자 이메일로 관리자 코드가 통해 의도하지 않은 경로가 생긴다 **(나)** 두 코드를 **항상 둘 다** 비교하고(일찍 빠져나오지 않는다) 실패는 401 하나로만 답한다 — 응답 시간·메시지로 "관리자 코드가 있다"가 새지 않게 **(다)** 이중 잠금(`PREVIEW_MODE` + Access 미설정)은 그대로라 **운영에는 영향이 없다**(fail-closed). ④ 테스트 7개 추가(총 15개) — 그중 하나는 **관리자 코드로 받은 쿠키가 관리자 경로가 진 env 에서 거부되는지** 본다(토큰이 코드에 묶여 있다는 증거). ⑤ `red` **머지 후 Q 액션**: 시크릿 값은 에이전트가 만들지도 보지도 않는다(값이 로그·채팅에 닿으면 안 된다) — Q 가 `wrangler secret put` 으로 직접 넣는다. ⑥ 곁가지: `search-input.tsx` 의 "'date' 는 비제어 전용, 제어형 경로를 먼저 만들어야 한다" 주석이 D48 이후 사실과 달라져 정정했다.
- 2026-07-30 (Claude Code): **동의 2종 통합 — D49 결정 + 구현**(브랜치 `feat/consent-two-kinds`, `docs/adr/0021-consent-two-kinds.md`). ① **결정 전 실측이 질문을 바꿨다**: D15 의 분리 동의 근거는 미동의 경로("녹음 미동의여도 수기 메모 텍스트 AI 정리 제공")인데, `consent_text_ai_at` 을 **읽는** 곳이 화면 표시와 이력 조회뿐이었다 — 게이트는 `consent_recording_at` 하나(`gateway.ts:4823` + 전사 후보 조회 3곳). 즉 그 경로는 구현된 적이 없어 합쳐도 **잃는 기능이 0**이다. Q 에게 이 사실과 함께 물었고 '그대로 합친다'를 선택했다(사라지는 것은 미래의 중간 상태다). ② **DB 변경 0**: 3컬럼·이력 표를 그대로 두고 ② 한 체크가 `consent_recording_at`·`consent_text_ai_at` 에 **같은 시각**을 찍는다(0008·0014 insert 가드가 "NULL 아닌 동의 시각 = recorded_at" 을 요구해 값이 갈리면 트리거가 거부한다). 이름은 인테이크가 이미 쓰던 `recordingAi` 로 통일했다. ③ **바꾼 표면**: 게이트웨이 4경로(등록·동의 수정·두 번째 사업·자기 가입) · API 요청 파싱 4곳·응답 2곳 · 웹 클라이언트·서버 액션 · 화면 4곳(등록·자기 가입·허브·인테이크 읽기) · 문안 2곳(정본 `consent-draft-v0.md` + 화면 `consent-copy.ts`, **DRAFT 배지 유지** — D46 동결 조건) · 킷·시드. ④ **함께 닫은 구멍**: `createSupportCase` 가 `consentPrivacy` 만 받아 **두 번째 참여 사업에서 ② 를 기록할 경로가 없었다**(사업을 만든 뒤 허브에서 따로 고쳐야 했다) — `consentRecordingAi` 를 선택 인자로 넓히고 **재생 해시에 넣었다**(같은 제출 id 로 동의만 바꾼 재시도가 조용한 재생이 되면 안 된다). ⑤ `yellow` **불변식이 성립하지 않는 레거시 경로 1개를 ADR 에 이름으로 남겼다**: `POST /cases`(구 케이스 생성)는 두 시각을 각각 받아 갈린 상태를 만들 수 있다 — 사람이 지나는 길이 아니라 구 데이터 호환용이라 건드리지 않고, 아래 표시 규칙이 그 상태를 흡수한다. ⑥ **구 3종 데이터 표시 규칙 신설**: `recording !== null || textAi !== null` 이면 ② 동의로 읽는다. `yellow` 구 '텍스트 AI 만 동의' 행은 화면상 녹음까지 동의한 것처럼 보이지만 **녹음 게이트는 컬럼을 직접 보므로 녹음이 열리지는 않는다** — 이 두 가지를 테스트로 고정했다(구 '항목별 분리' 테스트를 이것으로 대체). ⑦ 게이트: `pnpm typecheck` ✓ · **API 394 ✓**(신규 2건: 두 번째 사업의 ② 기록 · ② 생략 시 미동의) · **웹 228 ✓**(신규 3: 동의 수정 허브 체크박스 이름·개수·초기 체크) · `pnpm guard:db` ✓ · `pnpm build` ✓. `yellow` **실측 스크린샷은 찍지 않았다** — 변경이 체크박스 개수·문안이라 DOM 단정으로 대체했고, 등록 폼·허브 테스트에 '구 체크 2개가 사라졌다'를 명시적으로 넣었다. **허브에 테스트를 새로 만든 이유**: 체크박스 `name` 이 서버 액션이 읽는 키와 어긋나면 오류가 아니라 **조용한 철회**가 저장된다(`checkbox` 헬퍼는 키가 없으면 false) — 철회가 사는 유일한 화면이라 이름을 DOM 으로 못박았다. `yellow` **시드 스크립트는 `pnpm typecheck` 범위 밖이다**(루트 tsconfig 가 `db/**` 만 include) — 임시 프로젝트 설정으로 따로 컴파일해 확인했고, 남은 오류 4건은 이 변경과 무관한 기존 것들이다(변경 전 5건).
- 2026-07-30 (Claude Code): **레인 D 2군 — 상담 일시·기한 4곳에 달력을 달았다**(브랜치 `feat/lane-d-calendar`, **D48** · `docs/adr/0020-date-picker-calendar.md`). ① **결정**: Q 가 "달력을 쓰되 직접 입력도 되게" 를 골랐다 — 이것은 KRDS 요구이기도 하다(*"날짜 선택기를 사용하는 경우에도 입력 필드를 사용 불가나 읽기전용 상태로 변경하지 않고 키보드를 이용하여 직접 입력할 수 있도록 한다"*). 그래서 달력은 입력칸을 **대체하지 않고 옆에 붙는다** — 본체는 1군이 만든 `DateTextInput` 이다. ② **부품 선택**: 달력 알맹이는 **`react-day-picker`(MIT, v10.0.1, npm 레지스트리 실측)** 이고 **이 레포의 첫 화면 라이브러리**다(지금까지 `wire/` 부품은 전부 손으로 만들었다). 직접 만들지 않은 이유는 **키보드 방향키 격자 이동과 스크린 리더 낭독**이 손으로 만들 때 가장 조용히 틀리는 영역이기 때문이다(Q 선택). `yellow` ADR 이 이를 **선례가 아니라 예외**로 못박았다 — 판단 기준은 "접근성처럼 손으로 만들면 조용히 틀리는가". ③ **겉모습은 새 값 0개**: 팝오버=`--panel`·`--radius-card` 12·`--shadow-soft`·z 30(드롭다운 층), 선택일=`--blue-base` **면** + `--ink` 글자(**읽는 값에 `deep` 글자를 쓰지 않는다** — `--blue-deep` 은 흰 위 대비 2.47 로 미달이고 §9 예외는 보조 정보 한정), 그라데이션 테두리 금지(§5 락). 전부 기존 D34 토큰 조합이다. ④ **새 부품** `components/wire/date-picker-control.tsx` — `DatePickerControl`(날짜) · `DateTimePickerControl`(날짜+시각). 달력에 시간이 없어 상담 일시는 **칸이 둘**이 되고 합친 값을 숨은 칸으로 낸다 — **제출 형식이 예전과 같아 서버 액션·게이트웨이·마이그레이션 변경 0**. ⑤ **구현 중 잡은 함정 3개**: **(가)** `toISOString()` 을 쓰면 한국 시간 자정 직후가 **전날**이 된다 — 직접 포맷하고 테스트로 고정했다. **(나)** 폼 임시본(`useDomDraft`)은 **이름 있는 칸만** 저장·복원하므로, 보이는 두 칸에도 이름(`heldAtDate`·`heldAtTime`)을 줘야 상담 일시가 조용히 사라지지 않는다(서버 액션은 읽는 키만 꺼내 쓴다). **(다)** `wire-styles.ts` 는 자바스크립트 템플릿 문자열이라 **CSS 주석에 백틱을 쓰면 문자열이 끊긴다**(심각도 표기를 그대로 옮기다 겪었다). ⑥ 곁가지: 1군에서 '비제어 전용'으로 남겨 뒀던 `DateTextInput` 에 **제어형 경로를 만들었다** — 달력이 값을 밀어 넣어야 해서 이번에 실제로 필요해졌다. 게이트: `pnpm typecheck` · 웹 테스트 **221개 통과**(새 15개) · `pnpm build` 전부 통과.

- 2026-07-30 (Claude Code): **레인 D 1군 — 생년월일 날짜 칸을 글자 입력으로 교체**(PR #25, 근거 문서 `artifacts/ui-revision-sweep-v1/lane-d-prep.md`). ① **원인**: 네이티브 `<input type="date">` 는 제출값(`YYYY-MM-DD`)과 보이는 글자가 다른 부품이고, **보이는 글자는 보는 사람의 브라우저 UI 언어**를 따른다(MDN 명시). 훑기에서 본 `mm/dd/yyyy` 는 헤드리스 브라우저 기본값이었고 실제로는 **팀원마다 다른 것을 본다** — CSS·`lang`·로케일로는 못 고친다. ② **조사에서 나온 것**: Q 지시("한국어 사이트에서 실제로 쓰는 방식을 찾아라")대로 찾다가 **KRDS(대한민국 정부 디자인 시스템)에 `날짜 입력 필드`·`달력` 규격이 있고 둘의 사용 구분 기준까지 적혀 있는 것**을 발견했다. KRDS 는 **생년월일을 단일 입력 필드**로 지정하고("사용자가 이미 알고 있는 날짜"), 년/월/일 3칸은 "가장 효율성이 낮은 입력 방식"이라 하며, 달력은 "멀리 떨어진 날짜나 **요일 정보가 중요한 상황**"에 쓰라고 한다. ③ **구현**: 새 부품 `components/wire/date-text-input.tsx` 하나를 등록·수정 두 화면이 공유한다 — `type="text"` + `inputMode="numeric"` + 자동 하이픈(`19850327`→`1985-03-27`, 점·슬래시 붙여넣기도 받음) + `pattern` 검증 + KRDS 요구대로 **칸 아래 도움말**(자리 표시자는 입력하면 사라지므로) + `aria-describedby` 연결. **제출값이 예전과 글자 그대로 같아 서버·게이트웨이·마이그레이션 변경 0**(서버는 이미 같은 정규식으로 검증한다). ④ **2군 4곳은 이 PR 에서 손대지 않았고, 같은 날 Q 가 방향을 정했다** — 달력 + 직접 입력 병행, 달력 알맹이는 공개 부품 `react-day-picker`(MIT). 새 D 번호 + ADR 이 선행한다(다음 할 일 1번). ⑤ 곁가지: `basic-info-form.test.tsx` 에 `afterEach(cleanup)` 추가(다음 할 일 3번이 11개→10개). `yellow` **발견**: D37 이 말한 `guard:layout` 스크립트가 **레포에 없다**(`package.json`·CI·git 훅 어디에도) — 이 레인 범위 밖이라 기록만 남겼다. 게이트: `pnpm typecheck` · 웹 테스트 **206개 통과** · `pnpm build` 전부 통과.
- 2026-07-30 (Claude Code): **레인 B 마무리 정리**. PR #21(당사자 등록 화면 8건, `973805e`)이 `origin/main` 에 들어간 것을 확인하고 작업 공간을 걷었다 — 워크트리 `.worktrees/lane-b-register` 제거 · 로컬/원격 브랜치 `fix/lane-b-register-form` 삭제 · 커밋하지 않은 QA 스크린샷 3벌(`artifacts/ia-shots/after-b*`, 30 MB) 삭제(Q 승인, `scripts/design/shots.py` 로 재생성 가능). `yellow` 정리 시점에 이 레포에는 레인 D 워크트리(`.worktrees/lane-d-dates`)가 새로 생겨 있어 CCC-new 의 워크트리 수는 3개 그대로다 — 기계 단위 상한(4개) 초과는 아직 해소되지 않았고, 줄이는 길은 삭제가 아니라 머지다. **UI 수정 스윕은 레인 A·B·C 머지 완료, 레인 D 하나만 남았다.**
- 2026-07-30 (Claude Code): **남은 RED 3건 중 ②·③ 실행**(문서 §10). ① `green` **user-scope MCP 9개 → 4개** — 근거를 먼저 만들었다: 최근 2주 세션 기록 605개에서 **도구 정의 노출이 아니라 실제 호출**(`"name":"mcp__<서버>__"`)만 셌다. `composio-composio` 254회 · `pencil` 244회 · `refero` 8회는 유지, **호출 0회 4개 제거**(`agentmemory`·`memory-bank`·`aside`·`react-grab-mcp`). `context7` 은 0회지만 **유지** — 워크스페이스 규칙이 SDK 문서 조회 경로로 지정한 도구라 없애면 다음 작업의 정확성이 떨어진다. 절감은 **세션당 57.6 MB**(세션 6개면 약 350 MB)이고 `yellow` **MB보다 큰 이득은 컨텍스트**(매 요청 도구 스키마 4벌 제거)다. `red` **수치 정정**: 처음 `ps | grep agentmemory` 로 **903 MB**라고 보고했는데 그 값에 **Claude 데스크톱 앱 프로세스(254+247 MB)**가 섞였다 — 같은 서버가 CLI·데스크톱 양쪽에 등록돼 있다. CLI 세션 프로세스 트리로 귀속시켜 다시 세면 호출 0회 5개가 **세션당 75.6 MB**다. **교훈: 이름 grep 이 아니라 세션 프로세스 트리로 센다.** ② `green` **동시 상한을 기계 단위로 재정의** — §6-2 의 '워크트리 3개'가 레포 단위여서 새어 나갔으므로 두 층에 걸었다: **규칙**은 `~/.claude/CLAUDE.md`(레포 밖 세션도 읽는 유일한 자리, §8 과 같은 이유)에 `## 동시 실행 상한 — 기계 단위` 신설 — **CLI 세션 동시 4개**, 워크트리도 **기계 전체 합산**, 넘기려면 GUI 앱 먼저 닫기. **강제**는 `~/.claude/hooks/session-count-guard.sh`(SessionStart) — 실제 대화 세션만 세고(`claude mcp` 등 단발 서브커맨드 제외) 초과 시 경고만 한다(차단 아님, `CLAUDE_SESSION_CAP` 조정). 검증 2회 ✓(상한 이하 무출력 / 상한 1로 낮춰 경고 발화). ③ `green` **워크트리 실측·정리 846 MB 회수** — `bss-web-controller` 6개 중 머지 완료 2개 제거(`ticket-08` 439M·`wordpress-site-controller` 406M), `.claude/worktrees` **1.2 G → 369 M**, 체크아웃 7→5. **브랜치는 보존**(폴더만 제거). 제거 전 확인: 미커밋은 `.omc/{state,sessions}`·`.claude/` 뿐(`.omc/skills/` 없음) · stash 0 · 6시간 활동 0 · 붙은 세션 0. `red` **첫 집계 한 줄이 틀렸다** — `git branch --merged main | grep` 느슨한 매칭이 `ggbss-web-business-apply` 를 '머지됨'으로 읽었고, `git rev-list --count main..<브랜치>` 로는 **미포함 커밋 1개**다(제거 대상에서 뺐다). `yellow` **아직 상한 위**: 기계 전체 워크트리 **6개**(CCC-new 2 + bss 4)이고 남은 4개는 전부 진행 중(커밋 10·61·5·1) — **줄이는 길은 삭제가 아니라 머지**다. 훅은 세션 수만 보므로 이 초과는 잡지 못한다. ④ `red` **남은 것은 ① GUI 앱 약 15 GB**(Figma 4.4·Chrome 4.1·`2.1.220` 2.2·Notion 2.0·Codex 1.2·Claude 데스크톱 1.9 / RAM 24 GB) — **이번에 줄인 전부를 합쳐도 이 한 항목의 6%가 안 된다.** 안 쓰는 앱 닫기는 에이전트가 대신할 수 없다. 곁가지: `agentmemory` 가 **Claude 데스크톱에도 등록**돼 있다(이번 범위 밖).

- 2026-07-30 (Claude Code): **보존·파기 파이프라인 실측 — 전날 기록한 `red` 를 정정**. Q 가 `wrangler login` 을 해 준 뒤 확인한 사실 4가지. ① **Cloudflare 계정은 개설돼 있다**(`account@bss.or.kr`), 운영 워커 `ccc-api` 마지막 배포 **2026-07-15**, 파기 크론 `0 3 * * *` 설정 존재 — "계정 미개설이라 안 돌 것"이라던 추측은 틀렸다. ② **그런데 파기는 일어날 수 없다**: `purge_due` 에 값을 넣는 코드가 레포 전체에 **0건**이고 모든 쓰기가 `purge_due = NULL` 이다. `closeSupportCase` 도 설정하지 않는다 → **D10 의 보존 시계가 애초에 시작되지 않는다**. ③ **운영 D1 실측**(counts only, PII 미조회): `participant_pii_vault` 22행 전부 미파기 · `purge_due` 보유 **0** · 도래 **0** · 종결 케이스 **0** · 데이터 생성 2026-07-15~17. ④ 곁가지 `yellow`: 운영 D1 이 마이그레이션 **0012~0028(17개) 미적용**(운영 워커는 Access·fail-closed 로 잠겨 실사용 경로는 아님). **결론**: 데이터가 지워질 위험은 현재 없다 → 등급 `red`→`yellow`. 재개 시 할 일도 바뀐다 — "아카이브를 추가한다"가 아니라 **보존 시계 설정 + 아카이브 전환 + 5년 상한 검토 큐를 한 묶음으로 구현**하고 G2 법적 보류를 그 위에 얹는다. 게이트 문서 §5 의 G2 착수 함정도 정정했다(`closeSupportCase` 로도 `purge_due` 픽스처를 만들 수 없어 보존 시계 구현이 **선행**이다). 반영: `CLAUDE.md` §8·D46, `docs/consent/consent-implementation-gates-v1.md` §5.
- 2026-07-30 (Claude Code): **같은 날 재발 재조사 + 자동 색인·자동 유도 전면 비활성화**(문서 §9). ① **사실관계**: `last reboot` 기준 하루에 비정상 재부팅 **3회**(01:23 · 07:49 · 14:16), 셋 다 `shutdown` 기록 없음(마지막 정상 종료는 7/19, 7/19~7/29 재부팅 0회). 01:23 은 커널 패닉 **`watchdog timeout: no checkins from watchdogd in 90 seconds`**(멈춤 계열). `yellow` **14:15 다운의 직접 증거는 없다** — 패닉 파일 없음 + 직전 시각대 통합 로그 0줄(이전 부팅 로그 미보존) → 강제 재시작 추정. ② **09:58 JetsamEvent 실측(프로세스 903개 이름별 합산)**: 전체 **32,691 MB / RAM 24,576 MB**, 압축기 7,917 MB가 25,961 MB를 물고 있었다. 실제 킬은 `spotlightknowledged.updater`·사유 `per-process-limit` 로 **전역 OOM 킬은 아니다**. ③ **끄려던 대상은 범인이 아니었다**: `codebase-memory-mcp` 6개 **합계 31 MB** · `codegraph` 프로세스 **0개**(§6-3 범위 축소가 먹혔다) · Grep 훅 `hook-augment` 1회 **0.00초/2.1 MB** · launchd 색인 등록 **0건**. 지배적 비용은 **`node` 81개 = 3,867 MB(user-scope MCP 8개)** 와 **GUI 앱 약 15 GB**(Figma 4.4·Chrome 4.1·`2.1.220` 2.2·Notion 2.0·Codex 1.2·Claude 데스크톱 1.9). 세션당 약 645 MB인데 `yellow` 이건 시작 비용이 아니라 **누적**이다 — 갓 뜬 세션 2개는 합계 212 MB. ④ `red` **왜 또**: §6-2 동시 3개 상한을 **CCC-new 레포에만** 걸었고, 그 사이 **`bss-web-controller` 가 워크트리 6개**로 늘었다(`.claude/worktrees/`). 14:14~14:15 살아 있던 세션은 CCC-new 3 + bss-web-controller 3 = **6개**. **상한은 레포가 아니라 기계 전체 세션 수에 걸어야 한다.** 곁가지: 그 6개 경로가 대소문자 혼재(§6-3 '알려진 빈틈' 그대로). ⑤ `red` **지난 조치가 놓친 훅 2개** — 설치 스크립트가 심은 것은 3계열이었고 §7은 하나만 고쳤다: **`cbm-code-discovery-gate`(PreToolUse `Grep|Glob` — 검색마다)** · **`cbm-subagent-reminder`(SubagentStart — 서브에이전트마다, 팬아웃하면 지시도 곱해진다)**. 비용은 작지만 **지시**가 해롭다. `red` **자기 증식 고리 발견**: 하드 크래시 → 색인 db 손상(`CCC-new.db.corrupt` 12.6 MB) → 다음 세션이 손상 감지 → **전체 재색인**(10:55 `CCC-new.db` 13.9 MB 재생성) → 다시 부하. 07:49 재부팅 → 09:58 압박 → 10:55 재색인이 그 순서다. ⑥ **Q 지시("요청할 때만") 실행 6건**: cbm 훅 등록 **6건 전부 제거**(파일은 복구용으로 보존) · **`rtk hook claude` 훅 제거** + `@RTK.md` 임포트 주석화(바이너리 유지 — 이번 조사에서 rtk가 `find` 출력을 눌러 색인 시각을 오독하게 만들었다) · `claude mcp remove codebase-memory-mcp -s user`(9→8, 재색인 고리 차단) · `~/.claude/CLAUDE.md` 의 "prefer codegraph over native search" 절을 **'요청 시에만'** 으로 전면 교체 · **`codemap` 스킬 description 을 명시 호출 전용으로 축소**(`분석해줘`·`구조 분석` 등 광역 트리거 제거 — 이게 애초 발화 경로였다) · **`codebase-memory` 스킬 런타임 링크 제거**(벤더 원본 보존). 백업 4개(`~/.claude/backups/` 2개는 600·레포 밖, 스킬 사본 2개는 `docs/postmortems/backups/`). ⑦ **하지 않은 것 2건**: **codegraph 프로젝트 등록 10곳 유지** — 등록은 지연 기동이라 부르는 지시가 사라진 지금 **이미 요청 시에만** 동작하고, 유휴 15~38 MB·CPU 0.0% 대비 지우면 10개 레포 기능을 잃는다(§6-3 결정을 몇 시간 만에 뒤집는 것). **understand-anything 은 끌 것이 없다** — launchd 0·산출물 0·`/understand` 명시 호출 전용. ⑧ **검증 6건 ✓**: `settings.json` cbm·rtk 참조 0 / JSON 유효·훅 12종 유지·**보안 가드 2종(secret-dump-guard·gh-account-guard) 보존** / user-scope MCP 8개 / 스킬 목록에서 `codebase-memory` 소멸·`codemap` 새 description 반영 / 복구용 훅 파일 3개 잔존 / `yellow` `codebase-memory-mcp` **2개(각 2 MB) 잔존은 등록 제거 전 시작된 기존 세션 2개**의 서버(종료 시 소멸, 신규 세션 미부착). `yellow` 미검증 1건: 새 세션 첫 화면·컴팩션 재주입은 다음 새 세션에서 확인. ⑨ `red` **남은 진짜 지렛대 3건(§9-6, 실행 안 함 — Q 결정)**: **(가)** GUI 앱 약 15 GB — 단일 최대 효과, 안 쓰는 앱 닫기 **(나)** user-scope MCP 8개를 실사용만 남기고 프로젝트 범위로 내리기(세션당 수백 MB) **(다)** 동시 세션 상한을 **기계 단위 3~4개**로 재정의(현 규칙은 이를 표현하지 못한다).

- 2026-07-30 (Claude Code): **`/codemap` 사용 후 orca-claude 멈춤 원인 조사 — 진단만, 변경 0건.** 정본 `docs/postmortems/2026-07-30-codemap-overload.md`. ① **codemap 스킬 자체는 무겁지 않다**(64줄 라우터, 코드를 훑지 않음). 무거워진 건 라우팅이 고른 백엔드(`codebase-memory`)가 미설치라 **에이전트가 그 자리에서 설치로 간 곁길**이고, 그 설치가 `--skip-config` 를 무시해 타 런타임 설정 9개를 고쳐 쓴 사고다(2026-07-29 15:22~15:35, 세션 `a6944e18`). ② **멈춤의 지배적 원인은 codemap이 아니라 동시성이다** — 전 세션 타임스탬프를 기계 계산해 *여러 세션이 같은 시각에 동시에* 멈춘 구간만 신호로 봤더니, 07-29 **11:32·11:36·11:46 에 세션 3개가 동시에 45~57분** 멈췄다. codemap 실행 **3시간 50분 전**이다. 조건: 워크트리 **7개 · 8.6 GB · 각자 `node_modules` · 워크트리당 파일 약 4만**(레포 전체 걷기 24만), 2분 안에 세션 4~5개 동시 기동. ③ **남은 상시 비용 2건**: 설치 도구가 심은 SessionStart 훅 `~/.claude/hooks/cbm-session-reminder`(모든 세션에 "코드 탐색은 codebase-memory-mcp 먼저" 주입 — 원복 대상에서 빠졌다) + 겹치는 코드 색인 MCP 서버 2개(`codegraph`·`codebase-memory-mcp`). "세션마다 259 MB가 뜬다"는 **사실이 아니다** — `ps` 확인 결과 등록만으로는 프로세스가 뜨지 않는다(지연 기동). ④ **측정 못 한 것**: 멈춤 당시 메모리·CPU 증거는 없다(현재 관련 프로세스 0, 워커 로그 358 B). ⑤ 곁가지 2건: `.gitignore` 에 **`.worktrees/` 가 없다**, `~/.claude/skills/codemap` 이 심볼릭 링크가 아닌 실디렉터리(스킬 SSOT 규칙 위반). **권고는 Q 결정 대기** — 멈춤 대책은 워크트리 동시 실행 상한 + 끝난 워크트리 4개 정리(약 7 GB), 재발 방지는 codemap SKILL.md 에 "라우팅 중 도구 설치 금지, 미설치면 보고·폴백만" 명시. ⑥ **Q 지시 반영해 같은 문서에 `## 6. 구현 방향` 추가**(계획만, 실행 0건) — 훅 조항 제거·동시 워크트리 상한·codegraph 범위 축소·스킬 게이트 4건을 각각 무엇/왜/검증/되돌리기로 정리하고 실행 순서를 제안했다. 이때 Q의 전제 1개를 정정했다: **훅이 인덱싱을 직접 돌리는 게 아니다**(스크립트는 텍스트 `cat` 뿐) — 대신 훅 본문 3번 줄이 *"인덱스 없으면 `index_repository` 부터 실행"* 을 **모델에게 지시**하고, 발화 조건이 `startup/resume/clear/compact` 라 **컴팩션마다 재주입**된다. 결론(훅 강제 조항 제거)은 그대로이고 근거가 더 강해졌다. 또 `codebase-memory-mcp` 는 07-29 15:22에야 존재했으므로 11:32 동시 멈춤의 원인일 수 없고, CCC-new 에는 `.codegraph` 인덱스가 아예 없어 codegraph 도 이번 사건과 무관하다 — codegraph 기본 비활성은 **다른 8개 인덱스 보유 프로젝트를 위한 위생 조치**로 등급을 내렸다(데몬 여부는 문서상 동작이고 미실측 — `[추론]` 표기). ⑦ **Q "진행" 승인으로 안전한 2건 적용·검증**(문서 §7): **(가)** codemap `SKILL.md` 에 설치 금지 게이트 — Step 0 은 "색인은 사용자 승인 후에만", 'Graceful degradation' 은 제목부터 **NEVER install anything** 으로 금지형 재작성(설치·다운로드·MCP 설정 편집·`index_repository` 전부 금지, 폴백만) + 사고 근거 인용. **(나)** `~/.claude/hooks/cbm-session-reminder` 를 **조건부 정보문**으로 교체 — `ALWAYS`·`run index_repository FIRST` 삭제, **인덱스가 이미 있는 프로젝트에서만 출력**(cwd → `~/.cache/codebase-memory-mcp/<slug>.db` 존재 검사, 없으면 무출력 `exit 0`). 훅 4회 직접 실행 검증 ✓(CCC-new 출력·강제조항 0건 / 워크트리·홈 무출력). 사본 2개 `docs/postmortems/backups/`. **(다) 중단 2건**: 스킬 SSOT 이동은 `codemap` 이 자체 `.git`+리모트를 가져 `mine/`(중첩 레포) vs `repos/`(**런타임 미링크 → 스킬 소실**) 결과가 갈려 멈췄다 — `~/.claude/skills/cockpit` 도 같은 위반이라 **레포형 스킬 배치 규칙 자체가 결정 사항**이다. 워크트리 정리는 실측이 초안을 뒤집어 승인 대기: `git branch --merged main` 기준 머지된 것은 **`intake-wizard`·`plan-revision` 2개뿐**, `consent-g1`(8커밋)·`consent-g2`(13)·`consent-g3`(11)·`hero-register`(3)·`ui-revision`(2, 미커밋 92건) 은 지우면 유실이다 → **회수 가능 용량은 약 7 GB가 아니라 171 M(안전)~1.8 G**. 나머지 8.2 GB는 삭제가 아니라 머지 속도 문제다. ⑧ **Q 결정 2건 반영·실행**: **(가)** 워크트리는 **`plan-revision` 1개만 제거**(안전 확인 5개 통과 — 미커밋 0·main 미포함 커밋 0·머지됨·stash 0·붙은 세션 없음). `.worktrees` **171 MB 감소**, 7개 → 6개, **브랜치 `plan/revision` 은 보존**(폴더만 제거). `.gitignore` 에 `.worktrees/` 가드 추가(`git check-ignore` 적중 ✓, 남은 워크트리 `git status` 정상 ✓) — 등록된 워크트리는 git 이 이미 무시하므로 **지금 새는 걸 막는 게 아니라 등록 풀린 잔여 폴더의 `add -A` 사고를 막는 가드**다. **운영 규칙 기록: 워크트리 동시 3개 상한.** **(나)** 스킬 배치는 **현 위치 유지 + 예외 2건 등록**(`codemap`·`cockpit` 둘 다 실디렉터리) — 레포형 스킬의 링크 규칙은 **별도 과제**로 분리했다(`repos/` 의 `portwright`·`im-not-ai`·`hermes-agent` 가 어느 런타임에도 링크돼 있지 않아, 규칙을 잘못 정하면 스킬이 조용히 사라진다). ⑨ **6-3 codegraph 데몬 실측 완료 — 등급 `yellow` → `red` 상향, Q의 원래 진단이 맞았다.** ① `codegraph serve --help` 에 **`--no-watch`(파일 감시 끄기) 옵션이 따로 있다** = 감시가 기본 켜짐인데, 현재 등록 `{"args":["serve","--mcp"]}` 에는 **없다**. ② 인덱스 있는 프로젝트에서 20초 띄워보니 **한 호출에 프로세스 5개가 사슬로** 뜬다(래퍼→node→네이티브 node→`--path` 작업 프로세스 128 MB→**watchdog**: 메인 스레드 60초 무응답 시 부모 `SIGKILL`). ③ 테스트와 무관하게 **이미 떠 있던 것: 프로세스 15개 · 총 RSS 322 MB · 최상위 사슬 4개(부모 PID 4개 = 세션 4개) · 최장 생존 1시간 17분.** **세션 하나당 사슬 하나이고 세션이 끝날 때까지 상주한다.** ④ **내 앞선 진술 정정**: "등록만으로는 안 뜬다(지연 기동)"는 맞지만 **"그래서 쌓이지 않는다"는 틀렸다** — 한 번 뜨면 상주하고 세션 수만큼 곱해진다. 0개로 보였던 건 그 시점에 아무 세션도 호출하지 않았을 뿐이다. **즉 "세션마다 자동 부착 + 동시 다발 = 곱셈"은 codebase-memory 가 아니라 codegraph 에서 사실이며, 이 항목만 §3 의 동시성 가설을 프로세스로 증명한다.** ⑤ 처방이 하나 늘었다: **범위 축소(인덱스 있는 8개 프로젝트로 한정) 먼저** — CCC-new 에는 `.codegraph` 가 없어 **답할 데이터도 없는데 데몬이 뜨므로 잃는 기능이 0이다** — 그다음 **`--no-watch` 추가**(감시 끄기, 대가는 수동 재색인). 둘은 배타적이지 않다. ⑩ **Q 승인으로 6-3 1단계(범위 축소) 적용 완료.** `~/.claude.json` 은 실행 중 세션들이 계속 쓰는 208 KB·키 204개 라이브 파일이라 **손으로 고치지 않고 공식 `claude mcp` CLI로** 처리했다(사본 `docs/postmortems/backups/claude.json.pre-codegraph-scope`). `remove codegraph -s user` → user-scope 10개 → 9개, 인덱스 보유 프로젝트 **10곳**에 `add codegraph -s local` → project-scope 10건. `yellow` **정정**: 앞서 "인덱스 보유 8개"는 얕은 탐색 오류로 틀렸다 — `BSS/bss-wiki`·`BSS/edu-content-pipeline` 을 놓쳤고 **실제 10곳**이다. **검증 4건 ✓**: user-scope 제거 확인 / project-scope 10건 확인 / `claude mcp list` 가 CCC-new(인덱스 없음)에서 codegraph **미표시** / archive-mcp(인덱스 있음)에서 `✔ Connected`. `yellow` **알려진 빈틈**: `.claude.json` 프로젝트 키가 대소문자 변형으로 중복 존재해(`developer/projects/bss/...` vs `DEVELOPER/PROJECTS/BSS/...`) 세션이 다른 표기로 열리면 codegraph 가 조용히 안 붙는다 — 고장은 아니고 그 폴더에서 `claude mcp add` 한 번 더 하면 된다(변형 키 선제 추가는 추측이라 안 했다). ⑪ **6-3 후속 — Q가 내 추천을 뒤집었고 그게 맞다.** 나는 "인덱스 없는 3곳은 등록에서 빼자"고 했는데 Q가 *"오히려 돌려서 커밋해두는 게 낫지 않을까, 모두 사용 레포야"*. 내가 갖지 못한 사실(그 3곳이 실제 사용 레포)이 판단을 뒤집는다 — 인덱스 없음은 "안 쓰는 레포"가 아니라 **아직 색인 안 했다는** 증거였다. `yellow` 단 **"커밋"은 구조상 불가능**: codegraph 가 `.codegraph/` 안에 `*`+`!.gitignore` 짜리 `.gitignore` 를 직접 넣고 그 주석이 *"local to each machine, **not for committing**"* 이다(추적 파일 0건, 색인 후 `git status` 노출 0건). 인덱스는 **기계별 로컬 캐시** — 만들어 두는 건 되고 커밋해 두는 건 안 된다. **먼저 치운 것**: `bss-wiki`·`edu-content-pipeline` 의 `.codegraph` 는 디렉터리가 아니라 **`~/.omo/codegraph/...` 를 가리키는 끊긴 심볼릭 링크**였고 **`~/.omo` 자체가 사라져 있다**(OMO 제거 흔적) — 그래서 "인덱스 있음"으로 보이면서 데이터가 0이었다. 죽은 링크 2개 제거 후 색인: `bss-wiki` 116파일·2,298노드·561ms·4.7MB / `edu-content-pipeline` 13파일·112노드·120ms·0.5MB / `oh-my-openagent` **168.6MB**. **검증**: 등록 10곳 **전부 실제 db 보유(10/10)** ✓ · `git status` 노출 0건 ✓ · **고아 데몬 0개** ✓ · 현재 13프로세스·68MB ✓. **"등록됐는데 답할 데이터가 없는 프로젝트"는 0이 됐다.** ⑫ **수치·결론 정정 2건**: **(가)** "고아 프로세스"는 **항상 생기지 않는다** — 정상 종료 경로(`claude mcp list`, `codegraph init` 3회) 전부 고아 0개였고, 앞서 본 고아는 `sleep 20 | codegraph serve --mcp` 파이프 끊김이라는 **인위적 경로** 때문이었다(등급 하향). "세션이 사는 동안 사슬 1개 상주"는 유효(최장 1시간 56분 관측). **(나)** "이미 떠 있던 것 15개 **322 MB**"는 **내 테스트 프로세스 약 120 MB를 포함한 과대 수치**다 — 실제 정상 상태는 12~13개·60~150 MB, **세션당 약 15~38 MB**. `--no-watch` 실익 판단은 이 수치로 한다. ⑬ **`--no-watch` 는 하지 않는다 — 6-3 종결(Q 판단 위임 → 실측 근거로 결정).** ① **감시 자체가 싸다**: 상주 사슬 하나가 **15~38 MB · CPU 0.0%**. 앞서 나온 128~268 MB 는 전부 **색인 중** 수치였다(`--path` 워커 128 MB, `oh-my-openagent` 색인 워커 268 MB). ② **끄면 없어지는 건 감시가 아니라 자동 갱신**이다 — help 가 `--no-watch` = `no auto-sync` 로 못 박으므로 대가는 "코드 고칠 때마다 수동 재색인"이고 아끼는 메모리보다 크다. ③ **대상 분포가 결정적**: 등록 10곳 중 **8곳이 5~7주째 쉬는 중**이라 바뀌는 파일이 없어 감시해도 재색인이 돌지 않는다(비용 ≈ 0). 활발한 건 `ggbss-web` 1곳. ④ **이번 멈춤의 원인은 감시가 아니었다** — 원인("인덱스도 없는 프로젝트에서까지 세션마다 데몬")은 범위 축소로 이미 해소됐고, 감시 끄기는 원인과 무관한 추가 대가다. `yellow` **다시 열 조건**: `ggbss-web` 작업 중 체감 부담이 생기면 **감시가 촉발하는 부분 갱신(`sync`)의 실제 수치를 측정**한다(이번에 안 재본 유일한 값 — 측정한 128~268 MB 는 전체 색인 `init`) → 크면 **`ggbss-web` 한 곳만** 적용, 10곳 일괄은 근거 없음. **남은 것은 워크트리 2건뿐**: `intake-wizard` 제거는 미커밋 1건 확인 후 · 나머지 5개(8.2 GB)는 삭제가 아니라 머지로. `yellow` **커밋 시 `docs/postmortems/backups/` 2개를 반드시 포함**한다(훅 주석이 되돌리기 원본으로 그 경로를 가리킨다), 로컬 `main` 이 `behind 12` 이므로 `pull` 선행.

- 2026-07-30 (Claude Code): **UI 수정 레인 B 나머지 — 당사자 등록 화면 8건 수정** (워크트리 `.worktrees/lane-b-register`, 브랜치 `fix/lane-b-register-form`). R7 은 이미 PR #16 으로 머지됐으므로 **리베이스가 아니라 `origin/main` 에서 새로 뗐다**(`fix/hero-mobile-and-register` 는 다 쓴 브랜치다 — Next Actions 2번의 "리베이스" 지시를 이 사실로 대체한다). ① **훑기 결함 4건**(`artifacts/ui-revision-sweep-v1/findings.md`): **Y6** 폼 전체를 `WireCard` 안으로 + 풀폭 제출 버튼을 콤팩트 알약으로 · **Y7** `가입하기` → `등록하기`(자기 가입 폼 `join/participant` 는 '가입하기' 가 맞아 건드리지 않았다) · **Y8** 이름·이메일·연락처 라벨에 `wire-form-required` 별표 · **Y10** 동의 블록을 배경 한 톤(`--muted`) + legend 민트로. ② **같은 날 Q 요청 3건**(훑기 목록 **밖** — diff 를 findings.md 와 맞춰 볼 사람을 위해 구분해 적는다): 참여 사업을 select → 고정 표시 조각(초대 시점에 정해지는 값이다) · 성별을 생년월일 위로 · **서명 동의서 첨부 자리**(라벨 + '준비 중' 배지만, `<input type="file">` 없음). ③ **Y10 은 원안을 바꿔 적용했다** — 원안의 `--shadow-soft` 는 Y6 로 폼이 카드에 들어가면 **카드 안에 또 카드**가 되어 카드 계약(`wire-card.tsx`)·동의 수정 허브(`wire-styles.ts` '동의 3종 수정')와 어긋난다. 실제 앱 CSS 로 3안을 그려 Q 에게 보였고(`artifacts/ui-revision-sweep-v1/lane-b-y10-options.html`, 생성기는 스타일 문자열을 파일에서 뽑아 인라인 — 손으로 옮겨 적지 않는다) **흰 카드 위 흰 상자라 그림자가 거의 보이지 않는다**는 것이 시안에서 드러나 Q 가 추천안을 골랐다. ④ **파일 소유 함정 1건**: `work-design.md` 의 "교집합 0" 은 화면 `.tsx` 끼리만 비교한 것이고, 실제 CSS(`.wire-register-form`·`.consent-fieldset`·`.wire-register-submit`)는 **`layout.tsx` 에 있으며 자기 가입 폼·동의 수정 허브와 공유**한다. 그래서 새 규칙은 전부 레인 B 소유인 `wire-styles.ts` 에 넣고 등록 화면 전용 클래스로 범위를 좁혔다 — 선택자에 두 클래스를 겹친 이유는 `registerStyles` 가 `wireStyles` 보다 **뒤에** 합쳐져 같은 특정도면 저쪽이 이기기 때문이다. ⑤ `search-input.tsx` 의 `label` 타입을 `string` → `ReactNode` 로 넓혔다(필수 별표는 라벨 안에서 색이 달라져 문자열로 표현할 수 없다). 타입 확장뿐이라 **기존 호출부 24곳(9파일: 당사자 필터·자기 가입 폼·admin 초대·admin 배정·admin 사용자·kit·상담 등록 위저드·온보딩 위저드·이 화면)은 전부 문자열을 넘기고 그대로 유효하다**(전수 grep + typecheck ✓ — 넓힌 쪽이라 깨질 수 없다). **이 파일은 레인 소유 목록에 없다** — 레인 A 는 머지됐고 C 는 D47 대기라 지금 충돌 상대가 없다는 판단으로 손댔다. ⑥ 곁가지: `register-form.test.tsx` 에 `afterEach(cleanup)` 추가(Q 승인) — Next Actions 1번의 12개 목록 중 **1개 해소, 11개 남음**. ⑦ 게이트: typecheck ✓ · 웹 테스트 **179 ✓**(신규 6건: 카드·고정 표시·필드 순서·필수 별표·업로더 비활성·Y10 범위) · `pnpm build` ✓ · **실측 ✓** — 로컬 프리뷰를 띄워 `ia-shots.py after-b` 로 21화면 × 2뷰포트를 찍었고 **비200·콘솔 오류 0건**, 1440·390 양쪽에서 8건이 의도대로 렌더(390 에서 1열·버튼 1줄·넘침 없음). PNG 는 **커밋하지 않는다**(레포 관례 + 2026-07-29 Q 방침). `yellow` **로컬 프리뷰 절차 함정 재확인**: `scripts/seed/out/local-preload.sql` 은 시드 생성이 만들어 주지 않아 `preload-data.ts` 의 `preloadStatements()` 에서 구워 넣어야 한다(`docs/ops.md` 함정 ② 그대로 — 문서가 맞다). `yellow` **화면에서 확인된 다음 세션 거리 1건**: 생년월일 칸이 `mm/dd/yyyy` 로 렌더된다 — Q 의 '한국어화' 요청과 훑기 R6 이 같은 원인이며 레인 D 로 묶었다. 계획·다음 세션 묶음(동의 2종 통합 = **결정 필요, 마이그레이션 불필요** · 업로더 본체 · 레인 D)은 `artifacts/ui-revision-sweep-v1/lane-b-prep.md` §7~§9. ⑧ **레인 C 머지 뒤 리베이스·재검증**(2026-07-30): 레인 C(#20·#22)를 리뷰하고 머지한 뒤 이 브랜치를 `origin/main` 위로 리베이스했다. `wire-styles.ts` 를 양쪽이 건드렸으나 **위치가 달라 자동 병합**됐고(레인 C 는 파일 앞쪽 `surface-card[open]`, 이쪽은 끝쪽 등록 화면 전용 규칙), 손으로 푼 것은 `STATUS.md` 뿐이다. 합친 상태 게이트: typecheck ✓ · **API 385 ✓ · 웹 191 ✓**(185+6) · build ✓ · 실측 재실행 ✓ — 등록 화면은 리베이스 전과 동일하게 렌더되고(레인 C 의 `surface-card[open]` 규칙이 이 화면의 `details` 로 **번지지 않는다** — 그 `details` 는 `consent-detail` 이라 카드 표면이 아니다), 레인 C 의 상담 기록 화면도 정상 렌더를 확인했다. `yellow` **실측 함정 1건 기록**: `ia-shots.py` 의 `SHOT_CASE` 기본값이 하드코딩된 UUID 라, 시드를 재생성한 로컬에서는 케이스 의존 화면 4개(기록·브리핑·인테이크·기록 작성)가 **200 을 반환하면서 본문만 "확인할 수 없습니다"** 로 찍힌다 — 상태 코드만 보면 통과로 보인다. 로컬 D1 에서 실제 케이스 ID 를 뽑아 `SHOT_CASE` 로 넘겨야 그 화면들이 실제로 검증된다. `yellow` **API 테스트는 로컬 `wrangler dev` 가 떠 있으면 조용히 중단된다**(vitest-pool-workers 와 충돌) — 실측용 서버를 내린 뒤 돌려야 한다.
- 2026-07-30 (Claude Code): **레인 C 구현 — 상담 기록 화면 (D47 · ADR-0019).** 워크트리 `.worktrees/lane-c-records`. ① **API**: 조회 응답에 `aiOneLiner`(0025)·`memoExcerpt`·`sessionGoals`·`overallGoal`·`caseStatus`·`programType` 추가. **새 마이그레이션 없음** — 저장된 값을 싣기만 한다. 세션 목표는 일정 쪽 `schedule_session_goals`(SSOT) → 없으면 `record_details.sessionGoalNote`(0016) 순으로 읽고, 완료 일정 ID 로만 좁혀 쿼리한다. 전체 목표·상태 태그를 위해 `assertSupportCaseAccess` 를 한 번 더 부르는데 **그 함수는 감사를 쓰지 않아** 이 화면의 read 감사는 한 건 그대로다(D14). ② **조회 화면**: 브레드크럼 제거 + `ParticipantHeroCard`(R2·R3 해소), 전체 목표 읽기 전용 한 줄, 회차는 `details` 로 접어 **최신 1개만 펼침** — 서버 렌더만으로 성립하고 브리핑 앵커 진입 시의 추가 펼침만 `RecordHashOpener`(클라이언트)가 맡는다. GAS 섹션·'읽기 전용 목표' 카드 제거(R1 해소), 그 자리에 세션 목표. 표현은 `record-list.tsx` 로 갈랐다(브리핑이 `briefing-cards.tsx` 를 가른 것과 같은 이유 — 테스트 가능성). ③ **작성 화면**: GAS 아코디언·'목표 종료+신설' fieldset 제거, `goals` prop 까지 제거. ④ **색**: D34 축 그대로 안 쓰던 자리를 채웠다 — 유형 칩(블루 tint+`--ink`) · 담당 칩(민트) · `AI 제안`(라벤더) · 확인된 플래그만 레드 · **펼친 회차 카드에 그라데이션 테두리**(DESIGN.md §5 '활성일 때만'의 첫 사용처, `surface-card[open]` 로 기존 규칙에 얹었다). `red` **구현 중 결함 1건 잡아 수정**: 1024 폭에서 이 화면만 가로 스크롤(1198>1024). 원인은 **그리드 아이템의 자동 최소 크기가 min-content** 라 접힌 줄의 nowrap 전체 길이가 컨테이너 바닥이 된 것 — 한 줄의 `min-width:0` 으로는 안 풀리고 `.record-list` 와 `.record-list>details` **두 층**에 줘야 한다. 게이트: typecheck ✓ · API 385 ✓ · 웹 185 ✓(회차 목록 12건 신설, `afterEach(cleanup)` 포함) · build ✓ · **레이아웃 실측 8화면 × 5폭 = 40조합 위반 0건**(`artifacts/layout-measure/d47-records-fix/`) · 앵커 진입 실측(3회차 링크 → 최신+그 회차만 열림) · 실제 화면 1440·390 스크린샷(`artifacts/d47-records-screen-v1/app-*.png`). `yellow` **ADR 과 달라진 것 2건은 ADR 에 반영해 뒀다**: 폴백 배지가 `승인 대기` 가 아니라 `수기`(조회 API 에 승인 대기 여부가 없어 '녹음 없음'까지 승인 대기로 보이게 된다), 섹션 제목 옆 카운트 배지는 만들지 않음(그 값이 브리핑 응답에만 있다 — 덕분에 ADR 이 걱정한 배지 중복이 사라졌다). 곁가지로 작성 화면의 `전체 목표 N` 카운트도 내렸다(세던 것은 보류된 **세부 목표** 개수였다). `yellow` 시드에 승인 AI 초안·세션 목표가 없어 **그 두 재료가 있는 화면은 눈으로 못 봤다** — 단위 테스트로만 고정돼 있다.
- 2026-07-30 (Claude Code): **그릴링 4차 — D47 상담 기록 화면 정리 (ADR-0019).** 워크트리 `.worktrees/lane-c-records`. 이 그릴링은 **새 결정보다 미이행 결정을 닫는 쪽**이 컸다 — UI 훑기의 `red` 6건 중 3건(R1 GAS 잔존·R2 HERO 없음·R3 브레드크럼·가명 ID 노출)이 이 화면 하나에 몰려 있었고 셋 다 D43·D38·D35·D31 위반이었다. D45 구현이 브리핑만 손댄 결과다(`briefing-cards.tsx` 에는 "GAS 는 D43 보류로 뺐다" 주석까지 있는데 `records/page.tsx` 는 안 건드렸다). 확정 6건: ① 최신 회차만 펼치고 나머지는 접힌 한 줄, 브리핑 앵커(`#record-{회차ID}`)로 들어오면 그 회차가 펼쳐진 채 열린다 — **브리핑 영역 ②가 한 줄 목록이면 이 화면은 그 줄을 펴서 보는 곳**이라는 분업 ② GAS 자리에 **그 회차의 세션 목표**, '읽기 전용 목표' 카드는 제거(존재 이유가 GAS 기준 제시였다) ③ HERO 버튼은 `당사자 정보` + 인테이크 유무로 갈아끼우는 프라이머리 — 지금 3개까지 뜨던 것을 D38 의 2개 제한 안에 넣는다 ④ 접힌 줄 `N회차 · 날짜 · 유형 · 핵심 한 줄`(오름차순, 인테이크가 1회차) ⑤ **리스크 배너를 두지 않는다** — D9 배너는 '상담 5분 전' 장치라 브리핑 계약이고 이 화면은 대개 브리핑을 거쳐 오므로 같은 경고가 두 번 나온다 ⑥ 작성 화면의 GAS 입력과 '목표 종료+신설' **둘 다 제거**(D43 이 보류한 것은 두 층 다). **Q 지시로 색을 적극 활용**하되 D34 축은 바꾸지 않고 안 쓰던 자리를 채웠다 — 블루=유형 칩·완료 액션 / 민트=세션 목표 블록·담당 칩 / 라벤더=`승인 대기`·`AI 제안` / 레드=확인된 플래그 / **그라데이션=펼친 회차 카드 테두리**(DESIGN.md §5 '활성일 때만' 규정의 첫 사용처). 시안을 만들며 **제 시안의 계약 위반 1건을 잡아 고쳤다** — '제외됨' 플래그에 리스크 레드를 썼는데 레드는 확인된 리스크 전용이다. **읽어야 하는 값에는 deep 글자를 쓰지 않는다**는 규칙도 §9 대비 예외에서 역산해 명문화했다(칩은 tint 배경 + `--ink`). 산출물: `docs/adr/0019-records-screen-structure.md` · CLAUDE.md **D47 행** + D42 ③ 해소 표기 · DESIGN.md §5 **계열 칩 3종 · 활성 카드 테두리** 2행 · 시안 `artifacts/d47-records-screen-v1/`(1440 실측 스크린샷 동봉). **코드 변경 없음** — 구현은 레인 C 로 이어진다. `green` **새 마이그레이션이 필요 없다**(그릴링 중 스키마로 확인): 핵심 한 줄은 0025, 세션 목표는 일정 쪽 `schedule_session_goals`(0009, SSOT)와 회차 쪽 `record_details.sessionGoalNote`(0016) **두 출처**에 이미 있다 — 게이트웨이에서 합쳐 응답에 싣기만 하면 된다. 처음에 "일정에서 온 값"으로만 적었다가 조회 경로가 없다는 것을 발견해 ADR §2 에 표로 못 박았다. `yellow` 구현하며 실물로 판단할 것 1건 — 승인 대기 배지가 섹션 제목 옆과 회차 줄 두 자리에 나와 회차가 적으면 중복으로 읽힌다.
- 2026-07-30 (Claude Code): **동의 게이트 트랙 MVP 종료·완료 처리 (Q 결정, D46).** 법률 검토 트랙은 같은 날 이미 닫혔고, 이 결정이 그 **구현 쪽 짝**이다 — 동의·보존 영역은 자문도 구현도 MVP 선까지만 하고 멈춘다. ① **종료 시점 상태를 사실대로 스냅샷**: G1·G3 머지 완료(PR #17·#18) / **G2 는 구조·API 까지만**(브랜치 `feat/consent-g2` `262928c` push 완료, 완료 기준 테스트가 없어 PR 을 올리지 않았다) / G4·G5 미착수. G1 의 잔여 2건(만료 전 담당 실무자 알림 = 실무자 개인에게 보낼 통로 자체가 없음 · 보완 대상 리포트 화면 없음)도 함께 적었다. ② `red` **알고 받아들이는 리스크**: **D32 아카이브 전환이 미구현**이라 코드의 cron 은 여전히 기간 경과 시 즉시 파기하고, 그것을 막는 G2 의 법적 보류는 **아직 main 에 없다**(브랜치에만). 그 cron 이 실제로 구동되는지도 **미확인**(Cloudflare 계정 미개설 — 검증한 사실이 아니다). 실서비스 개시 전 필수이며 재개 시 첫 확인 항목으로 못 박았다. ③ **재개 트리거**는 §3 법률 검토 종료 기록과 동일(실제 당사자 대상 운영 개시 시점)이고, 재개 순서 3가지(아카이브 전환 → G2 테스트·머지 → G4 문안)와 각 착수 함정을 적었다 — 특히 G2 테스트는 금고 UPDATE 가드가 엄격해 `t.db` 직접 UPDATE 로 `purge_due` 를 넣을 수 없다. ④ 설정값 3종(보완 기한 14일·보존 상한 5년·현행 문안)은 **분리 유지**한 채 기본값으로 굳고, 화면 DRAFT 배지·L1·L3·L4 동결은 그대로다. 문서: 게이트 문서 **§5 신설** + CLAUDE.md **D46 확정 행**(그동안 '후보 행'으로 승인 대기였던 것을 이 결정으로 확정) + §8 미결에 아카이브 전환 `red` 항목 신설. 코드 변경 없음.

- 2026-07-30 (Claude Code): **UI 수정 레인 B — R7(390px 에서 HERO 행동 버튼 라벨이 쪼개짐) 수정.** 워크트리 `.worktrees/hero-register`, 브랜치 `fix/hero-mobile-and-register`. ① **근본 원인은 버튼이 아니라 헤더였다**: 768 미만에서 `.page-header` 가 `flex-direction:column` 으로 눕는데 `align-items:flex-start`(97행)가 그대로라 `.page-actions` 가 내용 크기로 쪼그라들고, 그 안의 버튼이 `width:100%` 를 받으면 기준 폭이 "내용에 맞춰 줄어든 폭" 이라 폭이 순환 참조에 걸린다 → 두 버튼이 좁은 상자를 반씩 나눠 갖고 `당사자 정 / 보` 로 쪼개진다. ② 고친 곳 **2줄**: `layout.tsx` 768 미만 블록에 `align-items:stretch` + `.page-actions{width:100%;flex-direction:column;align-items:stretch}`, `wire-styles.ts` 의 `.wire-button` 에 `white-space:nowrap`. **좁을 때 세로 배치**를 고른 이유는 가로로 둔 채 폭만 나누면 라벨이 길어질 때마다 같은 결함이 돌아오기 때문이다 — 변수는 버튼 개수가 아니라 라벨 길이다. ③ **실측 증거**(390px, 로컬 프리뷰 + Playwright, 판정 기준은 라벨 텍스트 노드의 `Range.getClientRects().length`): 브리핑 `당사자 정보` **2줄·높이 52 → 1줄·높이 40**, 당사자 허브·기본정보 수정도 1줄 유지, 가로 오버플로 0. 회귀 검사 **14화면 × 3뷰포트(1440·768·390)** 에서 잘린 라벨 0건·가로 스크롤 0건. **768 을 넣은 이유**: 미디어 쿼리가 `max-width:767px` 라 정확히 768 은 아직 가로 배치이고, 사이드바 240 을 뺀 헤더 폭 448 이 행 배치가 감당해야 하는 가장 좁은 값이다 — nowrap 을 전역으로 걸었으니 넘친다면 여기서 넘친다. 버튼 2개짜리 3화면(일정·상세 기록·브리핑) 실측 결과 버튼 묶음 210~255px 로 여유가 있고 라벨 전부 1줄. 레거시 `.button` 은 tsx 에서 쓰이는 곳이 0건이라(`page-actions` 5곳 전부 `WireButton`) nowrap 을 `.wire-button` 에만 건 것으로 '전 화면' 이 성립한다. ④ 계약 기록: `DESIGN.md` §5 '버튼 4종' 행에 줄바꿈 금지, '모바일' 행에 세로 배치를 추가(D38 이 라벨 줄바꿈을 규정하지 않은 자리를 메운다). ⑤ 게이트: typecheck ✓ · 웹 168 ✓ · build ✓. `yellow` **알아 둘 것 2건**: 브리핑 HERO 는 `ParticipantHeroCard` 를 안 쓰고 `.briefing-hero` 로 **따로 구현돼 있다**(D38 드리프트) — 이번 수정이 `.page-header`/`.page-actions` 라는 공통 자리에 놓인 덕에 두 구현 모두 고쳐졌지만, 부품 통합 자체는 손대지 않았다(레인 C·D45 영역). 그리고 레인 B 의 나머지 Y6~Y10 은 G1 과 `register-form.tsx` 가 겹쳐 착수하지 않았다(Next Actions 2번). ⑥ **곁가지로 CI 플레이크의 진짜 원인을 잡았다**: 이 PR 의 push 런에서 `verify` 가 `ReferenceError: window is not defined` 로 실패했는데(같은 커밋의 pull_request 런은 통과), 원인은 `schedule-cards.test.tsx` 에 `afterEach(cleanup)` 이 없어 파일이 끝난 뒤 jsdom 이 내려가는 동안 React 가 남은 작업을 돌린 것이다 — **테스트는 전부 통과해도 종료코드가 1** 이 된다. `participant-hero-card.test.tsx` 가 이미 문서화해 둔 그 원인이고, 지금까지 '플레이크' 로 넘겼던 실패가 실은 이것이다(부하에 따라 갈리는 타이밍 의존). 그 파일 하나만 고쳤다. `yellow` **같은 누락이 남은 테스트 파일 12개에 있다** — `settings/page` · `edit/basic-info-form` · `intake-wizard` · `intake-draft` · `life-area-fields` · `open-action-resolutions` · `record-draft` · `risk-banner` · `intake-saved-notice` · `admin-sidebar` · `register-form` · `list-row`. 지금 한 번에 훑지 않은 이유는 그 목록에 G1 이 고치는 중인 `register-form.test.tsx` 가 있어서다 — **G1 머지 뒤 단독 커밋으로 훑는 것이 맞다**(그러면 이 원인의 CI 실패가 구조적으로 끝난다).
- 2026-07-30 (Claude Code): **G3 — 질병명·진단명 마스킹 확대** (워크트리 `.worktrees/consent-g3`, 브랜치 `feat/consent-g3`). 2차 마스킹(D2·R3)에 **질병명 사전 계층**을 얹었다 — 이 계층은 NER 설정과 무관하게 **항상 동작**한다(인명 마스킹은 여전히 NER 모델이 있어야 하고, 로컬엔 미설정). ① 새 `condition_terms.py` — 정신건강 37항목 + 신체 질환 60여 항목. **무엇을 일부러 뺐는지도 사전에 적었다**: `암` 단독(암호·어두움과 겹침 — 부위 붙은 복합어만), `우울` 단독(감정 표현이고 진단명이 아니다 — R4), `간질`("간질간질"과 겹쳐 현행 진단명 `뇌전증`으로 대체), `투석`·`입원`·`수술`(치료·경과이지 병명이 아니다 — 병명을 역추론할 단서지만 이것까지 지우면 상담 맥락이 무너져 잔여 리스크로 남기고 동의서 고지, D2). ② 사전 표기 규칙: 자연스러운 분절 위치에 공백을 넣어 적고 마스킹 쪽이 그 공백을 `\s*` 로 컴파일한다 — **한 항목이 붙여 쓴 형태와 띄어 쓴 형태를 둘 다** 잡는다(`공황 장애` → "공황장애"·"공황 장애"). 긴 항목을 먼저 정렬해 "제2형 당뇨병"이 "당뇨병"·"당뇨"에 조각나지 않게 했다. ③ **집계 로그**(완료 기준): `mask_text_with_report` 가 토큰별 건수만 돌려주고 워커가 그 숫자만 로그에 남긴다 — 치환된 원문은 보고서·로그 어디에도 담지 않는다(R3). ④ 질병명 NER 은 인명 NER 과 다른 모델이라 설정을 갈랐다(`CCC_CONDITION_NER_MODEL_ID`) — 사전의 **보완재이지 대체재가 아니며**, 라벨 접두는 실측 확인 전이라 주석으로 명시했다. ⑤ **구현 중 결함 1건 자체 발견·수정**: 인명·질병명 두 NER 목록을 각각 따로 치환하면 앞 목록의 치환이 뒤 목록 스팬의 오프셋을 밀어 엉뚱한 자리를 지운다 — 두 목록을 합쳐 한 번에 뒤에서 앞으로 치환하도록 고쳤다(겹치는 스팬은 뒤쪽만 살린다). ⑥ 완료 기준의 **치환율**을 테스트로 고정: 병명 문장 15개 셋에서 치환율 **100%**, 오탐 확인용 맥락 문장 6개는 **한 글자도 안 바뀜**. 게이트: 파이프라인 테스트 **44개 ✓**(신규 11개). `yellow` **곁가지 2건 함께 처리**: 파이프라인 테스트가 **CI 에 아예 없어서** G3 의 치환율 검증이 아무도 안 돌리는 검증이 될 판이었다 — `pipeline-test` 잡을 추가했다(표준 라이브러리만 쓰므로 ML 설치 불필요). 그리고 README 의 실행 명령이 **파이썬 3.12+ 에서 깨져 있었다**(`unittest discover` 가 시작 디렉터리의 임포트 가능성을 요구하는데 `tests/` 에 `__init__.py` 가 없다) — 동작하는 명령으로 고치고 이유를 적었다. `yellow` **한계**: 사전은 병명만 잡는다 — 대화 속 제3자 인명은 여전히 NER 모델이 있어야 지워지고 로컬은 미설정이다(기존 상태). 문안 v0.3 의 "구체적인 질병명 등" 문구는 **이 게이트가 배포된 뒤에만** 쓸 수 있다(G4 와 연동).

- 2026-07-30 (Claude Code): **인테이크 위저드 1단계 화면 결함 2건 수정**(워크트리 `.worktrees/intake-wizard`). ① **소절 번호 중복·순서 깨짐** — 자동으로 채워지는 항목(상담일·실무자·상담 회차)을 별도 상자로 빼놓은 탓에 `1-3`이 화면에 두 번 뜨고 그 상자가 `1-2` 위에 앉아 정본(`PRD/intake-questionnaire-v1.md`) 순서가 어긋났다. `renderGroups`에 소절 제목을 열쇠로 쓰는 `extras` 인자를 더해 **그 소절 안 맨 위**에 끼워 넣는 방식으로 교체 — 번호는 언제나 소절 하나에 하나다. ② **진행 단계 버튼 세로 늘어남** — grid 행이 본문 길이만큼 커진 컬럼 높이를 균등 분배해 버튼 하나가 500px 넘게 벌어졌다. 진행 표시 `<nav>`에 `alignContent: 'start'` 추가. 회귀 테스트 2건 추가(소절 번호가 정본 순서대로 하나씩만 뜨는지 · 자동 채움 3항목이 `1-3` 상자 안에 있는지). 이 작업이 **UI 수정 스윕의 레인 A**(`artifacts/ui-revision-sweep-v1/work-design.md` R4·R5)다. 게이트: typecheck ✓ · 웹 테스트 170 ✓(인테이크 위저드 13) · `pnpm build` ✓ · **실측 ✓** — 1440px 라이브 화면에서 단계 버튼 높이 **568px → 44px**(4개 전부), 소절 제목이 정본 4개와 정확히 일치(중복 0). `yellow` **레인 A 완료 기준의 `layout-measure.py after-a` 는 쓸 수 없다** — 그 스크립트의 대상 화면 목록에 인테이크가 없다(레이아웃 계약 실측 전용). 대신 이 화면 전용 측정을 돌렸고 결과는 `artifacts/lane-a-after/after-a/measure.json`·`intake-desktop.png` — **임시 산출물이라 커밋하지 않는다**(2026-07-29 Q 방침). `yellow` 남은 참고: `nav` 요소 자체는 여전히 컬럼 높이(2,775px)를 차지하지만 이는 페이지 그리드 컬럼이라 정상이고, 눈에 보이던 결함(버튼이 늘어남)은 사라졌다.

- 2026-07-30 (Claude Code): **G1 — ① 개인정보 동의 하드 게이트 + 긴급 등록 예외** (워크트리 `.worktrees/consent-g1`, 브랜치 `feat/consent-g1`). 앞선 세션이 잠긴 에이전트 워크트리(`.claude/worktrees/agent-…`)에 커밋 2건을 두고 멈춰 있던 것을 이어받았다 — 잠금을 쥔 프로세스는 이미 죽어 있었고, 미커밋 5파일을 먼저 커밋해 잃을 작업을 없앤 뒤 브랜치 이름을 알아볼 수 있게 바꾸고 워크트리를 프로젝트 규칙대로 옮겼다. ① **마이그레이션 0028** `support_case_emergency_registration` — 긴급 등록 플래그·사유·보완 기한(기본 14일, **설정값 분리 유지**). ② 게이트웨이: `createSupportCase` 에 ① 검증(긴급 플래그가 없으면 `consent_privacy` 필수) — **기존 당사자의 추가 참여 사업 생성에도 동일 적용**(D44 로 두 번째 사업은 동의 3종이 미체크로 시작하므로 서버 검증만 두면 화면에서 원인 없는 실패로 보인다), 긴급 등록 전건 `record_consent` 계열 감사(D14). ③ 완료 기준의 **보완 대상 리포트** `listPrivacyConsentFollowUps` — 긴급 등록분과 하드 게이트 이전·레거시 경로로 생긴 ① 미기록분을 함께 내며 **기존 데이터를 고치지 않는다**(마이그레이션 대상 아님). **활성 케이스만** 낸다 — 종결 케이스는 보완할 상담이 남아 있지 않아 작업 큐에 영원히 쌓이기만 한다. ④ 등록 화면: ① 체크 필수화 + '긴급 등록' 전환(사유 필수), 그리고 **① 동의와 긴급 토글은 상호 배타** — 서버가 "동의가 있는데 긴급 예외까지 왔다"를 거부하므로 화면에서 그 조합을 못 만들게 했다(한 번의 클릭으로 원인 없는 실패에 닿지 않게). ⑤ 리베이스는 충돌 0건이었고, main 의 CCC-42 후속 수정(`10319dc`, 같은 두 파일)이 삼켜지지 않았음을 diff 로 확인했다. 게이트: typecheck ✓ · guard:db ✓ · build ✓ · API 385 ✓(31파일) · 웹 171 ✓(26파일). `yellow` **미구현 1건**: 게이트 문서 G1 의 **"만료 전 담당 실무자 알림"** 은 없다 — `apps/api/src/notify.ts` 에는 관리자용 `notifyAdmins` 하나뿐이고 실무자 개인에게 보내는 통로가 아직 없다. 알림을 어디로 낼지(사이드바 배지·메일 등)는 결정이 필요해 별도로 뺐다. `yellow` **화면 없음 1건**: 보완 대상 리포트는 게이트웨이·API 까지만 있고 이를 보여 주는 화면이 없다(완료 기준 문구는 '목록화'만 요구하므로 기준 자체는 충족).

- 2026-07-30 (Claude Code): **열린 PR 3건 전부 정리 + 법률 검토 트랙 MVP 종료 + CCC-42 사후 리뷰 수정 + G1 착수.** ① **PR #14(CCC-42)**: 전날 이미 머지된 상태였음을 확인, 사후 리뷰(opus)에서 6건 발견 — **HIGH**: 처리 3종 PUT 라우트가 URL 범위 검사를 **변경·감사 커밋 뒤에** 수행(403 응답인데 상태는 이미 바뀜, D14 위배) + 그걸 숨기는 테스트. **MEDIUM 3**: 권한 게이트 실제 테스트 부재 / AI 승인 경로 재검출로 같은 쌍이 미처리 중복 등재 가능 / 처리 이력 무상한 증가. 수정 커밋 `10319dc` — 범위 검사를 게이트웨이 안 UPDATE 전으로, 중복 차단은 "처리됨 전체 + 타 트리거 미처리"로(동일 트리거 재검출의 삭제-후-미삽입 데이터 손실을 피하는 필터), 이력은 참여 사업별 최신 20건 캡(윈도 함수 파티션 — 전역 LIMIT 이면 한 사업이 다른 사업 몫을 굶김), 구형 코드 재구성으로 테스트가 실제로 결함을 잡는지 실증. 게이트: typecheck ✓ · guard:db ✓ · API 370 ✓. LOW 2건은 의도적 잔여(Next 3). ② **PR #4(CCC-36)**: STATUS.md 충돌만 해소(main 유지 + CCC-36 한 줄)하고 머지(`223d625`). push 이벤트 verify 1회 실패는 웹 테스트 `window is not defined` 플레이크 — 같은 커밋의 pull_request verify 는 통과, 문서 전용 PR 이라 무관 판정. ③ **PR #3(시크릿 스캐너)**: 리뷰(차단급 0, MEDIUM 3) 후 수정 커밋 `c4dce44` — **문서가 참조하는 PNG 12개 복원**(ADR-0012 근거 이미지 포함 — PR 이 참조본을 지우고 미참조본을 남기는 뒤집힘), 루트 미참조 1개 삭제, gitleaks 미설치 시 훅 설치 화면에 안내 출력, 가드 스크립트 정리 4건(PLANTED 토큰을 github-pat 규칙이 실제로 잡는 36자로 교체·`gitleaks:allow` 위치 교정·스캔 실패 명시적 전파·`fileURLToPath`). 리뷰 브리핑의 오류 1건 정정: 유지된 PNG 2개는 실제로 ADR-0012 가 참조 — 삭제 안 함. 머지 `b836faf`. `yellow` 수용 리스크(기존 Q 결정): git 이력에는 구 캡처(직원 이름 픽셀)가 남아 있다 — HEAD 제거일 뿐. ④ **법률 검토(D26) MVP 종료(Q 결정)**: 게이트 문서에 '법률 검토 트랙 종료 기록' 신설(`0f7abfa`) — 패킷 보존·L1/L3/L4 동결·DRAFT 배지 유지·설정값 기본값 운영. ⑤ **G1 착수**: ① 하드 게이트 + 긴급 등록 예외를 별도 워크트리에서 구현 중.(도구 설치 — 코드 변경 없음).** `/codemap` 의 '복잡한 구조 분석' 경로가 쓰는 `codebase-memory-mcp`(MIT·100% 로컬)를 설치하고 이 레포를 색인했다 — **3,971 노드 / 12,386 엣지 / 0.6초**, TypeScript 156파일·SQL 27·Python 25, 라우트 노드 16개·`HTTP_CALLS` 엣지 24개까지 잡힌다. 다음 세션부터 `mcp__codebase-memory-mcp__*` 로 web↔api 경계·죽은 코드·영향 범위를 물을 수 있다(코드가 바뀌면 `index_repository` 재실행). `yellow` **설치 부작용 1건**: 설치 도구가 `--skip-config` 를 무시하고 Codex·Gemini·Cursor·Kiro·VS Code·OpenCode 설정 9개 파일까지 고쳐 써서 Claude 것만 남기고 되돌렸다 — 재발 조건·되돌리는 법은 `~/developer/tools/portwright/failures/2026-07-30-codebase-memory-mcp-skip-config-ignored.md`, 사용법은 같은 곳 `services/codebase-memory-mcp.md`.

- 2026-07-29 (Claude Code): **CCC-42 — 내용 불일치 처리 3종(상황 변경·기록 오류·확인 완료) + 접힌 이력(D45·ADR-0018, 워크트리 `.worktrees/ccc-42`).** CCC-43이 비워 둔 `resolution_*` 3컬럼 위에 올렸다 — **마이그레이션 없음**(`audit_log.action`이 자유 문자열이라 새 감사 동사도 스키마를 건드리지 않는다). ① gateway `resolveSessionDiscrepancy` — 권한은 `assertSupportCaseAccess`(**담당 실무자·기관 관리자**, D7 — 같은 화면의 전체 목표 카드가 '담당 실무자만'인 것과 층이 다르다), 처리 3컬럼만 UPDATE(인용·회차·유형은 0027 트리거가 막는다), `resolve_discrepancy` 감사 1건에 `{status, previousStatus}`(인용 원문 미포함, R3). ② **Q 결정 2건**(ADR-0018 '개정' 절에 기록): **재검출은 이미 처리한 쌍을 다시 올리지 않는다**(동일성 = 유형 + 양쪽 (회차, 인용), 좌우 순서 무시, 범위는 참여 사업 전체) / **처리 종류는 다시 바꿀 수 있다**(변경 전건 감사). ③ 브리핑 읽기: `resolution_status IS NULL` 필터를 걷고 미처리·처리됨을 함께 싣되 **미처리를 앞세운다**, 응답에 `resolution{status,resolvedAt}` 추가(처리자 userId는 화면에 안 쓰므로 미탑재 — 감사에만). 화면 배지는 **미처리만** 센다(이력이 쌓여도 '남은 일' 숫자가 유지된다). ④ 화면: 영역 ③에 처리 3종 버튼(서버 액션 `resolveDiscrepancyAction`, 현재 상태 버튼은 비활성) + `처리된 항목 N건` 접힌 이력. `WireButton`에 `name`/`value` 추가(한 폼에 선택지 3개를 담기 위해 — 공용 부품 변경 1건). ⑤ 상세 기록 화면: `기록 오류` 처리된 회차 카드 위에 처리됨 안내 한 줄(원본은 그대로), 재료는 새 gateway `listRecordErrorSessionIds` → `GET /support-cases/:id/records`의 `recordErrorSessionIds`. **0027에 어느 쪽이 오류인지 담는 칸이 없어 표시는 쌍의 양쪽 회차에 붙는다**(한쪽 지목은 스키마 변경, 범위 밖). ⑥ 새 라우트 `PUT /support-cases/:id/discrepancies/:discrepancyId/resolution` — 주소의 참여 사업과 항목 소속이 어긋나면 403(게이트웨이는 항목에서 스스로 범위를 찾으므로 URL만으로는 안 걸린다). 게이트: typecheck ✓ · guard:db ✓ · build ✓ · 웹 168 ✓ · API 367 ✓(discrepancies 26 — 신규 9). `yellow` **미검증 1건**: 상세 기록 화면의 `기록 오류 처리됨` 표시는 데이터 경로(gateway·라우트)만 테스트로 고정했고 **렌더 자체를 검증하는 테스트는 없다**(records 페이지에 테스트 파일이 없고 `RecordCard`가 미노출). `yellow` **알려진 한계**: 재검출 중복 차단은 **인용 텍스트가 정확히 같을 때만** 걸린다 — 프로바이더가 같은 불일치를 다른 범위로 발췌하면 새 건으로 올라온다.

- 2026-07-29 (Claude Code): **PR #5 머지 — 당사자 초대·자기 가입(CCC-28·29)이 main 에 들어왔다(3f1966b). L2 게이트는 Q 권한으로 해제.** ① **L2 해제 결정**: 서비스 출시 계획이 없고 지금 목표가 기능 완성이라는 Q 판단 — 근거와 "이 결정이 성립하는 사실 조건 3개"(화면 흐름 프로토타입·미리보기에 공개 표면 없음·문안 DRAFT 배지 유지)를 게이트 문서 §3 '개방 기록'에 남겼다. **L1·L3·L4 는 그대로 잠겨 있고, 실제 당사자 대상 운영 개시는 L1 선행**이다. ② **리베이스**: 오전 리베이스 뒤 브리핑 3영역 5건이 머지되며 재발한 충돌 6건을 양쪽 의도 보존으로 해소 — CLAUDE.md(§9 제목에 D39·D45 병기, D45 행 유지) · schema.sql(`invite_tokens` 0021·이중소비 방지 트리거 0022 를 번호 순서대로 0020 뒤에 배치, **재번호 불필요** — main 이 0021·0022 를 비워 뒀다) · invite/page.tsx(PR 의 카드형 '초대 대상' + main 의 온보딩 저장 이름 CCC-32) · **layout.tsx**(공개 경로 `/join` 은 셸 없이 렌더 + **그 경로에서 표시 이름 조회를 건너뜀** — 신원 없는 요청이라 그 조회 자체가 401 을 만든다) · routes.test.ts(양쪽 describe 보존). ③ 게이트: build ✓ · typecheck ✓ · guard:db ✓ · API 359 ✓ · 웹 166 ✓ · CI 4종 ✓. 구 용어 잔여는 소스 0건, `artifacts/journey/ccc-29/report.json` 2건은 캡처 증거라 의도적 존치(PNG 와 어긋나지 않게). ④ 곁가지: 사용 가이드 아티팩트 2종(`artifacts/user-guide-onboarding-v1.*`)은 **임시 산출물이라 커밋하지 않는다**(Q) — 아래 가이드 기록이 가리키는 경로는 레포에 없다.

- 2026-07-29 (Claude Code): **사용 가이드 v1 아티팩트** — 기관 관리자 가입→사업 등록→실무자 초대→당사자 등록→인테이크→브리핑 생성→첫 세션 목표 설정→브리핑 반영의 8단계 온보딩 워크플로우 가이드를 `artifacts/user-guide-onboarding-v1.html`(SVG 목업 8장 + 주황 주목 표시, CCC 디자인 토큰 적용)·`.md` 2종으로 생성. 본문은 humanize-korean light 경로 윤문(변경률 0.3%, 게이트 통과). 당사자 직접 가입은 현행 대행 등록(D23)과 구분해 '현행과 예정'으로 표기(D26 법률 검토 후 예정). 헤드리스 Chrome 렌더 검증 완료.
- 2026-07-29 (Claude Code): **CCC-39 — 영역 ① AI 제안 구조화(제목+이유+근거 회차 링크, D45·ADR-0018).** 단문 AI 질문을 구조화 제안으로 대체. 생성 시점 설계: 생성은 기존 세션 초안 생성 시(파이프라인 그대로), **공식화는 승인 시**(R2), 브리핑은 저장된 승인본만 읽는다(D8 — 화면이 AI 응답을 기다리지 않음). ① 프로바이더 스키마 v2(`phase1.grounded.v2` — 질문 항목 {title(≤80자), reason, evidence}, Codex JSON 스키마·인스트럭션 갱신. 버전 범프로 활성 설정 해시가 어긋나므로 **배포 환경은 프로바이더 설정 재활성화 필요** — 의도된 fail-closed) ② 마이그레이션 **0026**: `ai_draft_versions_insert_guard` 재생성 — questions_json 원소로 {title, reason} 객체 허용(v1 문자열도 계속 허용 — human_edited 프러버넌스 왕복), 중복 판정은 제목 기준 ③ gateway: `AiBriefingSuggestion` 타입(v1 문자열은 reason=null 로 정규화), 저장 직렬화 왕복(`questionsToJson`), `getParticipantBriefing.aiSuggestions` — **승인본만·최신 승인순·참여 사업당 최대 3개**, 근거 회차 sessionId·heldAt 동봉. 구 케이스 브리핑(`getBriefing`)은 제목 문자열 계약 유지 ④ 웹: 영역 ① 'AI 질문'→'AI 제안' — 제목·이유·`근거 회차 보기 (날짜)` 링크 3층, 링크는 기록 페이지 회차 앵커(`#record-{sessionId}`), 빈 상태 안내 유지 ⑤ 테스트: 승인 전 초안 미노출(R2)·상한 3개·근거 회차 일치를 gateway·routes 양층에서 고정, 웹 컴포넌트 링크·상한·reason=null 폴백·빈 상태 4건 추가. 게이트: typecheck ✓ · guard:db ✓ · API 319 ✓ · 웹 158 ✓ · build ✓.
- 2026-07-29 (Claude Code): **CCC-43 — 내용 불일치 검출·저장·읽기 전용 표시(D45·ADR-0018, 워크트리 `.worktrees/ccc-43`).** ① 마이그레이션 **0027** `session_discrepancies`(0025=CCC-38·0026=CCC-39 예약 회피) — 양쪽 원문 인용+회차 참조+`resolution_*` 3컬럼(CCC-42 자리 예약, NULL=미처리), 인용·회차는 DB 트리거로 불변·처리된 행은 삭제 불가(ADR-0018 "처리는 표시일 뿐, 접힌 이력 보존"). ② 검출은 **기록 공식화 시점**에 라우트 훅으로 실행: 수기 저장 3경로(정규 records·인테이크·레거시 sessions)와 AI 정리 승인(review approved) — **최선 노력**(D8): 동의 부재(D15 텍스트 AI 게이트)·프로바이더 미구성/실패·검증 거부 전부 저장을 막지 않고 스킵, 브리핑 열람 시 AI 호출 없음(실시간 검사 기각). ③ 전송 재료는 gateway `collectDiscrepancyDetectionSources` — 공식 기록만(수기 메모+승인 AI 정리, R2), 등록 PII 가명 치환(R3)+decrypt_pii 감사. ④ 프로바이더 계약(ai-provider): `detectDiscrepancies` 선택 메서드 + 출력 검증 fail-closed — 인용은 소스 원문의 부분 문자열 강제, 트리거 회차 미포함·유형/회차 모순·판단 필드 전부 거부(R5). ⑤ 저장은 gateway `replaceSessionDiscrepancies` — 재검출 시 미처리 행만 교체, create 감사(D14). ⑥ 브리핑 영역 ③: 저장된 미처리 결과만 읽어 유형 라벨(회차 간 불일치/회차 내 모순)+양쪽 인용+상담일+`#record-` 앵커 링크를 나란히, 판단 표현 없음. 게이트: typecheck ✓ · guard:db ✓ · 신규 테스트 18(검증 6·gateway 7·라우트 훅 5) 포함 API 336 ✓ · 웹 156 ✓ · build ✓ (CCC-38 머지 후 리베이스 재검증). 후속: CCC-42(처리 3종 UI)가 이 스키마 위에서 착수 가능.
- 2026-07-29 (Claude Code): **CCC-38 — 회차별 핵심 한 줄(D45 영역 ②·ADR-0018).** AI가 회차마다 핵심 한 줄을 만들고 기존 산출물(요약·질문)과 **같은 초안 버전으로 함께 승인**되게 했다(R2). ① 마이그레이션 **0025**: `ai_draft_versions.one_liner` nullable 추가(레거시 초안=NULL, 백필 없음) + 개행·빈 값 차단 트리거 + `approved_ai_briefing_v1`·`grounded_ai_quality_v1` 뷰 재생성으로 승인분만 노출 ② gateway: 생성 경로 `oneLiner` 필수(120자·개행 금지), PII 마스킹 경로 포함(R3 — 등록값 치환 후 저장), 편집(증거 재선택)은 부모 초안의 한 줄을 그대로 승계(provenance 검사에 포함), `getParticipantBriefing().sessionRows`에 `aiOneLiner`(회차당 최신 승인 초안) ③ ai-provider: 프롬프트·스키마 버전 **v2**(`phase1.grounded.v2`) — 출력에 `oneLiner` 필수, 금지 판단·PII 유사 패턴 검사를 claims와 동일 적용 ④ API: 초안 응답(승인 화면 재료)·브리핑 `sessionRows`에 `oneLiner`/`aiOneLiner` ⑤ 브리핑 영역 ②: 승인된 AI 한 줄 우선, 없으면 수기 발췌 + **'수기' 배지**(D5 폴백, 메모 없으면 배지 없이 "수기 메모 없음") ⑥ 테스트: 캐너리에 `UNAPPROVED_ONE_LINER_CANARY` 추가 — 승인 전 참여자 브리핑·공식 투영 전부 미노출, 승인 후 `sessionRows`·승인 뷰 노출 검증. 게이트: typecheck ✓ · guard:db ✓ · 테스트 474개(API 318 + 웹 156) ✓ · build ✓.
- 2026-07-29 (Claude Code): **CCC-32 — 관리자 온보딩 2단계(조직 이름 → 첫 사업 이름, 이름만 저장).** 스펙 CCC-archive#78 US 1·2. ① 마이그레이션 **0023**: `organization_settings`에 `org_name`·`program_display_name` nullable 추가(0021·0022는 PR #5, 0024는 CCC-41 선점 — 충돌 회피). programs 테이블 신설 없음 — 내부 유형은 `financial_support_v1` 고정(스펙 저장 깊이 결정). ② 게이트웨이: `completeOrganizationOnboarding`(admin 전용·재실행=덮어쓰기 — 온보딩 재방문이 409로 막히지 않음·감사 D14, 설정 행 부재 시 시간대를 지어내지 않고 거부 — 0005 "no guessed default" 유지) + `getOrganizationProfile`(전 역할 조회, 미설정 시 null — 셸이 앱을 잠그지 않음, 감사 없음 근거는 0017과 동일). ③ API: `GET /organization/profile` · `POST /organization/onboarding`. ④ 웹: `/onboarding` 2단계 위저드(admin 전용, 재방문 시 수정 겸용) + 완료 화면에서 실무자 초대(`/admin/invite`) 안내. **라벨 동적화**: 신규 `getDisplayLabels()`(React cache — 요청당 API 1회, ApiError 시 labels.ts 하드코딩 폴백)로 사이드바(AppSidebar 프롭 주입)·당사자 허브·브리핑·당사자 초대·일정 등록·관리자 배정·당사자 등록 화면이 저장 이름을 되비침. `admin-format.programLabel` 상수 제거, 클라이언트 컴포넌트(등록 폼)는 프롭으로 받음. 게이트: build ✓ · guard:db ✓ · 웹 155 ✓ · API 전체 ✓. 교훈: CCC-41과 같은 체크아웃에서 병렬 작업이 겹쳐 커밋을 41 머지 뒤로 미뤘다 — 이후 병렬 티켓은 `.worktrees/` 분리.
- 2026-07-29 (Claude Code): **CCC-41 — 전체 목표 카드, HERO 아래 그 자리 입력·수정(D45·ADR-0018, D43 미결 해소).** ① 마이그레이션 **0024** `support_cases.overall_goal`(케이스당 1개·수정 가능·점수 없음 — goals 테이블의 세부 목표 층과 분리, 번호는 PR #5의 0021·0022와 병렬 CCC-32의 0023을 피해 0024) ② gateway `setSupportCaseOverallGoal` — **담당 실무자만**(admin 열람만, ADR-0018), 활성 케이스만, 200자 상한, 빈 값=설정 전 복귀, 변경 전건 감사(detail에 목표 문장 미포함) + `getParticipantBriefing`에 `overallGoal`·`canEditOverallGoal` ③ API `PUT /support-cases/:id/overall-goal` ④ 브리핑 화면: 리스크 배너 아래 카드형 한 줄, 비면 "설정 전", 담당 실무자에게만 입력/수정 버튼 → 그 자리 폼(maxLength 200), 실패 시 카드 안 오류 한 줄. 게이트: 웹 155 ✓ · API 318 ✓ (build는 커밋 검증 워크트리에서 재확인). ⚠️ 같은 체크아웃에서 CCC-32 세션이 병렬 작업 중이어서 41 몫 변경만 훅 단위 선별 커밋 — 재발 방지는 세션별 `.worktrees/` 분리(메모리 기록).
- 2026-07-29 (Claude Code): **동의서 법률 검토 패킷 v1 + 문안 v0.2.** ① 신규 `docs/consent/legal-review-packet-v1.md` — 자문용 패킷: 서비스 개요, 시스템의 실제 동의 처리(3종 구분·대행 입력·append-only 철회 이력·참여 사업 단위 재동의·해외 AI 위탁·아카이브 보존), 검토 질문 7건(문안 전반 / D36 표시 범위의 제3자 제공 여부 / AI 국외 이전 / 제21조 아카이브 정합·1년 근거 / 사업 단위 재동의 타당성 / 전자서명·직접 작성 요건 / 대행 입력 증빙력), 동의 노출 화면 4곳. ② `consent-draft-v0.md` v0.1→v0.2: 4절에 D36 문구("같은 기관의 다른 실무자에게는 참여 중인 사업 목록과 담당 실무자 이름이 표시됩니다") 추가 + 구 용어(상담사·배정 책임자·참여자) D40 정규화. ③ `consent-copy.ts` 4절에 같은 문구 동기화(정본-화면 동기 규칙). 게이트: 빌드 · 웹 테스트 145개 통과. 다음: Q가 패킷으로 자문 의뢰 → 회신 반영 → PR #5 머지 게이트 해제.

- 2026-07-29 (Claude Code): **CCC-37 당사자 기본정보 수정 화면** — 등록 화면이 받는 7종(이름·휴대전화·이메일·계좌·생년월일·주소/거주지역·성별)을 등록 뒤에 고치는 자리를 `/participants/:id/edit` 에 신설했다. ① 저장은 기존 단일 관문 `updateParticipantPii`(R1) 그대로 — 새 복호화·저장 경로를 만들지 않았다. ② **권한 층 1건 변경**: `updateParticipantPii` 의 `assertAdmin` 을 뗐다. 근거는 등록(`createBeneficiaryWithInitialSupportCase`)이 이미 counselor 에게 같은 금고의 **6종**(이름·연락처·이메일·생년월일·주소·성별)을 열어 준다는 것 — 등록 때 쓸 수 있는 값을 등록 뒤에 못 고치면 오타 하나를 관리자에게 부탁해야 한다. **계좌만 예외**로 지금까지 admin 전용이었고, CCC-37 이 7종에 포함시켜 이번에 함께 열렸다(항목별 권한 분기는 두지 않는다 — 한 화면이 한 번에 저장하는 값에 축을 더하면 화면·게이트웨이·감사가 어긋난다). 케이스 단위 게이트는 그대로다(`assertActiveSupportCaseContext` → `assertSupportCaseAccess`: admin 또는 **활성 배정된 담당 실무자**). 레거시 `registerPii` 는 자기 자리의 `assertAdmin` 을 유지한다. ③ 읽기는 새 관문 `getParticipantBasicInfo` — 쓰기와 같은 문을 지나고 활성 참여 사업 컨텍스트까지 정해 돌려준다(화면이 사업을 고르지 않는다). 감사는 **화면 조회 1행**이며 추가 항목은 행을 나누지 않고 같은 행 `fields` 에 합친다(D24, `getIntakeRecordContext` 와 같은 방식). ④ 인테이크 1-1 의 '당사자 등록 정보에서 수정' 링크가 이 화면을 가리킨다 — 동의 링크(허브)와 목적지를 갈라 D44 코드와 얽지 않았다. ⑤ PII 는 브라우저 임시본·리다이렉트 주소에 담지 않는다(R3, 테스트로 고정). 게이트: build · guard:db · 테스트 457개(API 312 + 웹 145) 전부 통과. **PR #7로 머지**(계좌 권한 실무자 개방은 Q 승인 2026-07-29, PR 코멘트 기록).
- 2026-07-29 (Claude Code): **PR #5 리베이스 반영 — 참여자 초대·자기 가입(CCC-28·29)을 D40~D44 반영된 새 main 위로.** 준비 브랜치(`prep/ccc-28-29-rebase-d40`)의 커밋 4개(PR 원래 커밋 3 + 정합 커밋 1: D40 용어 스윕 111건 · 마이그레이션 0018·0019→0021·0022 재번호 · 자기 가입 동의 2종→D44 3종)를 검증 후 `feat/ccc-28-29-participant-signup`에 force-push. 게이트: 빌드 · guard:db · API 테스트 330 + 웹 테스트 145 전부 통과, PR 파일 구 용어 잔여 0건. PR #5는 CONFLICTING→MERGEABLE. 머지는 동의서 법률 검토 게이트(Q 결정) 대기.
- 2026-07-29 (Claude Code): **CCC-40 — 브리핑 3영역 골격 재구성 + GAS 아코디언 제거(D45·ADR-0018).** ① gateway `getParticipantBriefing`에 `sessionRows` 추가(기존 sessions 쿼리 재사용 — 회차마다 상담일·유형(regular/intake)·수기 첫 줄 60자 발췌, 메모 전문은 싣지 않음) ② API `normalizeParticipantBriefing` 섹션에 `sessionRows` 매핑 ③ 브리핑 화면을 3영역으로 재편 — 영역 ①(실무자 입력 위 + AI 질문 아래, 구 '지난 상담 브리핑'·'오늘 확인할 질문' 대체) / 영역 ②(회차별 한 줄 + '승인 대기 N건' 배지 이동) / 영역 ③(빈 상태 + 준비 중 안내), 미해결 액션·개인정보 카드는 그리드에 유지 ④ GAS 게이지·아코디언 컴포넌트와 전용 CSS 제거(D43 이행 — 스키마·gateway·데이터 유지) ⑤ 테스트: 웹 컴포넌트 테스트 3영역 계약으로 재작성, API routes 강한 단언에 sessionRows 반영 + gateway 비인가 회차 미노출 검증 추가. 게이트: build ✓ · guard:db ✓ · 웹 140 ✓ · API 308 ✓(재실행 68 포함). guard:layout은 스크립트 미작성(DESIGN.md '작성 예정')이라 해당 없음.
- 2026-07-29 (Claude Code): **D45 구현 Linear 티켓 분할 — CCC-38~43 발행 + CCC-15 Canceled.** 트레이서 불릿 6건: CCC-40 골격(선행 없음)이 CCC-41·38·39·43 넷을 차단, CCC-43이 CCC-42를 차단(blocks 방향 inverseRelations로 검증). CCC-15(목표 3층 구현, D33)는 D43 보류·D45 재구성으로 범위가 대체돼 경위 코멘트 후 Canceled — 전체 목표는 CCC-41이 승계.
- 2026-07-29 (Claude Code): **그릴링 3차 — D45 브리핑 3영역 재구성(ADR-0018).** Q의 브리핑 카드 수정 요청이 D43이 기다리던 시안 — 그릴링으로 계약화해 봉인 해제. 확정: ① '오늘 만나기 전 꼭 기억할 것'(구 지난 상담 브리핑+오늘 확인할 질문 통합 — 실무자 입력 위·AI 제안 최대 3개 아래, 제목+이유+근거 회차 링크 필수) ② 회차별 정리(상담일·유형 2종·핵심 한 줄 — AI 생성+승인 흐름, 수기 폴백) ③ 내용 불일치(공식화 시점 검출·저장, AI 판단 없이 양쪽 인용, 처리 3종은 표시만·원본 불변). 전체 목표는 HERO 아래 카드형 한 줄+그 자리 편집(D43 미결 해소). 상담 유형 세분화(초기상담·사정·개입)는 §8 미결에 유지. 문서: ADR-0018 신설, CLAUDE.md §6 재작성+D45 행, CONTEXT.md '내용 불일치'·'핵심 한 줄' 신설+브리핑·전체 목표·맞춤형 질문 갱신. 코드 변경 없음(구현은 후속).
- 2026-07-29 (Claude Code): **D44 동의 3종 — 등록에서 받고, 당사자 정보 페이지에서 고치고, 인테이크는 읽기만.** ① 마이그레이션 **0020**: `support_cases.consent_privacy_at` 신설(0018·0019는 열린 PR #5가 선점). 개인정보 동의를 녹음·텍스트 AI와 **같은 층**에 맞춘 것 — 두 동의는 이미 `support_cases`(현재값) + `participant_consent_records`(append-only 이력) 두 곳에 살았고, 개인정보 동의만 0014로 이력 쪽에만 들어가 있었다. 빠진 반쪽을 채웠다. ② 등록 화면에 개인정보 동의 체크 추가(3종, 기본 미체크, 미동의여도 등록 진행 — D15) + 게이트웨이·API 파서(`consentPrivacy`) 확장. ③ 당사자 정보 페이지의 **담당 참여 사업 카드마다** 동의 3종 수정·철회 UI. 저장은 게이트웨이 단일 관문 `updateParticipantConsent`(R1) — 권한은 `assertSupportCaseAccess`(담당 실무자·기관 관리자)가 판정하고, 변경 전건이 `record_consent` 감사로 남으며(D14) 철회도 이력 표에 **새 행**으로 쌓인다(행 UPDATE로 이력을 지우지 않음, D23). ④ **버그 1건 동시 수정**: 인테이크 1단계의 개인정보 동의 조회가 이력에서 `consent_privacy_at IS NOT NULL` 행만 골라, (a) 철회가 화면에 영영 반영되지 않고 (b) 그 당사자의 **다른 참여 사업** 기록까지 긁어 왔다 — 3종 모두 해당 사업의 현재값을 읽도록 교체. ⑤ 남긴 예외 3건: (a) 이력 표의 insert 가드가 "NULL이 아닌 동의 시각 = recorded_at"을 요구해 한 행은 언제나 전체 스냅샷이다 → 하나만 고쳐도 나머지 시각이 갱신되므로 화면은 '마지막 기록 시각'으로 표기하고, 그 값도 동의 시각이 아니라 이력의 `recorded_at`에서 읽는다(3종 전부 철회하면 동의 시각이 모두 NULL이라 방금 남긴 철회가 '기록 없음'으로 보인다) (b) 층이 참여 사업이라 **같은 당사자의 두 번째 참여 사업은 동의 3종이 전부 미체크로 시작한다** — 사업마다 다시 받는 것이 맞는지는 동의서 법률 검토(D26) 몫 (c) `pilot_text_ai_consent_evidence`(Phase-1 별도 게이트)는 이 경로가 건드리지 않는다. 게이트: build · guard:db · 테스트 447개(API 308 + 웹 139) 전부 통과.
- 2026-07-29 (Claude Code): **D42 인테이크 화면 재구현 — 위저드 6단계 → 정본 질문지 4부와 1:1인 4단계.** ① 1단계 기본정보(이름·생년월일·연락처·이메일·주소·성별)와 동의 3종은 **읽기 전용 표시**(입력 컨트롤 0개, 감사는 기존 `getIntakeRecordContext` 1건 유지) ② 동의·목표·GAS 기준·원하는 도움 3문·6영역 상태·다음 행동·다음 만남 입력 **전부 삭제** — 게이트웨이도 그 5종(consent·helpNarrative·lifeAreas·goals·actionItems)을 선택으로 바꿨고 consent 가 없으면 동의 기록을 만들지 않는다 ③ 질문 어휘를 정본 4부 전 항목으로 확장(`INTAKE_ANSWER_KEYS` +39), '무응답'은 새 코드 없이 기존 `unknown`, '해당 없음'은 `not_applicable` 재사용 ④ 반복 행 표 2종 신설(2-1 부채·3-3 연계 기관) + 4-2 추가 확인사항은 기존 `additionalItems` 확장, 전부 `intake_details` JSON 격리 + 제출 해시 포함 ⑤ 당사자 등록 화면이 생년월일·주소(거주지역)·성별을 받아 금고에 암호화 저장(0015 컬럼 재사용, **마이그레이션 없음**), `updateParticipantPii` 도 같은 3종을 열었다 ⑥ 긴급도(일반/주의/긴급/즉시 개입)는 실무자 직접 선택·AI 제안 없음(R5). 게이트: build · guard:db · 테스트 435개(API 297 + 웹 138) 전부 통과.
- 2026-07-28 (Claude Code): **그릴링 2차 — D42 인테이크 화면 계약 + D43 GAS 보류·목표 2층.** D42: 위저드를 질문지 4부와 1:1인 4단계로 재편, 기본정보는 당사자 등록 값 표시만, 동의 UI는 당사자 등록으로 이동(D23 원위치), 목표 입력 전부 제거, 금고 확장 4컬럼 전부 암호화. D43: GAS·세부 목표 기능 전체 보류(스키마·데이터 유지), 전체 목표·세션 목표 2층은 유지 — 전체 목표는 첫 상담 대화로 설정(입력 UI는 브리핑 시안과 함께), 세션 목표는 상담 등록 시(D35 유지). 브리핑 재구성은 Q 별도 설계 중 — 시안 전 착수 금지. CONTEXT.md(GAS·세부 목표·브리핑 보류 표기)·CLAUDE.md(§6 재설계 대기 배너, D42·D43 행) 갱신.
- 2026-07-28 (Claude Code): **그릴링 — D40 용어 개편 + D41 인테이크 정본.** ① D40: 조직→기관 / 상담사→실무자 / 담당자→담당 실무자 / 참여자→당사자 / 시스템 관리자+배정 책임자→기관 관리자(역할 통합, 권한 경계는 승계 — 상담 내용은 담당 케이스만+긴급 접근 예외). 화면·문서만, 코드 식별자 유지. 스윕 85파일·624건, 게이트: build·guard:db·테스트 426개(API 289+웹 137) 전부 통과. 상세 ADR-0017. ② D41: 인테이크 상담지를 Q 제공 질문지 v1로 확정(`PRD/intake-questionnaire-v1.md`) — 1-1 PII는 당사자 등록(pii_vault)으로만 저장+금고 확장(생년월일·주소·성별), 인테이크는 고정 폼(D29 재구성은 정기 상담만), '긴급도'는 리스크 플래그와 별개 필드(실무자 직접, AI 제안 없음). ③ GAS: 기능 전체 보류·스키마 유지 방향 확정, 후속 재설계는 화면 내용 정리 그릴링으로 보류. ④ PR 검토: PR #4(CCC-36) 충돌 없음·수정 불요 / PR #5(CCC-28·29) 리베이스+신규 문자열 치환 필요(기능 자체는 새 결정과 무충돌).
- 2026-07-28 (Claude Code): **CCC-36 반영 — 내부 운영 정책 v0.2 (PR #4).** 공개 레포의 정책 문서 6장에서 미구현 목록(심각도 순 4건)을 빼고 운영 규칙 4가지만 남겼다 — 미구현 목록은 삭제가 아니라 비공개(Linear CCC-36 · CCC-archive)로 이동. 쉬운 버전(`artifacts/ccc-policy-for-workers-v0.1.html`)의 대응 절도 같은 기준으로 정리(⚠️ Slack #ccc-policy의 구 사본은 뺀 내용이 남아 있어 교체 필요 — 사람 단계). `.gitignore`에 `docs/handoffs/`·`omp-session-*.html` 추가(전환 때 제외한 디렉터리가 `add -A`로 되돌아오는 사고 방지).
- 2026-07-28 (Claude Code): **gitleaks 자동 검사 연결** — CI에 `secret-scan` 잡 추가(바이너리 v8.30.1 직접 다운로드 — gitleaks-action은 조직 레포 유료 라이선스 필요, `fetch-depth: 0`으로 전체 히스토리 스캔) + `.githooks/pre-commit`에 스테이징 스캔(gitleaks 설치 머신만, CI가 최종 방어선). 로컬 검증: 히스토리 7커밋 no leaks · staged 스캔 정상.
- 2026-07-28 (Claude Code): **공개 레포 노출 점검 + 민감 문단 정리** — 검토 결과 자격증명·실직원 PII 0건(green). 운영 ID(Slack `#ccc-tickets` 채널 ID·에이전트 계정 ID·Linear 연동 UUID)와 과거 보안 대응 경위의 세부 문구를 공개 문서에서 제거, ID 실값은 `~/developer/tools/portwright/services/linear.md`로 이관. URL(`*.workers.dev`)은 기능 설정에 필수 + Access/코드 게이트 보호라 존치. 잔여 권고: gitleaks를 CI에 연결(현재 설정 파일만 있고 자동 실행 없음).
- 2026-07-28 (Claude Code): **CCC-31 완료 — 인테이크 저장→브리핑 직행 + 브리핑 상단 '다음 상담 등록' 안내 한 줄(스펙 #78 US 17·18).** 인테이크 위저드 '완료' 성공 시 기록지 화면이 아니라 그 참여 사업의 **브리핑으로 router.push**(`?notice=intake_saved` 부착). 브리핑은 `IntakeSavedNotice`를 HERO 위(=페이지 상단)에 1회 렌더 — 값이 `intake_saved`일 때만, 마운트 직후 `history.replaceState`로 파라미터 제거(새로고침 재표시 차단 + 서버 컴포넌트/감사 중복 회피 D14). 안내줄 시각은 블루 tint(시간·상태 축 D34) + `--line` 1px + radius 12, 리스크 배너 어휘 차용 안 함(D9), 버튼은 세컨더리(프라이머리는 HERO 몫 §4-5)로 `/schedules/new?target=<beneficiary>|<case>` 직행(참여 사업 선택 선입력). 게이트: build + 웹 테스트 137개 + guard:db + 브라우저 journey(`scripts/design/journey-intake-redirect.py`, 증거 `artifacts/journey/intake-redirect/` — 저장→`/briefing?notice=intake_saved` 도착 · 배너 표시 · 파라미터 제거 · CTA target 선입력 · 재로드 시 배너 없음, page_errors 0). 마이그레이션 없음(웹+문서).
- 2026-07-28 (에이전트): **레포 전환** — 구레포 `CCC-archive` private 보관 + 새 `CCC` 공개 레포 스냅샷 이전. 상세는 아카이브 STATUS.md와 로컬 핸드오프 `docs/handoffs/2026-07-28-repo-reset.md`(미추적) 참조.
- 전환 이전의 전체 이력: `SocialSolidarityBank/CCC-archive`의 STATUS.md (비공개).
