# STATUS — 비영리 사례관리 프로그램

> **이 레포는 2026-07-28 전환으로 새로 시작했습니다.** 옛 히스토리와 상세 이력은 비공개 아카이브 `SocialSolidarityBank/CCC-archive`에 보존돼 있습니다. 이 문서의 과거 기록은 아카이브의 STATUS.md가 정본이며, 여기에는 개인정보가 포함된 원문을 싣지 않습니다. 옛 이슈·PR을 인용할 때는 `CCC-archive#NN` 형식을 씁니다.

- **Last updated**: 2026-07-29 (Claude Code — CCC-32 관리자 온보딩 구현. 브랜치 `feat/ccc-32-admin-onboarding`.)
- **Current Phase**: **브리핑 3영역 병렬 구현 중** — CCC-41(전체 목표 카드) 머지 완료(PR #9), CCC-32(관리자 온보딩) PR 진행, 남은 병렬 3건(CCC-38 핵심 한 줄 · CCC-39 AI 제안 구조화 · CCC-43 불일치 검출) + CCC-42(CCC-43 뒤).

## Next 3 Actions

1. `yellow` **동의 문안·법률 검토(D26) — 검토 의뢰 준비 완료, 남은 것은 Q의 자문 의뢰**: 패킷 `docs/consent/legal-review-packet-v1.md`(질문 7건 포함)를 자문에게 전달하면 된다. D36 열람 범위 문구는 문안 v0.2와 등록 화면 아코디언에 반영 완료(2026-07-29). 남은 인테이크 판단 1건: `emergencyContact`는 질문지에 없어 화면 입력 경로가 없다(CCC-37 기본정보 수정 화면도 7종만 다룬다). 기본정보 수정 화면 부재는 **CCC-37로 해소**(PR #7 머지).
2. `yellow` **PR #5 머지 대기** — 리베이스 완료·반영됨(2026-07-29): 새 main 기준으로 재작성 + 용어 스윕 111건 + 마이그레이션 0018·0019→0021·0022 재번호 + 자기 가입 동의 D44 3종 정합. 검증 전부 통과(빌드 · guard:db · API 330 + 웹 145) 후 PR 브랜치에 force-push, 충돌 해소(MERGEABLE). 머지 시점은 동의서 법률 검토 게이트(Q 결정). PR #4는 충돌 없음, 머지 가능.
3. `yellow` **브리핑 3영역 구현(D45, 브랜치 `feat/briefing-3sections`)** — Linear 티켓 분할 완료(2026-07-29): **CCC-40**(골격+GAS 제거, 선행 없음 — 여기부터) → 병렬 **CCC-41**(전체 목표 카드)·**CCC-38**(핵심 한 줄)·**CCC-39**(영역① AI 제안)·**CCC-43**(불일치 검출·표시) → **CCC-42**(불일치 처리 3종, CCC-43 뒤). 구 CCC-15는 D43·D45로 대체돼 Canceled. ⚠️ 문서·준비 PR 브랜치명에 티켓 번호 금지(머지 시 자동 Done). (기존 red 항목 Actions secret 재등록·브랜치 보호·CCC-35 토큰 로테이션은 미완이면 유지.)

## 전환 요약 (2026-07-28)

- **왜**: 공개 레포의 git 히스토리에 실직원 이메일 커밋이 SHA 직접 조회로 접근 가능했다(과거 가명화가 새 커밋으로만 덮고 히스토리 재작성은 안 함).
- **어떻게**: 구레포를 `CCC-archive`로 이름 변경 + private 전환(무인증 접근 차단) → `origin/main`의 깨끗한 스냅샷을 새 `CCC` 공개 레포로 이전.
- **제외**: STATUS.md(이 파일)·`docs/handoffs/`는 실 이메일·로컬 경로 포함이라 스냅샷에서 제외하고 가명화해 재생성.
- **검증**: 실직원 개인 이메일 0건 · gitleaks no leaks · 첫 커밋 author는 조직 계정 · 무인증 차단 404(아카이브) / 공개 200(새 레포).
- **함정**: 이름 재사용으로 옛 클론의 origin URL이 새 공개 레포로 해석된다 — 모든 옛 클론은 origin을 `CCC-archive.git`으로 재지정하거나 **새로 clone**해야 한다(옛 클론에서 새 CCC로 push 금지).

## Blockers

- 노트북 파이프라인(Part B)은 해당 하드웨어 위 세션 필요.
- Claude 대조 프롬프트(2단계-c)는 상담 템플릿 스펙 확정(D29) 선행.

## History

- 2026-07-29 (Claude Code): **CCC-32 — 관리자 온보딩 2단계(조직 이름 → 첫 사업 이름, 이름만 저장).** 스펙 CCC-archive#78 US 1·2. ① 마이그레이션 **0023**: `organization_settings`에 `org_name`·`program_display_name` nullable 추가(0021·0022는 PR #5, 0024는 CCC-41 선점 — 충돌 회피). programs 테이블 신설 없음 — 내부 유형은 `financial_support_v1` 고정(스펙 저장 깊이 결정). ② 게이트웨이: `completeOrganizationOnboarding`(admin 전용·재실행=덮어쓰기 — 온보딩 재방문이 409로 막히지 않음·감사 D14, 설정 행 부재 시 시간대를 지어내지 않고 거부 — 0005 "no guessed default" 유지) + `getOrganizationProfile`(전 역할 조회, 미설정 시 null — 셸이 앱을 잠그지 않음, 감사 없음 근거는 0017과 동일). ③ API: `GET /organization/profile` · `POST /organization/onboarding`. ④ 웹: `/onboarding` 2단계 위저드(admin 전용, 재방문 시 수정 겸용) + 완료 화면에서 실무자 초대(`/admin/invite`) 안내. **라벨 동적화**: 신규 `getDisplayLabels()`(React cache — 요청당 API 1회, ApiError 시 labels.ts 하드코딩 폴백)로 사이드바(AppSidebar 프롭 주입)·당사자 허브·브리핑·당사자 초대·일정 등록·관리자 배정·당사자 등록 화면이 저장 이름을 되비침. `admin-format.programLabel` 상수 제거, 클라이언트 컴포넌트(등록 폼)는 프롭으로 받음. 게이트: build ✓ · guard:db ✓ · 웹 155 ✓ · API 전체 ✓. 교훈: CCC-41과 같은 체크아웃에서 병렬 작업이 겹쳐 커밋을 41 머지 뒤로 미뤘다 — 이후 병렬 티켓은 `.worktrees/` 분리.
- 2026-07-29 (Claude Code): **CCC-41 — 전체 목표 카드, HERO 아래 그 자리 입력·수정(D45·ADR-0018, D43 미결 해소).** ① 마이그레이션 **0024** `support_cases.overall_goal`(케이스당 1개·수정 가능·점수 없음 — goals 테이블의 세부 목표 층과 분리, 번호는 PR #5의 0021·0022와 병렬 CCC-32의 0023을 피해 0024) ② gateway `setSupportCaseOverallGoal` — **담당 실무자만**(admin 열람만, ADR-0018), 활성 케이스만, 200자 상한, 빈 값=설정 전 복귀, 변경 전건 감사(detail에 목표 문장 미포함) + `getParticipantBriefing`에 `overallGoal`·`canEditOverallGoal` ③ API `PUT /support-cases/:id/overall-goal` ④ 브리핑 화면: 리스크 배너 아래 카드형 한 줄, 비면 "설정 전", 담당 실무자에게만 입력/수정 버튼 → 그 자리 폼(maxLength 200), 실패 시 카드 안 오류 한 줄. 게이트: 웹 155 ✓ · API 318 ✓ (build는 커밋 검증 워크트리에서 재확인). ⚠️ 같은 체크아웃에서 CCC-32 세션이 병렬 작업 중이어서 41 몫 변경만 훅 단위 선별 커밋 — 재발 방지는 세션별 `.worktrees/` 분리(메모리 기록).
- 2026-07-29 (Claude Code): **동의서 법률 검토 패킷 v1 + 문안 v0.2.** ① 신규 `docs/consent/legal-review-packet-v1.md` — 자문용 패킷: 서비스 개요, 시스템의 실제 동의 처리(3종 구분·대행 입력·append-only 철회 이력·참여 사업 단위 재동의·해외 AI 위탁·아카이브 보존), 검토 질문 7건(문안 전반 / D36 표시 범위의 제3자 제공 여부 / AI 국외 이전 / 제21조 아카이브 정합·1년 근거 / 사업 단위 재동의 타당성 / 전자서명·직접 작성 요건 / 대행 입력 증빙력), 동의 노출 화면 4곳. ② `consent-draft-v0.md` v0.1→v0.2: 4절에 D36 문구("같은 기관의 다른 실무자에게는 참여 중인 사업 목록과 담당 실무자 이름이 표시됩니다") 추가 + 구 용어(상담사·배정 책임자·참여자) D40 정규화. ③ `consent-copy.ts` 4절에 같은 문구 동기화(정본-화면 동기 규칙). 게이트: 빌드 · 웹 테스트 145개 통과. 다음: Q가 패킷으로 자문 의뢰 → 회신 반영 → PR #5 머지 게이트 해제.

- 2026-07-29 (Claude Code): **CCC-37 당사자 기본정보 수정 화면** — 등록 화면이 받는 7종(이름·휴대전화·이메일·계좌·생년월일·주소/거주지역·성별)을 등록 뒤에 고치는 자리를 `/participants/:id/edit` 에 신설했다. ① 저장은 기존 단일 관문 `updateParticipantPii`(R1) 그대로 — 새 복호화·저장 경로를 만들지 않았다. ② **권한 층 1건 변경**: `updateParticipantPii` 의 `assertAdmin` 을 뗐다. 근거는 등록(`createBeneficiaryWithInitialSupportCase`)이 이미 counselor 에게 같은 금고의 **6종**(이름·연락처·이메일·생년월일·주소·성별)을 열어 준다는 것 — 등록 때 쓸 수 있는 값을 등록 뒤에 못 고치면 오타 하나를 관리자에게 부탁해야 한다. **계좌만 예외**로 지금까지 admin 전용이었고, CCC-37 이 7종에 포함시켜 이번에 함께 열렸다(항목별 권한 분기는 두지 않는다 — 한 화면이 한 번에 저장하는 값에 축을 더하면 화면·게이트웨이·감사가 어긋난다). 케이스 단위 게이트는 그대로다(`assertActiveSupportCaseContext` → `assertSupportCaseAccess`: admin 또는 **활성 배정된 담당 실무자**). 레거시 `registerPii` 는 자기 자리의 `assertAdmin` 을 유지한다. ③ 읽기는 새 관문 `getParticipantBasicInfo` — 쓰기와 같은 문을 지나고 활성 참여 사업 컨텍스트까지 정해 돌려준다(화면이 사업을 고르지 않는다). 감사는 **화면 조회 1행**이며 추가 항목은 행을 나누지 않고 같은 행 `fields` 에 합친다(D24, `getIntakeRecordContext` 와 같은 방식). ④ 인테이크 1-1 의 '당사자 등록 정보에서 수정' 링크가 이 화면을 가리킨다 — 동의 링크(허브)와 목적지를 갈라 D44 코드와 얽지 않았다. ⑤ PII 는 브라우저 임시본·리다이렉트 주소에 담지 않는다(R3, 테스트로 고정). 게이트: build · guard:db · 테스트 457개(API 312 + 웹 145) 전부 통과. **PR #7로 머지**(계좌 권한 실무자 개방은 Q 승인 2026-07-29, PR 코멘트 기록).
- 2026-07-29 (Claude Code): **PR #5 리베이스 반영 — 참여자 초대·자기 가입(CCC-28·29)을 D40~D44 반영된 새 main 위로.** 준비 브랜치(`prep/ccc-28-29-rebase-d40`)의 커밋 4개(PR 원래 커밋 3 + 정합 커밋 1: D40 용어 스윕 111건 · 마이그레이션 0018·0019→0021·0022 재번호 · 자기 가입 동의 2종→D44 3종)를 검증 후 `feat/ccc-28-29-participant-signup`에 force-push. 게이트: 빌드 · guard:db · API 테스트 330 + 웹 테스트 145 전부 통과, PR 파일 구 용어 잔여 0건. PR #5는 CONFLICTING→MERGEABLE. 머지는 동의서 법률 검토 게이트(Q 결정) 대기.
- 2026-07-29 (Claude Code): **CCC-40 — 브리핑 3영역 골격 재구성 + GAS 아코디언 제거(D45·ADR-0018).** ① gateway `getParticipantBriefing`에 `sessionRows` 추가(기존 sessions 쿼리 재사용 — 회차마다 상담일·유형(regular/intake)·수기 첫 줄 60자 발췌, 메모 전문은 싣지 않음) ② API `normalizeParticipantBriefing` 섹션에 `sessionRows` 매핑 ③ 브리핑 화면을 3영역으로 재편 — 영역 ①(실무자 입력 위 + AI 질문 아래, 구 '지난 상담 브리핑'·'오늘 확인할 질문' 대체) / 영역 ②(회차별 한 줄 + '승인 대기 N건' 배지 이동) / 영역 ③(빈 상태 + 준비 중 안내), 미해결 액션·개인정보 카드는 그리드에 유지 ④ GAS 게이지·아코디언 컴포넌트와 전용 CSS 제거(D43 이행 — 스키마·gateway·데이터 유지) ⑤ 테스트: 웹 컴포넌트 테스트 3영역 계약으로 재작성, API routes 강한 단언에 sessionRows 반영 + gateway 비인가 회차 미노출 검증 추가. 게이트: build ✓ · guard:db ✓ · 웹 140 ✓ · API 308 ✓(재실행 68 포함). guard:layout은 스크립트 미작성(DESIGN.md '작성 예정')이라 해당 없음.
- 2026-07-29 (Claude Code): **D45 구현 Linear 티켓 분할 — CCC-38~43 발행 + CCC-15 Canceled.** 트레이서 불릿 6건: CCC-40 골격(선행 없음)이 CCC-41·38·39·43 넷을 차단, CCC-43이 CCC-42를 차단(blocks 방향 inverseRelations로 검증). CCC-15(목표 3층 구현, D33)는 D43 보류·D45 재구성으로 범위가 대체돼 경위 코멘트 후 Canceled — 전체 목표는 CCC-41이 승계.
- 2026-07-29 (Claude Code): **그릴링 3차 — D45 브리핑 3영역 재구성(ADR-0018).** Q의 브리핑 카드 수정 요청이 D43이 기다리던 시안 — 그릴링으로 계약화해 봉인 해제. 확정: ① '오늘 만나기 전 꼭 기억할 것'(구 지난 상담 브리핑+오늘 확인할 질문 통합 — 실무자 입력 위·AI 제안 최대 3개 아래, 제목+이유+근거 회차 링크 필수) ② 회차별 정리(상담일·유형 2종·핵심 한 줄 — AI 생성+승인 흐름, 수기 폴백) ③ 내용 불일치(공식화 시점 검출·저장, AI 판단 없이 양쪽 인용, 처리 3종은 표시만·원본 불변). 전체 목표는 HERO 아래 카드형 한 줄+그 자리 편집(D43 미결 해소). 상담 유형 세분화(초기상담·사정·개입)는 §8 미결에 유지. 문서: ADR-0018 신설, CLAUDE.md §6 재작성+D45 행, CONTEXT.md '내용 불일치'·'핵심 한 줄' 신설+브리핑·전체 목표·맞춤형 질문 갱신. 코드 변경 없음(구현은 후속).
- 2026-07-29 (Claude Code): **D44 동의 3종 — 등록에서 받고, 당사자 정보 페이지에서 고치고, 인테이크는 읽기만.** ① 마이그레이션 **0020**: `support_cases.consent_privacy_at` 신설(0018·0019는 열린 PR #5가 선점). 개인정보 동의를 녹음·텍스트 AI와 **같은 층**에 맞춘 것 — 두 동의는 이미 `support_cases`(현재값) + `participant_consent_records`(append-only 이력) 두 곳에 살았고, 개인정보 동의만 0014로 이력 쪽에만 들어가 있었다. 빠진 반쪽을 채웠다. ② 등록 화면에 개인정보 동의 체크 추가(3종, 기본 미체크, 미동의여도 등록 진행 — D15) + 게이트웨이·API 파서(`consentPrivacy`) 확장. ③ 당사자 정보 페이지의 **담당 참여 사업 카드마다** 동의 3종 수정·철회 UI. 저장은 게이트웨이 단일 관문 `updateParticipantConsent`(R1) — 권한은 `assertSupportCaseAccess`(담당 실무자·기관 관리자)가 판정하고, 변경 전건이 `record_consent` 감사로 남으며(D14) 철회도 이력 표에 **새 행**으로 쌓인다(행 UPDATE로 이력을 지우지 않음, D23). ④ **버그 1건 동시 수정**: 인테이크 1단계의 개인정보 동의 조회가 이력에서 `consent_privacy_at IS NOT NULL` 행만 골라, (a) 철회가 화면에 영영 반영되지 않고 (b) 그 당사자의 **다른 참여 사업** 기록까지 긁어 왔다 — 3종 모두 해당 사업의 현재값을 읽도록 교체. ⑤ 남긴 예외 3건: (a) 이력 표의 insert 가드가 "NULL이 아닌 동의 시각 = recorded_at"을 요구해 한 행은 언제나 전체 스냅샷이다 → 하나만 고쳐도 나머지 시각이 갱신되므로 화면은 '마지막 기록 시각'으로 표기하고, 그 값도 동의 시각이 아니라 이력의 `recorded_at`에서 읽는다(3종 전부 철회하면 동의 시각이 모두 NULL이라 방금 남긴 철회가 '기록 없음'으로 보인다) (b) 층이 참여 사업이라 **같은 당사자의 두 번째 참여 사업은 동의 3종이 전부 미체크로 시작한다** — 사업마다 다시 받는 것이 맞는지는 동의서 법률 검토(D26) 몫 (c) `pilot_text_ai_consent_evidence`(Phase-1 별도 게이트)는 이 경로가 건드리지 않는다. 게이트: build · guard:db · 테스트 447개(API 308 + 웹 139) 전부 통과.
- 2026-07-29 (Claude Code): **D42 인테이크 화면 재구현 — 위저드 6단계 → 정본 질문지 4부와 1:1인 4단계.** ① 1단계 기본정보(이름·생년월일·연락처·이메일·주소·성별)와 동의 3종은 **읽기 전용 표시**(입력 컨트롤 0개, 감사는 기존 `getIntakeRecordContext` 1건 유지) ② 동의·목표·GAS 기준·원하는 도움 3문·6영역 상태·다음 행동·다음 만남 입력 **전부 삭제** — 게이트웨이도 그 5종(consent·helpNarrative·lifeAreas·goals·actionItems)을 선택으로 바꿨고 consent 가 없으면 동의 기록을 만들지 않는다 ③ 질문 어휘를 정본 4부 전 항목으로 확장(`INTAKE_ANSWER_KEYS` +39), '무응답'은 새 코드 없이 기존 `unknown`, '해당 없음'은 `not_applicable` 재사용 ④ 반복 행 표 2종 신설(2-1 부채·3-3 연계 기관) + 4-2 추가 확인사항은 기존 `additionalItems` 확장, 전부 `intake_details` JSON 격리 + 제출 해시 포함 ⑤ 당사자 등록 화면이 생년월일·주소(거주지역)·성별을 받아 금고에 암호화 저장(0015 컬럼 재사용, **마이그레이션 없음**), `updateParticipantPii` 도 같은 3종을 열었다 ⑥ 긴급도(일반/주의/긴급/즉시 개입)는 실무자 직접 선택·AI 제안 없음(R5). 게이트: build · guard:db · 테스트 435개(API 297 + 웹 138) 전부 통과.
- 2026-07-28 (Claude Code): **그릴링 2차 — D42 인테이크 화면 계약 + D43 GAS 보류·목표 2층.** D42: 위저드를 질문지 4부와 1:1인 4단계로 재편, 기본정보는 당사자 등록 값 표시만, 동의 UI는 당사자 등록으로 이동(D23 원위치), 목표 입력 전부 제거, 금고 확장 4컬럼 전부 암호화. D43: GAS·세부 목표 기능 전체 보류(스키마·데이터 유지), 전체 목표·세션 목표 2층은 유지 — 전체 목표는 첫 상담 대화로 설정(입력 UI는 브리핑 시안과 함께), 세션 목표는 상담 등록 시(D35 유지). 브리핑 재구성은 Q 별도 설계 중 — 시안 전 착수 금지. CONTEXT.md(GAS·세부 목표·브리핑 보류 표기)·CLAUDE.md(§6 재설계 대기 배너, D42·D43 행) 갱신.
- 2026-07-28 (Claude Code): **그릴링 — D40 용어 개편 + D41 인테이크 정본.** ① D40: 조직→기관 / 상담사→실무자 / 담당자→담당 실무자 / 참여자→당사자 / 시스템 관리자+배정 책임자→기관 관리자(역할 통합, 권한 경계는 승계 — 상담 내용은 담당 케이스만+긴급 접근 예외). 화면·문서만, 코드 식별자 유지. 스윕 85파일·624건, 게이트: build·guard:db·테스트 426개(API 289+웹 137) 전부 통과. 상세 ADR-0017. ② D41: 인테이크 상담지를 Q 제공 질문지 v1로 확정(`PRD/intake-questionnaire-v1.md`) — 1-1 PII는 당사자 등록(pii_vault)으로만 저장+금고 확장(생년월일·주소·성별), 인테이크는 고정 폼(D29 재구성은 정기 상담만), '긴급도'는 리스크 플래그와 별개 필드(실무자 직접, AI 제안 없음). ③ GAS: 기능 전체 보류·스키마 유지 방향 확정, 후속 재설계는 화면 내용 정리 그릴링으로 보류. ④ PR 검토: PR #4(CCC-36) 충돌 없음·수정 불요 / PR #5(CCC-28·29) 리베이스+신규 문자열 치환 필요(기능 자체는 새 결정과 무충돌).
- 2026-07-28 (Claude Code): **gitleaks 자동 검사 연결** — CI에 `secret-scan` 잡 추가(바이너리 v8.30.1 직접 다운로드 — gitleaks-action은 조직 레포 유료 라이선스 필요, `fetch-depth: 0`으로 전체 히스토리 스캔) + `.githooks/pre-commit`에 스테이징 스캔(gitleaks 설치 머신만, CI가 최종 방어선). 로컬 검증: 히스토리 7커밋 no leaks · staged 스캔 정상.
- 2026-07-28 (Claude Code): **공개 레포 노출 점검 + 민감 문단 정리** — 검토 결과 자격증명·실직원 PII 0건(green). 운영 ID(Slack `#ccc-tickets` 채널 ID·에이전트 계정 ID·Linear 연동 UUID)와 과거 보안 대응 경위의 세부 문구를 공개 문서에서 제거, ID 실값은 `~/developer/tools/portwright/services/linear.md`로 이관. URL(`*.workers.dev`)은 기능 설정에 필수 + Access/코드 게이트 보호라 존치. 잔여 권고: gitleaks를 CI에 연결(현재 설정 파일만 있고 자동 실행 없음).
- 2026-07-28 (Claude Code): **CCC-31 완료 — 인테이크 저장→브리핑 직행 + 브리핑 상단 '다음 상담 등록' 안내 한 줄(스펙 #78 US 17·18).** 인테이크 위저드 '완료' 성공 시 기록지 화면이 아니라 그 참여 사업의 **브리핑으로 router.push**(`?notice=intake_saved` 부착). 브리핑은 `IntakeSavedNotice`를 HERO 위(=페이지 상단)에 1회 렌더 — 값이 `intake_saved`일 때만, 마운트 직후 `history.replaceState`로 파라미터 제거(새로고침 재표시 차단 + 서버 컴포넌트/감사 중복 회피 D14). 안내줄 시각은 블루 tint(시간·상태 축 D34) + `--line` 1px + radius 12, 리스크 배너 어휘 차용 안 함(D9), 버튼은 세컨더리(프라이머리는 HERO 몫 §4-5)로 `/schedules/new?target=<beneficiary>|<case>` 직행(참여 사업 선택 선입력). 게이트: build + 웹 테스트 137개 + guard:db + 브라우저 journey(`scripts/design/journey-intake-redirect.py`, 증거 `artifacts/journey/intake-redirect/` — 저장→`/briefing?notice=intake_saved` 도착 · 배너 표시 · 파라미터 제거 · CTA target 선입력 · 재로드 시 배너 없음, page_errors 0). 마이그레이션 없음(웹+문서).
- 2026-07-28 (에이전트): **레포 전환** — 구레포 `CCC-archive` private 보관 + 새 `CCC` 공개 레포 스냅샷 이전. 상세는 아카이브 STATUS.md와 로컬 핸드오프 `docs/handoffs/2026-07-28-repo-reset.md`(미추적) 참조.
- 전환 이전의 전체 이력: `SocialSolidarityBank/CCC-archive`의 STATUS.md (비공개).
